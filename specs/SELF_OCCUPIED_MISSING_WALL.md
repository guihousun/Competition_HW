# 空闲工人自占缺墙位：单格施工闭环

类别：策略优化。基线 ec1865abdfcbaa899a17999c8eb1d022cd75b8f7。
依据 R01/R02/R03（任务书§4.1–4.5）：一角色一动作、八邻格合法移动、建筑/角色占格、
工人仅白天邻接建墙，保留既有返防截止和施工两轮余量。不调整规则或模拟器。

已完结seed19红方记录：r1041工人20012移动到尚未建墙的(32,4)并持3石；
r1050–1090全队空指令。公开r1042帧经审计与原trace前后hash一致，但该旧trace
没有逐轮PlannerState，不得虚构历史派工记忆。测试从此公开帧与显式空PlannerState
复现局部问题，不声称完整历史规划器重演。

根因是 `_day` 的free_walls剔除所有占格，含工人自己；普通_worker_day只看到此列表。
现有construction_trip能从自己的未来墙格先合法离开，再邻接建墙并回内侧炮位，
但只在已有施工承诺或其它路由拒绝分支才被调用。r1042独立预期是
20012从(32,4)移动到(33,5)，下一轮建(32,4)，共2动作；(33,5)本来就是合法炮位。

新增窄触发：白天普通工作规划之后，炮位已建齐、工人仍没有动作和其它工作归属、
没有另一施工承诺，且其当前位置正是既有布局的合法缺墙格时，只为这一格调用
现有construction_trip。保持同轮claimed、采购RouteGuard、既有截止/余量和最终dispatch。
本次不能替换已选任务/购物/采矿指令，不能让别的角色挪开，不能原地建墙、穿过占格，
不能启动无限墙集合lease。施工失败/失去通路依现有观测重评，真实返回炮位才释放。
既有本地errand若因商店/小贩临时不可达而没有当前动作，仍保留其owner；
不因busy为空就抢走该角色。没有该可选ledger的正式公开请求不依赖它才能运行。

验证包含公开帧、红蓝镜像/ID变化、另一角色堵住合法站位、夜间/临近截止、
活动采购路径保护、未最终发出则不登记、preview不改变真实planner。
仅专项回归；本轮不自行运行整局或宣称修复能挽回第九夜的具体血量/积分。

## 当前候选验证

- 11项新增回归通过。真实公开帧＋空planner的完整管线输出为move(33,5)→
  邻接build(32,4)→下一帧确认已经在墙内合法炮位并释放。红蓝镜像及ID替换同样通过。
- 在另一工人同轮已暂存construction时不创建第二owner；已有purchase即使return受阻
  无命令也不抢；临时不可达vendor的existing errand没有命令但归属仍保留。
- 现有采购精确首步(37,6)之后仍以31行动/31预算证明兼容，RouteGuard耗尽时延期施工。
  两动作加2轮余量恰好能在原deadline前完成时允许；差一轮不新派施工，仅合法返防。
- 86项施工/团队行程/协调（含新增11项）通过，9.156s；19项原让路/武器预算回归通过，0.647s。
  `git diff --check`通过。未运行全量测试、整局对照或官方平台；等待主线程独立审核。
