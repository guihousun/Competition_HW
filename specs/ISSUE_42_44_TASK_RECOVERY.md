# Issues #42–44：任务check恢复与实战统计核对

负责人/执行/审核：Codex。输入由GitHub连接器于2026-09-21读取完整正文及全部评论（#42–44均0条）。输入快照已用workflow/state.py摄入，不以轮询通知正文代替证据。开发基线0d0ddb2；实测包源码c0743915、包9d615b66，与本次开发版本分开登记。

类别：R01/R07下的内部工具实现修复及观测诊断；依据接口§1.1 lastCmdResult、§1.7、§2.1，任务书§五。不改官方题目/check内容、答案、次数或时延限额。

## 已知与未知

- #43提供裸`./check`的exit126及`./check: line 1: #!/usr/bin/env bash^M: bad interpreter: No such file or directory`。当前预转换只接受绝对路径，恢复还限于/bin/bash签名，属于已确认覆盖缺口，执行#38已有合同。
- 同场另一任务后续check成功，不能据此判断是确定性恢复还是模型重发；未提供完整已派发命令链。故障文件来自任务沙盒，参赛归档没有check脚本；不可声称重建包会消除官方任务模板CRLF来源。
- #42/#44的“0个official_success_confirmed”不等于0成功或4失败。当前日志没有正向官方判题字段，提交后增分只能列观察值，不能把所有差额算成任务分。保持unknown并增加明确语义字段。
- stream由本地运行UUID及队伍/地图/基地身份哈希构成，变化不证明半场或换边。仅凭摘要不能确认“第二半场全程空转”。r241后数值仍从429升到433也与“433封顶”描述不完全一致，需按原始逐轮数据分段。
- 正确关联的need_info会立即停止当前题；非法JSON/关联/证据计划可能反复请求模型直到8次上限。现有摘要不足以证明北京题究竟在哪条路径，禁止猜题或盲改WAIT_ROUNDS。

## 本次实施

1. 精确裸`./check`使用现有结构化check工具；路径在执行这条命令的沙盒内解析当前目录，不能用HTTP宿主cwd或上一题路径。只在内存规范CRLF，真实解释器执行，保持原文件、原cwd、退出码、超时和限额。
2. 不支持新任意相对路径、参数、重定向、管道或复杂Shell重写。显式关联126且匹配CRLF解释器签名时可仅恢复check，不重放前缀修改；复用现有命令/重试预算。
3. TaskAgent模型run→兼容工具→实际派发→关联回执链做独立测试；成功token只来自当前真实check结果，不因日志“提交未确认”改变判题。
4. 任务摘要增加正向结果不可确认的显式说明。维护旧字段和真实负向反馈，不把unknown计为失败，不凭金币、合法动作、题文消失制造成功。
5. 对不合法模型计划记录有界静态拒绝原因，并将最近拒绝回合/原因加入agent_state日志，区分JSON格式、关联和证据ID问题。保留原8次工程上限与正常回执等待，不依据未知北京题擅改停止策略；不回显原始模型文本或凭据。

允许文件：task_tools.py、必要task_agent.py、task_progress.py、team_agent.py和task_journal.py诊断字段及对应测试、Spec/报告；#29同步只更新独立回放测试及资料说明。禁止改官方原文、模拟结算、任务答案或防御/经济参数。独立专员不得自行提交推送。

## 验收与交付

真实POSIX临时工作区：CRLF脚本、非零退出、缺文件、链接拒绝、两题不同cwd、原文件字节不变、复杂命令不变、预算内派发、重复错误有限恢复。精确TOKEN必须由脚本产生；脚本模型回执不代表实LLM成功。

全量unittest、Python3.11.10参赛包独立HTTP检查；从冻结源码构建官方原入口tar.gz，审核后提交codex/sgh。记录源SHA/包SHA/交付SHA，待公司复测，不关闭来源Issue。

## 下一份公司证据

无需重跑才能开始本次修复。请从既有北京题记录导出同generation按轮排列的issued_prompt/模型回执/agent_state及实际executeCmd；换边核对原始teamOur.type、基地ID/位置、官方battle/half字段。缺项保持unknown。这部分阻断精确归因，不阻断裸check修复。
