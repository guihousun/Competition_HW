# Issue #48：模拟器与官方回放一致性审计分诊

状态：已实施首列坐标对齐；其余逐帧核验等待原始回放。不修改官方规则。

## 版本与证据边界

- Issue 声称对照 `pk-696563`，但正文未附回放文件或可下载地址。
- 官方规则与行为基准不随参赛包源码版本变化；`28c9922` 只标识该场我方实现，不能用来否定回放对官方规则的证据。
- 源码 SHA 仍需保留，用于区分“旧实现的表现”和“当前模拟器的复现结果”。
- 单场回放可以作为该地图/该版本的行为证据，不能推广为所有地图、波次或官方通用 tie-break。

## 当前代码核对

- 当前机器人偏航边界为 3 格；Issue 中的“4 格”属于旧模拟器/旧假设，需与回放逐帧核对官方行为，但不因回放使用旧包而降低证据等级。
- 2026-09-22 用户明确要求采用官方位置：新建模拟对局首列从 x=27 改为 Issue 摘录的 x=22；对侧暂按镜像 x=18。纵向排列、后续列和对侧镜像继续标注为本地补全；不改写旧录像保存的刷新池。
- 当前帧和伤害日志已有结算后状态标记；需要用原始 JSONL 确认回放帧与本地 frame 的比较时点。
- Issue 所述经济冻结属于策略诊断，不是协议或模拟器规则缺口；应单独用当前版本、同种子、红蓝换边回测。

## 需要补充的最小证据

1. `pk-696563` 脱敏回放文件或可公开的附件/链接。
2. 回放对应包内源码 SHA、地图档和阵营映射。
3. 至少首夜刷怪坐标、r71/r369 的结算后快照，以及一段机器人移动/攻击记录。

变更类别：官方观测对齐 / R05、任务书 §4.7.3；未公开的其余几何仍属本地假设。
行为例子：41 列地图左侧基地的首列固定在 22，原来人为增加的 5 格行程已移除。
验证：固定刷新池、双方隔离、首夜 35 只、跨夜复用和旧录像几何保留测试。
旧 x=27/13 生成的报告属于旧模拟器证据，不能直接作为新出生距离下的生存评估。
完整回放到位后继续逐字段核验，不影响本次已知坐标的修正。
