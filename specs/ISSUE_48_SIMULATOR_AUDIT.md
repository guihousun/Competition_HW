# Issue #48：模拟器与官方回放一致性审计分诊

状态：`needs-info`；不派 DSH，不修改官方规则。

## 版本与证据边界

- Issue 声称对照 `pk-696563`，但正文未附回放文件或可下载地址。
- 结论绑定旧源码 `28c9922`，不能直接归因到当前 `codex/sgh`。
- 单场回放可以作为该地图/该版本的行为证据，不能推广为所有地图、波次或官方通用 tie-break。

## 当前代码核对

- 当前机器人偏航边界为 3 格；Issue 中的“4 格”属于旧版本/旧假设，需与回放逐帧重新核对。
- 当前本地固定列阵仍是本地假设；若 `pk-696563` 的首列确为 x=22，只能新增该地图的 observed profile，不能把全局刷怪比例静默改成 x=22。
- 当前帧和伤害日志已有结算后状态标记；需要用原始 JSONL 确认回放帧与本地 frame 的比较时点。
- Issue 所述经济冻结属于策略诊断，不是协议或模拟器规则缺口；应单独用当前版本、同种子、红蓝换边回测。

## 需要补充的最小证据

1. `pk-696563` 脱敏回放文件或可公开的附件/链接。
2. 回放对应包内源码 SHA、地图档和阵营映射。
3. 至少首夜刷怪坐标、r71/r369 的结算后快照，以及一段机器人移动/攻击记录。

拿到证据后再生成逐字段差异表。没有这些材料时只保留审计登记，不改变模拟器全局规则，也不宣称官方认证。
