# Issue 27：明确错误回执驱动的任务纠错

类别：策略与工程修复；依据 DEVELOPMENT_RULES R01、R07，任务书 §5.3/§六，接口 §1.7/§2。
来源：Issue #27 评论 5694722267，事件 evt-1643c6e0864c9e6b。
基线：485e69b97570e903a566d09b6a948eed8325847a。Codex直接处理跨回执关联与状态恢复；不占用或重派五路游戏实验。

## 已证实与尚未证实

- 8fa0625 的北京题五次提交，oldest_era 在两个已拒绝值之间来回切换。反馈只指明错误字段，没有给正确答案；新片段题文也有 omitted，不得硬编码答案。
- 所附两个HTTP结果已从401/400恢复为200；401是探测过程，不是这两次最终失败。南京页10条/总12条，只能证明此页不完整，不能证明整场未继续取数。
- 复合shell末尾 ./check 因CRLF退出126，此前独立check兼容器未覆盖。不得重放前缀写文件命令，也不得修改check原件。
- 败局只剩1名ready worker及火箭损失是线索，无完整位置/攻击链，不能断言采购导致失守。蓝方有268金币仍买墙券属旧8fa策略证据；武器优先已有后续交付，不重复派单。

## 实施范围

1. 任务级持久拒答记录：只关联相邻轮、确实发出的答案与errorCode 2；规范化JSON对象键序/空白后去重。同一任务阻止重交明确拒绝的完整答案；允许新答案与继续工具调查。反馈完整短摘要在下一次prompt突出显示，拒绝列表不随近期历史裁剪消失。跨任务清空，旧状态保守迁移，不制造裁决。
2. 对经过路由核验且由本工具生成的HTTP结果分析页数/总数/截断，输出可定位的取数缺口提示；未知结构标未知。提示用于进一步取证，不自行推算分页参数、不猜答案、不将单页不全等同整任务失败。
3. 已确认CRLF126且命令属于明确绝对cwd、简单mkdir/chmod/echo/printf链、最后独立./check的情况，仅提议结构化check重试。复杂shell回到模型修复；保留单轮动作、工具计数和原判题器内容。
4. 不更改官方额度、判题通过率、沙盒权限、模拟器答案、API数据；不改运行中实验冻结源码。

## 验收

- 独立模型fixture重现A→B→A，第三次不发submit；JSON重排仍拒绝；新任务允许；未知/非相邻反馈不拉黑；状态持久化/旧版迁移/非法状态验证。
- HTTP独立结构10/12不全、15/15完整、200错误码、截断及未知；成功401→400→200仍判取数成功而非鉴权失败。
- CRLF复合链后只重试check，原文件hash/前缀副作用不变；复杂或相对路径不自动恢复；POSIX实际执行与退出码验证。
- 全量unittest、git diff --check；实际包双阵营POST。数据来自片段及合成fixture，不宣称完整实机重放或内网PASS。

## 后续

实包Linux探针发现并复现旧的日志并发缺陷：线程先写HTTP再直接做有状态汇总，r4可能抢在r2/r3之前更新TaskJournal，漏记真实check。追加工程修复：正式入口与本地入口均在写HTTP前预留有界日志队列槽，写完后解锁；单一后台消费者按槽顺序汇总。日志不阻塞HTTP，队列满明确记录丢弃，写失败不记录为已发动作。通过确定性线程门闩复现及实际包重复连续请求验证；不修改官方响应。

多页集合自动覆盖证明、字段级纠错与完整防线败因仍需独立设计/证据。现修复先避免确定性的重复浪费，不以提示词代替模型正确率验证。五路探索保持原上下文和冻结版本；最终脚本策略另作配对评估。
