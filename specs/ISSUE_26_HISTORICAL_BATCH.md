# Issue 26 — 旧批次证据与提示回显回归

来源：[Issue 26](https://github.com/guihousun/Competition_HW/issues/26) 正文及全部评论（本次读取为零条）。被测源码为 `1831136d5a30f89caba3cb5ec1d2b91838684c2a`；开发基线为 `83285bcc32822117a7909863ec2ae4ddad64185c`，包内源码为 `2a95c26c715c22aedf3263dc95ccd64f7ea0dc60`。

类别：测试工具与证据文档；不修改运行策略、官方规则、来源文档或参赛包。R01（接口）、R07/R08（任务及模型边界），规则依据沿用 Issue 25 Spec 的任务书§五、接口§1.1/§1.7/§2。

## 分诊

1. 用户报告六场共36个 episode、1次提交，唯一提交为 `requirement: 遵守题目原文指定的格式、字段与单位`，收到非法JSON错误。胜率、回合和题目hash目前属于用户提供证据；不可把它们写成已解析原始文件的结论。
2. 已 fetch `origin/codex/sgh`，远端仍为83285bc，六个596xxx日志目录尚不存在。用户已说明公司代理延迟上传，无需再次要求解释。日志到达后核对版本、角色归属、事件计数与失败原因，不运行日志中的沙盒命令。
3. 后续66a4d9a加入定位/读取与预算处理；9f1a2c8加入HTTP恢复和check工具；2a95c26加入原题答案契约与围栏校验。旧批次没有验证这些变更。不能宣称36次超时都已解决。
4. 当前明确JSON对象契约已经拒绝提示回显。补充真实planner/router路径回归，覆盖双方、裸文本与JSON字符串形式、拒绝后同episode修正、状态序列化和截止窗口。未知原题格式仍不擅自强制JSON；这不是按固定答案关键词过滤。
5. Issue 26沿用的#25对比计数尚未应用其更正评论。按已发布原始审计，598595/598290两场共7次提交、3个token对象；再加598291为10次提交、6个token对象。与本批合并时必须声明两场或三场口径，本批原始计数尚待日志核验。
6. `unknown_without_judge_feedback` 只表示本地未确认，不能据此证明任务无奖励或胜负完全由战斗贡献。token重复也不证明判题器永远不返回正向反馈。

## 交付与验证

- 允许文件：tests/test_task_answer_contract.py、本Spec、reports/issue-26-historical-batch/README.md。
- 运行答案契约及任务pipeline相关测试；独立fixture明确要求JSON对象，不伪装原场景重放或真实模型表现。
- git diff --check；核对交付参赛包及源码身份保持当前已验证候选。
- 发布测试/文档到固定codex/sgh；沿用既有包，不制造无运行代码变化的新比赛版本。
- Issue保持open、awaiting-intranet；证据缺口与当前候选复测并行，不派重复DSH任务。
