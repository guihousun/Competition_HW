# Issue 28：保留任务字段语义与校验来源

类别：策略/工程修复；依据 DEVELOPMENT_RULES R01/R07、任务书§5.3、接口§1.7/§2。
来源：Issue28 task-summary，事件evt-7104a874ad638998。基线75cf3ae。

## 证据

新增题目说明明确oldest_era要“年代最早的遗产名称”，此前Issue27重复提交年代值。问题不限于JSON形状：字段英文名不能替代中文语义。
汇总开头称5项，实际列6个模板；没有本批完整SHA/判题序列，不把汇总当6项官方PASS。成都提到全国重点指标，与其他城市的世界遗产指标不能静默互换。
历史城市答案、API凭据和TOKEN只作来源，禁止写入策略答案库；真实题文、服务回执和当前check优先。

## 实施

1. 仅从当前命名任务文件的明确答案格式章节提取字段说明，包含JSON样例/注释与`<中文占位说明>`模板。保留来源哈希，不从API样例提升答案契约，不从示例值造答案。
2. 字段描述在prompt的提交契约中优先保留；遇到要求某记录名称的排名字段，明确返回name而不是比较用的era/日期。
3. 仅当当前任务已核验HTTP回执提供完整records时，拒绝“要求记录名称，却提交该批记录的era/date等比较字段值”的可证明误用；不在缺页、失败、截断或未知结构上猜错，不自动猜出正确排名答案。
4. 对题文明示运行check取得TOKEN的任务，提交必须引用当前真实成功check结果；不复用历史token或格式样例。通用非check的token任务保持原语义。
5. 增加脱敏任务方法说明与变化城市/字段名/指标/年代值/TOKEN的独立回归，红蓝双方走真实任务管线。参数/API数据/校验器/官方原文不改，五路实验冻结源码不改。

## 验收

伪JSON占位模板、合法JSON注释、冲突说明、长文优先保留；不同城市/数据仍选当前名称，不接受年代替代名称；不完整查询不误断正确答案；历史token拒绝、当前check值通过、无check要求不误限。全量unittest、实际包POST、git diff --check；审核后固定codex/sgh交付根目录tar.gz与哈希/manifest，回写Issue不自动关闭、不标内网PASS。
