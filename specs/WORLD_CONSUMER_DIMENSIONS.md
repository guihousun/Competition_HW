# 世界信息消费与未解决项语义

类别：Agent内部实现修复、提示词优化；R01/R06/R07/R08。官方输入/规则/额度不变。

来自45557d7的真实模型重复试验：新闻2/3，价格事实的availability=unknown误取消独立停产事实；长传闻前两次已给出正确地点/材料/绝对时段，却保留与实际缺项无关的publication_time和prerequisites，未执行献祭。

新闻消费仅依据明确availability声明，unknown保持中性。矛盾、日期不确定、更正覆盖和丢失证据仍按现有账本抑制行动；禁止用unknown创建禁采。手写正负反例涵盖分拆事实、顺序交换、JSON存取、只知价格和真实相反声明。

宝藏只澄清提示词：未知项是当前尚未解决项，检索后要重算；绝对游戏日不需要文章发布日期；prerequisite须来自原文实际条件，实际背包/行程由执行层检查；不添加假想条件。禁止物品依然作为exclude候选保留。

**不在代码中强制清空模型unknowns，不把uncertain覆盖为false，不跳过材料/窗口/路径或前置条件验证。** 真实存在、尚未核验的条件继续阻止行动。不得把新的提示词视为已经通过真实模型验证；需保留三次基线与三个新试验全部结果、调用成本和源码哈希。

本工作树用于隔离仍在运行的455基线真实模型试验。后续审核后再整合回主候选，统一测试和交付到codex/sgh。

本地验证：102项新闻测试、15项记忆测试通过；完整895项通过（Python3.11.10，362.312秒）。新提示词尚未完成三次新的真实模型复验。用户随后要求先交付一版用于真实场景测试；可作为明确标注的内网测试候选交付，不宣称模型复验或整个Agent目标已完成。
