# Issue 27：8fa0625 实机复测分诊

输入：event `evt-b2d375a7c2d974c7`，完整 Issue 正文与两条评论，最新评论 5691964673。
输入 digest：`c0469c3285ebbe32a8d8c39875a03a5483597ff7b71400a3b3855d73161b6f41`。
实测包内源码：`8fa062536ecc20ba30d813136d20a6c8559eca8b`，交付 `66e262f`；本轮开发候选基于同一交付分支。

## 证据与结论

- 用户确认 15 场 identity=8fa0625，trading_catalog / upgrade_itinerary / weapon_readiness 各场都出现。确认部署和日志链路，不能扩大为行为/比赛 PASS。
- 6W9L；用户列的此前 65b03f9 批次 14W2L。不是相同地图、对手、随机条件的配对对照，不能据此确认阵容或采购是唯一败因，也不能忽略退化信号。
- 39 个 task_submission_feedback 中 14 个 value_mismatch；其余 25 个没有该类错误不等于全部成功。没有足够裁决与积分窗口不能算成功率。
- CRLF 126、API 401 仍出现。之前补丁只覆盖可确定绝对路径的独立 check，不覆盖所有复杂 shell；没有宣称所有 API 鉴权问题修复。

## 同线程执行范围

1. 正在实现的迎敌半圈墙、战斗期内侧站位、错位火箭—激光—火箭、升级优先级继续在隔离分支验证，不因本事件重复派单。完成本地审核后作为待内网验证候选，不称治愈全部败因。
2. 先获取以下可公开的最小事件片段（不要求整个日志/内网 URL/凭据）：
   - 604764 B 方：五次 value_mismatch 所属任务的原题提交要求、实际提交、错误码、前后两轮 goldNum/totalScore、任务结束状态；城市/集合聚合需保留实际数据的类型/数量和分页结束证据。
   - 任一 8fa0625 下 CRLF126：发出的 executeCmd 与 lastCmdResult、其前一轮 agent_state/plan_purpose；区分独立check还是复合shell。
   - 任一 API401：脱敏后的 executeCmd、响应status/错误文字、下一轮计划及后续请求。保留头名称，不保留头值、真实密钥或内网域名。
   - 一场败局及一场胜局：从第一次基地/前墙明显掉血前10轮到后10轮，记录己方角色位置/HP/背包、塔型/等级/冷却、武器等待原因、动作与结果、机器人分布和 upgrade_itinerary。另给一次完整 trading_catalog 与购买/使用券的回执。
3. 根据片段建独立脱敏fixture：鉴权错误不得降级成合法零结果；明确答案契约的类型/枚举/聚合语义必须验证；工具失败与题目无数据必须区分。修复绑定实际可复现路径，不把城市名、答案或内网接口硬编码进策略。
4. 待确认：原始日志无法同步；14次错误的具体字段是否相同、采购离岗是否导致基地失守尚未知。这只阻断归因与依赖原文的补丁，不阻断当前防线实现和局部回归。

验收：每个新增修复须有从公开/授权片段抽取的失败fixture与独立期望；全量测试、打包入口与对应 SHA 内网复测。不得由聚合战绩推断官方 PASS，不自动关闭 Issue。
