# Issue #41：四日宝藏线索验收案例

- 来源：[宝藏机制 #41](https://github.com/guihousun/Competition_HW/issues/41)，2026-09-21 创建；GitHub 连接器读取完整正文和全部评论（0 条）。
- 输入 digest：`96d68229feae2ef0c2221b29659034cfc45301a22f4886e1d7fed66e9e0a3673`。
- 审查基线：`df73adf1762e59854b8045b05c496409c19fb8a3`；包内源码 `c0743915a5ca4b470259a4b7fa9f40fed9b94774`。Issue 未标明对应实机版本，不反推成该包的失败报告。
- 类别：资料补充、验收设计。依据 R02/R06/R07，任务书 §4.6.3、§五和接口召唤动作；Codex 审核。本文不改比赛规则或策略参数。

## 采纳的案例及证据边界

将 Issue 给出的 `(3,3)`、第 5 天白天、三件物品各 1 份登记为**用户给出的本例期望答案**，不是所有地图的固定配方，也不是已观察到的判题成功。当前材料是分析摘要与部分引文，缺少逐日完整公开观测、当局商品列表及召唤反馈。

| 本例表述 | v1.0 官方参考商品 | 对应依据与限制 |
|---|---|---|
| 铭文石板 | 古符石板 / `AcientTablet` | 灰白色、刻有古文字的石板；最终使用当局商店的协议名，不改官方拼写 |
| 光之尘 | 星辰之沙 / `StarSand` | 银白、暗处发光的粉末或细沙；作为描述匹配候选，不加入无条件别名字典 |
| 火焰精华 | 烈焰之息 / `FlameBreath` | 晶石瓶中的橙红色发光雾气；同样需要当局商品观测确认 |

每件一份的数量来自本例三封印、三容器解释，不推广为每次宝藏都需要三件。商品价格/库存取本轮观测，背景表不是库存。

`(3,3)` 属于 41×32 地图的西侧，和“西部石门”可以同时成立，无须仅因叙述变精确就标记旧线索冲突。真正不相容的坐标、物品或日期仍须保留冲突证据，不能简单采用最新文本。

第 5 天白天首轮为 521；若原文确定只在第 5 天白天开放，窗口为 521–590。只有“第五日白昼方可松动”时，不能擅自补出关闭时间。对于本例可以保留用户期望坐标，但泛化时“公里”转换成格数需要比例或额外坐标证据；不能由本例建立通用的一公里一格规则。

## 现有实现核对

- `world_context.item_context` 已将官方六种用品外观参考及本轮商店观测放入背景，保留未知商品名称与缺失价格。
- `world_agent` 已要求跨日合并、逐字证据、绝对回合、保留未知项和真实冲突；提示中已有原点/方向、距离量纲及不编造关闭时间的约束。
- 现有执行链检查物品、到位、时间和公开召唤回执；失败会撤销未经证实的推断后再规划。
- 本次运行 `test_issue29_world_context` 与 `test_world_agent`：19 项通过（Python 3.11.10，1.013 秒）。覆盖官方外观/实时目录、跨日记忆、日期同义词、两阵营实际召唤、库存限制和失败重规划。模型回答由测试脚本提供，**没有证明真实模型已独立解出 #41，也没有实机成功证明**。

## 后续验收与实施边界

1. 已完成：登记本例期望、核对官方商品参考与已有能力、运行相关回归。现有能力已覆盖背景与跨日流程，不重复派发同类实现。
2. 收到当局 Day1–Day4 新闻原文及日期/回合、`weaponShopList`、相关 `lastSummonTreasureResult` 后，建立公开观测逐轮回放。保留首次看到各线索的时间，不能提前灌入 Day4 信息。
3. 用不同坐标、日期、商品组合及红蓝身份做对照；删去坐标/单位比例、明确窗口或配方证据时，应保留相应未知项。虚构商品名、未来线索、地图私有答案不得进入动作输出。
4. 若回放暴露资料截断、名称映射、冲突消解或采购/到达失败，再针对可复现的环节派给同类 DSH 专员。绑定该输入 digest、冻结源码和独立预期，不直接硬编码本例答案。已有执行应先检查 session/status 并等待原执行。
5. 允许的候选改动范围：`world_agent.py`、`world_context.py`、宝藏公开推理/行程模块及相关测试。官方原件、物品效果、计分、动作额度、隐藏答案和默认防御配置不在本 Spec 修改范围。
6. 行为改动需完整回归、左右换边和不同线索的留出案例，审核后才重打包。公司回测报告对应源码 SHA 和真实召唤结果，不自动关闭 #41。

本次交付只有资料与 Spec；运行时代码和根参赛包保持 `c074391` 候选。尚无需要依据猜测改动的故障定位，因此未新派 DSH，也未将该资料标为官方 PASS。
