# Issue #45：测试包归属、任务切换与基地消失核对

负责人/当前执行与审核：Codex。来源：[Issue #45](https://github.com/guihousun/Competition_HW/issues/45)，2026-09-21由连接器读取全文和全部评论（0条），输入已通过workflow/state.py摄入。用户随后在本对话确认“就是刚刚的交付版本”，据此将被测版本绑定b0e837bb4537f2d2364929150915ee4257b79cbf、包内源码043918d495313229b3d123aeca7b92a4a45d1281、包SHA256 bc72c72742e5a010f98d86ac3bc0ae8d9993bb8391ee585bac0c063402b34e2f。版本确认来自用户，不再作为实施阻塞，也不重复要求先提供哈希。

类别：证据核对／任务生命周期验收；依据R01/R07、接口§1.1/§1.7/§2和任务书§五。没有更改官方题目、check、结算、任务次数或防御规则。

## 已执行的源码与归档检查

1. 当前运行源码和真正上传的tar.gz所有Python成员中，均未找到日志所示`b_spec`、`SVA_ANS`、`策略引擎初始化成功`、`任务消失2回合`、`任务消失但解题器活跃`。旧c0743915运行源码中同样未找到对应状态/固定日志文案。
2. 当前#43实现只在内存规范CRLF，使用task-check/1并记录path_source；它不通过sed修改check原件。Issue45中sed成功不能证明#43兼容路径实际触发。模型也可能主动生成sed命令，或日志经另一工具加工；仅凭字符串缺失不能反过来断言日志一定不是我方运行。
3. TaskAgent.begin在generation/source变化时清空旧任务状态；LLMRouter已有旧代际、未验证、重复和迟到回执隔离。当前不存在b_spec状态和所述两轮强制重置分支，因此按摘要中的观测顺序复现真实当前规划器，而非按摘要状态名修改代码。
4. 当前b0e837b下已执行test_task_agent、test_llm_router、test_task_pipeline、test_task_progress共62项，Windows Python3.11.10、0跳过、全部通过，0.368秒。覆盖既有代际隔离/真实规划流水线/未知结果语义，不宣称复现了本条公司原始请求序列。既有冻结源码全量1289项与真实包200轮HTTP见[上次交付](../reports/issue42-44-recovery/README.md)。

## 对摘要的校正

- 表中有6个不同任务名，4个已提交、2个未提交；标题/正文的“5任务”口径不一致。不得将4次提交直接作为4次官方成功，也不能把两次消失都确定归因于任务超时。
- 提交邻近积分增量合计425是观察量，不能仅凭这些差额确认全部来自任务。仍需明确且可关联的官方判题结果。
- r236观测摘要是基地HP=无且我方各类角色消失，不是明确记录HP=0。可能已被摧毁/终局清空，也可能是读取字段或队伍归属问题；核对原请求再归因。其后日志存在不证明仍有合法可行动角色。
- 按70白天+60夜晚，r236为第2天夜间，日内索引106；“第106回合”不能解释为夜间持续106轮。原HP=1095不是完整逐轮损伤序列，不能据此计算某一次伤害。

## 最小资料补充（先利用已有文件，无需重跑）

公司侧请在本Issue集中补充：

1. 版本已由用户确认。仅补充这批中文行是程序原始日志还是另一工具加工的摘要，帮助将摘要字段对应到当前程序；这不是继续执行的前置条件。
2. r47–53脱敏原始请求/响应：roundNo、phaseTask、任务点ID/状态/期限、开拓者ID/位置/血量、实际executeCmd、lastCmdResult、acceptTask/submitAnswer及角色动作回执。保留原样空字符串、null、缺字段的区别；不要跨队/跨流拼接。用于确定beta消失、新任务出现、旧回执到达的真实顺序。
3. r234–237的teamOur.type及完整己方角色id/type/HP、robot观测、终局字段（若平台提供）；同时附官方battle/half信息（没有则unknown）。用于区分基地真实摧毁、角色列表清空与观测归属。

## 后续确定性实施合同

- 若上述序列在当前引擎可复现：先新增按同一观测顺序的回归；活动任务不能重复领取，新generation不可被旧回执/旧求解器重置或污染，计数只按实际派发，不能扩大动作或调用额度。修改范围限task_context/team_agent/llm_router/task_agent及测试，逐项独立验证后再集成。
- 不照搬北京答案、TOKEN、题目名字或摘要中的b_spec状态机；以通用观测、代际及回执关联决定动作，不让旧回执冒充新题结果。
- 基地确证被毁后依据墙/炮/工人轨迹另立防御复现，不能为修日志臆改机器人伤害或提高血量。若只是观测缺失，修字段解释和诊断，不能伪造存活单位。

## 本轮实施

增加真实规划器/路由器及JSON恢复的手定切换序列测试，覆盖旧任务消失、新任务出现及旧回执迟到；这是按摘要构造的独立验收，不冒充原始公司请求重放。发现确定性失败则先修复再冻结。

已实际复现：beta工具结果在空题轮因task_no_longer_confirmed被隔离；nanjing发出新工具命令后同结果再次出现，无nonce的single_inflight关联将其送入新题evidence和prompt，双阵营均失败。其余旧回合不回滚新题、活动题不重领、真正空题结束等测试通过，因此不修改正常代际重置或凭空增加两轮宽限。

修复：仅对无nonce的任务工具回执，若哈希等于当前命令发出前已因旧任务失效/代际变化隔离的结果，且保留的请求记录能证明旧generation不同，标记ambiguous_prior_task_output并继续等待当前请求，保留原截止时间。禁止全局按文本去重；正常成功读取相同内容仍允许。新鲜输出到达可继续，持续歧义则按原期限过期。该保护不覆盖从未见过的迟到输出或已从有界历史淘汰的证据；相同内容也可能恰好是合法新结果，此时宁可标记歧义等待，不猜测归属。完整输出nonce协议可另行评估，不能静默扩大命令预算或改写复杂Shell。

期限沿用现有语义：命令N轮发出，非空合法回执可在N+WAIT_ROUNDS最后一轮被接收，之后过期；模糊旧输出不修改sent_round，不延长该窗口。现有空回执分支在N+WAIT_ROUNDS结束等待，本轮不全局改动这一差异。

针对角色全空日志增加our_roster_state，区分明确空列表、缺失/非法列表与存在的角色；明确空列表时empty_reason=observed_empty_team_roster，不再只写unclassified，也不据此断言基地死亡。实际基地HP=0仍单独记录base_observed_dead。诊断不改变响应或游戏动作。允许修改diagnostics及独立测试，运行相关和完整检查后交付新包；来源Issue保持打开，不宣称官方PASS。
