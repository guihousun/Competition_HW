# Issue 38：check 兼容覆盖与胜负记录核对

类别：工具实现缺口／证据核对。规则依据 R01、R07，官方接口文档v1.0 §1.1 lastCmdResult、§1.7、§2.1；不改变判题内容、答案、资源限额或胜负规则。

## 分诊：胜率结论存在冲突

2026-09-21从连接的GitHub工具读取#38正文及全部评论（0条），同时读取#37正文和两条评论。以下同场结果相反：

| pk | #37正文或公司补评 | #38 |
|---|---|---|
| 668299 | 胜 | 负，我方B |
| 668436 | 胜 | 负，我方B |
| 668586 | 胜 | 负，我方B |
| 668746 | 胜 | 负，我方B |
| 668891 | 负 | 胜，我方B |
| 669702 | 胜，2373:374，我方B（评论5748455923） | 负，我方B |

这些冲突集中在标为我方B的行，提示A/B比分转为我方视角时可能未交换，但没有原record.json就不能认定具体解析错误。暂不能采用“2胜18负/10%”作为已证实回归，也不能认定#37全部正确。重叠pk按比赛ID及半场去重，不把8场和20场当独立样本。

公司侧验收：为上述6场及其余14场导出脱敏原始队伍ID、A/B比分、字段名、winner/teamID（若有）、记录状态、半场标识；明确比分原来是A:B还是已经是我方:对方。以teamId4474一次映射，不能重复交换。未知保留unknown。需用A胜/A负/B胜/B负/平局/缺字段手定记录验证分析器，然后重新汇总。不得仅凭totalScore推断赛事胜负。

## check定位证据

根目录包SHA256仍为388582d9649c1253c0484414502562aae57b725f9b3c40b7e9822a2474fdac55，源码2296d9a。已核验归档无check/check.sh/check.py成员；包内task_tools.py/task_agent.py与当前读到源码一致（比较时只规范检出换行）。失败文件为公司所述任务工作区提供的check，不是我方run.sh。其原始CRLF来源尚未查证，错误只能直接证明这次执行找不到带CR的解释器。

当前已有结构化check工具，在内存规范CRLF、使用原脚本内容、不改原件，保留工作目录和真实退出码。选择器实测：

- 裸 `./check`：预转换不命中、错误后自动恢复也不命中。
- `cd /tmp/ws && ./check`：预转换命中结构化check。
- `cd /tmp/ws && mkdir -p logs && ./check`：先收到关联126错误后，只重试check，不重放mkdir。

11项现有反馈回归在Linux全部通过，含真实POSIX脚本执行/原文件不变/修改前缀仅执行一次；Windows10通过1项跳过。该证据证明已有兼容机制及覆盖边界，不证明公司4个任务已经修好。

## 下一阶段实现合同（尚未交付）

1. 对严格裸 `./check` 增加兼容路由，当前目录只能在实际执行它的官方沙盒内取得，禁止在HTTP宿主机推断或沿用上一题cwd。
2. 不扩展到任意Shell、参数、重定向、管道、命令替换、复合修改语句；不重放修改前缀。保持现有绝对路径check行为。结构化check使用公开任务路径时沿用原校验。
3. 用原始脚本内容只在内存处理CRLF，保留解释器支持范围、工作目录、真实退出/超时、命令长度和重试预算；不可伪造TOKEN/通过率或修改官方check文件。
4. 新增独立预期测试：真实沙盒cwd下裸check成功、非零退出仍失败、缺文件、非脚本/链接、不同任务目录、字符预算、复杂命令不转换、重复错误有限恢复。真实TaskAgent收到模型run→最终派发→工具回执可关联，不把提案计作执行。
5. 补有界任务诊断：题目generation、提交回合、答案hash/类型/长度、是否截断、错误回执来源、check路径来源、兼容恢复是否实际派发/结果。原题和答案需要完整调试记录时沿用已有脱敏导出，不能把凭据/完整任务资料无界打印到控制台。
6. 审核及专项/fullsuite通过后，从冻结源码构建新包，Python3.11.10实际HTTP验证，再交付固定codex/sgh。当前只提交Spec和复现证据；当前包仍未覆盖裸check。

## 公司端最小补充（不阻断本地覆盖修复）

四场中任选一条：实际完整executeCmd（“例如/bin/bash ./check”不等于原命令）、任务代际、发出回合、lastCmdResult原字段与下一轮实际命令。用于判断是否进入现有恢复、是否超预算、是否已恢复后又失败。无需公司端自行定位根因。

CRLF失败不等于整场输掉；#37甚至报告带CRLF的669702获胜。value_mismatch仍只证明答案不正确/不完全正确，不能猜成确定字段。保留来源Issue开放，未宣称内网PASS。


## 2026-09-21 实施更新

上述裸check实施合同已在源码043918d完成，专项/全量及真实包HTTP结果见[交付报告](../reports/issue42-44-recovery/README.md)。保留前文作为旧版本定位证据；胜负归属冲突与官方模板CRLF来源仍未确认。
