# 默认模拟地图的中央商贩

类别：用户补充对齐 / 本地几何假设 / UI工具。依据 R02（任务书 §4.1、接口 §1.2）、R06（任务书 §4.6）。

用户此前明确“小贩在地图正中央”，并确认“武器商店在小贩右上方一点”；2026-09-16 再次要求核对。原始 `docs/request.txt` 为 vendor=(20,16)、weaponShop=(25,20)，与两条描述一致。官方正文未定义所有地图通用的精确坐标，因此采用样例作为默认模拟定位，不升级为比赛协议常量，不修改原文。

- 新生成场景采用 `central-sample-v1`，双方、种子和波次配置不改变商贩绝对位置。
- 保留随机生成流程及多数旧矿点/任务点；仅中立点冲突时移动相关点，并考虑任务点2双格足迹，避免重叠或压到建造区域。
- `legacy-random-v0` 仅用于明确复现历史随机几何；旧录像/导入请求不搬动商贩，不静默套用新标签。
- 元数据只放 `_demo.market_layout`，正式策略仍由观测 zones 查询目标，无中央坐标捷径；观测去除私有元数据。
- 页面注明当前几何来源；默认入口、LLM演示、双队对局共同使用生成器。更新后重启服务，新建对局查看。
- 旧报告属于旧随机商贩几何证据，不能直接与新地图比较为策略提升。

验证：独立坐标断言、多种子双阵营/波次、商贩及双格任务点占位不冲突、旧布局复现、导入快照不迁移、策略不读取 `_demo`；全量回归、真实HTTP新场景与双方短局。精确地图坐标仍待正式请求确认。
