# Issue 46 — attack_map 已观测布局与显示坐标对齐

基线：4cb0a83710ffe99e6013e8c38e8b13d51648caf1。来源：用户 Issue46 正文（2026-09-21，5场 attack_map 首帧摘录），docs/request.txt；官方原件不改。
类别：本地假设的证据校准 + UI 工具修复。R02（任务书4.1–4.3、接口1.2–1.5）、R07任务点占地。并非完整官方地图认证。

## 设计

1. 新增独立地图档 `attack-map-observed-v1`；网页新建/本地录制默认此档，可选择 `seeded-local-v1` 保留旧随机压力地图。底层 scenario() 历史调用缺省仍保留旧档，避免悄悄重定义旧实验；调试API显式选新默认。波次profile和map_layout是独立选项。正式策略不读固定地图档或_demo。
2. 坐标统一用协议左下原点。Issue网格摘录转换 y=31-grid_y。基地左上锚点 challenger(9,22)、defender(30,10)，占地2×2向右下。小贩(20,16)、武器商店(25,20)原已正确。任务点左锚：c1(14,14)、c2(16,17)横2格；d1(23,14)、d2(26,17)横2格。固定已确认要素不随seed或己方阵营镜像重定位。
3. challenger初始工人(8,21),(8,22)，开拓者(8,23)；defender已知一工人(30,8)。其他defender角色仍采用合法附近位置并明确为本地补位，不能伪称来自完整首帧。station角色的ID是本地ID，不能按ID断言阵营。允许双方固定基地、相同中立地图。
4. 原文数字mapType23/24/25仅保存坐标与未解析类型证据；没有枚举映射时不能当永久墙/水/矿。当前矿点沿用本地生成并标识待校准；不能说全图逐格完全一致。元数据列出已确认项、未知类型、未确认地形/资源/完整出生/建造区域/刷怪点，覆盖仅attack_map五场。其他地图名不套用。
5. 新档在taskworld.attach、刷怪配置、首帧vis_prev生成之前完成布局。中立点真实占地互斥，不在基地/造区/角色上生成矿。导入旧快照、回放不搬动其坐标；元数据随录制保存，缓存键必须含map_layout。
6. 显示使用矩形占地：station2×2从左上锚向右下，任务点2是2×1；绘制、点击、聚焦一致。现有方形actor.size不能决定任务点宽高。明确本地重模拟不是官方录像，同轮炮台数量不保证相同；新档也不意味着任务/战斗已复刻官方。

## 验收

- 手算坐标与反转回grid断言；多seed/左右阵营已知要素不变；2×2基地、2×1任务点、出生不占用、未知数字类型不擅自加入阻挡。
- 旧seeded档可复现不变；非法档报错；debug新建/series/异步recording/LLM场景选档及缓存身份，导入不改位置；只读渲染几何测试覆盖画面与拾取。
- 新档左右各跑一个昼夜检查合法动作和序列化；全量unittest及相关前端检查。非策略调参，不以新地图提高分数为验收。
- 报告记录源SHA/测试范围，旧城墙六场结果仍属于旧seeded地图，不能改标为新attack_map结果。

## 向公司请求的补证（不阻断以上有依据的实施）

可公开脱敏的完整start事件/对应首轮HTTP请求、mapType图例（特别23/24/25）、完整双方roles、地形资源/合法建造区/机器人首夜刷点；另一个地图需独立首帧。仅据现有材料不推断缺失数据。
