# #39/#40：任务吞吐定位及实机统计核对

类别：策略/工程诊断。依据 R01/R07、接口§1.1/§1.7/§2和任务书§五；不改变官方额度、超时、答案或结算。两Issue完整正文及全部评论（均0）已由GitHub连接器读取。当前包源码2296d9a。用户已授权任务纠错、经济与防守改进，本轮不派DSH候选。

## 已核对的源码事实

- llm_router.ingest每轮调用_ingest_channel；非空回执下一轮即可处理。WAIT_ROUNDS=3用于空回执过期/迟到隔离，并不是每三轮检查一次。不为了加速把它改成1，以免丢弃合法延迟回执。
- team_agent.solve在remaining<=1且没有submit提案时停止新的模型/工具操作；现成submit明确例外，随后仍校验契约并提交。remaining<=2并非新的放宽条件，先复现再改。
- 领取确认等待上限和拒绝退避只适用相应路径，不叠加到每次正常确认或每个工具步骤。需逐episode事件证明实际触发。
- 自进化任务不计普通每日3次LLM限额。TaskAgent的工程次数上限和无回执超时须分别统计，不能混称官方限流。

## 统计冲突及证据边界

#40报pk-669702我方2373分胜、pk-669704我方2144分胜；#38同两场记负。#37与#38亦有同场相反胜负。#40中2144分场同时记录0次submitAnswer，不能仅凭总分断言其为任务分。#39“8场4W4L”列出的胜负分差数量也超出8场；47个prompt不清楚是发出次数、请求与回执合计，还是文本匹配次数。

公司端需按battle_id/half/teamId/source_commit建立唯一行：原始teamAid/teamBid、scoreA/scoreB及结算字段名、我方角色映射、首末轮、日志完整性。以同一行核对#37/#38/#40冲突，保留旧报告更正轨迹。不同半场、不同代码、重复digest和对手日志不得混算。最新8场与20场窗口重叠，不能作为独立新样本相加。

## 实施与验证要求

1. 定位pk-667884单episode：accept、任务发布、每次发出prompt/executeCmd、关联回执、inspect/重读、submit及反馈、end。分别统计正常下一轮回执、空等、关联拒绝、重复读取、工程上限、回防中止、真正超时；不用关键词频数推算。
2. 保留现有下一轮接收与末轮就绪提交回归。只有独立复现额外空转才删等待；不得盲改3→1或扩大官方动作数。
3. 解题加速优先合并已知路径下的合法读取/计算和验证；不能硬编码北京等题答案、猜token，不能以check通过直接推断submit满分。规则求解仅对明确题目契约及真实工具结果启用。
4. CRLF按#38实际沙盒./check报错复现。现有结构化check有兼容处理，核对哪些调用未覆盖；避免把模板check当成我方包内文件，避免重放修改前缀。
5. 单人轮换三炮与任务优化在现有solo-three-guns隔离树继续；阶段性输出必须注明已实现/已测试/已打包，不能把Spec推送当作策略已部署。

## 公司端最小补充

请在已有本地日志中生成上述关联摘要，不用等待新一场。优先pk-667884、pk-669702、pk-669704，附脱敏题文/答案格式、实际提交或摘要哈希、执行命令与真实回执。缺字段记unknown；原始材料未提供不阻断可独立复现的代码修复。

当前结论：任务低提交密度值得调查；固定三轮等待说法不成立；高分来源和胜率仍待统一映射。无官方PASS声明，来源Issue不关闭。
