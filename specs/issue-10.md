# Issue #10：可复现参赛包与诊断证据

来源：[Issue #10](https://github.com/guihousun/Competition_HW/issues/10)，以及用户 2026-09-14
在 Codex 任务中的澄清。开发基线 `aa6eb3e79c96e15e40ebc0c58817dd031ac6c933`；
这不是对实际被拒包版本的认定。

## 问题与目标

用户明确：256 轮日志来自官方示例；我方程序通过直接上传压缩包提交，平台提示
`cant get package`。没有被拒文件与平台内部日志，不能确定失败原因。
先提供经本地完整验证、包含精确版本身份的候选，让下一次测试可追溯并继续诊断。

## 范围与边界

类别为本地工程工具、运行诊断与使用说明。依据 `docs/DEVELOPMENT_RULES.md` R01/R08
及《接口文档》开头运行要求；R02–R07 的行为与数值不变。

- 新增可复现打包、校验与安全解包工具；只打包已提交的允许文件。
- 保留 `CoreGeek/main3.py` 示例入口与 `bash run.sh <port>`，运行代码保留嵌套目录。
- 清单记录代码 SHA、逐文件哈希和别名来源；压缩包 SHA256 通过独立文件提供。
- 启动记录版本；每次请求输出有界的观测摘要。未知值保持未知，不制造动作或推断败因。
- 官方响应不增加字段，诊断失败不能改变原响应；不增加全量日志采集或外部网络请求。
- 交付下载、启动、平台分级测试、反馈模板；更新缺失证据时的推进规则。
- 官方原文、原始示例压缩包、策略、结算与前端行为不修改。

## 验收

1. 相同提交两次构建字节一致，未提交修改不入包，既有输出不覆盖。
2. 拒绝伪装 ZIP、gzip 截断/CRC 损坏、不安全路径、特殊/重复成员、清单及哈希不符。
3. 真实解压候选后，从包外工作目录启动入口；GET 本地页面、POST 官方样例可用。
4. 包内启动身份与候选提交、清单一致；缺血量/冷却/机器人字段不误报死亡、就绪或清空。
5. 诊断失败及异常输入的响应契约有独立回归；执行完整 unittest 与 diff 检查。
6. Codex 审核后发布候选 PR 和带 SHA256 的 Release 资产，不把本地验收写成内网 PASS。

## 发布与剩余证据

目标分支 `codex/sgh`，候选分支 `codex/issue-10-diagnostics`。最终精确提交、包 SHA256、
测试结果和下载链接写入发布评论。DSH 使用既有 deepseek-flash/max 会话实现；Codex
承担设计、独立验证、文档完善与发布。未关闭来源 Issue，不自动合并运行入口改动。

待公司复测：是否接收该包、实际启动方式/Python 版本、能否看到诊断行、接口与完整对局结果。
打包名称和兼容布局依据原始示例选择，并非已证实的平台验收合同。
