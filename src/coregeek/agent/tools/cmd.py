"""`executeCmd` —— 把命令原文搬进响应顶层的 `executeCmd` 字段。

本地不执行任何东西：执行者是判题器的沙盒（本回合执行、限时 15 秒，结果下一回合从
`lastCmdResult` 回来；沙盒故障与命令超时不计异常 ⇒ 不吃红线）。不做校验、不做清洗、
不做解释 —— 原文里有什么（换行、引号、重定向、`<`）就原样送过去。
"""


def executeCmd(cmd: str) -> str:
    """交给判题器沙盒执行的那条命令 —— 原样返回。"""
    return cmd
