"""`executeCmd` —— 把一条命令**搬进**响应顶层的 `executeCmd` 字段。

⚠️ **我们不在本地执行任何东西。** 执行者是**判题器的沙盒**：`executeCmd` 是响应顶层字段
（接口文档 L208/210），判题器**本回合**执行它、限时 15 秒，结果下一回合从 payload 顶层的
`lastCmdResult` 回来（格式 `[exitCode:N]\\n<输出>`）。沙盒故障与命令超时**不计入异常次数**
⇒ 这条通道不吃红线，可以放心用。

所以这个函数就是"原文搬过去"这一步 —— **不做校验、不做清洗、不做解释**：命令原文里有什么
（换行、引号、重定向、中文、`cat < input.txt` 里的 `<`）就原样送过去，那是沙盒的事。
本地跑一遍既不安全（判题环境里没有我们要的东西）也没有意义（答案得来自判题器的沙盒）。
"""


def executeCmd(cmd: str) -> str:
    """交给判题器沙盒执行的那条命令 —— **原样**返回。"""
    return cmd
