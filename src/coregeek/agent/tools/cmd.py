"""`executeCmd` —— 把一条命令搬进响应顶层的 `executeCmd` 字段。

我们不在本地执行任何东西：执行者是判题器的沙盒，本回合执行、限时 15 秒，
结果下一回合从 payload 顶层的 `lastCmdResult` 回来。沙盒故障与命令超时
不计入异常次数 ⇒ 这条通道不吃红线。

所以这个函数就是"原文搬过去"这一步 —— 不做校验、不做清洗、不做解释：
命令原文里有什么（换行、引号、重定向、中文、`cat < input.txt` 里的 `<`）
就原样送过去。
"""


def executeCmd(cmd: str) -> str:
    """交给判题器沙盒执行的那条命令 —— 原样返回。"""
    return cmd
