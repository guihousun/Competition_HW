"""Agent 包：与判题器的 LLM、与它的沙盒打交道的全部。

唯一的实例 = `AGENT`：跨回合状态（SOP 流程表、新闻指纹与价格期望、任务内会话）都在
它身上 ⇒ 每回合得用同一个它，新建等于失忆。构造发生在 import 期，只赋空值、不读文件。

另有一处跨回合状态不在实例上：`cmd_explore` 的沙盒探查队列（模块级，**整场存活** ——
探明一次就用到进程结束，任务换了也不复位）。
"""

from .agent import Agent

#: 全项目唯一的 Agent 实例：跨回合、跨任务地活着。
AGENT = Agent()
