# 《未来战争》每日行为策略（按天梳理）

> 本文基于 `docs/任务书.md` 规则 + `docs/design/strategy.md` 当前实现，按**每一天**的局势分析来组织
> 工人 / 开拓者每天应该做什么。代码是唯一权威，本文是"每天该干什么"的导读。
>
> 核心方针（贯穿全10天）：
> 1. **第 1 天必须把墙砌满**（环 18 格 + 封口格，共 19 格含封口；实测 R41 收官）；
> 2. **每天最高优先级 = 修墙**（半血墙必先修，再砌墙/建武器/经济线）；
> 3. **工人不能什么都不做** —— 一件干不了就落到下一件；
> 4. **杀机器人是极大得分手段** —— 夜里三座炮必须满员操炮、最大伤害落点；
> 5. **5 次异常即出局** —— 所有"打不了"的情形一律退化成空指令，绝不冒泡。
>
> 每天结构：白天 70 回合 + 夜晚 60 回合。白天 = 建造/采集/卖矿/做任务；夜晚 = 操炮防守。
> 三座武器固定阵形：**前排上方相邻两格 = 2 火箭**（一人可同时操作两座），**前排下方一格 = 加特林**。放弃电磁炮（单目标、对满血机器人不穿透，群体价值最低）。

---

## Day 1（开局日）—— 关键日：墙必须砌满

### 白天局势（回合 1–70）

- **初始资源**：3 角色 + 基地 + **75 金币**、0 武器、0 围墙、0 石头。
- 75 金 = 刚好 3 座武器（25×3）⇒ 开局最优解锁定为**先造满 3 武器**。
- 墙未砌，第 1 天是**唯一不用临时门机制的一天**（环上缺口 = 真的没砌，由 `_ring`/`_build_walls` 处理）。
- 任务点全部冷却完毕，开拓者可立即接任务。
- 实测：环 **R41 收官**、三座武器 **R44 建满**（`DayOneFinishTest`）。

### 工人（2 人）白天动作链

**优先级**：建武器 → 砌墙（含封口）→ 卖矿 → 升级 → 采闲矿

1. **建武器（三座必建满）**：份额 = `len(roles) − len(weapons)`，>0 才产出名额（全局最多 3 座）。
   - 顺序由 `WEAPONS_BY_SITE` 决定：火箭（前排上格）→ 火箭（前排上格，相邻）→ 加特林（前排下格）。两火箭相邻 ⇒ 一人可同时操作两座。
   - 站位即建造位（`step_toward` 恰好停在贴着目标的一格）。
   - 金币 ≥ 25 且落点空才建；建不了/走不到就落到下一件。
2. **砌墙（最高优先级，共 18 格 + 封口）**：
   - **正面列 6（含封口格）+ 两侧行各 5 + 背面 2**，背面中间 2 格是永久的门。
   - 顺序 = 沿环走一圈（正面列 → 一侧行 → 背面 2 格 → 另一侧行 → **封口格排最后**）。
   - **封口格 = 正面列正中（左半基地 `(13,24)`）**，白天开着方便通行、天黑前砌上封死。
   - **石头只认石矿**（铁/铜砌不了墙），取最近的；每工人背包保底留 1 块（封口用）。
   - 采几块按回合预算现算（`_stones_to_mine`）：`走到矿 + 采 k 块 + 从矿回工地 + 手里 s 块的"挪+砌" ≤ 白天剩余 − 5`。
   - **闸门 `gated`**：砌满之后有人出不去 ⇒ 这回合一格都不砌、待命（不发指令也不落兜底）。
3. **卖矿（墙未砌满时不卖石头）**：石头只在"墙砌完了"之后才卖，多余的石头才能卖 `石头数 − 1`。
4. **升级 / 采闲矿**：第 1 天金币刚花完，无余力升级；墙砌满后可采闲矿备第二天用。

### 开拓者（1 人）白天动作链

1. **走到最近一个能接的任务点**（己方任务点周围 1 格内）⇒ `acceptTask`。
2. **接上任务后被钉死**：不能离开任务点周围 1 格，否则任务作废 ⇒ 每回合 `submitAnswer`（判题器按"提交过的通过率最高"算分，反复提交是预期用法）。
3. **任务点全空（都在冷却/做完了）⇒ 真空闲**：领买券/用券差事；`_sell_ore` 兜底（但开拓者没矿，基本退役）。

### 夜晚局势（回合 71–130）

- 第 1 夜机器人最少（应该只有小机器人，**具体表格未给出，需实测**）。
- 三座武器都已建好（L1），**2 个角色即可操作全部 3 座**（1 人操双火箭、1 人操加特林），第 3 人（开拓者）可自由行动。
- 开拓者若任务未完成 ⇒ 被钉死在任务点，但**不影响火力**（2 工人照样操满 3 座炮）。

### 夜里所有角色（含开拓者，若未钉死）

- **回炮位**：选最近且没被本回合别人认领的**一组**武器（2 火箭一组、加特林单独一组），走到操作位就**认领整组**。组内多座时挑冷却好了的那座开火（火箭 3 回合冷却 ⇒ 两火箭交替 = 一人操两座）。**2 个角色即可操作 3 座武器，释放开拓者夜间行动。**
- **开火 `_fire`**：
  - **火箭**：落点任选（导弹不被阻挡）⇒ 全场算账，中心 20 + 周围 8 格溅射 10，取总分最高的格子。
  - **加特林**：落点在某台机器人身上，取有效伤害 = `min(伤害, 剩余血)` 最高的，并列打近的。
- **只打打我方基地的机器人**（`_foe_robots`）：`target_team` 明确是对方阵营的机器人 ⇒ 跳过（火箭射程远尤其需要，否则导弹常砸到对方半场的机器人簇里——既浪费火力、又帮对方减轻基地压力）；`our_team` / `target_team` 缺失 ⇒ 照打（安全降级）。
- **冷却中不打**；字段缺失解析成 -1 ⇒ 不算冷却。
- **`assigned` 记账**：先开火的炮把估计伤害记在机器人身上，后开的按剩余血算 ⇒ 不挤同一个将死的目标。
- 没敌人/炮在冷却/够不着 ⇒ 站着待命，什么都不发（绝不能在白天发 `attack`，那是非法指令）。

---

## Day 2 – Day 9（常规日）—— 临时门 + 升级线 + 任务循环

### 白天局势（回合 `(d-1)*130+1 .. (d-1)*130+70`）

- **环开局必然是满的** ⇒ 环上一切缺口只可能来自我们自己的 `remove` ⇒ 临时门机制启用。
- 每个白天的第 1 回合发布**世界新闻**：官方消息（影响卖价）+ 民间传闻（宝藏线索）。
- 任务点：开拓者若上一天任务未完成（开拓者死亡/超时/主动结束）⇒ 进入 **30 回合刷新冷却**。
- 机器人浪潮随天数增加，**当晚可能出现中型/大型/BOSS**（具体曲线未给，需实测）。
- 武器 / 基地升级券已可买（Day 1 卖矿攒了钱），升级线开始推进。
- 角色可能阵亡（夜里被机器人打死）⇒ 第二天白天开始后 20 回合可在基地复活，**背包保留**。

### 工人（2 人）白天动作链（**修墙是最高优先级**）

**优先级**：**修墙** → 砌墙 → 建武器 → 卖矿 → 升级 → 采闲矿

1. **修墙（`_repair_line`，最高优先级）**：
   - 半血墙（`health×2 < WALL_MAX_HP[level]`）**包优先、重建兜底**：
     - 持 `WallFixer` 包 ⇒ 走到最近的半血墙 `use WallFixer`（10 金回满血）；
     - 没包但买得起 ⇒ 去商店 `buy`；
     - 都不行但贴着半血墙且有石头 ⇒ `remove` 重建。
   - **排在砌墙与建武器之前** —— 防御面上有薄弱格时别的都靠后。
2. **砌墙（`_build_walls`）**：
   - 环已砌满 ⇒ 主要任务 = 修墙 + 临时门补墙。
   - **临时门（`_dig`）**：工人贴着一格墙、拆了它能少走 ≥ `HOLE_MIN_SAVING`(5) 步 ⇒ 拆它当通道。
     - 闸门：白天 ∧ 工人 ∧ `_after_first_day` ∧ **环已砌满**（⇒ 同时最多一个洞）∧ 本回合没人拆过 ∧ 有石头 ∧ `day_rounds_left > HOLE_MIN_LEFT`(30)。
     - 末段 `day_rounds_left <= HOLE_PATCH_LEFT`(15) ⇒ 必须补回（天黑前封死）。
     - ⚠️ 工人阵亡 / `gated` / 没石头 ⇒ 当天补不上，夜里墙上留着我们自己的洞（夜里不能 `build`）。
     - **白天末尾强制补墙**：`day_rounds_left <= HOLE_PATCH_LEFT` 且环上有缺口时 ⇒ **禁止落兜底**去卖矿/采闲矿，哪怕这回合砌不了也必须走向缺口待命，绝不让墙带着缺口过夜。
   - 站在目标格上就先挪开一格（`_step_aside`），这一回合不砌。
3. **建武器（若阵亡导致份额恢复或损毁重建）**：份额 `need = len(roles) − len(weapons) > 0` 才产出。
4. **卖矿（`_sell_ore`，四道门）**：
   - 有货（从 `SELLABLE` 石/铁/铜里挑收购价最高、同价件数最多的那种）；
   - 有小贩；够本（`货值 ≥ 2 × BFS 步数`，贴着小贩就跳过这条）；回得来（`走到小贩 + 从小贩回基地 ≤ 白天剩余 − 5`）。
   - 价格逐回合从载荷读，**不写死**（铜 > 铁 > 石只是样例价目，新闻会让它波动）。
5. **升级（`_upgrade_line`）**：
   - 跑腿者唯一：持券的那个工人；没人持券 ⇒ 名册第一个工人（别的工人照常卖/采）。
   - 只在白天跑，整趟（走到商店 + 买到武器 + 买/用两个动作回合）来得及才出发；走不到 ⇒ 待命。
   - **升级优先链 `UPGRADE_CHAIN`**：`gatling→2 → rocket→2 → rocket→2 → gatling→3 → rocket→3 → rocket→3`。
   - 判据是群体打击：加特林 +1 颗子弹 = 每回合 +10、无冷却、弹道必命中、两颗可分打两台、射程 +2 更早接敌（一夜最多 +600）；双火箭各升一级，+1 枚导弹对簇约 +30/齐射（3 回合冷却，依赖扎堆）。同类出现两次 ⇒ 两座火箭都升。
6. **采闲矿（`_mine_spare_ore`）**：先把"走得动、回得来"的矿筛出来、再按价挑；回程参照 = 最近的武器位。顺带 `_detour_sell`：去矿路上绕去小贩比直走多花 ≤ 2 格 ⇒ 先绕过去。

### 工人收工闸门（跨在 ④⑤ 之上，第 33 步）

环砌完了（`_ring` 为空）且回炮位的 BFS 步数 ≥ 白天剩余 − 1（或不可达）⇒ 这一天不再干活、**只发 `move`** 往最近的空闲炮位挪。
**绝不能复用 `_defend`**：那是夜里那一支，白天发 `attack` 是**非法指令**（红线）。

### 开拓者（1 人）白天动作链

1. **任务点非空** ⇒ 走到最近能接的点，贴着就 `acceptTask`（"已经贴着就领"与"走过去"分两步判）。
2. **任务点全空（都在冷却/做完了）⇒ 真空闲**：领**买券 → 用券**差事（跑腿者优先级 = 持券者 > 真空闲的开拓者 > 第一个工人）。
3. **接上任务之后**：每回合 `submitAnswer`，**从来不移动**，**空答案不发**。
4. **任务线契约**（`task_channel`，第 43 步修订）：
   - 任务提问不与命令同轮（同一轮既提问又发命令 ⇒ LLM 拿着过期结果作答）；
   - 压缩闸门在回合最末尾，判据链算完 `prompt` 仍空才填压缩请求；
   - ②必须压在③前面（`lastCmdResult` 不粘，`llmResp` 可能粘）；
   - ④是"修饰符"不是"分支"；⑤必须用 `answer_of` 判，不能用"取不出命令"判。

### 夜晚局势（回合 `(d-1)*130+71 .. d*130`）

- 机器人数量随天数增加；购买召唤令叠加（但慎用，避免给对面送 `score2`）。
- 残余机器人第二天早上第 1 回合自动清除。
- 武器若已升级（L2/L3），打击侧需跟上等级（**当前已知缺口**：`GATLING_SHOT` 等没跟 `Weapon.level` 联动 ⇒ 升级后目标选择退化成"按 L1 估计"；`Attack.to_wire` 恒发**一个** `targetPos`，而约定是"长度 = 武器当前等级" ⇒ 升级后可能算"指令非法"）。

### 夜里所有角色（含开拓者，若未钉死）

同 Day 1 夜里：回炮位 → 选目标 → 开火；冷却中/无目标 ⇒ 待命不发指令。

---

## Day 10（终局日）—— 生存分最高

### 白天局势

- 第 10 天存活一天值 **100 分**（`score3 = 10 × 10 × 1`），**全力防守**。
- 若之前有阵亡角色且复活时机允许，确保夜里 3 人满员操炮。
- 任务窗口若还能开就开（哪怕只赚部分积分）；不能再开就放弃，专心防御。
- 卖矿攒钱买消耗品：`DizzyWeapon`（眩晕 5 回合，100 金）、`Bomb`（3×3 内 100 伤害，100 金）—— 终局夜可救场。

### 工人（2 人）白天动作链

1. **修墙（最高优先级）**：把所有半血墙都修满血。
2. **砌墙**：临时门末段必须补回（天黑前墙必须封死）。
3. **买消耗品**：金币 ≥ 100 且有闲 ⇒ 买 `DizzyWeapon` / `Bomb` 备夜里救场。
4. **建武器 / 升级**：份额 / 金币够才做（终局不一定划算）。
5. **卖矿 / 采闲矿**：兜底。

### 开拓者（1 人）白天动作链

1. 任务点若可接且能完成 ⇒ 接任务（最后一份积分）；
2. 否则空闲 ⇒ 买券用券（最后一波升级）；
3. 都不行 ⇒ `summonTreasure`（若已推断出宝藏地点/祭品/时间）。

### 夜晚局势

- 最强一波机器人（含 BOSS，数量最多）。
- 必须确保 3 人满员操炮、墙满血、武器尽量高等级。
- 消耗品 `DizzyWeapon` / `Bomb` 在机器人簇集时丢出去（结算优先级 > 机器人移动）。
- 火箭 L3 射程全图，可打着对面基地/机器人刷新点（理论上唯一能越过前排的输出）。

### 夜里所有角色

同前几夜，但**全力输出**：优先打 BOSS（10 分/只）和大型（4 分/只）。

---

## 角色行为速查表

### 工人（2 人）— 每天

| 时段 | 优先级链 | 关键约束 |
|---|---|---|
| 白天 | **修墙** → 砌墙 → 建武器 → 卖 → 升级 → 采闲矿 | 一件不行才轮到下一件；**绝不能什么都不做**；环砌完后启用临时门 |
| 收工闸门 | 环砌完且回炮位步数 ≥ 白天剩余 − 1 ⇒ 只发 `move` | 白天绝不能发 `attack`（红线） |
| 夜里 | 回炮位 → 操炮开火 → 冷却/无目标则待命 | 1 人只能操 1 座；选最近未被认领的炮 |

### 开拓者（1 人）— 每天

| 时段 | 动作 | 关键约束 |
|---|---|---|
| 白天·任务点非空 | 走到任务点 ⇒ `acceptTask` ⇒ 每回合 `submitAnswer` | 接上任务后被钉死，不能离开任务点周围 1 格 |
| 白天·任务点全空 | 买券 → 用券（跑腿者优先级 = 持券者 > 真空闲开拓者 > 第一个工人） | `_sell_ore` 退居兜底（开拓者没矿，基本退役） |
| 夜里·未钉死 | 回炮位 → 操炮开火 | 选最近未被认领的炮；1 人只能操 1 座 |
| 夜里·被钉死 | 任务点上 `submitAnswer`（不移动） | 火力打折 1/3（由任务积分覆盖） |

---

## 旋钮速查（改策略先看这张表）

| 常量 | 值 | 作用 / 症状 |
|---|---|---|
| `WEAPONS_BY_SITE` | `(rocket, rocket, gatling)` | 种类与落点**绑死**；2 火箭相邻可一人操作 |
| `WEAPON_COST` / `WALL_COST` | 25 / 1 | 建造代价 |
| `ROUNDS_PER_STONE` | 3 | "每多采一块石头净花 3 回合" |
| `TIME_MARGIN` | 5 | 容错余量；第 33 步起距离走 BFS 真实步数，它现在是真余量 |
| 收工闸门 | `steps ≥ 白天剩余 − 1` | 白天末尾回程来不及就先往炮位挪 |
| `HOLE_MIN_SAVING` | 5 | 开洞能省这么多步以上才值得拆（用户口径） |
| `HOLE_MIN_LEFT` | 30 | 白天剩这么多回合**以上**才允许拆 |
| `HOLE_PATCH_LEFT` | 15 | 白天剩这么多回合**以内**必须补回 |
| `DETOUR_MAX` | 2 | 顺路卖矿的绕路上限 |
| 卖矿够本门 | `货值 ≥ 2 × BFS 步数` | 卖矿线唯一的旋钮 |
| `UPGRADE_CHAIN` | 6 项 | 升级优先顺序（我的判断，未实测） |
| `GATLING_SHOT` / `RAILGUN_ENERGY` / `ROCKET_CENTER` / `ROCKET_SPLASH` | 10 / 10 / 20 / 10 | L1 伤害，升级线做全之前是常量（升级后**不再准**） |

---

## 已知薄弱处（按嫌疑排序）

1. **工具协议与沙盒链路一次实盘都没跑过** —— 判题器的 LLM 认不认嵌套形状是最大的单点风险；
2. **答案格式完全未知**（通过率 = 正确字段数 / 全量字段数 ⇒ 多字段结构化答案），`prompt.py` 的措辞是唯一杠杆；
3. **`sell` / `buy` / `use` 三个动作没有实证报文** —— 若判题器报"指令非法"，先查这三处的 `to_wire`；
4. **`vendorShopList` 是收购价还是售价是推断** —— "价 0 的矿不去采"在售价口径下会误伤；
5. **卖矿阈值（`2 × 距离`）与 `UPGRADE_CHAIN` 都是拍的**，没有文档依据；
6. **可建造区公式、机器人刷新方向都只来自图片**；
7. **弹道是否被墙挡没有明写**（只明说火箭不被挡）—— 若被挡，整个防守阵形要重想；
8. **会话 prompt 无上界增长**（先不压缩，用户拍板）⇒ 长任务可能撞上"响应超时"这条红线；
9. **拆了墙当天没补上 ⇒ 夜里墙上留着我们自己的洞**（工人阵亡 / `gated` / 没石头），日志症状 = "**一条 `remove` 找不到对应的 `build`**"；⚠️ 已大幅缓解：白天末尾有缺口时强制走向缺口待命；但工人阵亡仍可能补不上；
10. **首日豁免的残留误判**：第 1 天被打断的残局（17/18）⇒ 次日起那个缺口被当成"门"、推迟到当天末尾才补；
11. **打击侧还没跟上等级**：升级后目标选择退化成"按 L1 估计"；`Attack.to_wire` 恒发一个 `targetPos`，升级后可能算"指令非法"。

每一处都留了单点回退：协议形状改 `chat.tool_of` 一处、报文形状改对应 `to_wire` 一处、阈值改一个常量、刷新方向翻 `grid._front_back` 一处。
