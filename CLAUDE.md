# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

华为云核心网第十届编程大赛《未来战争》参赛 Agent。判题器按约定拉起进程，每回合通过 HTTP 推送全量战场状态，要求 **5 秒内**返回一条合法 JSON 指令。判题器是黑盒——**没有"打一局试试"这回事**，改动的正确性只能靠本地实测 + 赛后复盘。

## 编码规则

1. **禁止冗余设计。** 加任何东西（模块、抽象、常量、配置项、防御性分支）之前先问："**这一步**用得上吗？" 用不上就不加。判据是"现在有没有第二个使用者/第二种情况"，不是"以后可能需要"。历史教训：上一版重写前搞了四层架构 + 加密日志 + 分层 import-lint + 后台落盘线程，而当时决策逻辑只有一条经济线，绝大部分基建在交付时都没被真正用到。**宁可后续重构，不要预先抽象。**
2. **每一步都要在 `docs/design/code-task.md` 留痕。** 每节三段：目标 / 产出（含验证结果）/ **仍生效的已知不确定性**。**新增一律只追加。**
   - **已完成的小节可以压缩**，只留目的与结果：与 `CLAUDE.md` 重复的规则推导、决策理由、已失效的「不做什么」、粘贴的逐条命令输出都可以删。
   - ⚠️ **仍生效的已知不确定性一条都不准删** —— 那是**重新推导代价最高**的部分，删了等于把学费再交一遍。已经兑现或被后续步骤推翻的可以删（就地写明"已被第 N 步撤销"）。
   - 未完成的下一步统一收在文末「**当前仍悬着的事**」一张表里，不散在各步。
   - 压缩时在文件头记一句"旧版见 git 历史哪个提交"。
3. **红线优先于一切优雅。** 见下节——5 次异常即出局，任何设计取舍在这一条面前让步。
4. 判题环境只有标准库：`pyproject.toml` 的 `dependencies` 保持为空，语法不得超出 3.11。
5. `logs/` 等运行期产物**绝不入库**。

## 硬约束

1. **`main3.py` 的文件名是平台契约，不要重命名。** 平台照官方 demo 的约定拉起它。改错的症状极具迷惑性：进程**从未启动** → 没有日志、没有报错、判题器也不报错，只是"所有单位一动不动"，而本地 `run.sh` 怎么跑都是对的，排查会被完全带偏。
2. **异常红线 = 5 次**，之后该队整场不再被调度。判题器只认三类：连接/响应超时、**响应格式错**、**指令非法**（字段缺失 / 动作码非法）。
   - 所有失败路径都必须退化成合法空指令 `{"roleCommandMap":{},"prompt":"","executeCmd":""}`——空指令**合法且不计异常**，宁可丢一个回合，不赌全队资格。
   - 注意区分：**指令非法**（计异常）vs **指令执行失败**（移动碰撞、攻击落点无目标 → 该条标记无效，**不计**异常）。
   - 响应三个顶层字段必须**永远都在**（官方 demo 只发了 `roleCommandMap`，别照抄这个）。
3. **动作有角色权限**（任务书 §4.4 表的**最右一列**）：`build` / `remove` / `collect` 仅**工人**；`acceptTask` / `submitAnswer` / `summonTreasure` 仅**开拓者**；其余动作全部角色可用。
   - 闸门在 **Action 的构造函数**里（`protocol/actions.py`）：`roleType` 不对就抛 `PermissionError`，**非法动作根本造不出来**。planner 侧接住它、丢那一条并告警，不连坐同回合其他角色（抛出去会变成"每回合空指令 → 全队冻结一整局"，现象与 `main3.py` 改名事故一样难排查）。
   - 这类 bug 的特征是**本地全绿**（格式完全合法，只有判题器会说"不"），而 `collect` 被误发给开拓者会每天吃一个异常——**红线只有 5 次**。所以每加一个受限动作，都要在 `tests/test_protocol_actions.py` 补一条对应用例。
4. 判题器侧超时：建连 > 10s 或响应 > 5s。
5. **每回合的复盘日志（`app._log`）写在 `try` 里面，别挪出去。** 日志代码再不起眼也是代码，
   `web/server.py` 的 `do_POST` **不接异常**（`server.py:22` 直接 `handler(...)`）—— 逃出去连接就断了，
   判题器那边是"响应超时"（红线第一条）。放进 `try` 里，最坏只是退化成空指令（丢一个回合，合法、不计异常）。
   ⚠️ **第 26 步起策略变了（用户手改 + 拍板）**：① 局面 = **摘要单条**（**图例与整张地图退出日志**）；
   ② `LOG_TEXT_MAX` 400 → **40000**、`LOG_PROMPT_MAX` 1000 → **100000**（两度放宽，
   **日志基本不截**，"日志长度不能是数据相关的量"那条旧原则就此让位）。实测（第 28 步起
   prompt 是**单行** messages JSON，换行都被转义）：

   | 局面 | 行 | 字节 |
   |---|---|---|
   | 干净回合 | 7 | 593 |
   | 有回执 | 9 | 741 |
   | 提问那轮（题目 400 字） | 9 | **7701** |
   | **顶格（题目/回复/沙盒各 40000 字）** | 10 | **≈605276** |

   ⚠️ **第 41 步（压缩独立成轮）把提问那轮从 8382 降到 7701、顶格降到 605276**（同为
   **−681**：任务 system 卸掉搭车的摘要教学）。**命令轮现在多出一条 prompt 行（压缩
   请求）**：随原始历史线性变大、`LOG_PROMPT_MAX` 兜底截断留痕——上表没有这一行。
   历史：第 39 步 +881、第 38 步 +546、第 37 步 +467。**只有带 prompt 的那两行会动**；
   **跨步骤比绝对值没意义，同一次测量的增量才是信号**。量法与三个坑在 `app._log` 的
   docstring 里（顶格行的 `llmResp` 必须工具形状；量真 stdout 形状；每格先 `AGENT.reset()`）。

   ⇒ 若判题器**不读** stdout，顶格回合**一回合**就写满 64KB 管道 ⇒ 阻塞到响应超时 ⇒ 直通红线。
   **已知并接受**（第 26 步用户拍板"观察优先"；"最坏 ≤ 9300"的旧守卫随策略一起撤销，换成
   结构性守卫 `test_a_clean_round_stays_small`：干净回合 < 1500 字节）。**数字与 `app._log`
   的 docstring 对齐，别凭记忆写。**

   ⚠️ **有两条日志不在 `app` 名下**：SOP 那条走 `coregeek.agent.tools.sop`，
   任务行与沙盒行走 `coregeek.game.planner`（第 23 步搬过去的，那两样字段只在
   `task_channel` 的作用域里）⇒ 只盯 `coregeek.app` 的守卫**看不见它们**。
   守卫一律 `assertLogs(level="INFO")`（root）。**加任何新 logger 之前先想这一条。**

   ⚠️ **banner 是 `_log` 管不到的那一条**（`handle` 打的 `###第N回合###`）：它不受上限约束，
   数记录数时要单独算。

## 架构

骨架（逐步记录见 `docs/design/code-task.md`）：

```
main3.py              入口：读端口 → chdir → src/ 进 sys.path → 起服务。不放策略
run.sh                接口文档规定的 bash run.sh <port>；陪跑本地（python3 / py）
src/coregeek/
├── utils.py          **叶子模块**（第 22 步）：日志层的两条**规则** —— `LOG_TEXT_MAX` 与 `_clip`。
│                     不放状态、不放 logger、不放格式化函数。从 `app.py` 搬出来，见"复盘日志"那节
├── app.py            组装根：handle(bytes) + run(port) + 每回合复盘日志 `_log`。**红线所在**，异常一律退化成空指令
├── web/server.py     HTTP：收字节 → handler → 回字节。handler 由 app 注入，不认识游戏概念
├── protocol/         线上格式：读与写，**只有这里知道字段名**
│   ├── model.py      payload → Turn（容错解析；含判题器回执 `errors` / `lastRoundRoleActionResults`
│   │                 与**沙盒回执** `lastCmdResult`）
│   └── actions.py    BaseAction + 各动作（`move` / `build` / `collect` / `attack` / `sell` / `buy` / `use` / `acceptTask` / `submitAnswer`）。**创建即校验**，唯一懂线上动作格式的地方（`to_wire` 写、`describe` 读）。`describe` 第 20 步起**通用摊开**（有什么字段打什么，字符串值过 `app` 递进来的 `clip`），不再维护字段清单 ⇒ `sell` 的 `name`/`num`、`buy` 的 `name`/`num`、`use` 的 `name`/`targetPos` 自动就打得出来
├── agent/            **叶子包，只依赖标准库**：跟 LLM 说什么、怎么解析它的回复
│   ├── __init__.py   **不再是空的**（第 19 步）：`from .agent import Agent` + **`AGENT = Agent()`**
│   │                 —— ⚠️ **跨回合状态（两处）都在这一个实例上**（`_sop` 整场；`_context` 任务内）
│   ├── agent.py      `class Agent`：`_sop`（**流程表** `{流程名: 正文}`，第 37 步）/ `_context` / `_tools`
│   │                 （**表由实例构造**，`SOP2Prompt` 是绑定方法）
│   │                 + `chat`（**编排**：建/续会话、刷新 system、渲染）/ `hear` / `tool_call`
│   │                 / `SOP2Prompt(name, sop)`（先 `chat.strip_answers` 清洗再入库）
│   │                 / `sop`(property) / `reset`（流程表与会话**两处一起清**）。
│   │                 `tool_call` **只收具名参数**（第 37 步严格解析的推论）：认不出的名字
│   │                 **不进 resolved**、声明参数一个不少且非空才 `impl(**resolved)`
│   ├── chat.py       四个谓词（第 39 步起）：`tool_of`（严格，**只认嵌套形状**）/ `looks_like_tool`（宽）/
│   │                 `answer_of`（三级，**先挖全部结构块**：工具块 + 摘要块）/ `summary_of`（执行摘要，
│   │                 **best-effort**：取不到 ⇒ `""` 不回落）+ 一处清洗 `strip_answers`
│   │                 （挖掉成对的 `<answer>…</answer>`，唯一的调用者是 `SOP2Prompt`
│   │                 —— **SOP 正文里不许有这对串**，第 36 步用户口径）
│   │                 ⚠️ 标签解析全是模块顶部的 `re.compile`（成对性由模式里的 `</…>` 自带；
│   │                 只有 `looks_like_tool` 故意只认前缀 `<tool`）+ `_unescape`（五个 XML
│   │                 预定义实体、`&amp;` 最后换）。**模板不在这里**（第 37 步起在 prompt.py）
│   │                 **纯函数，零包内 import**
│   ├── context.py    `Context`（第 25 步；第 39 步起**存储全量、渲染有窗**）：消息表 + `render()`
│   │                 （**标准 messages JSON**，第 28 步弃了自造文本版式——用户实测效果非常差；
│   │                   第 39 步起渲染 = **题目 + `【历史摘要】` + 最近 `_WINDOW`(2) 轮**，
│   │                   窗口按"最后 N 条 assistant 及其后全部"切，短任务掐不着；
│   │                   沙盒回执 = `tool` 消息、纠错 = user —— 用户第 39 步分的角色）；
│   │                 `feed` 回灌 / `nudge` 重问收尾 / `hear` 记回复（**粘住去重**）/
│   │                 `summary` 字段（`Agent.hear` 被动提取）。
│   │                 **只依赖标准库，状态不在这里**（在 Agent 上）
│   ├── prompt.py     **system 消息的段模板与生成函数**（第 37 步，用户的文件）：六段
│   │                 （role定位 / 工具描述 / 输出格式 / 示例（第 38 步用户启用）/
│   │                   沉淀的SOP / 注意事项；第 39 步起输出约定含 `<summary>` 四槽那条）
│   │                 + `gen_system_prompt(tools, sop)` / `gen_all_tool_prompt` / `gen_tool_prompt`
│   │                 / `gen_sop_prompt`。工具块 `## ToolName - 名` 与流程块 `## SopName - 名`
│   │                 **由注册表/流程表生成、不手写第二份**；两个 `{}` 槽的模板正文里不许出现
│   │                 别的裸 `{}`（`format` 的 `KeyError` ⇒ 整回合退化空指令）
│   └── tools/
│       ├── __init__.py  只剩一段 docstring（注册表搬去 `Agent.__init__`）
│       ├── cmd.py       `executeCmd(cmd)` —— **原样返回**（执行者是判题器的沙盒，我们不在本地跑）
│       └── sop.py       `store(current, name, sop, *, stripped)` / `describe(name, received, stored, stripped)`
│                        / `SOP_MAX`（**单条流程**的上限）/ `SOP_FLOWS_MAX`（条数上限，超了丢最旧
│                        且日志点名）—— **规则**，状态不在这里；`stripped` 只进日志 ——
│                        它是"LLM 又把答案格式写进 SOP 了"的唯一信号）
│                        ⚠️ `LOGGER` 留在这里是有意的：logger 名 `coregeek.agent.tools.sop` **不变**
└── game/             领域与策略
    ├── grid.py       Pos / 8 方向 / **两个距离口径**（第 33 步）：
    │                 `Pos.dist` = 切比雪夫直线（**选点**用）/ `steps_between` = BFS 真实步数
    │                 （**回合预算**用，不可达返回 **-1**）；`step_toward`（**BFS 最短路**）
    │                 + 基地几何：`base_cells` / `weapon_cells` / `weapon_sites` / `wall_cells`
    │                 （正/背面方向判据在 `_front_back`，**只此一处**）/ `door_cells`（永远不砌的 2 格）
    │                 + `box_cells`（防御盒子 36 格）/ `step_outside`（迈出盒子的第一步，第 17 步的闸门用）
    ├── map.py        Map：格子矩阵（每格一个**类别**）+ `blocked`/`ores`/`vendors`/`shops`/`station`
    │                 + `render()`（**整块**：上下各一行 `—` 标尺 + 每行 `│`…`|`；
    │                   第 23 步把行号槽与两行数字标尺去掉了）+ `LEGEND`（图例，**由 `_NAMES` 生成**）。
    │                 **纯地形**：武器名册不在这里（见 world.py），第 10 步把 `Map.weapons` 删了。
    │                 `shops`（第 29 步）= 武器商店格子（`zones` 的 `weaponShop`），买券线的目标点
    ├── roles.py      §4.5.2 的 Pioneer / Worker（各带自己的 `bag`，`stone` 是它的**派生属性**）；`make()` 只认角色，建筑返回 None
    ├── world.py      Turn（round_no / map / roles / gold / weapons / robots / task_points
    │                 / phase_task / llm_resp / cmd_result / vendor_prices / shop_prices / errors / action_results）
    │                 + `within` / `is_day` / `day_rounds_left` / **`summary()`（四块，各占一行）**
    │                 + `Weapon`（id/kind/pos/attack_range/cooldown/**level**）、`Robot`（pos/health）、
    │                 `Error`（code/description，**码不翻译成文字**）三个 NamedTuple
    └── planner.py    决策。**策略只写在这里**（**第 40 步起工人走"差事黑板"**：`plan()` 回合内
                      现算 `segments`（环缺口按在场工人数**切段**，A 前段 B 后段沿环推进）、
                      `ore_taken`（矿格认领，B 不跟 A 奔同一座）、`paths`（路径预留，后处理的
                      工人当 `avoid` 整条让开；`_step` 的 avoid 从此是**软的**——绕不开退回硬障碍，
                      绝不原地卡死；**预留只在动身成功后**——先记再走会绕开自己的路，实测过），
                      **黑板不跨回合**（第 11 步教训）。白天：**开拓者去接任务**（任务点全空 ⇒
                      真空闲，领**买券→用券**差事（第 40 步；第 29 步"开拓者卖矿"死线退役，
                      `_sell_ore` 退居兜底）、工人建武器 → 采石砌墙
                      （18 格 + 防关人闸门 `_trapped`；**站在目标格上先挪开** `_step_aside`）
                      → 墙砌完**先卖、再升级、后就近采**（`_sell_ore` → `_upgrade_line`
                      买券用券（第 29 步，优先链 gatling>rocket>railgun；跑腿者 = 持券者 >
                      真空闲开拓者 > 第一个工人，第 40 步）→ `_mine_spare_ore`
                      **可行矿里挑价高的**（第 29 步修闲置）+ 顺路 `_detour_sell` 绕小贩）；
                      白天末尾还有一道**收工闸门 `_leave_for_the_post`**（第 33 步：环砌完了才回家，
                      **只发 move**）；**所有回合预算走 BFS**（`steps_between`，第 33 步）；
                      白天还有**拆墙/临时门**（第 34 步）：`_rescue` 先救被关住的人（含工人自己），
                      `_dig` 钩在 `_step` 里（**省 ≥5 步才拆、只拆贴着的那一格**），
                      `_door_open` 决定那个洞白天当门、`day_rounds_left <= HOLE_PATCH_LEFT` 时补回；
                      夜里：**所有没被任务钉住的角色**回炮位，贴着就按**最大伤害落点**开火
                      （火箭全场算中心+溅射、加特林/电磁打有效伤害最高的，`assigned` 记账防挤将死者）
                      + `task_channel(turn)` 产出响应顶层的 `(prompt, executeCmd)`
                      （任务线唯一的对外通道，**两者互斥**；判据链的顺序是本步的核心，见下）
```

依赖方向：

```
app → web / protocol / game / utils   protocol → game
game/planner → protocol/actions       ← 第一条"由内往外"，只走指令编码
game/planner → agent                  ← 第二条"由内往外"，**只收字符串、不收 Turn**
                                        **走包根那个单实例**：`from ..agent import AGENT`（不是 `from ..agent import agent`）
game/planner → utils                  ← 第 23 步新增：任务行自己打 ⇒ 要 `_clip`（只借规则，不借状态）
```

⚠️ `utils` 是**叶子**（一个包内 import 都没有），目前 `app` 与 `game/planner` 取它。
`protocol.actions.describe` **能** import 它了（第 22 步它搬出 `app` 之后），
但那条边**仍然走参数注入** —— 见"复盘日志"那节的取舍。

- 三件事：**怎么收（web）/ 报文长什么样（protocol）/ 打什么（game）**，`app` 把它们接起来。**没有第四件事就不加第四个包**，也不提前建空目录。
- **`agent` 与 `planner` 的分工**：决定"**什么时候**跟 LLM 说话"（判据链、纠错、开拓者在不在）是**策略**，
  留在 planner；"**说什么、怎么说、怎么解析回复**"全在 agent。机械保证是 **`chat(request: str, …)`
  收字符串、不收 `Turn`** —— 收了 `Turn`，这条边就变成双向，那时该把"什么时候说话"也搬进 agent。
- `app.py` 与 `planner.py` 的分工是**红线 vs 策略**：`app` 管"永远回得出合法报文"，`planner` 管"该做什么"。别让策略代码有机会破坏报文格式。
- `roleCommandMap` 的 key 用**字符串**（JSON 对象的 key 本来就是字符串）。
- ⚠️ **`handle(raw: bytes) -> bytes` 从此**不是**严格无状态的纯函数**：`agent/__init__.py` 那个
  **单实例 `AGENT`** 身上有**两处跨回合状态**（都是实例属性）：`_sop`（第 18 步用户拍板的
  PromptSOP 自进化沉淀，**整场**存活、跨任务）与 `_context`（第 25 步的任务内会话 `Context`，
  **题目变了即换新**）。共同的账：① 它们只影响 prompt 的**内容**（答得好不好），
  **报文字节照旧合法、不碰红线** —— 会话丢了 ⇒ 退化成单轮提问；② **不加锁**（判题器逐回合
  同步请求，GIL 下赋值/读取不撕裂）；③ SOP **不按任务分区**（任务 A 的沉淀灌进任务 B，
  用户拍板、记录不修），会话**按任务分区**（渲染有窗，第 39 步）；④ 加第三处
  跨回合状态之前先想清楚是否需要、以及失败时的退化路径（第 11 步那条"跨回合标志位一旦卡住
  会**静默关掉整条任务线**"仍然成立）。
- ⚠️ **单实例的代价**：`from ..agent import AGENT` 绑的是**对象**，测试**换不掉**它 ⇒
  用例只能靠 `Agent.reset()`（生产代码里唯一一个只为用例存在的公开方法，SOP 与会话**两处一起清**）隔离。
  想知道"现在的 SOP 是什么"，只有 `AGENT.sop` 一个地方可去。
- 领域对象只带**当前步骤真正用到**的字段（`BaseRole` 目前只有 id/pos/type_name）。加字段之前先问这一步用不用得上——**尤其别把 §4.5.2 的 HP/背包上限写成类常量**，payload 里的 `health`/`backpack` 才是权威的当前值。
- **不加分层 import-lint**：现在一共 4 个子包，违规肉眼可见；上一版为此写的 ast 检查属于过度设计。

## 会影响策略正确性的领域事实

以下几条是读了任务书/接口文档/样例交叉验证得到的，**重新推导一遍的代价很高**：

- **日历**：130 回合 = 1 天（白 70 + 夜 60），10 天 1300 回合。`within = (roundNo-1) % 130 + 1`；`within <= 70` 为白天（`Turn.is_day`；`round_no` 缺失是 -1 ⇒ 判成夜里 ⇒ 不建造）。样例 `roundNo=85`（夜晚，且场上确有机器人）已交叉验证。**昼夜不只是氛围**：`build` / `remove` 仅白天（`remove` 那半边是**项目口径**：§4.4 没写它，取保守方向，见下）。
- **开局是 0 武器 + 75 金 + 3 角色**（1 开拓者 + 2 工人），恰好买满 3 座武器（25×3=75）。⚠️ `docs/request.txt` 是 `roundNo=85` 的**中局快照**（已 3 武器 + 20 金），不是开局——曾把它当开局状态，推出"造武器是死支出线"，后果是第 1 天一座武器都不造。
- **固定 36 格防御盒子**：基地 2×2（`pos` 是**左上角**）+ 12 格武器环 + **20 格围墙环（单层，不可加厚）**。**一切坐标由己方 station 的 pos 推导，禁止绝对坐标**（上下半场换边后基地会挪）。可建造区在 `mapInfo.zones` 里**不提供**，只能这样推。
- **可建造区公式只存在于图里，任务书正文没有**（`docs/pic/build_map.png`）。基地 `(bx,by)` 是左上角 ⇒ **武器环 = `[bx-1, bx+2] × [by-2, by+1]` 去掉基地 4 格 = 12 格**；围墙环再往外一圈 = 20 格。实现在 `grid.weapon_cells`。样例几何整体是手画的、不可作校准，但武器环这一处对得上（样例三座武器都落在环上）。**算错 ⇒ `build` 落点非法、那 25 金币白花。**
- **围墙砌 18 格，背面中间 2 格是永远不砌的门**（第 33 步把门从"整列 6 格"收窄）：6×6 边框 20 格里砌 18 —— **正面列 6（含封口格）+ 两侧行各 5（含背面两角）+ 背面 2**（背面剩 `(bx-2,by)` 与 `(bx-2,by-1)` 两格永不砌，`grid.door_cells` 给的就是它们）。`grid.wall_cells` 给的顺序 = **沿环走一圈**：正面列（一端扫到另一端）→ 一侧行 → 背面（先砌中间那 2 格把门收窄、穿过门）→ 另一侧行 → **封口格**；正面/背面的判据与 `weapon_sites` 同源（都在 `grid._front_back`；左半 ⇒ 正面 `bx+3`、背面 `bx-2`）。⚠️ **走一圈不是顺手排的**：同一份合成局面实测，旧的"正面→顶行→底行→背面→封口格"70 回合只砌上 17 格、封口格当天没砌上（正面整晚开着口），走一圈 18 格全砌上、第 65 回合收官、手里正好 0 块石头。⚠️ **门那 2 格里没有任何建筑**（后列炮位 `(bx-1,by-1)` 在**武器环**的后列、不在这一列）⇒ 能堵门的只有**单位**，堵满 2 格比堵满 6 格容易得多 ⇒ `_trapped` 闸门反而更常真的合上。第 17 步的原始版是 14 格（背面整列留门），第 33 步改定现版。
- **封口格 = 正面列正中 `(front_x, by)`（左半基地就是 `(13,24)`），排在 `wall_cells` 最后一个** —— 这就是"白天开着方便通行、天黑前砌上封死"的落地方式，**零新机制**：靠建造优先级表达（`_ring` 保序、`_build_walls` 取 `free[0]`）。**第 1 天**靠"排最后 ⇒ 白天最后才砌它"表达；**第 2 天起环开局就是满的**，开口改由第 34 步的**临时门**机制重开（`planner._dig` 拆一格当白天的通道、`day_rounds_left <= HOLE_PATCH_LEFT` 时再砌回去）—— 语义还是"白天开着、天黑前封死"，只是从"排在建墙顺序最后"换成了"排到时间窗最后"。⚠️ 也正因为它排最后，**它的落位决定工人一天够不够用**（见上：换回旧排法它当天砌不上）。
- **`_ring` 的例外（踩过坑，别改回去）**：候选墙格要排除 `blocked`，但**我方角色站的那一格除外**——`model._entries` 会把 `teamOur.roles` 写进网格，于是"谁站在待砌的墙格上"会让那一格从候选表里消失，另一个工人的 `free[0]` 跟着后移、掉头去砌更靠后的墙；等前一个工人一挪窝目标又变回来，**两人在两格之间对着改目标转到天黑，一格都砌不上**（端到端实测 26 回合 0 座）。已砌的墙/中立元素/机器人照旧排除。
- **`_entries` 把单位铺在最后 ⇒ 人站在墙上时那一格"看不见墙"**（第 17 步 e2e 揪出来的重砌死循环，两头都要记）：那一格照旧在 `Map.blocked` 里，但**看不出是墙还是空地**。于是"已砌的格子"会因为有人站在上面而被 `_ring` 的例外**复活成候选**，`dist == 0` 又是合法建造位 ⇒ **每回合对脚下同一格 `build`，永远轮不到下一格、石头白花，`free` 也永不为空**（连"砌完了去采最值钱的矿"都进不去）。实测：40 回合卡在 12/14，最后 4 回合反复砌同一格。**修法 = `role.pos == target` 时这一回合只挪开一格（`_step_aside`）、下一回合再砌**：砌过的没人站着就现形（出候选表），没砌过的下一回合从邻格稳稳砌上。别改成"把脚下那格从自己的候选里剔掉"——那会漏掉"脚下正好是**最后一格**没砌的墙"，工人站在上面干等、墙永远停在 13/14。
- **防关人闸门（第 17 步用户给的建墙原则）**：`planner._trapped` 逐角色问"**假设墙砌满**（`_walled`）之后还出不出得去"（`step_outside`），返回要救的 id 集合 `leaving`；`plan` 里两处用：① `gated=bool(leaving)` 传进 `_build_walls`（**一处** `if gated: return`，位置在 `if not free:` **之后** —— "砌完了"与"这一回合不许砌"含义相反，混在一起工人会掉头奔地图另一头采铜）；② 白天把 `leaving` 里的人先送出去（用"**现在**"的障碍，与 `_walled` 那套不同，两套之差正是还没砌的墙）。⚠️ 夜里**不**送人出去（夜里不砌墙，这一支只会把人从炮位上拉走）。⚠️ `_walled` **把自己人只在门那 2 格算成障碍**（第 33 步改定，与 `_ring` 的"自己人一律算路过"**故意相反**，但也不是"一律算障碍"）：那 18 格墙里没有任何建筑 ⇒ **能堵门的只有单位**，所以"自己人站在待砌的格子上"这个问题**只在门口有意义**；环内那 16 格（基地 + 武器环）站着的自己人是**过路的**（不是墙，下一回合就走）。⚠️ **旧口径（自己人一律算障碍）在门收窄之后变成了活锁源**：环一闭合进出只剩环内那几条走廊 ⇒"一个工人正好站在走廊格上"变成常事 ⇒ 走廊另一头的工人被判成"砌满就出不去"、闸门把他送出去一格 ⇒ 下一回合他回来砌墙、两人来回踱步、**一整天不砌墙**（合成局面实测 16 回合 0 座）。只认门口那 2 格之后这条消失，而**真正会关人的局面照样接得住**（用例 `test_a_colleague_in_the_door_still_holds_the_wall_back`）。`remove`（拆墙）没实现 ⇒ 关进去就是整场出不来，这就是闸门存在的理由。
- **手搓 `Map` 的合成局面里"角色不在 `blocked` 里"**：只有 `protocol.model._entries` 会把我方角色写进网格。所以涉及"别人挡路/自己站位"的用例必须自己把角色铺进 `entries`（`{**地形, **{r.pos: "worker" ...}}`）——第 8 步的新用例第一版漏了这一点，于是**它连旧代码都放过去了**。写完可疑用例要**反向验证**：退回旧实现，用例必须挂。
- **"基地后方" = 远离地图中心那一侧**：机器人从基地**面向地图中心**的那一侧水平逼近（`docs/pic/大致地图信息.png`，**正文没写刷新点**）。判据实现在 `grid._front_back`（返回 `(方向 d, 朝机器人的边, 背向的边)`），**只此一处**，`wall_cells` 与 `weapon_sites` 都从这里取。基地在左半 ⇒ 后方是 `x = bx-1`；右半 ⇒ `bx+2`。首场比赛后按实测翻转。按基地坐标判而不用 `teamOur.type`——换边后队伍身份不变、基地会挪。
- **三座武器的阵形 = 后列上角 + 前排两角**（第 24 步改定，`grid.weapon_sites`）：左半基地 `(10,24)` ⇒ `(9,25)` / `(12,22)` / `(12,25)`。**理由有两条**：① **"够得着"**：武器升级券、基地升级券、围墙升级券均需在**目标建筑周围一格内**使用并指定目标位置（任务书 **L292**），围墙修复包要站在待修围墙一格范围内（**L314**），`attack` 也要求角色站在炮旁。三个落点**各自与基地的一角切比雪夫距离 1** ⇒ 一个角色站在落点上，脚下的炮和旁边的基地**一够就是两个**。② **操作者互不堵**（第 24 步的动机）：后列在**上角** ⇒ 其环内站位全在顶线一侧（`(9,24)/(10,25)`），**底行路线**（门 → `(9,22)` → `(10,22)` → `(11,22)`，通往前排两角的通道）一个都压不着，且武器贴门柱（外面就能操、不必进走廊）。旧阵后列在 `(9,23)`（贴基地下沿）时站位含 `(9,22)/(10,22)`——正在底行路线上，加上前上角操作者站 `(12,24)`，前下角的两个站位 `(11,22)/(12,23)` 就成了孤岛：**操作者堵死操作者**。⚠️ 买券那条线**还没做**，所以"够得着"目前只兑现了位置这一半。种类与落点**绑死**：`planner.WEAPONS_BY_SITE` 与 `weapon_sites()` 下标一一对应（**按射程配**：后列最远离机器人 ⇒ 给射程最长的火箭；样例 4/7/∞ 与任务书表格 3/6/10 两套矛盾数据下都是这个结论）。
- **`build` 的三条硬规则**：① **仅工人 + 仅白天**（§4.4；夜里发 `build` 是一次执行失败）；② 目标须在**自身切比雪夫 ≤1** 内（"站位即建造位"——`step_toward` 恰好把人停在贴着目标的一格，不用先挪开）；③ 建武器 **25 金/座**，新建**一律 level1**，**目标格已有武器则原武器被覆盖降级** ⇒ 必须避开占用格，否则 25 金币打水漂还倒亏一座。
- **`remove`（拆墙，第 34 步）的事实**：§4.4 **仅工人**、"需指定与自身距离一格内的围墙位置"（**与 `build` 同一站位契约**，切比雪夫 ≤1）；拆后那格变**可穿越空地**（L73）、**不回收石头**（L209）⇒ 拆墙不是免费的。**报文有实证**（`docs/response.txt:42-50`）= `{"action":"remove","targetPos":[{"x":29,"y":7}]}`，**没有 `name`**（`build` 才有）⇒ 用例钉了 `"name" not in wire`。⚠️ **昼夜那半边是项目口径、不是原文**：§4.4 给 `remove` 的那一格**没写**，取保守方向**只在白天发** —— 夜里不发最多少拆一次，反过来若实际禁夜就是指令非法。**用户拍板的四条口径**：能省 **≥ `HOLE_MIN_SAVING`(5) 步**才拆、取**开洞后步数最少的那一格**（逐格试算、并列取坐标序）、**当天当临时门、天黑前补回**、**开拓者被挡 ⇒ 工人去救**。⚠️ **`HOLE_MIN_LEFT`(30) > `HOLE_PATCH_LEFT`(15) ⇒ 两个窗口不相交 ⇒ 一天最多拆一次** —— **这才是防"反复拆墙净亏"的机制**（不是门槛 5：门槛只防"开了也不划算"）。
- **`_dig` 钩在 `_step` 里**（`walk = blocked | claimed | avoid` 之后、`step_toward` 之前）⇒ 一处覆盖全部差事（采矿 / 卖矿 / 顺路卖 / 升级 / 砌墙 / 回炮位），**8~9 个调用点一个字没改**。候选**只取当前已经贴着的墙格**（`remove` 的站位要求就是这么定的），**刻意不做"走到最优的那一格再拆"**：会引入"在路上"的中间态（目标每回合重算 ⇒ 来回抖），且省下的步数没扣掉走过去的回合 ⇒ **系统性高估收益**。`now < 0`（现在压根到不了）而拆了能到 ⇒ 更该拆 —— "出不来"与"绕太远"在这一个函数里合流。
- **首日豁免 `_after_first_day(turn) = round_no > ROUNDS_PER_DAY` 是整套机制成立的前提**（第 34 步探索阶段揪出的硬错误）：环上"孤零零一个缺口"这**一个状态既是"刚挖的洞"也是"还差一格没砌"**，地图上逐字节同形，任何无状态判据都分不开 ⇒ 只能靠**时间**区分。第 1 天缺口是真的没砌（`_ring`/`_build_walls` 现在就是对的，一个字别改）；**第 2 天起环开局必然是满的 ⇒ 环上一切缺口只可能来自我们自己的 `remove`**。没有它，第 1 天砌到只剩中段某一格时那格会被当成"洞"推迟到当天末尾、**白天再也不补**（当场挂掉 `test_a_worker_on_the_last_cell_still_finishes_the_ring`）。代价见 `code-task.md` 表第 29 项。
- **`_stuck_inside` / `_rescue` 是补救，与 `_trapped`/`_walled` 的预防并存**（不许因为"反正能救"就把闸门拆了）：前者用**"现在"的障碍**（`_trapped` 问的是"假设砌满"——一个预防一个补救），且**自己人一律不算障碍**（同事站在门格上只是路过，把他算成障碍会**白拆一次** —— 与第 33 步改 `_walled` 口径治的是同一个病）。`_rescue` 只让**工人**动手、开"**开了真能让某个被困的人迈出去**且离救援者最近"的那一格。⚠️ **被关住的工人自己也走这一支、而且先走它**（真服务实测：门被两台机器人堵死、工人在盒内 ⇒ 它自己 `remove (13,23)`，压根没轮到差事）；`_dig` 的 `now < 0` 是**第二道**，留给"差事目标不通但人还出得去"。**被任务钉死的开拓者**才非靠别人救不可（它走 ① 支路 `continue`，永远到不了这里）。
- **补墙没有任何新函数**：`_build_walls` 里 `if free and _door_open(turn): free = []` ⇒ 白天中段那个洞当门用，`free` 空掉后**自然落进**既有的"卖 → 升级 → 采闲矿"那一支。⚠️ **已知缺口**：工人阵亡 / `gated` / 没石头 ⇒ **夜里墙上留着自己的一个洞**（而夜里不能 `build`、当天无法补救），日志上的症状是"**一条 `remove` 找不到对应的 `build`**"。
- **武器上限原文自相矛盾**：§4.5.1 表格写"每种 ≤3"（合计 9 座），补充说明写"**全局同时最多 3 座**"。取**保守的全局 ≤3**（与"武器只有建立三个才有意义"一致）。落点按类别数、份额 = `len(roles)`，两处都读 **`Turn.weapons`**（网格里那份 `Map.weapons` 第 10 步删了）。
- **中立元素的字段名是 `neutralType`，不是 `zoneType`**（接口文档 §1.2.1，在 `mapInfo.zones` 里）：`stone`/`iron`/`copper`/`vendor`/`weaponShop`/`challengerTaskPoint1|2`/`defenderTaskPoint1|2`。**只有前三种是矿**，小贩/武器商店/任务点**不是矿，但一样挡路**——`Map.ores`（第 15 步从 `stones` 改来）只装这三种、且带矿种（`{矿点: 矿种}`），第 22 步又加了 `Map.vendors`（`frozenset[Pos]`，只要坐标、不要矿种），`Map.blocked` 全都装。
- **`Map` 的不变量：格子非空即挡路**（任务书 L85 那张清单），寻路只问 `blocked`、不问格子里是什么。类别原样取自 payload（`enemy:` / `robot:` 前缀区分来源），**未知类别也照旧挡路**——判错方向只会多挡、不会放行。`render()` 的字符表**是有损的**（机器人不分体型），`cells` 才是真相。
- **基地 2×2 对双方都成立**（接口文档原文："**双方**基地大小为 2*2，基地对应的 pos 传递的是左上角的坐标"）。`Map` 把**敌我双方**基地都展成 4 格——曾有一版只展了我方，敌方基地只挡 1 格。
- **`teamEnemy.roles` 是逐回合观测，不是固定名册**：敌方角色/武器离开视野就消失，**消失 ≠ 被摧毁**（任务书 L97）。敌方基地与围墙全图可见。**不要据此做跨回合战损推断**——`handle` 里除了 SOP 与任务内会话那两处，没有任何跨回合记忆。
- **走向矿 = 站到采集位，是同一件事。** 矿格挡路（任务书 L85），所以工人只能停在**矿周围一格**，而那正好就是 `collect` 的站位（§4.4）。不需要写两段逻辑（"走过去"+"停在旁边"），`step_toward` 撞上矿格自然停。
- **`collect`（仅工人）的事实**：`targetPos` 是**矿石的坐标**（不是自己的站位），与 `move` 同形（`docs/response.txt` 里**没有实证报文**）；一次采 **1 块**，一座矿采满 10 次就消失（payload **没有**"剩余次数"字段，不记跨回合计数）；**§4.4 的能力列没给它写昼夜限制**（`build`/`remove` 写"仅白天"、`attack` 写"仅黑夜"，它那格是空的），但这一步夜里不采（用户选定），昼夜门由 `planner` 把关、**不进闸门**（闸门只管"谁"）。背包 `backpack` 是**物品名数组**（重复即计数），容量字段是 `backPackCapability`（**大写 P**，暂不读）。
- **围墙代价 = 石头×1，从建造者自己的背包扣**（不是全队共享），拆了不返还；**没有转移物品的指令** ⇒ 多采的石头给不了另一个工人。**攒多少石头不写死**：`planner._stones_to_mine` 每回合按用户给的**回合预算**现算 —— `赶路到矿 + 采 k 块 + 从矿回工地 + 手里 s 块的"挪+砌"(2s−1) ≤ 白天还剩的回合 − TIME_MARGIN(5)`，解出 k（每多采一块净花 3 回合），再与"还差几格墙"取小。距离用 **BFS 真实步数**（第 33 步从切比雪夫改来：绕障时切比雪夫**低估**，而回炮位必须绕到背面那道门再横穿盒子，直线 3~4 格、真实 10+ 格 ⇒ 低估得很离谱），`TIME_MARGIN(5)` 从"吸收低估的补丁"变成**真余量**；`steps_between` 返回 **-1（不可达）⇒ 这趟不去**，**不为估算去改 `step_toward` 的契约**。
- **采什么矿看价格，但价格不是常量**（第 15 步，用户策略指导那条）。用户的原话是
  「手里面保持能建造墙的石头量就行，**然后**选择价格最高的矿，没有波动的情况下是铜 > 铁 > 石头」
  —— **"然后"是顺序，不是并列**：墙只吃石头，先追最贵的铜就永远砌不成墙。所以两段：
  墙上还缺石头 ⇒ **只认石矿、取最近的**（`_pick_ore(want_stone=True)`，这就是第 8 步本来就有的行为）；
  18 格砌完 ⇒ 才按**收购价**挑（`_mine_spare_ore` + `_pick_ore(want_stone=False)`）。
  **"保持够砌墙的量"那一半本来就有**（`_stones_to_mine` 的上限是"还差几格墙"），第 15 步新增的
  只有"砌完之后工人不再原地闲置"。
  ⚠️ **"铜 > 铁 > 石"是样例的价目（1/3/5），不是规则**：任务书 L386 明说官方消息会让价格波动
  （示例：铁矿塌方 ⇒ 铁稀缺 ⇒ 回收价上涨）。价格**逐回合从 `vendorShopList` 读**
  （`Turn.vendor_prices`，`_int` 给的 -1 被挡掉 —— 负的收购价不存在），**写死的排序在事件期间恰好是错的**。
  价目缺失或矿种不在表里 ⇒ 按 0 算 ⇒ **不为它多走一步**（也顺带实现"空价目表 ⇒ 谁也不采"）。
  ⚠️ **`sell` 第 22 步才实现**（在那之前铜/铁只是压在背包里换不成钱）；
  代价仍是背包占用与**离炮位更远**。
- **`sell`（卖矿换金币）的事实**（第 22 步，任务书 §4.4 / §4.6.1）：
  - **须站在「小贩周围一格内」**（不是矿周围）—— 与 `collect` 同形，小贩格本身挡路
    ⇒ `step_toward` 撞上它自然停在贴着一格，**不用写两段逻辑**。
  - ⚠️ **可用角色是"全部"**（§4.4 最右列）—— 与 `build`/`collect` 的"仅工人"**相反**。
    窄成工人是"本地全绿、判题器说不"的典型：报文完全合法，只有判题器知道开拓者也能卖。
  - **报文形状** `{"action":"sell","name":<矿种>,"num":<件数>}`，`num` 是 **int**（§2.2）、
    默认 1，**支持批量贩卖**（一回合一条指令 ⇒ 一次卖光手上那一种，`num` = 全部件数）。
    ⚠️ **没有实证报文**（`docs/response.txt` 只有 `move`/`build`/`remove`），
    `name` 取**英文矿种名**（与 `neutralType` / `vendorShopList.name` 同一套词）。
  - **§4.4 的能力列没给它写昼夜限制** ⇒ 昼夜门由 `planner` 把关、**不进闸门**（与 `collect` 同）；
    planner 把它放在**白天**那一支（夜里所有角色回炮位）。
  - **小贩的坐标在 `Map.vendors`**（`frozenset[Pos]`，第 22 步加）—— 它是**格子**不是
    `teamOur.roles` 里的单位，只能从网格认。与 `ores` 对称：一个是料源，一个是目标点。
  - **石头只在"墙砌完了"之后才卖**：`_sell_ore` 的**唯一调用点**在 `_build_walls` 的
    `if not free:` 那一支。墙没砌完时手里的石头一律有用（`_stones_to_mine` 的上限正是
    "还差几格墙"）⇒ **`free` 非空时不存在"多余的石头"**，那个不变量就是它的定义。
  - **够本门 = 货值 ≥ 2 × 到小贩的 BFS 步数**（第 33 步从切比雪夫改来，≈ 每回合至少换 1 金币），
    **已经贴着小贩就跳过这一条**（这趟路早付过了）。⚠️ **「1 金币 ≈ 1 回合」是拍的**，
    没有文档依据 —— 它是这一步唯一的调参旋钮。样例小贩 `(20,16)` / 基地 `(10,24)` ⇒
    切比雪夫 10 ⇒ 阈值 20 ⇒ 铜要攒 4 块（改 BFS 后这条阈值只会更大、更保守）。
    **若实际地图更远，"矿堆着却永远不去卖"就是这个阈值。**
  - **每工人背包保底 1 块石头**（第 33 步，用户要求）：`_sell_ore` 卖石头时 `num = max(0, 石头数 − 1)`
    —— 收工时手里得有货才能封 `(13,24)`。为 0 就不发石头那条指令。`WALL_COST` 与
    `_stones_to_mine` 的"还差几格墙"口径**不动**：石头照旧只用于砌墙。
  - **背包是一张名字表**：payload 的 `backpack` 是**物品名数组**（重复即计数），
    `model._bag` 数成 `{物品名: 件数}`，`BaseRole.stone` 是 `bag[STONE]` 的**派生属性**
    （第 22 步从 int 字段改来；`_stones_to_mine` / `_build_walls` 三处调用一字未改）。
      容量字段是 `backPackCapability`（**大写 P**，不读）。
- **买券/升级线（第 29 步，用户拍板"买得起就优先升级"）**：
  - **`buy`（武器商店旁，全部角色）**：`name` = 商品名（`weaponShopList.name` 那套词）、`num` Int 默认 1；价目从顶层 `weaponShopList` 逐回合读（`Turn.shop_prices`，样例实证：券1=100、券2=150）—— **不写死**，查不到按 0 ⇒ 买不起 ⇒ 不跑腿。⚠️ **没有实证报文**（与 `sell` 同一类风险）。
  - **`use`（全部角色）**：升级券须**站在目标建筑周围一格内**并指定 `targetPos`（任务书 L292）；已 level3 再用**不生效、券不消耗**，非法使用也不消耗（L293-294）⇒ 发错顶多白跑一趟、不碰红线。
  - **武器带 `level`**（接口文档：仅建筑持有、初始 1；`model` 缺失按 1 算，别给 -1—— 会被升级线当成"还升得动"白跑一趟）。摘要的武器行打 `L{level}`（"升级成没成"只有日志能回答）。
  - **升级优先链（我判的，群体打击口径）：加特林 > 火箭 > 电磁**。加特林 +1 颗子弹 = 每回合 +10、无冷却、弹道必命中、两颗可分打两台（90° 锥）、射程 +2 更早接敌；火箭 +1 枚对簇 +~30/齐射，但 3 回合冷却一夜只 ~20 轮、依赖扎堆；电磁单目标、能量对满血机器人（≥40 血）不穿透。链：gatling→2 → rocket→2 → gatling→3 → railgun→2 → rocket→3 → railgun→3（`UPGRADE_CHAIN`）。
  - **无状态编排**（`planner._upgrade_line`）：拿没拿券看**背包**（买完金变少、包里多一张，两阶段天然可分；跨夜背包保留）；跑腿者 = 持券的工人 / 名册第一个工人；整趟（商店→武器 + 买/用两动作）来得及才出发（`day_rounds_left − TIME_MARGIN`，第 33 步起那段路按 **BFS** 算、不可达 ⇒ 不出发）；**只在白天跑**（夜里 `_defend` 接回炮位，明早接着走）；持券阶段的终点就是炮位（用完正好站岗，不留回程）。
  - **就近采矿修闲置（同步第 29 步）**：`_mine_spare_ore` **先把"走得动、回得来"的矿筛出来再按价挑**（旧版挑了最贵的发现走不回来就整段放弃 —— "挖好石头在家等着"的根源）；回程参照 = **最近的武器位**（机器人到进攻范围前必须站回炮前）。**第 33 步起这里的距离全走 BFS**（`role.pos → 矿` 与 `矿 → 最近炮位`，各 `steps_between`；不可达 ⇒ 剔掉那颗矿），每个炮位的 BFS 提到循环外算一次，避免"每颗矿 × 每座炮"重复跑。**顺路卖矿 `_detour_sell`**：去矿路上绕去小贩 ≤ `DETOUR_MAX`(2) 格 ⇒ 先绕（贴上那回合 `_sell_ore` 自然出手，绕路量同样按 BFS 算）。
  - **开拓者无任务时卖矿（用户指定）**：任务点全空（冷却/做完）⇒ 白天开拓者走 `_sell_ore`。⚠️ **规则限制**：`collect` 仅工人、**没有转移物品的指令** ⇒ 开拓者背包里通常没矿 —— 结构在，要等它从任务/宝藏拿到可卖物才真正跑得起来。
- **地图边界不在任务书 L85 的"阻挡移动"清单里**（那一列只写了建筑/角色/机器人/中立单位/任务点/矿区）。贪心挪一格时几乎撞不到，**但 BFS 会绕到图外去**，所以 `step_toward` 自己按 `Map.size = (width, height)`（取自 `mapInfo.width/height`）挡住 `(0,0)~(width-1,height-1)` 之外。越界算"指令非法"还是"执行失败"文档没写，不走一定安全。`size` 无效（≤0）⇒ `Map` 矩阵为空 ⇒ 无格可走 ⇒ **单位不动**，这是故意的降级。
- **`step_toward` 的终点是"贴着 goal 的一格"，不是 goal 本身**——因为 goal 通常是挡路的（矿/建筑/武器操控位）。⚠️ `build` 的落点是**空地**（第 7 步回头核过这条契约）：**结论是不用改** —— 建造格在环上是空的，BFS 只在可通行格上展开、并停在距 goal 一格处，那正好就是 `build` 要求的站位。建墙同理。
- **没有转移物品的指令**（`drop` 只丢不捡，没有拾取）→ 每个角色的背包就是自己的料仓，"A 买 B 用"行不通。
- **`attack` 最易写反的两处**：`roleCommandMap` 的 **key 是武器 id**，`controllerId` 才是操控角色；`targetPos` 长度 = 武器当前等级（电磁狙击炮恒为 1）。攻击**仅黑夜**可用，且需要角色站在武器周围一格内（一人只能操一座武器）。**可用角色是"全部"**（开拓者也上炮位）。
  - **已毁的炮与阵亡的角色都不该收到指令**：`model._weapons` 丢掉 `health == 0` 的炮，`model._character` 同理丢掉阵亡角色（用 `== 0` 而非 `<= 0`：字段缺失是 -1，别当成死）。
  - **`roundNo` 缺失时 `attack` 一条都不发**（`planner._defend` 的硬门）：缺失的回合号被 `is_day` 判成**夜里** —— 对"白天不许建造"安全，对"白天开火非法"就是反的。
- **三种武器的伤害模型（§4.5.1 表格 + §4.5.4，L1 口径；升级线没做 ⇒ 恒为 L1）**：
  - **加特林**：每颗子弹 **10** 伤害，沿"武器→落点"弹道飞行，命中**该弹道上最近的一台**机器人即消耗。多目标（L2/L3）须落在**同一 90° 锥形**内（否则整次攻击非法）。
  - **电磁狙击炮**：一个目标，**能量**沿弹道穿透（L1=10）：路径上每台机器人受 `min(剩余能量, 当前血量)`，能量按伤害扣减。
  - **火箭发射台**：导弹**指哪打哪、不被弹道阻挡**；中心 **20** + 落点周围 8 格**溅射 10**，多枚叠加（L1 一枚）；发射后 3 回合冷却。
  - **机器人血量 40/60/500/800（小/中/大/BOSS，§4.7.2）全都 ≥ 20** ⇒ L1 对满血机器人不可能过量伤害；结算顺序 = **武器攻击 > 机器人移动**，伤害**回合末统一结算**；**残余机器人清晨自动清除**（每晚都是满血开局）。
  - **第 27 步的方针（用户改）：目标 = 最大伤害落点，打死所有机器人**（撤掉第 10 步的"补刀残血"）。`planner._fire`：火箭 `_rocket_site` 全场算账（候选 = 机器人格及其 8 邻格、落点须在射程内——**溅射可以够到射程外的机器人**；评分 = Σ min(伤害, 剩余血)，并列取坐标序）；加特林/电磁 `_beam_site` 打在机器人身上（终点必在弹道 ⇒ 必命中），取**有效伤害** = min(伤害, 剩余血) 最高的、并列打近的。**`assigned` 记账**：同回合先开火的炮把估计伤害记在机器人身上，后开的按剩余血算 ⇒ 不挤将死的目标（"集火补刀"就此作废）。⚠️ 加特林的实际命中是弹道上**最近**的那台（可能不是终点那台）—— 估计只用来挑目标，错了只是次优、不会非法；**弹道是否被墙挡没有明写**（火箭明说不被挡；加特林/电磁的"攻击路径"暗示会被什么挡——若实测子弹打不出墙内，整个防守阵形都要重想）。
- **`attackRange` / `cooldown` 都以 payload 为准**：任务书等级表与样例数据矛盾（样例 gatling L1=4 / railgun=7 / rocket=INT_MAX，表格是 3/6/10）；`cooldown`（火箭发射后 3 回合空窗）**样例三座炮都没有这个字段** ⇒ 解析成 -1 ⇒ 不当成冷却。**缺省方向与射程相反**：射程 -1 ⇒ 够不着 ⇒ 不开火；冷却 -1 ⇒ 照打。
- 夜里**不能建墙**（`build` 仅工人、仅白天）→ 墙一旦被拆，整夜都是缺口，所以夜间第一优先级是**预防性修复**而非爆了再补。**夜里所有角色（含开拓者）的动线 = 走到最近的一座还没被本回合别人认领的武器旁边**（一人只能操一座），贴着（切比雪夫 ≤1）就按**最大伤害落点**开火（见上）；**没敌人 / 炮在冷却 / 够不着 ⇒ 站在炮位待命，什么都不发**（空指令合法）。**不换炮**：站哪座炮是走到位那一刻定下的，每回合重挑会让角色在炮位之间来回走。已知代价：**角色数 < 武器数时火力打折**。
- **白天收工闸门 `planner._leave_for_the_post`（第 33 步，治"机器人到跟前了角色还没走到炮位"）**：白天最后那几回合，**环砌完了**（`_ring` 为空）才允许停下手里的活回家 —— 回程步数 `steps >= day_rounds_left - 1`（或不可达）⇒ 只发一条 `move` 往最近的空闲炮位挪。三件事：① **"环砌完了才收工"是有意的**（补墙 > 收工，与用户"防御时必须保证前面完全封死"一致）：第 1 天墙没砌完时闸门不生效，第 2 天起环通常是满的、全天生效；② ⚠️ **这一支只能发 `move`，绝不可复用 `_defend`** —— 它贴近炮位会调 `_fire` 发 `attack`，而 `attack` 仅黑夜 ⇒ 白天发就是**非法指令**（红线）；守门员是 `DayEndGateTest.test_a_day_round_never_fires`（扫 6 个白天回合号 × 5 个位置，断言一条 `attack` 都没有）；③ 贴住炮后 `steps == 0` ⇒ 条件不成立 ⇒ 剩余白天自然空指令待命（合法）。那个 `- 1` 是**拍的余量**。
- **任务线的三个权威事实**（第 11 步，全部交叉验证过）：
  - **任务点的权威来源是 `teamOur.playerTasks`**（数组，**只含我方 2 个点**，阵营已按 `teamOur.type` 滤好）—— 元素 `{taskType, taskPosition, coldDownRounds, scoreReward, goldReward, isValid}`。坐标字段名是 **`taskPosition`**（唯一不叫 `pos` 的），且**只认锚点格就够**（任务点 2 虽占两格，锚点本身就是其中一格）⇒ **不需要读 `mapInfo.zones` 的 `challengerTaskPoint*`、不需要读 `teamOur.type`、不需要算两格并集**（旧笔记已作废）。`isValid: false` = 冷却中**或**该点任务已做完；结束时刷新冷却 30 回合。
  - **`prompt` → `llmResp` 是唯一的 LLM 通道**：响应顶层 `prompt` 发出去，下一回合 payload 顶层 `llmResp` 回来。**任务存续期间不限量、不计数**（接口文档 L198），但**不在任务里一次都不要发**（每游戏日只有 3 次额度，那是任务线之外的资源）。答案**每回合都交** —— 接口文档 L140「以之前提交过的**通过率最高的**答案计算积分与金币」说明反复提交是判题器**预期的**用法。判据本身仍**不记跨回合状态**（跨回合标志位一旦卡住会**静默关掉整条线**）；例外是 SOP 与任务内会话（见架构那节）—— 它们只影响"问得好不好/答得好不好"，不影响"交不交"。
  - ⚠️ **沙盒是任务线的另一半，第 16 步之前整条都没接**（那时 `executeCmd` 恒为 `""` ⇒ 答案只能靠 LLM 硬猜 ⇒ 判题器只能报 `errorCode 2`，**这就是"任务一直失败"的根因**）。五个事实：
    - **响应顶层 `executeCmd`**（接口文档 L208/210）="沙盒环境中执行的命令，**仅在执行任务期间才能使用**"，判题器**本回合**执行、限时 15 秒，**沙盒故障与命令超时不计入异常次数** ⇒ 这条通道**不会吃红线**，可以放心用。**没有昼夜限制**（任务跨夜是常态，别加 `is_day` 门）。
    - **工具协议是我们自己定的**（第 18 步定形，第 30 步参数泛化，**第 37 步换嵌套形状且严格只认它**）—— 文档只写了沙盒"能执行基础的 shell 指令与 python 指令、**无法访问外部网络**"（任务书 L439），**没规定 LLM 该用什么形状要命令**。协议形状是最大的单点风险，只能靠实盘检验。两个形状：
      - **调工具**：`<tool><tool_name>工具名</tool_name><tool_param><参数名>参数值</参数名></tool_param></tool>` —— 参数是 `<tool_param>` 里的**具名元素**（可一个可多个、也可拆进多块；`\w+` 认中文参数名）；无参数的工具不写 `<tool_param>`。**严格只认这一种形状**（第 37 步用户拍板）：属性式 `<tool_param name=…>`、裸 `<tool_param>值</tool_param>`、裸 `<tool>cmd</tool>` **全部不再是工具调用** ⇒ 落重问（丢一回合、不碰红线）。**值过五实体反转义**（`&amp;` 最后换，`&amp;lt;` 只还原一层；LLM 没转义时是空操作）—— prompt 教了转义，解析就得接住；答案不走这条。
      - **直接作答**：`<answer>答案本身</answer>`
      - **两者同回合**（第 35 步起、第 36 步定形）：`SOP2Prompt` 不产出命令，所以"沉淀 + 作答"
        是**两块并列** —— `<tool>…</tool>` 之后跟一个**块外**的 `<answer>`。⚠️ **`sop` 正文里
        不许出现 `<answer>` 与 `</answer>` 这对串**（用户口径）：`chat.strip_answers` 在入库前把
        **成对的**挖掉（挖了几处进 `【SOP 更新】` 日志），规则本身也写在 `prompt.py` 的输出约定段里。
        反过来，答案**落在工具块内**一律不算 —— `answer_of` 先把整个工具块挖掉再扫
        （分不清那是真答案还是 SOP 里的示例，就宁可不交）。
    - **三个谓词分居宽严两端**（`agent/chat.py`，同住一个模块）：`tool_of` **严格**（⇒ `(名, [(参数名, 原文), …])`，**参数名永远不是 `None`** —— 嵌套形状的参数名就是标签名；**任何一块 `<tool_param>` 里没有具名元素 ⇒ 整次调用 `None`**；**只有参数没名字 ⇒ `None`**，绝不猜；**只有名字没参数是合法形状**——可能是无参数工具的调用，放行与否由 `tool_call` 按参数声明判）；`looks_like_tool` **宽**（只认**前缀** `<tool` 出现 —— ⚠️ 它是模块里唯一不要求成对的地方，别顺手改成 `_TOOL_RE`：那会把"想调但没凑对"的回复从重问变成"原文即答案"；第 37 步起**不再解析的旧形状也靠它接住** ⇒ 落重问、不被当答案）；`answer_of` **三级**（第 36 步：先把**第一个工具块整段挖掉**再扫 —— 有 `<answer` 标记 ⇒ 只认成对块内容，空/半截 ⇒ `""`；**否则用原文判**"像工具回复" ⇒ `""`；否则**原文即答案**）。⚠️ **"先挖掉工具块"是第 35 步那个污染缺陷的正面修法**：`SOP2Prompt` 沉淀的正文里几乎必然出现字面量 `<answer>…</answer>`（它讲的就是"答案要用 `<answer>` 包"），不挖就会把 **SOP 里那一段**当成答案交上去（真实缺陷，日志上看不出来）。`chat.py` **不认识任何参数名**（参数名是 LLM 写的标签、由 `tool_call` 对注册表认）。
      - 与它同住一处的是 `strip_answers(text) -> (正文, 挖掉几处)`：**唯一的调用者是 `SOP2Prompt`**（入库前清洗），住在 `chat.py` 是因为它认的就是 `_ANSWER_RE` 那一个模式 —— 分家等于把标签语法抄成两份。
      ⚠️ **`answer_of` 是「该提交什么」与「该骂什么」的同一个谓词** —— `_answer_task` 提交它、判据 ④ 用它当纠错内容、判据 ⑤ 用它判"已经有答案了"。三者一旦分家，纠错段就会把**带 `<answer>` 标签的原文**喂回去，LLM 看到自己上次的标记被原文骂回来，行为是未定义的。
    - **注册表是唯一真相**：`Agent.__init__` 里那张 `self._tools`（名 → (实现, 描述, **参数表** `((参数名, 用途), …)`）—— 参数表**一表两用**：`prompt.gen_all_tool_prompt` 生成 `## ToolName - 名` / `- Description: …` / `- Params:` + `    - 名: 用途` 的**块**（第 37 步起；无参数打 `- Params: （无参数）`、块间空一行；**由表生成、不手写第二份** —— `Agent.tool_desc` 已删，职责并入生成函数）、`tool_call` 按它**调度**（**只收具名参数**：带名按名对、认不出的名字**不进 resolved**、声明参数一个不少且非空才 `impl(**resolved)`；位置填充机制随严格解析一起删除）⇒ 加工具只改一处、描述与调度不会分家。⚠️ **第 37 步起 `SOP2Prompt` 的参数表是 `(("name", …), ("sop", …))` 两个参数**（多流程口径：`name` 是流程名，存储是**流程表**——同名覆盖、异名追加、超 `SOP_FLOWS_MAX`(5) 丢最旧且日志点名；`SOP_MAX`(1000) 收窄成**单条**的上限；空文本 = 删掉那条），且**只做流程沉淀**（第 36 步口径传承）—— 作答统一走块外 `<answer>`，与沉淀的唯一交汇点是"同一条回复里各写各的"。⚠️ **表必须由实例构造**（第 19 步）：`SOP2Prompt` 写的是 `self._sop`，只能是**绑定方法**，模块级常量表达不了这件事。**铁律：`tool_call` 的返回值就是那条要进 `executeCmd` 的命令**，`""` = 不产出命令（`SOP2Prompt`）/调用不成立（未知工具、形状不对、缺参数、空白值），**下游不需要区分**。`AGENT.tool_call` 是**副作用唯一发生点**，写在判据链之前 ⇒ 走"回灌结果"那一轮 SOP 照样生效（有意为之：LLM 不该因为"结果刚好回来了"就白调一次工具）。
    - **`planner.task_channel(turn) -> (prompt, executeCmd)`** 是这两条通道**唯一**的产出点，从上往下先命中先定夺（②–⑥ 各算出 `(prompt, cmd)`，**链尾另有回合末尾的压缩闸门**，见下面"两条通道的契约"）：① 没任务**或名册里没有开拓者** ⇒ 两个都不发（**在闸门之前** ⇒ 场外绝不压缩）；② `cmd_result` 非空 ⇒ 回灌结果、**这轮绝不发命令**；③ 工具给了命令 ⇒ 发命令，prompt 槽留给回合末尾的压缩闸门（**第 43 步改定**；此前第 41 步在这里捎压缩请求）；③′ *（隐式子路径）* **完整调用但拿不到命令**（`SOP2Prompt` / 未知工具 / 空参数）⇒ **不早返回、继续往下走**：有答案 ⇒ ⑤ 不发模型请求、没答案才落到 ⑥ 重问（第 32 步把它从边角料变成了**主路径** —— prompt 现在要求 `SOP2Prompt` 与答案写在同一条回复里，走的正是这一支：省掉的那一回合是实打实的分数）；④ 判题器报 `code 2` ⇒ 带"上次答错了"重问（**第 35 步起连它的 `description` 原话一起回灌**：那是黑盒里唯一能回答"错在哪一项"的东西，之前只进日志）；⑤ 是答案 ⇒ 都不发（prompt 槽同样留给压缩闸门）；⑥ 否则把问题问出去（首问 = 题目，重问轮 = 一句「请继续。」，见下面 Context 那条）。
      **判据 ② 必须压在 ③ 前面**：文档给 `lastCmdResult` 专门写了"**未发命令时为空字符串**"（L33）、对 `llmResp` **一个字没写**（L31）—— 所以 `cmd_result` 按**不粘**设计、`llm_resp` 必须按**可能粘住**设计，否则同一条命令会被反复丢进沙盒。
      **纠错（④）是"修饰符"不是"分支"**：`errors` 说的是**本轮产生**的错误，与我们上轮发了什么**不同步**（发命令那轮没有 prompt，下一轮却可能同时拿到沙盒结果和更早那次 `submitAnswer` 的判决）—— 做成独立分支就会把纠错整段吞掉。触发条件是 `code == 2` **且** `answer_of` 非空。
      **畸形工具回复**（`<tool` 有开无闭 / 只有名字没参数）落到判据 ⑥ 重问，**不能**当成答案 —— `looks_like_tool` 判宽正是为了这个：两边判据一旦分家，就会出现"提问与提交同时哑火、永久空转且日志上什么都看不出来"。
      **③′ 不活锁**：任务期间 prompt 不限量、不计数 ⇒ 不吃额度不碰红线；出口有三个 —— LLM 自己改口给答案、`code 2` 带来的纠错段、下一回合 prompt 里**它自己刚写进去的 SOP 段**（那就是"调用成功了"的回执；第 25 步起它的调用本身也在会话记录里，回执更明显）。**升级触发条件**：首场日志若出现「同一任务连续 ≥3 回合 `llm_resp` 都是 `SOP2Prompt` 调用、且始终没有 `submitAnswer`」⇒ 升级成"显式 ack"编排。**未实测。**
      **`executeCmd` 的闸门要显式补**：`submitAnswer` 走 `roleCommandMap`，阵亡的人不在 `roles` 里 ⇒ 白送一道闸门；而 `executeCmd` 是响应**顶层字段**，不经过角色循环也不经过 `Action` 的权限闸门 ⇒ 判据 ① 里那句"名册里得有开拓者"必须手写。
      **两条通道的契约（第 43 步修订）**：**任务提问不与命令同轮**（同一轮既提问又发命令 ⇒ LLM 拿着过期结果作答 ⇒ 活锁 —— 这一半原封不动）。**压缩判定 = 回合最末尾的闸门**（第 43 步，用户拍板"**在任务中，且本回合未生产模型请求才压缩**"）：判据链算完 `prompt` 仍空（只有 ③ 命令轮 / ⑤ 答案轮会这样）⇒ 填上**压缩请求**；②/④/⑥ 的模型请求永远优先（压缩是这条链上优先级最低的租客）。压缩回复（裸 `<summary>`）**内容路由进摘要、永不当任务材料**，活锁的成因对它不成立；该轮判据按"没回复"落 ⑥（nudge）⇒ **答案轮与压缩轮交替**，互不阻塞。裸摘要判别 = `chat.is_summary_reply`（挖掉摘要块后一个字不剩），在 `hear` **之前**分流：进摘要、不进会话表。判题器对"prompt 与 executeCmd 同轮非空"零承诺 ⇒ 降级方向安全（压缩在 ⑤ 轮走**纯 prompt 通道**——与任务提问同一条、文档明确支持；命令轮只是额外机会，丢了也无所谓），回退点 = 删闸门三行（`if prompt == "": prompt = AGENT.compression_request()`，回到第 41 步的命令轮捎带）。
      **不做流水线**（同一轮既提问又发命令）：回合数确实值钱（积分 `5 × 标准回合数 / (完成回合 − 接取回合)`），但 `llmResp` 滞后结果一回合 ⇒ 每条命令会被请求两遍、LLM 永远拿着过期结果作答，沙盒里若有带副作用的命令，答案直接被污染。
  - **回灌给 LLM 的是全文**（用户拍板），`app._log` 那一侧才 `_clip` —— 两个下游要的东西不同：一个要正确性，一个要人眼看得下。
  - **开拓者一旦接任务就被钉死**：离开己方任务点周围一格内、超时、开拓者死亡、完成 —— 四种情形任务都结束（任务书 L377-380）。所以**它连夜里都不回炮位**，这一支写在 `plan()` 循环的**最前面**（全局最早的判据，与昼夜无关）。代价：**夜里三座炮只有两个人操，火力打折 1/3**，由任务积分覆盖。⚠️ **没有"离开后重新入环还能续上"这回事**。"周围一格"按**切比雪夫**（含对角，文档没写，与全局距离度量一致）。**"答案格式"文档完全没写**（只有"通过率 = 正确字段数 / 全量字段数" ⇒ 是多字段结构化答案），`agent/prompt.py` 的段模板措辞是唯一的杠杆 —— 这是最大的单点风险（也是 `SOP2Prompt` 存在的理由：让 LLM 自己把摸索出的答法沉淀下来）。
- **任务内的会话上下文 = `Context`（第 25 步，`agent/context.py`）**：判题器的 LLM 每回合只看到 `prompt` 一段字符串（接口文档标 String）⇒ "同一个 task 同一个上下文"的落地形态 = **每回合把这道题的会话渲染进 prompt**（`AGENT._context`，跨回合状态之二）。**第 28 步起 prompt = 标准 messages JSON**：`[{"role": "system"/"user"/"assistant", "content": …}]`（紧凑 `json.dumps` 串）—— 第 25 步那版自造文本版式（`# 对话记录` + 标题块）**用户实测效果非常差，已弃**；结构由 role 表达，正文就是原文。事实与取舍：
  - **身份 = 题目原文**（`phaseTask`）：换题即换新会话；**冷却后同文再现 ⇒ 续上旧会话**（它记得试过什么，文档化行为）；任务结束不清（死会话，下场换题自然被替）。
  - **assistant 消息记原文**：`task_channel` 每回合把 `llm_resp` 交给 `AGENT.hear()` —— **发命令（③）/交答案（⑤）那两轮没有 prompt，回复照样得记**，否则回灌那轮 LLM 看见的是"题目 → 莫名其妙的结果"，它自己要的命令凭空消失。**粘住去重**：与**最后一条** assistant 相同 ⇒ 跳过（`llmResp` 按可能粘住设计、`lastCmdResult` 不粘 —— 判据 ② 那条结论的再利用）；中间隔了别的消息再来同文 ⇒ 记（真的又说了）。
  - **user 消息只在真发了 prompt 的轮次进表**：首问 = 题目**原文**（构造即问，**不加** `【题目】` 包装——那是文本版式的补丁）；回灌轮 = 结果块(+纠错块)（两个标题留在 content 里当**内容标签**，逐字沿用旧模板）；**无新内容的重问轮（畸形回复/③′）= 一句「请继续。」**（`Context.NUDGE`，拍的、实盘可调）—— 判题器 LLM 是黑盒，会话停在它自己的输出上是个含糊指令。
  - **system（`prompt.py` 的段模板）每轮现刷**（`Agent.chat` 里 `gen_system_prompt(self._tools, self._sop)`）：SOP 是活的，任务进行中沉淀的下一轮就得看得见 —— 那是 ③′ "调用成功"的回执。**别冻在构造时**。
  - **存储全量、渲染有窗（第 39 步；第 41 步起压缩独立成轮，第 43 步改定触发点）**：消息逐字进表、`json.dumps` 逐字序列化，但 `render()` 只输出 **题目 + `【历史摘要】` + 最近 `_WINDOW`(2) 轮** ⇒ 任务 prompt 从 O(全部历史) 变 O(窗口)。**摘要的来源（第 43 步改定）**：**回合末尾的压缩闸门**在任务线没产模型请求的轮（③ 命令轮 / ⑤ 答案轮）发**压缩请求**（`AGENT.compression_request()` = 独立指令 + **原始上下文全文** —— 用户拍板"原文永久保留、压缩总从原文重来"，消息表**永不截断**；任务 prompt 专注任务、system 里没有压缩教学），它的回复（**裸 `<summary>`**，`chat.is_summary_reply` 判别）经 `AGENT.adopt_summary` 进摘要、**不进会话表**；判题器不答就保旧摘要（best-effort，绝不重问）。`answer_of` **先挖摘要块再扫**（字面量 `<answer>` 不是答案）。**时序**：压缩在任务线落空的轮（③/⑤）发、下一回合初摘要已就位（结果在窗口里）。**日志侧有界**：`【本轮提问】` 行是单行 JSON，`_clip` 钉在 `LOG_PROMPT_MAX`（100000）。
- **`describe()` 读 `targetPos` 必须容缺**（第 11 步修，第 20 步从根上拆掉）：它跑在 `app.handle` 的 `try` 里，硬读 `cmd["targetPos"][0]` 遇到 `acceptTask` / `submitAnswer`（**报文里没有这个字段**）会 `IndexError` ⇒ **整回合退化成空指令**。第 11 步是"读之前先判有没有"，第 20 步改成**通用摊开**（`action` 之外的字段有什么打什么）⇒ 这类动作天然踩不到雷。⚠️ **别回头去手写一份字段清单**：那是第二份会跟报文漂移的真相，而**漏字段不会有人发现**（用例 `test_describe_*` 三条是守门员）。
- **`render()` 的图例里 `1`-`4` 是「阵营」的任务点，不是「我方/敌方」**（第 12 步）：`1`/`2` 来自 `zones` 的 `challengerTaskPoint*`、`3`/`4` 来自 `defenderTaskPoint*`，两队**同时存在**。样例里 `teamOur.type == "challenger"` 两套恰好重合 ⇒ **写"我方任务点"是一个在样例上永远测不出来的错**。我方可接的那两个点是 `Turn.task_points`。**别把 `mapInfo.zones` 的这 4 个字符和 `playerTasks` 混起来。**
- **地图字符表：空地是「空格」、我方围墙 `#`、敌方围墙 `%`**（**第 22 步定案**：用户手改 + 本步做完）。空地的历史是"`.` → 铺满 `#` → 空格"三段：第 21 步按"打满整个矩阵比较显著"把空地铺成 `#`、墙让位给 `=`；随后用户把空地的填充撤掉改成**空格**，`#` 空出来**收回给墙**。⚠️ **第 21 步已知不确定性 #2 当天就兑现了**（"满屏 `#` 之后背景反而比结构显眼"）—— 这才是撤销的动因。"这张图有多大、边界在哪"现在由**一圈 `—` 边框**负责（第 23 步把行号槽与两行数字标尺换成了它 —— 靠边框，不靠填满）。两条不变量照旧：**① `_char` 恒返回 1 个字符**（全角字符占 2 个显示列，一引入就整图错位）；**② 一个字符只能代表一样东西** —— 守门员 `assertEqual(len(set(chars)), len(chars))` 是**通用断言**（钉的是"不重复"，不是具体字符值），字符怎么改它都守着。⚠️ 图例里那一项写的是字面量 `空格=空地` 而不是 `f"{_char('')}=空地"`：空地就是一个空格，直接拼出来是 `" =空地"`，看着像少打了一个字符。字符变化是 1 字节对 1 字节 ⇒ 地图自身的体量不变（第 22 步那张字节表的变化来自 banner/摘要，与字符表无关）。
- **判题器的回执有两个字段，"为什么"与"哪一条"分家**（第 14 步）：
  - **`errors`（顶层，本轮）** —— `[{errorCode, description}]`，**这是"任务为什么一直失败"的唯一答案来源**。
    码的含义（接口文档 L181-198）：**0 未知 / 1 任务超时 / 2 答案错误 / 3 网络错误 / 4 指令错误 / 5 LLM 额度超限**；
    **2 专指 `submitAnswer` 交的答案"不正确或者不完全正确"**，**5 只在每个游戏日的 3 次额度用尽时出现**
    （任务存续期间不占额度、也不报这个错）。**这段含义不写进代码** —— 写进去就是一份会跟文档漂移的第二真相，
    码表只活在 `CLAUDE.md` 这一行里给读日志的人看。
  - **`lastRoundRoleActionResults`（顶层，上回合）** —— `Map<int, boolean>`，key 是**角色/武器 id**、value = **动作合法性**。
    它答的是"**哪一条**没通过"：撞墙、打空这类**执行失败**不计异常、`errors` 里一个字都没有，只有这里会翻 `false`。
    解析时**原样保留**（含我们不操控的基地格），**排序是打印时才做的事**。
    ⚠️ **`"false"` 是个非空字符串**，`bool("false") == True` —— 只能认真正的 JSON 布尔，猜一次就会把"不合法"读成"合法"。
  - **`lastCmdResult` 是任务线唯一的沙盒窗口**（第 16 步接上，旧笔记写的"我们永远用不上"已作废）：
    它是**上回合 `executeCmd` 的执行结果**，格式 `[exitCode:N]\n<输出>`（超时 `[TIMEOUT]`、
    判题器异常 `[JUDGER_ERROR]`、超 64KB 尾加 `[TRUNCATED]`）。
    **那行状态不解析** —— 非空即**原文**回灌给 LLM，让 LLM 自己读；解析它就是又一份会跟判题器漂移的真相。
  - **`worldNews` 不读**：它是宝藏线（`summonTreasure`）的线索（样例那段的"石门需三钥"就是），与任务线无关。
- **复盘日志的体量与格式**（第 12 步，第 14 步扩了触发条件，**第 26 步大改**）：`Turn.summary()` 的每个列表**必须有上界**（`SUMMARY_MAX_ITEMS`）—— `Turn.robots` 是逐回合全量、没有上界，摘要的长度不能是"数据相关的量"。摘要里 **-1 是"字段缺失"不是 0**（金币/回合号打成 `?`）。**第 26 步起局面 = 摘要单条**（用户手改：**图例与整张地图退出日志**，`render()`/`LEGEND` 仍在代码里、只剩用例在用）。
  **摘要第 22 步起是四个块、第 23 步起各占一行、块间不留空行**（`【回合】`/`【我方】`/`【机器】`/`【可接任务点】`），
  我方那一行还打出 `石N铁N铜N`（"工人有铜却不去卖"只有背包明细能回答）；
  ⚠️ 首行前面那个 `\n` 是留给 `logging` 前缀的（**空行也是物理行** ⇒ 数记录里的物理行时要把它算上）。
  **记录数不固定，每条都"有事才吭声"**：局面、动作每回合各一条；**判题器报错 / 回执名单 / 任务行 / 沙盒行 / 提问**五条各自只在有内容时出现（干净的白天回合 2 条，样例那种带假错误的局面 4 条）。`errors` / `action_results` / `cmd_result` / `prompt` 的空值是**天然的安全值**，所以正常局面一个字节都不花。
  ⚠️ **`handle` 在 `_log` 之前还打一条 banner**（`###第N回合###`，用户加的）——
  它**不受 `_log` 上限管**，数"这一回合打了几条记录"时要单独加上（干净回合 3 条不是 2 条）。
  ⚠️ **日志的实际排列是 banner → 任务 → 沙盒 → 局面 → 动作 → 回执 → 提问**：任务行与沙盒行
  由 **`planner.task_channel` 自己打**（第 23 步搬过去的 —— 那两个字段只在它的作用域里），
  而 `task_channel` 先于 `_log` 被调用。⚠️ 它们因此**不在 `app` 名下**，只盯 `coregeek.app`
  的守卫看不见（见硬约束 5）。
  ⚠️ **"任务行 / 沙盒行"曾被误删过一次**（第 22 步恢复、第 23 步搬家）：那两块是黑盒下**唯一**
  能看见沙盒回执的地方（本地 e2e 的"LLM"是我们自己写的，只证明解析自洽）⇒ 动它们时别顺手删。
  **沙盒行只记结果、不记发出去的命令**：发命令那一回合 `llm_resp` 就是那次工具调用（新旧形状都算），已经印在任务行的"上一轮模型回复"里了，再抄一遍是**同一回合、同一条字符串抄第二遍**；而且"沙盒行数 = 实际跑过的命令数"这个对账关系更干净（第 18 步 e2e 实测过：九轮里只有一轮带 `lastCmdResult` ⇒ 沙盒行恰好 1 条）。
  **截断留痕**：`_clip(text, limit)` 超长时打 `…（共 N 字）`。原来那版 `text[:120]` 是**静默**截断 —— 任务一长，日志里就是一段没头没尾的文字，看不出后面还有没有内容，于是"任务一直失败"根本无从查起（第 14 步就是被这个坑叫醒的）。**上限的单位是字不是字节**（中文 1 字 = 3 字节）：**第 26 步起两个上限一并放宽（用户拍板"观察优先"）—— `LOG_TEXT_MAX`=40000**（单个输入字段——题目/LLM 回复/沙盒输出——基本不截）、**`LOG_PROMPT_MAX`=100000**（提问行也基本不截；代价见硬约束 5 的顶格行）。截断规则只有 `_clip` 一份，第 22 步从 `app.py` **搬进叶子模块 `src/coregeek/utils.py`**（与 `LOG_TEXT_MAX` 一起）。⚠️ **但 `describe` 那条边照旧是注入、不是 import**：`describe` 的契约是"不认识日志"，给它塞一个日志默认值就等于让 `protocol` 认识日志层。那个文件里**不放状态、不放 logger、不放格式化函数**，只放"上限"与"截断"。
  **`prompt` 打出来**（第 20 步改全文，第 26 步起截在 `LOG_PROMPT_MAX`）：它 = 模板 + 这道题的会话往来，工具清单长什么样、SOP 那个槽填进去没有、会话接没接上，实盘上只有这一行能回答（本地 e2e 的"LLM"是我们自己写的）。它**单独一行**（`【本轮提问】：…`，由 `_log` 打 —— `planner` 那边拿到的还是"返回值"），题目原文与 LLM 回复留在任务行里。
  **回执那一行列全部实体**（第 20 步改）：`上回合合法性：10010=False 10011=True …`，按 id 升序。判题器只回它**收到**的那几条 ⇒ "这条没发指令"与"发了但没过"在只列未通过名单时长得一样，而两件事下一步的做法完全相反。触发条件是"**有回执**"（全 `True` 也打 —— 那正是"发出去的都合法"的唯一证据），不是"有未通过"。
  **SOP 更新那一行**（`【SOP 更新】：流程「名」存 N 字（…）｜ 前 80 字：…`，第 37 步起带流程名；
  括号里按需出现 `剔除 N 处 <answer> 段` / `收到 M 字，超上限截断` / `超 5 条，丢最旧：「名」`）
  由 `agent/tools/sop.py` 自己打，
  **只在内容变化时出现**（LLM 把同一段反复喂进来时静默）。换行转义成 `\n` ⇒ 一条记录恒为**一行**
  （时间戳前缀只加在第一条物理行上，与"三块拼成一条"同一条理由）；截断留痕与 `_clip` 同源。
  **任务行的触发条件带 `llm_resp`**：任务刚结束那一回合 `phase_task` 已经空了，而那是唯一一次能看见"判题器最后答了什么"的机会 —— ⚠️ **所以任务行必须写在 `task_channel` 那道早返回之前**（第 23 步的原始改动把它插在早返回之后，那一格就永远打不出来了）。

## 文档地图

| 文档 | 用途 |
|---|---|
| `docs/任务书.md`、`docs/接口文档.md` | **权威规则与协议**。冲突时以这两份为准 |
| `docs/design/code-task.md` | **迭代留痕**：每一步的目标/产出/**仍生效的已知不确定性**，加文末一张「当前仍悬着的事」。动手前先看这里知道走到哪了 |
| `docs/design/task-analysis.md` | 规则整理笔记（标注了"文档明示 / 我的推断 / 矛盾点"三类，以及待实测清单） |
| `docs/design/strategy.md` | **当前策略导读**（第 31 步）：打什么 / 每回合的判据链 / 旋钮在哪。**代码是唯一权威**，本文只讲"为什么"；逐条规则不重抄，未了结项只指向 `code-task.md` 文末那张表 |
| `docs/design/code-design.md` | 上一版重写的设计稿。**正文已在工作区删除**，需要时 `git show HEAD:docs/design/code-design.md` 取；其中 §19「实盘接入踩坑」仍然有效，但**代码架构部分已被本次重写取代** |
| `docs/request.txt` / `docs/response.txt` | **只能当字段形状参考**。`response.txt` 不是合法 JSON（缺逗号 + 同 id 重复），不能 `json.load`；`request.txt` 是合法 JSON 但几何是手工示意数据（挑战者那两座墙不在环上），**不可用于校准几何**，`lastRoundRoleActionResults` 的值也别当真实信号解读 |
| `example/CoreGeek/CoreGeek/` | 官方 demo（**双层嵌套目录**）。入口形态照抄；`brain.py` 的 `_wall_order` 等已验证实现可参考，但整体策略是玩具级，不继承 |

## 当前工作区状态

本次是**推倒重写**：`main3.py` / `src/` 等已按第 1 步重写（旧版本在 git 历史里，`git show 5b4dfcf^:<path>` 可取回）。
`tools/`（selfcheck / smoke / decrypt_log）、`README.md` 目前**不存在**——按需再加，别凭惯性建。
`tests/` **按 src 拆分**（第 38 步，TDD 最小读取——改哪个模块只读/只跑对应文件），**不建自研测试框架**：标准库 `unittest` 够用。**349 条**：

```
tests/
├── _fixtures.py                 SAMPLE / _records / _terrain（**≥2 个文件共用**的夹具才进这里；
│                                discover 的 test*.py 不收它，也不会当用例跑）
├── test_protocol_actions.py     GateTest MoveWireTest ← protocol/actions.py
├── test_protocol_model.py       Parse TaskParse JudgeReceipt ← protocol/model.py
├── test_app.py                  HandleTest（红线退化 / 日志版面与预算 / 任务线端到端）← app.py
├── test_game_grid.py            Grid BuildGeometry StepOutside StepsBetween Path ← game/grid.py
├── test_game_world.py           TurnSummary DayNight ← game/world.py
├── test_game_planner_wall.py    建造线 11 类（BuildWeapon / WallRing / BuildWall / WallGate /
│                                DayEndGate / Dig / Door / Rescue / HoleLifecycle /
│                                TwoWallBuilders / StandingOnTheTarget）← planner.py
├── test_game_planner_economy.py 经济线 4 类（MineApproach / SpareOre / SellOre / UpgradeLine）
├── test_game_planner_night.py   夜战线（NightWeapon）
├── test_game_planner_task.py    任务线 3 类（TaskAccept / TaskHold / TaskChannel）
├── test_agent.py                AgentToolCall ← agent/agent.py
├── test_agent_chat.py           ToolReplyParse AnswerParse ← agent/chat.py
├── test_agent_prompt.py         ChatPrompt ← agent/prompt.py
├── test_agent_context.py        Context ← agent/context.py
└── test_agent_sop.py            SopState ← agent/tools/sop.py
```

⚠️ **类名与拆分前一字不改** ⇒ 文档与注释里"见 `XxxTest.yyy`"的引用照旧成立；单文件可直跑
`py tests/test_xxx.py`（文件头 bootstrap 自带 sys.path，`_fixtures` 靠它找到）。
进度见 `docs/design/code-task.md`（当前到第 43 步：**压缩判定挪到回合最末尾**——
判据 ②–⑥ 从早返回改成算出 `(prompt, cmd)`、链尾一道闸门 `prompt == "" ⇒ 填压缩请求`
（用户拍板"任务中、未生产模型请求才压缩"）；③ 不再自己捎（第 41 步写法撤销、命令轮
行为等价）、⑤ 答案轮从此也压缩、没任务/没开拓者在闸门**之前**绝不压缩；稳态 = 答案轮
与压缩轮**交替**（摘要回复轮 adopt + nudge，nudge 是模型请求不落闸门）；TDD（RED 3）、
用例 348 → **349** 全绿、反向验证 **3/3**；
第 42 步：**角色操作大改**——实体解析
（`Turn.walls`/`station_health/station_level`/`news`，墙 L2/L3 满血 1500/2000 是推断）；
**修墙线 `_repair_line`**（半血墙 = health×2<WALL_MAX_HP[level]，包优先重建兜底，
黑板 `repair_taken` 认领，优先级救援>修墙>建墙）；挖矿**性价比** =
价×新闻修正÷(到矿+采+回炮位)；**新闻查价**（判据①开口：没任务+officialNews 变了
⇒ 发 NEWS_PROMPT，额度 3/日指纹去重；裸 `<prices>` 回复路由进 `AGENT` 的价格期望表
——**第三处跨回合状态**）；顺路交易（`_shopping_list`+`_detour_buy`，WallFixer>升级券）；
**夜间重排**（基地残血<1/4+持券 ⇒ 贴基地 use 升级 > 机器人在场 ⇒ _defend >
怪清完 ⇒ 工人近矿经济 `near=NIGHT_WANDER=8`，build/remove 夜里非法绝不发）；
三口径拍板：包优先重建兜底 / 近矿限制 / 期望表+去重；用例 328 → **348** 全绿、
反向验证 3/3；
第 41 步：**压缩独立成轮**——命令轮的 prompt 槽
（本来空着）随 `executeCmd` 同发**压缩请求**（`prompt.COMPRESSION_PROMPT` 四槽指令 +
`Context.material()` 的**原始上下文全文**——用户拍板"原文永久保留、压缩总从原文重来、
消息表永不截断"；任务 prompt 卸掉第 39 步的搭车教学，**−681 字节/轮**、专注任务）；
压缩回复（**裸 `<summary>`**，`chat.is_summary_reply` 判）经 `AGENT.adopt_summary` 进摘要、
**不进会话表**，粘住幂等；互斥契约修订为"**任务提问不与命令同轮**，命令轮可带压缩请求"
（回复内容路由、永不当任务材料 ⇒ 活锁成因不成立；判题器零承诺 ⇒ 降级安全，回退 =
判据③改回 `return "", command` 一行）；TDD（RED 8）、用例 319 → **328** 全绿、
反向验证 **3/3**、字节 8382 → **7701**（−681）；
第 40 步：**工人联合规划（差事黑板）**——
`plan()` 回合内现算三本账：环缺口按在场工人数**切段**（A 前段 B 后段沿环推进，一个工人
⇒ 整段）、**矿格认领**（`ore_taken`，B 就近换一座不跟 A 奔同一座）、**路径预留**
（`paths` + `_reserve_path`，后处理的工人当软 avoid 整条让开、绕不开退回硬障碍；
**预留只在动身成功后**——先记再走会绕开自己的路，实测过）；开拓者任务空隙改领
**买券→用券**（跑腿者 = 持券者 > 真空闲开拓者 > 第一个工人；第 29 步"开拓者卖矿"
死线退役）。三个口径用户拍板：买券+用券 / **防御环优先**（救援 > 环 > 采石 > 升级 >
经济）/ 路径 avoid 上。TDD（RED 6 条）、用例 319 → **325** 全绿、反向验证 **4/4**、
**环完成竞速 51 vs 60 回合（同局面快 15%）**、双方收官 0 石头；黑板**不跨回合**
（第 11 步教训）；
第 39 步：**上下文压缩（执行摘要 + 渲染窗口）**——
`render()` 改组 **题目 + `【历史摘要】` + 最近 `_WINDOW`(2) 轮**（存储仍全量，prompt 从
O(全部历史) 变 O(窗口)，兑现悬置表 #19）；LLM 每条回复搭 `<summary>` 四槽摘要
（总目标/关键数据/已完成未完成/下一步，措辞在输出约定第 4 条），`Agent.hear` 被动提取、
**缺失保留旧值绝不重问**；`answer_of` **先挖全部结构块再扫**（摘要里的字面量 `<answer>`
不是答案）。顺带追认用户手改：沙盒回执 = `tool` 消息、纠错 = `user`、"做完了吗"决策指引
挪进 system；TDD（RED 8 挂全在新能力上）、用例 306 → **319** 全绿、反向验证 **3/3**、
字节 +881（7501 → 8382 / 605076 → 605957，**单轮变贵是已知代价、收益在多轮长任务**）；
第 38 步：**tests 按 src 拆分 + 用户启用 `EXAMPLE_PROMPT`**——
用例 306 条与拆分前**逐一比对一致**、反向验证"删一个类 ⇒ 少 13 条"可察；顺手删了死常量 `TASK_LINE`、
重钉 maxdepth 断言；**示例段进 sections 让提问那轮 +546**（6955 → 7501 / 604530 → 605076，字节表已同步）；
第 37 步：**`prompt.py` 接管 system 模板 + 工具协议换
嵌套形状（严格模式）+ SOP 流程表**（用户带来 `agent/prompt.py` 新文件、四口径逐条拍板）——
① **prompt.py**：六段模板（role定位 / 工具描述 / 输出格式 / 示例（**占位未启用**）/ 沉淀的SOP /
注意事项）+ `gen_system_prompt` / `gen_all_tool_prompt`（工具块 `## ToolName - 名`，由注册表生成）/
`gen_sop_prompt`（流程块 `## SopName - 名`，空表打 `（暂无沉淀）`）；修掉草稿两处毛边
（带 `{sop}` 槽的 `SOP_PROMPT` 原样拼接会**漏字面量 `{sop}` 进 prompt**、各段 strip 后再 join），
`OUTPUT_PROMPT` 的 SOP2Prompt 示例加 `<name>` 行；
② **chat.py**：删 `PROMPT`（模板搬去 prompt.py），`_params` 换**嵌套解析**（参数是
`<tool_param>` 里的具名元素、反向引用认同名闭标签、中文参数名也认），**严格只认新形状**
（属性式 / 裸参数 / 裸工具块全部 `None` ⇒ 落重问），参数值过 `_unescape`（五实体、`&amp;` 最后换）；
③ **agent.py**：`_sop` 改**流程表** `{流程名: 正文}`、`SOP2Prompt(name, sop)`、
`chat` 换 `gen_system_prompt` 接线、**删 `tool_desc`**（并入生成函数）、`tool_call` 只收具名参数
（位置填充删除；**认不出的名字必须不进 resolved** —— 全塞进去 `impl(**resolved)` 会 TypeError，
先写测试当场抓出来的）；④ **sop.py**：流程表 store（同名覆盖、异名追加、超 `SOP_FLOWS_MAX`(5)
丢最旧且日志点名；`SOP_MAX` 收窄成单条；空文本删那条）；判据链 ①–⑥ 一个字节没动；
TDD（先全改测试、42 挂 + 17 错全在预期类里才动实现）、用例 301 → **306** 全绿、
反向验证 **5/5**（无具名块放行 / 不反转义 / 同名不覆盖 / 不填槽 / 不过滤未声明名字）、
真服务 **9/9**（嵌套出命令 / 反转义 / 同轮沉淀+作答交块外答案 / 旧形状落重问 /
system 里有 `## SopName - 名` 挖过的正文 / 干净回合）、日志预算同脚本对照
**+467**（提问那轮 6488 → 6955、顶格 604063 → 604530；干净两行逐字节不动）；
第 36 步是**SOP 工具只做流程沉淀 + 答案只走 `<answer>` + 标签解析正则化**（用户逐条拍板）——
① **`SOP2Prompt` 只剩 `sop` 一个参数**（用户口径：
"只做流程沉淀，提交统一用 `<answer>` 管理"）：第 35 步那个 `answer` 参数**整个作废**，
`answer_of` 的"参数优先"那一级连同 `_ANSWER_PARAM` 一起删掉、退回**三级**（先挖掉工具块再扫 ⇒
原文判"像工具回复" ⇒ 原文即答案）—— 那条路本来就是死路（"声明即必需"的参数当不了可选的作答通道）；
② **SOP 正文里禁含 `<answer>…</answer>` 这对串**：`chat.strip_answers` 在入库前把**成对的**挖掉、
挖了几处进 `【SOP 更新】` 日志（**挖掉而不是作废整次调用** —— 沉淀是那个工具的全部价值）；
`PROMPT` 里也写了这条规矩，示例改回**两块**（工具块 + 块外 `<answer>`）；
③ **标签解析全换正则**（七个模块级 `re.compile` 替掉 `str.find` + 下标算术，成对性由模式里的
`</…>` 自带；删掉 `_span`/`_block`），**与旧实现对拍 36 条 × 3 个谓词只差 2 处、且两处都是修好了
旧版的坑**（把 `<tool_name>` 当开标签 ⇒ 会把 `x</tool_name>` 当命令发进沙盒）；
用例仍 **301** 条全绿，反向验证 **3/3** 全被抓（不挖工具块 / 清洗不留痕 / 整个不清洗）；
真服务三局面打穿（SOP 正文塞 `<answer>假答案</answer>` + 块外真答案 ⇒ 交的是**块外那份**、
`prompt=""`、存下来的 SOP 里那对串被挖掉；只沉淀 ⇒ 落库且重问；作废的旧参数形状 ⇒ 沉淀照落、
那一回合不提交）；日志预算重测 **6164 → 6497**、**603814 → 604147**（同为 +333，
干净回合与有回执逐字节不动）；
第 35 步是**任务 Agent 三处修正**（用户逐条拍板；
**不做独立压缩轮**；其中 ① 已被第 36 步撤销）——① **答案改走工具参数**：`SOP2Prompt(sop, answer)`
的 `answer` 是**必需**参数，`answer_of` 升成**四级**（参数优先 ⇒ 挖掉工具块再扫 `<answer>` ⇒
原文判"像工具调用" ⇒ 原文即答案）—— 治的是"sop 正文里的字面量 `<answer>…</answer>` 被当成答案
交上去"（`code-task.md` 记过这个缺陷；**修法第 36 步换成了"挖掉工具块"+"入库前清洗"**）；
② **纠错回灌带上判题器的 `description` 原话**（`【判题器反馈】：…`，拼在 `answer` 之后；
没答案**照样不纠错**）；③ `# 注意事项` → **`# 工作流`**（三条编号步骤，附一段可照抄的
"一条命令找文件并读完"范式），同时**删掉工具描述里与之打架的那句**（"若无法判定文件的位置…"，
它更具体、就在调用点上，会把"合并成一条"顶掉）；用例 295 → **301** 条全绿，
反向验证 **4/4** 全被抓；日志预算重测（提问那轮 **5733 → 6020**、顶格 **603375 → ≈603662**，
两行同为 +287）；
第 34 步是**`remove`（拆墙）+ 临时门**（用户口径：
绕路 ≥5 步或出不来就拆，当天当临时门、天黑前补上）——
① `actions.Remove` 落地（**仅工人 / 仅白天按保守口径 / 报文有实证且无 `name`**）；
② `_dig` 钩在 `_step` 里（一处覆盖全部差事、**8~9 个调用点一个字没改**）：工人贴着墙、
开洞能省 ≥ `HOLE_MIN_SAVING`(5) 步就拆，取"开洞后步数最少的那一格"；
③ `_rescue` 救被关住的人（**含工人自己**，实测门被两台机器人堵死时它自救），
`_stuck_inside` 用"**现在**"的障碍、**自己人不算障碍**（同事站门格只是路过，算成障碍会白拆一次）；
④ ⚠️ **首日豁免 `_after_first_day` 是整套机制成立的前提**（环上"刚挖的洞"与"还差一格没砌"
**逐字节同形**，只能靠时间区分；没有它第 1 天那格白天再也不补）；
⑤ **补墙零新函数**（`_door_open` 把那个洞当白天的门、末尾自动砌回，自然落进"卖→升级→采闲矿"）；
⚠️ **`HOLE_MIN_LEFT`(30) > `HOLE_PATCH_LEFT`(15) ⇒ 两窗不相交 ⇒ 一天最多拆一次**（这才是防反复拆墙的机制）；
用例 272 → **295** 条全绿，反向验证 **8/8** 全被抓（其中一条第一版用例空转、改成"两格门都堵"才抓住），
真服务五局面打穿（第 2 天绕路 ⇒ `remove (13,23)`、被困自救、**开拓者被挡 ⇒ 盒外工人 `remove (8,25)`**、
第 1 天与夜里**零 `remove`**），往返 p50 33 ms（红线 5000）；第 33 步是**门收窄到 2 格 + 正面昼开夜封 + 全部距离换 BFS
+ 收工闸门**（用户从实盘日志定位的三个夜间防守问题，第 ③ 条"清完机器人站桩"本步不做）——
① **门收窄**：`wall_cells` 从 14 格改 **18 格**（背面只留中间 2 格，`door_cells`），
**顺序改成沿环走一圈**（旧的"正面列 → 顶行 → 底行 → 背面 → 封口"实测 70 回合只砌上 17 格、
封口格当天没砌上；走一圈 18 格全砌上、第 65 回合收官、手里正好 0 块石头）；
**封口格 = 正面列正中 `(13,24)`、排最后** = "白天开着通行、天黑前砌上封死"，零新机制；
⚠️ 砌上拆不掉（`remove` 那时还没实现）⇒ 白天开口**只在第 1 天**生效（**已被第 34 步撤销**）；
② **距离口径分家**：`grid.steps_between` = BFS 真实步数（**-1 = 不可达**，调用方必须接住），
`_stones_to_mine` / `_sell_ore` / `_mine_spare_ore` / `_detour_sell` / `_upgrade_line` 五处
从切比雪夫换过来（回炮位要绕到背面门再横穿盒子，直线 3~4 格、真实 10+ 格，低估得离谱）；
`TIME_MARGIN` 从"吸收低估的补丁"变成真余量；实测 `plan()` **0.47 ms/回合**（红线 5 秒）⇒ 不做距离场缓存；
③ **收工闸门 `_leave_for_the_post`**：白天末尾回程来得及才收工，**只发 move**（复用 `_defend` 会在白天发 `attack` = 直通红线）；
④ ⚠️ **`_walled` 改口径（对批准计划的一处偏离，必须记）**：自己人**只在门那 2 格**算障碍
（原来一律算）—— 门收窄后"一个工人站在环内走廊格上"变成常事，旧口径让两人互相判成"被围"、
**来回踱步 16 回合 0 座墙**（合成局面实测），新口径下真会被关门的情况照样接得住；
⑤ 顺手修掉 4 处 "14 格" 旧字面量；272 条全绿；日志体量不变（干净回合 512 字节，
`CLAUDE.md` 记的 506 已陈 6 字节，与本次改动无关）；第 32 步是**任务 Agent 两处优化**——① **省一回合**：
`chat.py` 的 `PROMPT` 现在要求 `SOP2Prompt` **与答案写在同一条回复里**（配一条同回合形状的
示例）；**代码一个字节没动** —— `answer_of` 先认 `<answer>`、它落在 `<tool>` 块之外不干扰
`tool_of`，判据 ③′ 不早返回、⑤ 命中 ⇒ 同回合就 `submitAnswer`（原来拦路的是 prompt 那句
"一回合只输出一样东西"）；任务得分分母就是回合数，省一回合 = 直接加分。② **`tool_desc()`
换成块格式**（块首现为 `## ToolName: 名`——用户随后手改，原为 `## 名`；`Description:` / `Params:` + `    - 名: 用途`，无参打
`Params: （无参数）`，块间空行）—— 仍是**由注册表生成**，不手写第二份。三个谓词、判据链、
`Context` 全部未动；262 条全绿；真服务两个局面打穿（同轮沉淀+作答：`submitAnswer` 与
`prompt=""` 同时出现）；日志预算重测（提问那轮 4541 → **5630** 字节，顶格实质持平）；
第 31 步是**策略导读**——新建 `docs/design/strategy.md`，
把 `game/` 里在跑的策略梳理成 8 节（判据链 / 旋钮表 / 薄弱处），**纯文档、无代码改动**；
顺带记下表里第 23 项：升级线让 L2/L3 可达而 `Attack` 恒发一个 `targetPos`，
**这是唯一可能直通红线的新缺口**；第 30 步是**工具协议参数泛化**——`tool_of` 返回
`(名, [(参数名|None, 原文), …])`，`<tool_param>` 可零可多、带 `name=` 按名收；注册表加
**参数表**（一表两用：`tool_desc` 生成参数行、`tool_call` 按声明调度——无名位置填充、
带名按名对、认不出的忽略、声明参数一个不少且非空才 `impl(**resolved)`）；旧裸形状
`<tool>cmd</tool>` 落成位置参数特例全兼容；"只有名字没参数"从畸形变成 0 参工具的
合法形状；prompt 加一句多参/无参规则。**值永远是原文，不上 JSON**（executeCmd 的
参数是任意 shell 文本）。真服务四形状全链路验证；第 29 步是**经济线四件套**——① 修工人闲置：
`_mine_spare_ore` 先筛可行（走得到 + 回得来，回程 = 最近武器位）再按价挑；② 顺路卖矿
`_detour_sell`（绕路 ≤ 2 格先绕小贩）；③ 任务点全空 ⇒ 开拓者去卖矿（规则限制：它采不了
矿、没转移 ⇒ 通常没货，结构留位）；④ **升级券线**：`buy`/`use` 入 actions、`Weapon.level`
/`Turn.shop_prices`/`Map.shops` 入解析，`_upgrade_line` 无状态编排（背包认券、跑腿者唯一、
来得及才出发），**优先链 gatling>rocket>railgun**（群体打击口径：加特林每回合+10 无冷却
双弹分打 > 火箭齐射+30 但 3 回合冷却 > 电磁单目标）；249 条全绿；第 28 步是**prompt 换成
标准 messages JSON**——
`Context.render` 输出 `[{"role":"system"/"user"/"assistant","content":…}]` 的紧凑
`json.dumps` 串（自造的 `# 对话记录` 文本版式用户实测**效果非常差**、已弃；结构由
role 表达、正文即原文，`【题目】`/`【你的回复】` 包装删除）；消息模型、判据链、
粘住去重、system 每轮现刷全部不变。231 条全绿；提问轮日志 34 行 → **9 行**
（prompt 是单行 JSON，换行全被转义）；第 27 步是**武器打击改"最大伤害落点"**（用户改方针：
**打死所有机器人**，撤掉"补刀残血"）—— 火箭 `_rocket_site` 全场算中心 20 + 溅射 10（候选 =
机器人格及 8 邻格、落点须在射程内、溅射可及射程外、评分按剩余血封顶）；加特林/电磁
`_beam_site` 打**有效伤害** = min(伤害, 剩余血) 最高的（并列打近的）；`assigned` 记账让
同回合后开的炮避开将死者。L1 伤害常量入 planner（10/10/20+10）；第 26 步是**日志策略大改**——
局面 = **摘要单条**（图例与整张地图退出日志，`render()`/`LEGEND` 只剩用例在用）、
`LOG_TEXT_MAX` 400 → **40000**、`LOG_PROMPT_MAX` 1000 → **100000**（**日志基本不截**）；
"最坏 ≤ 9300"守卫撤销，换成结构性守卫
`test_a_clean_round_stays_small`（干净回合 506 字节 < 1500；顶格 ≈603KB 已知并接受）；
第 25 步是**任务内全量会话上下文 `Context`** ——
`agent/context.py` 新叶子（消息表 + `render`，零包内 import），`Agent.chat` 从"转发拼装"变成
**编排**（同题续会话/换题换新、回灌 `feed`、无新内容重问 `nudge`「请继续。」、system 每轮现刷），
新增 `AGENT.hear`（`task_channel` 每回合记 `llm_resp`，**发命令/交答案那轮也记**；粘住去重）；
`chat.py` 的自由函数 `chat()` 删除、`PROMPT` 只剩四段 header；判据链 ①–⑥ 与三个 `AGENT.chat`
调用点一字未动；`reset` 清两处状态。e2e 金表重推导（第 N 轮 prompt 含前 N−1 轮会话），最坏回合
**75 行 / 8944 字节**（对话记录标题 +2~3 行；上限 9300，余量 356）；
第 24 步是**武器落点改成"后列上角 + 前排两角"**——
后列从贴基地下沿的 `(9,23)` 挪到上角 `(9,25)`，动机是**操作者互不堵**（旧阵后列的环内
站位 `(9,22)/(10,22)` 压在底行路线上，+前上角操作者站 `(12,24)` ⇒ 前下角成孤岛）；
一条 diff 的事，`WEAPONS_BY_SITE` 配对不动；第 23 步是**日志措辞与版面**（用户手改 + 收尾）——
标签统一成 `【…】：`、**任务行与沙盒行搬进 `planner.task_channel`**（因此有了第二条不在
`app` 名下的日志 ⇒ 三条用例改成收 root）、**摘要去掉块间空行**、**地图去掉行号槽与数字标尺**
换成一圈 `—` 边框；响应报文一个字节没动，最坏回合 **71 行 / 8861 字节**（上限 9300，
余量只剩 439）；顺带证明量法可信：同一脚本在旧版代码上能量出 `CLAUDE.md` 记的 8723；
第 22 步是**卖矿线 `sell`** —— 工人墙砌完之后
**先卖后采**（`_sell_ore` → `_mine_spare_ore`）：挑收购价最高的那种矿，够本
（**货值 ≥ 2 × 到小贩的距离**，拍的）就背到**小贩周围一格内**一次卖光，石头也卖；
`Sell` 的**可用角色是全部**（与 `build`/`collect` 相反）、`Map.vendors` 是新的索引、
`_stone` 升级成 `_bag`（`BaseRole.stone` 变派生属性）；
顺带三件事：`_clip` 搬进新叶子模块 **`utils.py`**、摘要改成**四块**并打出 `石N铁N铜N`
（最坏回合 → **71 行 / 8723 字节**，**第 23 步的空行撤销后是 71 行 / 8861 字节**）、**地图字符表反过来**
（空地 = 空格、墙收回 `#`，即第 21 步"铺满"撤销）；
第 21 步是**地图铺满** —— 空地打 `#`（原来是 `.`，用户嫌它太轻）、我方围墙让位给 `=`
（**已被第 22 步撤销**，见上）；只碰字符表，响应与日志体量都不变，
测试里补了一条"一个字符只能代表一样东西"的守门员（这条断言是通用的，字符改了照旧守着）；
第 20 步是**日志打详细** —— 动作行改成
**通用摊开**（`describe(cmds, *, clip)`，有什么字段打什么）+ `prompt` 打全文（新上限
`LOG_PROMPT_MAX`=1000）+ 回执列**全部实体**（`上回合合法性：id=True/False …`）；
**响应一个字节没变**（第 18 步 e2e 九轮表逐字重现），代价是最坏回合 6180 → 8574
字节（约 10 → 7 回合写满 64KB，用户拍板"接受上升、重新钉上限"，守卫上限 6900 → 9300；
**第 22 步再抬到 8723**、第 23 步 8861，上限一直没动）；
第 19 步是 **Agent 改成单实例 + 状态在实例上** ——
用户要的"单实例，开拓者每次来注入这个实例"落成 `agent/__init__.py` 的 `AGENT = Agent()`，
SOP 从 `tools/sop.py` 的模块级 `_current` 搬成 `Agent._sop`，`chat.py` 因此退回**纯函数**、
`planner` 走 `from ..agent import AGENT`（**协议 / 判据 / 模板一个字节没动**，
判据是第 18 步的 e2e 九轮输出表**逐字重现**，含每一个 prompt 字数）；
第 18 步是 **Agent 系统**（`src/coregeek/agent/` 叶子包 + 其中的 `tools/` 注册表）——
工具协议定形为 `<tool><tool_name>…</tool_name><tool_param>…</tool_param></tool>`，
直接作答用 `<answer>…</answer>` 包裹，`SOP2Prompt` 让 PromptSOP 逐回合自进化（**全项目唯一一处跨回合状态**），
`planner.task_channel` 的判据链一条没改、只换了料并多出一条隐式子路径 ③′，
旧形状 `<tool>整条命令</tool>` 与裸文本**全覆盖兼容**；
第 17 步是**围墙只砌三面**（14 格，背面整列留成"门"；**格数与门宽已被第 33 步改成 18 格 / 2 格**，
闸门本身照旧）+ **不能把工人关起来**的闸门
（`_trapped` / `_walled` / `step_outside`；e2e 还顺带揪出并修掉了"人站在墙上时对着脚下同一格反复
`build`"的死循环 —— `_step_aside`）；
第 16 步是给任务线接上沙盒（响应顶层 `executeCmd` 与 payload 顶层 `lastCmdResult` 靠工具协议
\+ 结果原文回灌 + "答案错误"时的纠错回路串成闭环）；
第 15 步是采矿落实（墙砌完转去采收购价最高的矿），第 14 步是把判题器的回执记进日志，
第 13 步是武器落点"后列 1 格 + 前排两角"，第 12 步是地图可视化，第 11 步是开拓者任务线，
第 10 步是夜里操炮防守 ——**别照记忆里的进度走**。

**常用命令**：

```bash
bash run.sh 18085                                          # 起服务（自动挑 python3/py 并验版本）
curl -s -X POST --data-binary @docs/request.txt http://127.0.0.1:18085/
PYTHONUTF8=1 py -m unittest discover -s tests -v           # 跑用例；不加 PYTHONUTF8 中文会乱码
```

⚠️ **起服务前先确认端口上只有一个监听者**：`netstat -ano | grep LISTENING | grep 18085`。
Windows 允许 `SO_REUSEADDR` 并存，上一轮遗留的进程会继续吃请求 —— **症状是"改了代码没反应"，
本地怎么调都是旧行为**（第 7 步真踩过：合成局面上 `build` 一条不发，其实是旧进程在答）。
杀干净再起：`netstat -ano | grep 18085 | awk '{print $5}' | xargs -n1 taskkill //F //PID`。

本地 **`python` 是 3.7.1**（Anaconda），必须用 `py`（3.13）；`run.sh` 已处理这个坑。
