# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

华为云核心网第十届编程大赛《未来战争》参赛 Agent。判题器按约定拉起进程，每回合通过 HTTP 推送全量战场状态，要求 **5 秒内**返回一条合法 JSON 指令。判题器是黑盒——**没有"打一局试试"这回事**，改动的正确性只能靠本地实测 + 赛后复盘。

## 编码规则

1. **禁止冗余设计。** 加任何东西（模块、抽象、常量、配置项、防御性分支）之前先问："**这一步**用得上吗？" 用不上就不加。判据是"现在有没有第二个使用者/第二种情况"，不是"以后可能需要"。**宁可后续重构，不要预先抽象。**
2. **每一步都要在 `docs/design/code-task.md` 留痕。** 每节三段：目标 / 产出（含验证结果）/ **仍生效的已知不确定性**。**新增一律只追加**；已完成的小节可压缩成"目的 + 结果"（决策理由、已失效的「不做什么」、粘贴的逐条命令输出都可以删）。⚠️ **仍生效的已知不确定性一条都不准删** —— 那是**重新推导代价最高**的部分，删了等于把学费再交一遍（已经兑现或被后续步骤推翻的可以删，就地写明）。未完成的下一步统一收在文末「**当前仍悬着的事**」一张表里，不散在各步。
3. **红线优先于一切优雅。** 见下节——5 次异常即出局，任何设计取舍在这一条面前让步。
4. 判题环境只有标准库：`pyproject.toml` 的 `dependencies` 保持为空，语法不得超出 3.11。
5. `logs/` 等运行期产物**绝不入库**。
6. **代码注释只写"现在"，且写到最短。** 格式以 **`planner.task_channel`** 为准：docstring = **是什么 + 契约**（一两行，必要时补几行边界/坑）；内联 `#` = **一句话的"做什么"标签**（例 `# ② 回灌结果、这轮绝不发命令（必须压在 ③ 前）`）。**关键判据压成一句保留，成段论证与决策史删掉**；`#:` 只留给模块级/类级常量，函数体内一律收成 `#`。
   只回答：是什么、契约（参数/返回/边界）、读代码必须知道的坑与红线。**不写步骤号（"第 N 步"）、决策史（谁拍板、口径、旧版如何、何时改的）——那些只进 `docs/design/code-task.md`**；也不用 `**加粗**`、`⚠️` 这类标记，平实中文。**"拍的/未实测"的一句话标注保留**，指向守门用例的 `test_xxx` 也保留。

## 硬约束

1. **`main3.py` 的文件名是平台契约，不要重命名。** 平台照官方 demo 的约定拉起它。改错的症状极具迷惑性：进程**从未启动** → 没有日志、没有报错、判题器也不报错，只是"所有单位一动不动"，而本地 `run.sh` 怎么跑都是对的，排查会被完全带偏。
2. **异常红线 = 5 次**，之后该队整场不再被调度。判题器只认三类：连接/响应超时、**响应格式错**、**指令非法**（字段缺失 / 动作码非法）。
   - 所有失败路径都必须退化成合法空指令 `{"roleCommandMap":{},"prompt":"","executeCmd":""}`——空指令**合法且不计异常**，宁可丢一个回合，不赌全队资格。
   - 注意区分：**指令非法**（计异常）vs **指令执行失败**（移动碰撞、攻击落点无目标 → 该条标记无效，**不计**异常）。
   - 响应三个顶层字段必须**永远都在**（官方 demo 只发了 `roleCommandMap`，别照抄这个）。
3. **动作有角色权限**（任务书 §4.4 表的**最右一列**）：`build` / `remove` / `collect` 仅**工人**；`acceptTask` / `submitAnswer` / `summonTreasure` 仅**开拓者**；其余动作全部角色可用。
   - 闸门在 **Action 的构造函数**里（`protocol/actions.py`）：`roleType` 不对就抛 `PermissionError`，**非法动作根本造不出来**。planner 侧接住它、丢那一条并告警，不连坐同回合其他角色（抛出去会变成"每回合空指令 → 全队冻结一整局"，现象与 `main3.py` 改名事故一样难排查）。
   - 这类 bug 的特征是**本地全绿**（格式完全合法，只有判题器会说"不"），而 `collect` 被误发给开拓者会每天吃一个异常——**红线只有 5 次**。所以每加一个受限动作，都要在 `tests/test_protocol_actions.py` 补一条对应用例。
4. 判题器侧超时：建连 > 10s 或响应 > 5s。
5. **每回合的复盘日志（`app._log`）写在 `try` 里面，别挪出去。** 日志代码再不起眼也是代码，`web/server.py` 的 `do_POST` **不接异常**（`server.py:22` 直接 `handler(...)`）—— 逃出去连接就断了，判题器那边是"响应超时"（红线第一条）。放进 `try` 里，最坏只是退化成空指令（丢一个回合，合法、不计异常）。
   - 局面 = **摘要单条**（图例与整张地图退出日志）；`LOG_TEXT_MAX` = 40000、`LOG_PROMPT_MAX` = 100000（用户拍板"观察优先"，**日志基本不截**，"日志长度不能是数据相关的量"那条旧原则就此让位）。
   - ⚠️ ⇒ 若判题器**不读** stdout，**顶格回合**（题目/回复/沙盒各 40000 字）**一回合**就写满 64KB 管道 ⇒ 阻塞到响应超时 ⇒ 直通红线。**已知并接受**。剩下的守卫是**结构性**的 `test_a_clean_round_stays_small`（干净回合 < 1500 字节），不是字节上限。
   - ⚠️ **具体字节数、量法（三个坑）都在 `app._log` 的 docstring 里**，那里是权威副本 —— **别凭记忆写，也别在本文件里维护第二份**。**只有带 prompt 的那两行会动**（跨步骤比绝对值没意义，同一次测量的增量才是信号）。
   - ⚠️ **有两条日志不在 `app` 名下**：SOP 那条走 `coregeek.agent.tools.sop`，任务行与沙盒行走 `coregeek.game.planner`（那两样字段只在 `task_channel` 的作用域里）⇒ 只盯 `coregeek.app` 的守卫**看不见它们**。守卫一律 `assertLogs(level="INFO")`（root）。**加任何新 logger 之前先想这一条。**
   - ⚠️ **banner 是 `_log` 管不到的那一条**（`handle` 打的 `###第N回合###`）：它不受上限约束，数记录数时要单独算。

## 架构

骨架（逐步记录见 `docs/design/code-task.md`）：

```
main3.py              入口：读端口 → chdir → src/ 进 sys.path → 起服务。不放策略
run.sh                接口文档规定的 bash run.sh <port>；陪跑本地（python3 / py）
src/coregeek/
├── utils.py          **叶子模块**：日志层的两条**规则** —— `LOG_TEXT_MAX` 与 `_clip`。
│                     不放状态、不放 logger、不放格式化函数
├── app.py            组装根：handle(bytes) + run(port) + 每回合复盘日志 `_log`。**红线所在**，异常一律退化成空指令
├── web/server.py     HTTP：收字节 → handler → 回字节。handler 由 app 注入，不认识游戏概念
├── protocol/         线上格式：读与写，**只有这里知道字段名**
│   ├── model.py      payload → Turn（容错解析；含判题器回执 `errors` / `lastRoundRoleActionResults`
│   │                 与**沙盒回执** `lastCmdResult`）
│   └── actions.py    BaseAction + 各动作（`move` / `build` / `remove` / `collect` / `attack` / `sell` / `buy` / `use` / `acceptTask` / `submitAnswer`）。
│                     **创建即校验**（角色权限），唯一懂线上动作格式的地方（`to_wire` 写、`describe` 读）。
│                     `describe` **通用摊开**（有什么字段打什么，字符串值过 `app` 递进来的 `clip`），
│                     不维护字段清单 ⇒ `sell` 的 `name`/`num`、`buy` 的 `name`/`num`、`use` 的 `name`/`targetPos` 自动就打得出来
├── agent/            **叶子包，只依赖标准库**：跟 LLM 说什么、怎么解析它的回复
│   ├── __init__.py   `from .agent import Agent` + **`AGENT = Agent()`**
│   │                 —— ⚠️ **跨回合状态（三处）都在这一个实例上**：`_sop` 整场存活、`_context` 任务内、价格期望表
│   ├── agent.py      `class Agent`：`_sop`（**流程表** `{流程名: 正文}`）/ `_context` / `_tools`
│   │                 （**表由实例构造**，`SOP2Prompt` 是绑定方法）
│   │                 + `chat`（**编排**：建/续会话、刷新 system、渲染）/ `hear` / `tool_call`
│   │                 / `SOP2Prompt(name, sop)`（先 `chat.strip_answers` 清洗再入库）
│   │                 / `sop`(property) / `reset`（流程表与会话**两处一起清**）。
│   │                 `tool_call` **只收具名参数**：认不出的名字**不进 resolved**、
│   │                 声明参数一个不少且非空才 `impl(**resolved)`
│   ├── chat.py       四个谓词：`tool_of`（严格，**只认嵌套形状**）/ `looks_like_tool`（宽）/
│   │                 `answer_of`（三级，**先挖全部结构块**：工具块 + 摘要块）/ `summary_of`（执行摘要，
│   │                 **best-effort**：取不到 ⇒ `""` 不回落）+ 一处清洗 `strip_answers`
│   │                 （挖掉成对的 `<answer>…</answer>`，唯一的调用者是 `SOP2Prompt`
│   │                 —— **SOP 正文里不许有这对串**）
│   │                 ⚠️ 标签解析全是模块顶部的 `re.compile`（成对性由模式里的 `</…>` 自带；
│   │                 只有 `looks_like_tool` 故意只认前缀 `<tool`）+ `_unescape`（五个 XML
│   │                 预定义实体、`&amp;` 最后换）。**模板不在这里**（在 prompt.py）
│   │                 **纯函数，零包内 import**
│   ├── context.py    `Context`（**存储全量、渲染有窗**）：消息表 + `render()`
│   │                 （**标准 messages JSON**；渲染 = **题目 + `【历史摘要】` + 最近 `_WINDOW`(2) 轮**，
│   │                   窗口按"最后 N 条 assistant 及其后全部"切，短任务掐不着；
│   │                   沙盒回执 = `tool` 消息、纠错 = user）；
│   │                 `feed` 回灌 / `nudge` 重问收尾 / `hear` 记回复（**粘住去重**）/
│   │                 `summary` 字段（`Agent.hear` 被动提取）。
│   │                 **只依赖标准库，状态不在这里**（在 Agent 上）
│   ├── prompt.py     **system 消息的段模板与生成函数**：六段
│   │                 （role定位 / 工具描述 / 输出格式 / 示例 / 沉淀的SOP / 注意事项；
│   │                   输出约定含 `<summary>` 四槽那条；ROLE 段带**沉淀时机**、
│   │                   【沉淀的SOP】段里没有就存、存过不重复、**正文只写通用流程**
│   │                   （名字泛化到"一类问题"；正文收**通用**的东西——接口定义、参数定义
│   │                    这类环境知识正是"第二次能跳过试探"的原因，**不收本次的取值**
│   │                    ——这次的目的地、这次拿到的凭证 —— 条目整场存活、跨任务复用，
│   │                    夹带了本次取值的比不沉淀更糟）；
│   │                   示例段两例 = "从 problem.txt 到 token"的范式 + **同一类任务两次的
│   │                   few-shot**（第一次沉淀、第二次跳过探索直接照做）；
│   │                   注意事项段 = **回合成本模型**（一条命令能干多少就干多少：循环/分支/
│   │                   批量都属于同一条命令）+ 产出要写给下一轮的自己看）
│   │                 + `gen_system_prompt(tools, sop)` / `gen_all_tool_prompt` / `gen_tool_prompt`
│   │                 / `gen_sop_prompt`。工具块 `## ToolName - 名` 与流程块 `## SopName - 名`
│   │                 **由注册表/流程表生成、不手写第二份**；两个 `{}` 槽的模板正文里不许出现
│   │                 别的裸 `{}`（`format` 的 `KeyError` ⇒ 整回合退化空指令）
│   └── tools/
│       ├── __init__.py  只剩一段 docstring（注册表在 `Agent.__init__`）
│       ├── cmd.py       `executeCmd(cmd)` —— **原样返回**（执行者是判题器的沙盒，我们不在本地跑）
│       ├── pyexec.py    `run(code, timeout=2.0)` —— **本地**纯计算执行器：
│       │                AST 白名单（import 仅限纯计算标准库模块）+ 内置白名单 +
│       │                守卫线程超时（**红线**：5 秒响应预算，死循环当场 `[TIMEOUT]`）
│       │                + 输出截断；**绝不抛异常**、零状态。护栏防的是 LLM **误伤**
│       │                不是恶意逃逸（LLM 是自己的解题者）。与 `cmd.executeCmd` 的分工：
│       │                那是判题器沙盒（能看任务文件、一回合往返），这是本地即时算
│       │                （产出当回合进 prompt、看不见沙盒）。
│       │                ⚠️ **白名单模块启动时预热**（实测踩出来的：本机 `import statistics`
│       │                冷加载 > 2 秒守卫超时 —— 5 秒响应预算是红线）
│       └── sop.py       `store(current, name, sop, *, stripped)` / `describe(name, received, stored, stripped)`
│                        / `SOP_MAX`（**单条流程**的上限）/ `SOP_FLOWS_MAX`（条数上限，超了丢最旧
│                        且日志点名）—— **规则**，状态不在这里；`stripped` 只进日志 ——
│                        它是"LLM 又把答案格式写进 SOP 了"的唯一信号。
│                        ⚠️ `LOGGER` 留在这里是有意的：logger 名 `coregeek.agent.tools.sop` **不变**
└── game/             领域与策略
    ├── grid.py       Pos / 8 方向 / **两个距离口径**：
    │                 `Pos.dist` = 切比雪夫直线（**选点**用）/ `steps_between` = BFS 真实步数
    │                 （**回合预算**用，不可达返回 **-1**）；`step_toward`（**BFS 最短路**，终点
    │                 是"贴着 goal 一格"）/ `step_onto`（同构，终点是 **goal 自己** —— 岗位用）
    │                 + 基地几何：`base_cells` / `weapon_cells` / `weapon_sites` / `wall_cells`
    │                 （正/背面方向判据在 `_front_back`，**只此一处**）/ `door_cells`（背面整列里不砌
│                 的那些格 = 后方通道：14 格时 6 个、`sealed` 时 4 个）
    │                 + `box_cells`（防御盒子 36 格）/ `step_outside`（迈出盒子的第一步，闸门用）
    ├── map.py        Map：格子矩阵（每格一个**类别**）+ `blocked`/`ores`/`vendors`/`shops`/`station`
    │                 + `render()`（**整块**：上下各一行 `—` 标尺 + 每行 `│`…`|`）
    │                 + `LEGEND`（图例，**由 `_NAMES` 生成**）。
    │                 **纯地形**：武器名册不在这里。
    │                 `shops` = 武器商店格子（`zones` 的 `weaponShop`），买券线的目标点
    ├── roles.py      §4.5.2 的 Pioneer / Worker（各带自己的 `bag`，`stone` 是它的**派生属性**）；`make()` 只认角色，建筑返回 None
    ├── world.py      Turn（round_no / map / roles / gold / weapons / robots / task_points
    │                 / phase_task / llm_resp / cmd_result / vendor_prices / shop_prices / errors / action_results)
    │                 + `within` / `is_day` / `day_rounds_left`（白天语义，夜里恒 0）/ **`rounds_left`**
    │                 （本段剩余回合，夜里同样成立 —— 经济线的预算用它）/ **`summary()`（四块，各占一行）**
    │                 + `Weapon`（id/kind/pos/attack_range/cooldown/**level**）、`Robot`（pos/health）、
    │                 `Error`（code/description，**码不翻译成文字**）三个 NamedTuple
    └── planner.py    决策。**策略只写在这里**（**两段式**：`plan()` = `_intents`（第一段，逐角色出
                      "初步行为"——能直接干的 act 当场落 `q.cmds`，要走路的只交 `(角色, 目标)` 意图）+
                      `_walk_out`（第二段，按同一顺序批量解走路意图——BFS 落一格、记**落子账 `claimed`**
                      与**路径预留账 `paths`**，后解的把已落的子当硬障碍、已预留的路当**软避让**——
                      绕不开退回硬障碍照走，绝不原地卡死；**预留只在动身成功后**——先记再走会绕开
                      自己的路，实测过）。黑板（`segments` 环缺口**切段**、`ore_taken` 矿格认领、
                      `sites` 建造格、`taken` 炮位、`repair_taken` 修墙格、`budget` 金币预留）
                      是**第一段的决策账**，逐角色顺序累计、**不跨回合**。
                      `_Queue.step` 是走路意图的公共出口；
                      `provider` 型意图（迈出盒子/挪开自己）第二段拿落子账现算。
                      白天：**开拓者去接任务**（任务点全空 ⇒ 领**买券→用券**差事；**武器有缺时
                      升级差事也不跑**）；工人的顺序（**用户拍板：武器最优先**，无论哪一天武器没了先建）：
                      **建武器（`_slots`，份额+钱够 ⇒ 建/走向落点）→ 筹资（武器有缺但钱不够且
                      `_can_fund`——有小贩有价可卖 ⇒ **整条墙线让位（含修墙）**：先卖背包的货、
                      再采最值钱的矿凑 25 金币；筹资不可行 ⇒ 照旧走墙线）→ 修墙 `_repair_line`
                      （弱墙 = 血 < 满血 1/5：**升级当修** —— L1/L2 用围墙升级券（20/30 金，
                      升级同时回满血、上限抬一档），L3 到顶才用 10 金的修复包）→ gated 待命 →
                      采石砌墙 `_build_walls`（**返回 bool**：`True` = 本回合指令已由墙线产出、
                      `False` = **什么都没发过** ⇒ 落兜底——这个不变量防的是 `_emit` 是 `cmds[id]=…`
                      后写覆盖先写；石矿认领只在 `want>0` 之后——石头够了的工人不占矿）
                      → 经济线兜底 `_economy`（`_sell_ore` → `_upgrade_line` 买券用券（优先链
                      rocket>gatling；跑腿者 = 持券者 > 真空闲开拓者 > 第一个工人；
                      **武器有缺时跳过**；钱不够但卖得起背包里的货 ⇒ 先卖矿凑）→
                      `_mine_spare_ore` **可行矿里挑性价比** —— 这一套夜里清场后原样复用）；
                      验收网 `DayOneFinishTest`：两座石矿的可行地图
                      70 回合内环 14/14 + 三座武器、环砌完前工人零闲置）；
                      白天末尾还有一道**收工闸门 `_leave_for_the_post`**（环砌完了才回那一组的
                      **岗位**，**只发 move**；判据 `步数 ≥ 白天剩余 − POST_MARGIN(3)`）；
                      **所有回合预算走 BFS**；
                      白天还有**拆墙放人**：`_rescue` 先救被关住的人（含工人自己）—— 开"开了真能
                      让某个被困的人迈出去、且离救援者最近"的那一格（`remove` **唯一**的调用点）；
                      夜里：**工人 + 场上没有活机器人（`_alive`）⇒ `_economy`**（白天那一套的复用）；
                      **其余角色**认领一组武器（`_weapon_groups`）走到它的
                      **岗位**（`_post_spots`：多座组共用一个操作位、单座组站在炮旁），到岗按
                      **最大伤害落点**开火（火箭全场算中心+溅射、加特林/电磁打有效伤害最高的，
                      `assigned` 记账防挤将死者；伤害与 `targetPos` 份数都随 `Weapon.level` 放大
                      —— 份数 = 等级、电磁恒 1、多发同点）；**先开火、打不了才挪岗**（开火那一支
                      排在岗位判据**之前**：岗位格被占/走不到都不是"这一组不用管"）；
                      ⚠️ **主岗位一个都站不上时退到 `_near_spots`**（贴着组内任意一座的格子），
                      只守得了一座也照打 —— 没有这条退路时整组一发不打（第 77 步）；
                      ⚠️ **开拓者是补位炮手**（`_pioneer_mans_guns`：工人数 ≥ 组数 ⇒ 它一组都不
                      认领），**夜里与收工闸门两处同源**（只拦一处 ⇒ 白天把它送进岗位、夜里又不
                      认领，那格被占着、火箭组整组没人操）；例外只在 `_defend`：它已经站在某组的
                      操作位上（`_stands_on_a_post`）⇒ 认领那一组（那一格它占着、工人站不上去）；
                      （`taken` 记的是**整组**、开火或动身那一刻入账 ⇒ 同组不会被两人同时打）
                      + `task_channel(turn)` 产出响应顶层的 `(prompt, executeCmd)`
                      （任务线唯一的对外通道，**两者互斥**）
```

依赖方向：

```
app → web / protocol / game / utils   protocol → game
game/planner → protocol/actions       ← 第一条"由内往外"，只走指令编码
game/planner → agent                  ← 第二条"由内往外"，**只收字符串、不收 Turn**
                                        **走包根那个单实例**：`from ..agent import AGENT`（不是 `from ..agent import agent`）
game/planner → utils                  ← 任务行自己打 ⇒ 要 `_clip`（只借规则，不借状态）
```

⚠️ `utils` 是**叶子**（一个包内 import 都没有），目前 `app` 与 `game/planner` 取它。
`protocol.actions.describe` **能** import 它了，但那条边**仍然走参数注入**（`describe` 的契约是"不认识日志"）。

- 三件事：**怎么收（web）/ 报文长什么样（protocol）/ 打什么（game）**，`app` 把它们接起来。**没有第四件事就不加第四个包**，也不提前建空目录。
- **`agent` 与 `planner` 的分工**：决定"**什么时候**跟 LLM 说话"（判据链、纠错、开拓者在不在）是**策略**，
  留在 planner；"**说什么、怎么说、怎么解析回复**"全在 agent。机械保证是 **`chat(request: str, …)`
  收字符串、不收 `Turn`** —— 收了 `Turn`，这条边就变成双向，那时该把"什么时候说话"也搬进 agent。
- `app.py` 与 `planner.py` 的分工是**红线 vs 策略**：`app` 管"永远回得出合法报文"，`planner` 管"该做什么"。别让策略代码有机会破坏报文格式。
- `roleCommandMap` 的 key 用**字符串**（JSON 对象的 key 本来就是字符串）。
- ⚠️ **`handle(raw: bytes) -> bytes` 不是**严格无状态的纯函数**：`agent/__init__.py` 那个
  **单实例 `AGENT`** 身上有**三处跨回合状态**（都是实例属性）：`_sop`（PromptSOP 自进化沉淀，
  **整场**存活、跨任务）、`_context`（任务内会话 `Context`，**题目变了即换新**）、价格期望表。
  共同的账：① 它们只影响 prompt 的**内容**（答得好不好），**报文字节照旧合法、不碰红线**；
  ② **不加锁**（判题器逐回合同步请求，GIL 下赋值/读取不撕裂）；③ SOP **不按任务分区**，
  会话**按任务分区**；④ 加第四处跨回合状态之前先想清楚是否需要、以及失败时的退化路径
  （"跨回合标志位一旦卡住会**静默关掉整条任务线**"这条教训仍然成立）。
  **火箭冷却的本地账加了第四处**：`planner._fired`（本地开火账，模块级 dict {武器 id: 回合号}，
  火箭冷却的观测状态——判题器不发 cooldown 字段）。它过得了 ④ 那道门：按 id 键控、
  期满自动过期、武器换 id 旧账自愈，卡住的最坏代价 = 某座火箭少打 ≤3 回合（不碰红线）。
  用例隔离 = `planner._fired.clear()`（夜间用例的 setUp 里）。
- ⚠️ **单实例的代价**：`from ..agent import AGENT` 绑的是**对象**，测试**换不掉**它 ⇒
  用例只能靠 `Agent.reset()`（生产代码里唯一一个只为用例存在的公开方法）隔离。
  想知道"现在的 SOP 是什么"，只有 `AGENT.sop` 一个地方可去。
- 领域对象只带**当前步骤真正用到**的字段（`BaseRole` 目前只有 id/pos/type_name）。加字段之前先问这一步用不用得上——**尤其别把 §4.5.2 的 HP/背包上限写成类常量**，payload 里的 `health`/`backpack` 才是权威的当前值。
- **不加分层 import-lint**：现在一共 4 个子包，违规肉眼可见；上一版为此写的 ast 检查属于过度设计。

## 会影响策略正确性的领域事实

以下几条是读了任务书/接口文档/样例交叉验证得到的，**重新推导一遍的代价很高**：

- **日历**：130 回合 = 1 天（白 70 + 夜 60），10 天 1300 回合。`within = (roundNo-1) % 130 + 1`；`within <= 70` 为白天（`Turn.is_day`；`round_no` 缺失是 -1 ⇒ 判成夜里 ⇒ 不建造）。样例 `roundNo=85`（夜晚，且场上确有机器人）已交叉验证。**昼夜不只是氛围**：`build` / `remove` 仅白天（`remove` 那半边是**项目口径**：§4.4 没写它，取保守方向，见下）。
- **开局是 0 武器 + 75 金 + 3 角色**（1 开拓者 + 2 工人），恰好买满 3 座武器（25×3=75）。⚠️ `docs/request.txt` 是 `roundNo=85` 的**中局快照**（已 3 武器 + 20 金），不是开局——曾把它当开局状态，推出"造武器是死支出线"，后果是第 1 天一座武器都不造。
- **固定 36 格防御盒子**：基地 2×2 + 12 格武器环 + **20 格围墙环（单层，不可加厚）**。**一切坐标由己方 station 的 pos 推导，禁止绝对坐标**（上下半场换边后基地会挪）。⚠️ **`pos` 展开成哪四格以 `grid.base_cells` 为准 = `[bx, bx+1] × [by-1, by]`**（左半基地 `(10,24)` ⇒ `(10,23)/(11,23)/(10,24)/(11,24)`）—— 接口文档说 `pos` 是"左上角"，**照那个说法展开的框对不上样例**（样例三座武器会掉出可建造环），按 `base_cells` 这个角展开才三座全在环上。可建造区在 `mapInfo.zones` 里**不提供**，只能这样推。
- **可建造区公式只存在于图里**（`docs/pic/build_map.png`）。基地 `(bx,by)` 是左上角 ⇒ **武器环 = `[bx-1, bx+2] × [by-2, by+1]` 去掉基地 4 格 = 12 格**；围墙环再往外一圈 = 20 格。实现在 `grid.weapon_cells`。样例几何整体是手画的、不可作校准，但武器环这一处对得上（样例三座武器都落在环上）。**算错 ⇒ `build` 落点非法、那 25 金币白花。**
- **围墙砌 14 格（第 3 天起 16 格），背面整列常年敞开**：6×6 边框 20 格里只砌 U 形 —— **正面列 6 + 上下两行各 4**（背面那 6 格全空，`grid.door_cells` 给的就是它们 = 后方通道）；`sealed=True`（`planner._sealed_back`：`round_no > 2 * ROUNDS_PER_DAY`）再补**背面两个角格**，通道从 6 格收到 4 格。`grid.wall_cells` 给的顺序 = **正面列（迎着机器人）→ 下行 →〔背面两角〕→ 上行**；正面/背面的判据与 `weapon_sites` 同源（都在 `grid._front_back`；左半 ⇒ 正面 `bx+3`、背面 `bx-2`）。⚠️ **正面列排第一位就是"回合不够时优先建面向敌人方向"的落地**，零新机制：`_ring` 保序、`_build_walls` 取 `free[0]`。⚠️ **这个顺序没有实测背书**（旧的 18 格"走一圈"是第 33 步实测出来的）⇒ 首场看"第一个白天末尾砌了几格"，14/14 才算过（第 57 步不确定性 1）。
- **后方通道里没有任何建筑**（三座炮全在**武器环的正面列**、不在这一列）⇒ 能堵门的只有**单位** —— "自己人站在待砌格上算不算障碍"只在这里有意义（`_walled` 的判据来源）。⚠️ 它**常年开着**：第 17/33/34 步那套"闭环 + 2 格永久门 + 白天拆一格当临时门"在第 57 步**整套删除**（`_dig` / `_door_open` / `HOLE_MIN_SAVING` / `HOLE_PATCH_LEFT` 全没了）—— 后方一敞开 `_door_open` 就恒真、墙被打掉也不补。
- **`_ring` 的例外（踩过坑，别改回去）**：候选墙格要排除 `blocked`，但**我方角色站的那一格除外**——`model._entries` 会把 `teamOur.roles` 写进网格，于是"谁站在待砌的墙格上"会让那一格从候选表里消失，另一个工人的 `free[0]` 跟着后移、掉头去砌更靠后的墙；等前一个工人一挪窝目标又变回来，**两人在两格之间对着改目标转到天黑，一格都砌不上**（端到端实测 26 回合 0 座）。已砌的墙/中立元素/机器人照旧排除。
- **`_entries` 把单位铺在最后 ⇒ 人站在墙上时那一格"看不见墙"**（e2e 揪出来的重砌死循环，两头都要记）：那一格照旧在 `Map.blocked` 里，但**看不出是墙还是空地**。于是"已砌的格子"会因为有人站在上面而被 `_ring` 的例外**复活成候选**，`dist == 0` 又是合法建造位 ⇒ **每回合对脚下同一格 `build`，永远轮不到下一格、石头白花，`free` 也永不为空**（连"砌完了去采最值钱的矿"都进不去）。实测：40 回合卡在 12/14，最后 4 回合反复砌同一格。**修法 = `role.pos == target` 时这一回合只挪开一格（`_aside_cell`）、下一回合再砌**：砌过的没人站着就现形（出候选表），没砌过的下一回合从邻格稳稳砌上。别改成"把脚下那格从自己的候选里剔掉"——那会漏掉"脚下正好是**最后一格**没砌的墙"，工人站在上面干等、墙永远停在 13/14。
- **防关人闸门**：`planner._trapped` 逐角色问"**假设墙砌满**（`_walled`）之后还出不出得去"（`step_outside`），返回要救的 id 集合 `leaving`；`plan` 里两处用：① `gated=bool(leaving)` 传进 `_build_walls`（**一处** `if gated: return True` = **待命**：不发指令也不落兜底——用户口径"安全：别跑远，下回合缺口还在"）；② 白天把 `leaving` 里的人先送出去（用"**现在**"的障碍，与 `_walled` 那套不同，两套之差正是还没砌的墙）。⚠️ 夜里**不**送人出去（夜里不砌墙，这一支只会把人从炮位上拉走）。⚠️ `_walled` **把自己人只在后方通道那几格算成障碍**（14 格时 6 格、第 3 天起 4 格；与 `_ring` 的"自己人一律算路过"**故意相反**，但也不是"一律算障碍"）：那 14/16 格墙里没有任何建筑 ⇒ **能堵门的只有单位**，所以"自己人站在待砌的格子上"这个问题**只在门口有意义**；环内那 16 格（基地 + 武器环）站着的自己人是**过路的**。⚠️ **旧口径（自己人一律算障碍）在门收窄之后变成了活锁源**：环一闭合进出只剩环内那几条走廊 ⇒"一个工人正好站在走廊格上"变成常事 ⇒ 走廊另一头的工人被判成"砌满就出不去"、闸门把他送出去一格 ⇒ 下一回合他回来砌墙、两人来回踱步、**一整天不砌墙**（合成局面实测 16 回合 0 座）。只认通道那几格之后这条消失（第 57 步把通道放回 6 格 ⇒ 更难堵满、闸门更少触发），而**真正会关人的局面照样接得住**（用例 `test_a_colleague_in_the_door_still_holds_the_wall_back`、`test_a_gated_worker_stands_by`）。
- **手搓 `Map` 的合成局面里"角色不在 `blocked` 里"**：只有 `protocol.model._entries` 会把我方角色写进网格。所以涉及"别人挡路/自己站位"的用例必须自己把角色铺进 `entries`（`{**地形, **{r.pos: "worker" ...}}`）—— 曾有一条新用例漏了这一点，于是**它连旧代码都放过去了**。写完可疑用例要**反向验证**：退回旧实现，用例必须挂。
- **两套距离地形口径，别混**（`planner._passable` 是这一条的落地）：**"这一回合实际怎么走"**（`_Queue.step` / `_walk_out`）必须把同事当**硬障碍**（撞上就是执行失败、双双停住）；**"路有多远"**（`_repair_line` / `_stones_to_mine` / `_sell_ore` / `_mine_spare_ore` / `_detour_buy` / `_detour_sell` / `_upgrade_line` / `_leave_for_the_post` / `_stuck_inside` / `_post_spots` 十处的估算）走 `_passable(turn)` = `map.blocked` **剔掉我方角色**。混错的代价不对称：**估算里不剔**才是重病 —— 环砌满之后盒内只剩一格宽的走廊，同事停在走廊上就让"矿 → 最近炮位"一律变 **-1**（不可达）⇒ 每座矿都判不可行 ⇒ 整段静默放弃、**工人一整天一条指令都不发**（第 56 步，用户报的症状）；反过来（走路时把同事算路过）是碰撞。`_ring` / `_walled` / `_slots` / `_rescue` 有各自的判据，**不跟着 `_passable` 走**。
- **`steps_between` 的 `0` = 已经贴着目标**（"还要走几步才贴上"，`-1` = 不可达）⇒ `sell` / `buy` 的"我贴着小贩/商店了"判据是 `== 0`，写成 `<= 1` 就是**离两格（切比雪夫 1）就发指令 = 非法指令**（红线，且每回合重发；本地全是合法报文，只有判题器会说"不"，第 56 步逮到三处）。同理 `_detour_*` 在 `via == 0` 时必须 `return False`：绕一步 = 原地不动，可函数声明"我处理了"⇒ 第二段发不出指令 ⇒ **这一回合空指令**。
- **"基地后方" = 远离地图中心那一侧**：机器人从基地**面向地图中心**的那一侧水平逼近（`docs/pic/大致地图信息.png`，**正文没写刷新点**）。判据实现在 `grid._front_back`（返回 `(方向 d, 朝机器人的边, 背向的边)`），**只此一处**，`wall_cells` 与 `weapon_sites` 都从这里取。基地在左半 ⇒ 后方是 `x = bx-1`；右半 ⇒ `bx+2`。首场比赛后按实测翻转。按基地坐标判而不用 `teamOur.type`——换边后队伍身份不变、基地会挪。
- **三座武器的阵形 = 全在正面列**（`grid.weapon_sites`）：左半基地 `(10,24)` ⇒ **`(12,24)` / `(12,25)` / `(12,22)`** —— 两座火箭**相邻**、加特林差两格（正面 = 迎着机器人的那一列，`_front_back` 的 `front_x = far + d` = `bx+2`）。**理由有两条**：① **"够得着"**：武器升级券、基地升级券、围墙升级券均需在**目标建筑周围一格内**使用并指定目标位置（任务书 **L292**），围墙修复包要站在待修围墙一格范围内（**L314**），`attack` 也要求角色站在炮旁。三个落点**各自与基地的一角切比雪夫距离 1** ⇒ 一个角色站在落点上，脚下的炮和旁边的基地**一够就是两个**（三张券线现在都实现了：`_upgrade_line` / `_upgrade_station` / `_repair_line`）。② **两火箭相邻** ⇒ 一个角色站内侧那格就能同时贴两座、按 3 回合冷却交替开火 ⇒ **2 个角色即可操满 3 座**，释放开拓者夜间行动（放弃电磁炮的理由同此：单目标、对满血机器人不穿透）。种类与落点**绑死**：`planner.WEAPONS_BY_SITE` 与 `weapon_sites()` 下标一一对应（`(rocket, rocket, gatling)`）—— **相邻的那两格必须是火箭**，挨着才谈得上交替；加特林那 +10/回合 的持续输出放差两格的那格。⚠️ **两座火箭共用一个操作位 `(11,25)`**（两座 8 邻域的交集只此一格：`(11,24)` 是基地、`(13,24)/(13,25)` 是墙、另两格是炮自己）⇒ 火箭对**必然**由同一个角色交替操（3 回合冷却、两座正好接上），"2 人操满 3 座"就是这个共用的产物。那格是**空地、要踩上去** —— `step_toward` 的契约是"贴着即到"、停不进这种格子 ⇒ `grid.step_onto` 专为它而加（第 55 步）；岗位口径在 `planner._post_spots` / `_steps_to_post`，守门员 `BuildGeometryTest.test_the_two_rockets_share_an_operator_spot`。⚠️ 它同时是个**口袋**：唯一通路是 `x=9` 那条一格宽的走廊（`(9,24)→(9,25)→(10,25)→(11,25)`，见 `code-task.md` 表 34）。
- **`build` 的三条硬规则**：① **仅工人 + 仅白天**（§4.4；夜里发 `build` 是一次执行失败）；② 目标须在**自身切比雪夫 ≤1** 内（"站位即建造位"——`step_toward` 恰好把人停在贴着目标的一格，不用先挪开）；③ 建武器 **25 金/座**，新建**一律 level1**，**目标格已有武器则原武器被覆盖降级** ⇒ 必须避开占用格，否则 25 金币打水漂还倒亏一座。
  ⚠️ ② 有一个**例外（用户口径）**：**环上只剩最后一格墙时，砌它的人得站在防御盒子外面**（`_build_walls` 的 `outside` 那一支 + `_outside_spots`；见 `code-task.md` 第 67 步）。落点仍要切比雪夫相邻，只是加了"**在 `box_cells` 之外**"；盒外三个邻格全被占/出图 ⇒ 退回盒内就近砌（尽力而为，拦封死的是 `_trapped`/`gated`）。⚠️ 走到盒外那一步**不把 `target` 圈进软避让**：缺口本身就是出盒子的近路（穿过它 2 步，从后方通道绕整圈是 12 步）。⚠️ 这一支**排在"站在目标格上先挪开一格"之前**，反过来会原地打转（守门用例 `LastCellFromOutsideTest`，两处新逻辑各有一条反向验证）。⚠️ 在当前几何下它**防不了封死**（没有一格墙落在敞开的背面列上）——别把它当成 `_trapped` 的替代。
- **`remove`（拆墙）的事实**：§4.4 **仅工人**、"需指定与自身距离一格内的围墙位置"（**与 `build` 同一站位契约**，切比雪夫 ≤1）；拆后那格变**可穿越空地**（L73）、**不回收石头**（L209）⇒ 拆墙不是免费的。**报文有实证**（`docs/response.txt:42-50`）= `{"action":"remove","targetPos":[{"x":29,"y":7}]}`，**没有 `name`**（`build` 才有）⇒ 用例钉了 `"name" not in wire`。⚠️ **昼夜那半边是项目口径、不是原文**：§4.4 给 `remove` 的那一格**没写**，取保守方向**只在白天发** —— 夜里不发最多少拆一次，反过来若实际禁夜就是指令非法。**`remove` 只有一个调用点 `_rescue`**（"省 ≥ `HOLE_MIN_SAVING`(5) 步就拆"的临时门线随 U 形几何删除；修墙线的 L1 支在第 71 步也删了 —— 那条支就是用户报的"工人在乱挖墙"）：开"开了真能让某个被困的人迈出去、且离救援者最近"的那一格（逐格试算、并列取坐标序），受时间门 `HOLE_MIN_LEFT`(30) 约束（剩不到 30 个白天回合就不拆 —— 拆了当天补不回来）。⚠️ **候选格只认 `turn.walls` 里真有墙的格**（第 68 步）：`wall_cells` 是几何，没砌的那一格被机器人踩着照样挡路 ⇒ 照它拆就是拆一格空地（白丢一回合、人还在里面）。
- **`_rescue` 的"环满不满"门（第 68 步取代了首日豁免 `_after_first_day`）**：环上"孤零零一个缺口"这**一个状态既是"刚挖的洞"也是"还差一格没砌"**，地图上逐字节同形，任何无状态判据都分不开 ⇒ 判据是**环满没满**（`_ring` 非空即退，第 1 天那个缺口是真的没砌；环敞着时人本来也走得出去），**不再看第几天** —— 用户口径"只放开砌完的第一天"。第 3 天补角格那一刻 `_ring` 非空 ⇒ 那段时间整个不生效（见第 57 步不确定性 2）。代价换了个形态：`_ring` 判"已砌"用的是 `blocked` 这个**代理** ⇒ 机器人踩在缺口上时环"看着齐了"（`_rescue` 已改成只拆真有墙的格；`_build_walls` 那边照旧，见 `code-task.md` 表 41）。
- **`_stuck_inside` / `_rescue` 是补救，与 `_trapped`/`_walled` 的预防并存**（不许因为"反正能救"就把闸门拆了）：前者用**"现在"的障碍**（`_trapped` 问的是"假设砌满"——一个预防一个补救），且**自己人一律不算障碍**（同事站在通道格上只是路过，把他算成障碍会**白拆一次** —— 与改 `_walled` 口径治的是同一个病）。`_rescue` 只让**工人**动手、开"**开了真能让某个被困的人迈出去**且离救援者最近"的那一格。⚠️ **被关住的工人自己也走这一支、而且先走它**（真服务实测：通道被两台机器人堵死、工人在盒内 ⇒ 它自己 `remove (13,23)`，压根没轮到差事）。**被任务钉死的开拓者**才非靠别人救不可。
- **第 3 天补那两个角格也没有新机制**：`_ring` 按 `sealed` 的 16 格取候选、已砌的 14 格在 `blocked` 里被滤掉 ⇒ 只剩那两格可砌，`_build_walls` 照旧取 `free[0]`（守门：`BuildWallTest.test_the_third_day_seals_the_back_corners_without_rebuilding`）。⚠️ **已知缺口**：`_rescue` 拆掉的那一格当天没补上（工人阵亡 / `gated` / 没石头 / 天亮前没轮到）⇒ **夜里正面留着一个洞**（而夜里不能 `build`、当天无法补救），日志上的症状是"**一条 `remove` 找不到对应的 `build`**"。
- **武器上限原文自相矛盾**：§4.5.1 表格写"每种 ≤3"（合计 9 座），补充说明写"**全局同时最多 3 座**"。取**保守的全局 ≤3**（与"武器只有建立三个才有意义"一致）。落点按类别数、份额 = `len(roles)`，两处都读 **`Turn.weapons`**（网格里那份 `Map.weapons` 已删）。
- **中立元素的字段名是 `neutralType`，不是 `zoneType`**（接口文档 §1.2.1，在 `mapInfo.zones` 里）：`stone`/`iron`/`copper`/`vendor`/`weaponShop`/`challengerTaskPoint1|2`/`defenderTaskPoint1|2`。**只有前三种是矿**，小贩/武器商店/任务点**不是矿，但一样挡路**——`Map.ores` 只装这三种、且带矿种（`{矿点: 矿种}`），`Map.vendors`（`frozenset[Pos]`，只要坐标、不要矿种），`Map.blocked` 全都装。
- **`Map` 的不变量：格子非空即挡路**（任务书 L85 那张清单），寻路只问 `blocked`、不问格子里是什么。类别原样取自 payload（`enemy:` / `robot:` 前缀区分来源），**未知类别也照旧挡路**——判错方向只会多挡、不会放行。`render()` 的字符表**是有损的**（机器人不分体型），`cells` 才是真相。
- **基地 2×2 对双方都成立**（接口文档原文："**双方**基地大小为 2*2，基地对应的 pos 传递的是左上角的坐标"）。`Map` 把**敌我双方**基地都展成 4 格——曾有一版只展了我方，敌方基地只挡 1 格。
- **`teamEnemy.roles` 是逐回合观测，不是固定名册**：敌方角色/武器离开视野就消失，**消失 ≠ 被摧毁**（任务书 L97）。敌方基地与围墙全图可见。**不要据此做跨回合战损推断**——`handle` 里除了 SOP、任务内会话、价格期望表三处，没有任何跨回合记忆。⚠️ **别把这条套到机器人头上**（踩过）：顶层 `robots` 是**另一个字段**，**全图可见**（任务书 L95「机器人为全图可见」/ 接口文档 L154「场上所有存活机器人信息，全图可见」），视野 4 那条只管**敌方队伍**。两条连带的：**一夜只有一波**（L352「夜晚第一个回合统一出现」）、**已毁的不从列表里移除**（`model._robots` 不丢 `health == 0`，与 `_walls`/`_character` 相反）⇒ **判空/判射程/记账一律过 `planner._alive`**，直接读 `turn.robots` 会把打死的机器人当活人。
- **走向矿 = 站到采集位，是同一件事。** 矿格挡路（任务书 L85），所以工人只能停在**矿周围一格**，而那正好就是 `collect` 的站位（§4.4）。不需要写两段逻辑（"走过去"+"停在旁边"），`step_toward` 撞上矿格自然停。
- **`collect`（仅工人）的事实**：`targetPos` 是**矿石的坐标**（不是自己的站位），与 `move` 同形（`docs/response.txt` 里**没有实证报文**）；一次采 **1 块**，一座矿采满 10 次就消失（payload **没有**"剩余次数"字段，不记跨回合计数）；**§4.4 的能力列没给它写昼夜限制**（`build`/`remove` 写"仅白天"、`attack` 写"仅黑夜"，它那格是空的），但这一步夜里不采（用户选定），昼夜门由 `planner` 把关、**不进闸门**（闸门只管"谁"）。背包 `backpack` 是**物品名数组**（重复即计数），容量字段是 `backPackCapability`（**大写 P**，暂不读）。
- **围墙代价 = 石头×1，从建造者自己的背包扣**（不是全队共享），拆了不返还；**没有转移物品的指令** ⇒ 多采的石头给不了另一个工人。**攒多少石头不写死**：`planner._stones_to_mine` 每回合按用户给的**回合预算**现算 —— `赶路到矿 + 采 k 块 + 从矿回工地 + 手里 s 块的"挪+砌"(2s−1) ≤ 白天还剩的回合 − TIME_MARGIN(5)`，解出 k（每多采一块净花 3 回合），再与"还差几格墙 + `STONE_RESERVE`(3)"取小 —— 多出来那 3 块是**砌完墙后的存货**（用户口径：回合多时每工人留 3 块；夜里被打掉一格，第二天手里有货就能立刻补上）。距离用 **BFS 真实步数**（绕障时切比雪夫**低估**，而回炮位必须绕到后方通道再横穿盒子，直线 3~4 格、真实 10+ 格 ⇒ 低估得很离谱），`TIME_MARGIN(5)` 从"吸收低估的补丁"变成**真余量**；`steps_between` 返回 **-1（不可达）⇒ 这趟不去**，**不为估算去改 `step_toward` 的契约**。
- **采什么矿看价格，但价格不是常量**。用户的原话是「手里面保持能建造墙的石头量就行，**然后**选择价格最高的矿，没有波动的情况下是铜 > 铁 > 石头」—— **"然后"是顺序，不是并列**：墙只吃石头，先追最贵的铜就永远砌不成墙。所以两段：墙上还缺石头 ⇒ **只认石矿、取最近的**（`_pick_ore(want_stone=True)`）；环砌完 ⇒ 才按**收购价**挑（`_mine_spare_ore` + `_pick_ore(want_stone=False)`）。**"保持够砌墙的量"那一半本来就有**（`_stones_to_mine` 的上限是"还差几格墙 + `STONE_RESERVE`"）。⚠️ **"铜 > 铁 > 石"是样例的价目（1/3/5），不是规则**：任务书 L386 明说官方消息会让价格波动（示例：铁矿塌方 ⇒ 铁稀缺 ⇒ 回收价上涨）。价格**逐回合从 `vendorShopList` 读**（`Turn.vendor_prices`，`_int` 给的 -1 被挡掉 —— 负的收购价不存在），**写死的排序在事件期间恰好是错的**。价目缺失或矿种不在表里 ⇒ 按 0 算 ⇒ **不为它多走一步**（也顺带实现"空价目表 ⇒ 谁也不采"）。⚠️ 代价是背包占用与**离炮位更远**。
- **`sell`（卖矿换金币）的事实**（§4.4 / §4.6.1）：
  - **须站在「小贩周围一格内」**（不是矿周围）—— 与 `collect` 同形，小贩格本身挡路 ⇒ `step_toward` 撞上它自然停在贴着一格，**不用写两段逻辑**。
  - ⚠️ **可用角色是"全部"**（§4.4 最右列）—— 与 `build`/`collect` 的"仅工人"**相反**。窄成工人是"本地全绿、判题器说不"的典型：报文完全合法，只有判题器知道开拓者也能卖。
  - **报文形状** `{"action":"sell","name":<矿种>,"num":<件数>}`，`num` 是 **int**（§2.2）、默认 1，**支持批量贩卖**（一回合一条指令 ⇒ 一次卖光手上那一种，`num` = 全部件数）。⚠️ **没有实证报文**（`docs/response.txt` 只有 `move`/`build`/`remove`），`name` 取**英文矿种名**（与 `neutralType` / `vendorShopList.name` 同一套词）。
  - **§4.4 的能力列没给它写昼夜限制** ⇒ 昼夜门由 `planner` 把关、**不进闸门**（与 `collect` 同）；planner 把它放在**经济兜底 `_economy` 的第一级** —— 白天阶梯末尾与夜里清场后都走它。
  - **小贩的坐标在 `Map.vendors`** —— 它是**格子**不是 `teamOur.roles` 里的单位，只能从网格认。与 `ores` 对称：一个是料源，一个是目标点。
  - **石头排在最后才卖**：`_best_load` 按**收购价**挑（铜/铁都比石头贵），所以墙线手里的石头在墙砌完前基本不会出手；唯二例外是**筹资**（武器有缺 ⇒ 先卖背包里的货，那时石头也可能被卖到只剩 1 块）与 `_build_walls` 的 `if not free:` 那一支（环砌完了、没有别的差事）。⚠️ 第 60 步起墙线还会攒 `STONE_RESERVE`(3) 块存货 ⇒ **`free` 非空时也可能有"多余的石头"**，那几块是给夜里补墙留的（第 64 步起**默认留底就是这 3 块**，筹资那两条路才降到 1）。
  - **够本门 = 货值 ≥ 2 × 到小贩的 BFS 步数**（≈ 每回合至少换 1 金币），**已经贴着小贩就跳过这一条**（这趟路早付过了）。⚠️ **「1 金币 ≈ 1 回合」是拍的**，没有文档依据 —— 它是这一步唯一的调参旋钮。样例小贩 `(20,16)` / 基地 `(10,24)` ⇒ 切比雪夫 10 ⇒ 阈值 20 ⇒ 铜要攒 4 块（改 BFS 后这条阈值只会更大、更保守）。**若实际地图更远，"矿堆着却永远不去卖"就是这个阈值。** ⚠️ **`urgent=True`（`_upgrade_line` 为买券而发）跳过这一条**，④ 照旧 —— 那趟路的回报是券不是矿价；④ 不跳是因为赶不回来就是白丢货。④ 的预算用 **`Turn.rounds_left`**（本段剩余，白天夜里都成立）。
  - **卖石头留底分两档**（第 64 步，用户口径）：`_best_load` / `_sell_ore` 的 `keep` 默认 `STONE_RESERVE`(3)—— 平常的卖矿（`_economy` / 开拓者空闲 / `_detour_sell`）不动那 3 块存货，夜里墙被打掉一格第二天手里有货；**筹资那两条路**（建武器 `_intents` 支、买券 `_upgrade_line` 支）传 `STONE_KEEP_RAISING`(1)，凑钱时只留封口那一块。⚠️ **`_upgrade_line` 里判"卖得起"与真卖必须同一个 `keep`**，否则这边算出"够了"、那边只肯卖到剩 3 块，白跑一趟。`WALL_COST` 与 `_stones_to_mine` 的"还差几格墙 + 存货"口径**不动**：石头照旧只用于砌墙。
  - **背包是一张名字表**：payload 的 `backpack` 是**物品名数组**（重复即计数），`model._bag` 数成 `{物品名: 件数}`，`BaseRole.stone` 是 `bag[STONE]` 的**派生属性**（`_stones_to_mine` / `_build_walls` 三处调用靠的就是它）。容量字段是 `backPackCapability`（**大写 P**，不读）。
- **买券/升级线（用户拍板"买得起就优先升级"）**：
  - **`buy`（武器商店旁，全部角色）**：`name` = 商品名（`weaponShopList.name` 那套词）、`num` Int 默认 1；价目从顶层 `weaponShopList` 逐回合读（`Turn.shop_prices`，样例实证：券1=100、券2=150）—— **不写死**，查不到按 0 ⇒ 买不起 ⇒ 不跑腿。⚠️ **没有实证报文**（与 `sell` 同一类风险）。
    **钱不够时看背包**：`现钱 + 背包里最好那一堆货 ≥ 券价` ⇒ 先去卖矿凑（`_sell_ore(urgent=True)`，见上一条）；用最好那一堆而不是背包总值 —— 一回合只发得出一条 `sell`，卖掉哪种到账哪种。
  - **`use`（全部角色）**：升级券须**站在目标建筑周围一格内**并指定 `targetPos`（任务书 L292）；已 level3 再用**不生效、券不消耗**，非法使用也不消耗（L293-294）⇒ 发错顶多白跑一趟、不碰红线。
  - **围墙那一半（用户口径"升级当修"）**：弱墙（血 < 满血 **1/5**）**一律先用围墙升级券** —— L1 用 `WallUpgradeVoucher1`(样例 20 金)、L2 用 `WallUpgradeVoucher2`(30 金)、L3 到顶升不动了才用 `WallFixer`(10 金)。依据是任务书 **L297**：**升级后建筑回满血** ⇒ 升级 = 顺带回满血 **且** 上限抬一档（围墙满血 1000/1500/2000）。价目同样逐回合读 `Turn.shop_prices`、不写死。⚠️ **"围墙升级也回满血"是 L297 的推广，没有实证报文**；`WallFixer` 的站位另有专条（L314：站在待修围墙一格范围内）。**修墙线不再拆墙**（`remove` 只剩 `_rescue` 一个调用点）。
  - **武器带 `level`**（接口文档：仅建筑持有、初始 1；`model` 缺失按 1 算，别给 -1—— 会被升级线当成"还升得动"白跑一趟）。摘要的武器行打 `L{level}`（"升级成没成"只有日志能回答）。
  - **升级优先链（用户口径，群体打击口径）：火箭 > 加特林 > 电磁**。火箭 +1 枚导弹 = 中心 20 + 周围 8 格溅射 10、多枚叠加 ⇒ 对成簇的机器人一炮翻倍，射程跟着涨（L3 全图）；加特林 +1 颗子弹 = 每回合 +10、无冷却、弹道必命中、两颗可分打两台（90° 锥）、射程 +2 更早接敌；电磁单目标、能量对满血机器人（≥40 血）不穿透。链：rocket→2 → rocket→2 → gatling→2 → rocket→3 → rocket→3 → gatling→3（`UPGRADE_CHAIN`）。⚠️ **武器有缺时整条升级差事不跑**（用户口径："武器 ok 才升级"）。
  - **无状态编排**（`planner._upgrade_line`）：拿没拿券看**背包**（买完金变少、包里多一张，两阶段天然可分；跨夜背包保留）；跑腿者 = 持券的工人 / 真空闲开拓者 / 名册第一个工人；整趟（商店→武器 + 买/用两动作）来得及才出发（`rounds_left − TIME_MARGIN`，那段路按 **BFS** 算、不可达 ⇒ 不出发）；白天跑，**夜里只有"清场后"那一支会走到这里**（`_economy`，一轮跑不完就待命、天亮接着走）；持券阶段的终点就是炮位（用完正好站岗，不留回程）；**钱不够买券时，卖掉背包里最好那一堆就够 ⇒ 先去卖矿**（判据 `现钱 + 那一堆 ≥ 券价`，券价未知/为 0 ⇒ 不跑）。
- **就近采矿修闲置**：`_mine_spare_ore` **先把"走得动、回得来"的矿筛出来再按价挑**（旧版挑了最贵的发现走不回来就整段放弃 —— "挖好石头在家等着"的根源）；回程参照 = **最近的武器位**（机器人到进攻范围前必须站回炮前）。这里的距离全走 BFS（`role.pos → 矿` 与 `矿 → 最近炮位`，各 `steps_between`；不可达 ⇒ 剔掉那颗矿），每个炮位的 BFS 提到循环外算一次，避免"每颗矿 × 每座炮"重复跑。**顺路卖矿 `_detour_sell`**：去矿路上绕去小贩 ≤ `DETOUR_MAX`(2) 格 ⇒ 先绕（贴上那回合 `_sell_ore` 自然出手，绕路量同样按 BFS 算）。
- **开拓者无任务时卖矿**：任务点全空（冷却/做完）⇒ 白天开拓者走 `_sell_ore`。⚠️ **规则限制**：`collect` 仅工人、**没有转移物品的指令** ⇒ 开拓者背包里通常没矿 —— 结构在，要等它从任务/宝藏拿到可卖物才真正跑得起来。
- **地图边界不在任务书 L85 的"阻挡移动"清单里**（那一列只写了建筑/角色/机器人/中立单位/任务点/矿区）。贪心挪一格时几乎撞不到，**但 BFS 会绕到图外去**，所以 `step_toward` 自己按 `Map.size = (width, height)`（取自 `mapInfo.width/height`）挡住 `(0,0)~(width-1,height-1)` 之外。越界算"指令非法"还是"执行失败"文档没写，不走一定安全。`size` 无效（≤0）⇒ `Map` 矩阵为空 ⇒ 无格可走 ⇒ **单位不动**，这是故意的降级。
- **`step_toward` 的终点是"贴着 goal 的一格"，不是 goal 本身**——因为 goal 通常是挡路的（矿/建筑/武器操控位）。⚠️ `build` 的落点是**空地**：**结论是不用改** —— 建造格在环上是空的，BFS 只在可通行格上展开、并停在距 goal 一格处，那正好就是 `build` 要求的站位。建墙同理。⚠️ **要停在 goal 自己身上（goal 是空地）得用 `grid.step_onto`**：目前唯一的使用者是武器岗位（多座组共用的那一格，见阵形那条）。**两者不能互换** —— `step_toward` 到不了这种格子（贴着就返回 `None`，人会原地卡死），`step_onto` 停不进挡路的 goal。
- **没有转移物品的指令**（`drop` 只丢不捡，没有拾取）→ 每个角色的背包就是自己的料仓，"A 买 B 用"行不通。
- **`attack` 最易写反的两处**：`roleCommandMap` 的 **key 是武器 id**，`controllerId` 才是操控角色；`targetPos` 长度 = 武器当前等级（电磁狙击炮恒为 1）。攻击**仅黑夜**可用，且需要角色站在武器周围一格内（一人只能操一座武器）。**可用角色是"全部"**（开拓者也上炮位）。**份数错了就是"指令非法"（红线）** ⇒ `planner._fire` 按 `Weapon.level` 定份数、`Attack` 照单编（同点重复也照编，不去重）；多出来的目标位**重复同一个最优落点**（用户口径的"最小合法化"，不是分点覆盖）。
  - **已毁的炮与阵亡的角色都不该收到指令**：`model._weapons` 丢掉 `health == 0` 的炮，`model._character` 同理丢掉阵亡角色（用 `== 0` 而非 `<= 0`：字段缺失是 -1，别当成死）。
  - **`roundNo` 缺失时 `attack` 一条都不发**（`planner._defend` 的硬门）：缺失的回合号被 `is_day` 判成**夜里** —— 对"白天不许建造"安全，对"白天开火非法"就是反的。
- **三种武器的伤害模型（§4.5.1 表格 + §4.5.4，按 `Weapon.level` 线性放大；表里写的是 L1）**：
  - **加特林**：每颗子弹 **10** 伤害，沿"武器→落点"弹道飞行，命中**该弹道上最近的一台**机器人即消耗。多目标（L2/L3）须落在**同一 90° 锥形**内（否则整次攻击非法）—— 我们多发**同点**，锥天然成立。
  - **电磁狙击炮**：一个目标，**能量**沿弹道穿透（L1=10）：路径上每台机器人受 `min(剩余能量, 当前血量)`，能量按伤害扣减。⚠️ 穿透**没建模**（只按命中那一台记账）。
  - **火箭发射台**：导弹**指哪打哪、不被弹道阻挡**；中心 **20** + 落点周围 8 格**溅射 10**，多枚叠加（L1 一枚）；发射后 3 回合冷却。
  - **"每级多发一份"的落地**（`planner._beam_damage` / `_rocket_damage`，两个助手各两个调用者）：**加特林/火箭的目标位数 = 等级**（多发全指同一格）、**电磁恒 1 位**但能量 `10 × 等级`；火箭按**枚数**叠加 ⇒ 中心 `20 × 等级`、溅射 `10 × 等级`。
  - **机器人血量 40/60/500/800（小/中/大/BOSS，§4.7.2）全都 ≥ 20** ⇒ L1 对满血机器人不可能过量伤害（L2/L3 会过量，所以估算一律走 `min(伤害, 剩余血)`）；结算顺序 = **武器攻击 > 机器人移动**，伤害**回合末统一结算**；**残余机器人清晨自动清除**（每晚都是满血开局）。
  - **方针（用户改）：目标 = 最大伤害落点，打死所有机器人**。`planner._fire`：火箭 `_rocket_site` 全场算账（候选 = 机器人格及其 8 邻格、落点须在射程内——**溅射可以够到射程外的机器人**；评分 = Σ min(伤害, 剩余血)，并列取坐标序）；加特林/电磁 `_beam_site` 打在机器人身上（终点必在弹道 ⇒ 必命中），取**有效伤害** = min(伤害, 剩余血) 最高的、并列打近的。**`assigned` 记账**：同回合先开火的炮把估计伤害记在机器人身上，后开的按剩余血算 ⇒ 不挤将死的目标（"集火补刀"就此作废）。⚠️ 加特林的实际命中是弹道上**最近**的那台（可能不是终点那台）—— 估计只用来挑目标，错了只是次优、不会非法；**弹道是否被墙挡没有明写**（火箭明说不被挡；加特林/电磁的"攻击路径"暗示会被什么挡——若实测子弹打不出墙内，整个防守阵形都要重想）。
- **`attackRange` / `cooldown` 都以 payload 为准**：任务书等级表与样例数据矛盾（样例 gatling L1=4 / railgun=7 / rocket=INT_MAX，表格是 3/6/10）；`cooldown`（火箭发射后 3 回合空窗）**样例三座炮都没有这个字段** ⇒ 解析成 -1。⚠️ **火箭的冷却有本地账兜底**（用户方案）：`planner._fired` 记下每次**真发出去**的火箭 `attack`（武器 id → 回合号），之后 `ROCKET_COOLDOWN`(3) 回合内 `_cooling` 判它冷却、不选它——payload 的 `cooldown` 字段仍优先，字段缺失时靠这本账。**这是 planner 侧第一处跨回合状态**（观测性、按 id 键控、期满自动过期；武器毁/重建换 id ⇒ 旧账自愈，卡住的最坏代价 = 某座火箭少打 ≤3 回合）。组内就绪的并列按 `round_no` 轮转；都冷却 ⇒ 待命一发不打（打冷却炮 = 执行失败、白丢一回合）。
- 夜里**不能建墙**（`build` 仅工人、仅白天）→ 墙一旦被拆，整夜都是缺口，所以夜间第一优先级是**预防性修复**而非爆了再补。⚠️ **夜里场上没有活机器人（`_alive(turn.robots)`）时，工人不走防守线，改跑白天最后一级那套经济兜底 `_economy`**（卖矿 → 券 → 采闲矿；`build`/`remove` 夜里非法 ⇒ 这一支一条都不会发；开拓者照旧回岗位）。**机器人全图可见（任务书 L95）、一夜一波（L352「夜晚第一个回合统一出现」）⇒「没有活的」是真清完**（旧口径"清完只是视野过滤的假象"**已被推翻**，第 63 步）；但夜里仍不留额外的紧半径 —— 走多远由 `Turn.rounds_left`（这一段还剩多少回合）与四道门兜着。**其余角色的动线 = 认领一组还没被本回合别人认领的武器（`_weapon_groups`），走到那一组的"岗位"**（一人只能操一组），到岗就按**最大伤害落点**开火（见上）。⚠️ **开拓者是补位炮手**（`_pioneer_mans_guns`，用户口径）：**工人数 ≥ 武器组数 ⇒ 它一个组都不认领**（炮位全留给工人，它腾出来）—— 两个工人活着时阵形两组正好被工人包圆，工人阵亡（只可能在夜里）后人手不够它才上。判据在夜里 `_defend` 与白天收工闸门 `_leave_for_the_post` **两处同源**，只改一处会踩坑（见上一条）。⚠️ **唯一例外在 `_defend`**（第 77 步）：它**已经站在**某组的操作位上（`_stands_on_a_post`）⇒ 认领那一组 —— 那一格被它占着、工人站不上去，它再不打就是整组白丢一夜；收工闸门不加这条（它只发 `move`，人已在岗位上本来就落到"待命"，见下一条）。**先开火、打不了才挪岗**：贴着组内某座就先打它（**只贴一座也打** —— 另一座就绪而这座在冷却时别白丢一回合），站在岗位上又都打不了 ⇒ 原地待命，只贴一部分 / 压根没贴着 ⇒ 朝岗位挪一格。⚠️ **这个顺序是第 77 步改出来的**：旧代码先算 `_post_spots`、岗位表一空就 `continue`，于是"贴着炮但岗位格被占"的角色跟着整组一发不打（用户报的死锁）。**主岗位一个都站不上时退到 `_near_spots`**（贴着组内**任意一座**的格子；主岗位被非我方单位堵死 / 走廊被堵时用）：只守得了一座也照打，总比整组无人可打强。⚠️ **退路排在第二遍**（主岗位先试完）—— 混在一起会让工人把同事站着的那一组抢过来（`test_a_colleague_on_the_spot_never_freezes_the_pair`）。**不换组**：站哪组是走到位那一刻定下的，每回合重挑会让角色在炮位之间来回走。⚠️ **自己人不算障碍、别人站着的那格要让开**：`model._entries` 把我方角色写进网格 ⇒ 整份 `blocked` 里混着队友和**自己**。`_post_spots` 先把自己人剔掉（否则站在岗位上的操作者**看不见自己的岗位** ⇒ 整组被跳过、转头去别的组），再排掉别人**站着**的岗位（那格被占就换一组，否则一个走不进去、一个干等着，两人一起卡住）。已知代价：**角色数 < 武器数时火力打折**；挪岗那几回合人在路上、火力空窗。
- **白天收工闸门 `planner._leave_for_the_post`**（治"机器人到跟前了角色还没走到炮位"）：白天最后那几回合，**环砌完了**（`_ring` 为空）才允许停下手里的活回家 —— 回程步数 `steps >= day_rounds_left - POST_MARGIN`(3)（或不可达）⇒ 只发一条 `move` 往**那一组的岗位**挪（同一个 `_post_spots`：多座组 = 夜里共用的那个操作位、单座组 = 炮旁；步数口径在 `_steps_to_post`，多座组的岗位是**空地、要站上去** ⇒ `steps_between` 的结果 +1）。三件事：① **"环砌完了才收工"是有意的**（补墙 > 收工，与用户"防御时必须保证前面完全封死"一致）：第 1 天墙没砌完时闸门不生效，第 2 天起环通常是满的、全天生效；② ⚠️ **这一支只能发 `move`，绝不可复用 `_defend`** —— 它到岗会调 `_fire` 发 `attack`，而 `attack` 仅黑夜 ⇒ 白天发就是**非法指令**（红线）；守门员是 `DayEndGateTest.test_a_day_round_never_fires`（扫 6 个白天回合号 × 5 个位置，断言一条 `attack` 都没有）；③ **已经在岗位上**（`steps == 0`）⇒ `q.step` 不成立 ⇒ 剩余白天自然空指令待命（合法）。那个 `- POST_MARGIN` 是**拍的余量**（第 59 步从 1 提到 3）。⚠️ **目标是"岗位"而不是"最近炮位"是有意的**：火箭对共用一个操作位，收工直接站上去 ⇒ 天黑第一回合就交替开火（第 55 步的 bug 就是这么治的）。⚠️ **开拓者（补位炮手）不走这一支**：`_pioneer_mans_guns` 为假时直接 `False`，与夜里 `_defend` **同一个判据** —— 只拦夜里那一处的话，白天照样把它送进岗位（火箭对只有一格岗位），夜里它又不认领 ⇒ 那格被占着、**整个火箭组没人操**。
- **任务线的三个权威事实**（全部交叉验证过）：
  - **任务点的权威来源是 `teamOur.playerTasks`**（数组，**只含我方 2 个点**，阵营已按 `teamOur.type` 滤好）—— 元素 `{taskType, taskPosition, coldDownRounds, scoreReward, goldReward, isValid}`。坐标字段名是 **`taskPosition`**（唯一不叫 `pos` 的），且**只认锚点格就够**（任务点 2 虽占两格，锚点本身就是其中一格）⇒ **不需要读 `mapInfo.zones` 的 `challengerTaskPoint*`、不需要读 `teamOur.type`、不需要算两格并集**。`isValid: false` = 冷却中**或**该点任务已做完；结束时刷新冷却 30 回合。
  - **`prompt` → `llmResp` 是唯一的 LLM 通道**：响应顶层 `prompt` 发出去，下一回合 payload 顶层 `llmResp` 回来。**任务存续期间不限量、不计数**（接口文档 L198），但**不在任务里一次都不要发**（每游戏日只有 3 次额度，那是任务线之外的资源）。答案**每回合都交** —— 接口文档 L140「以之前提交过的**通过率最高的**答案计算积分与金币」说明反复提交是判题器**预期的**用法。⚠️ **交错了判题器回一条 `code 2` 的 error、任务继续**（用户实测确认，比 L140 那句推断更硬）⇒ 交一版的部分答案没有下行风险，而**一版都没交过就超时** = 0 分。**第 83 步把这条计分规则写进了 prompt**（【注意事项】8 + ROLE 与输出约定各一句，三处同源）。判据本身仍**不记跨回合状态**（跨回合标志位一旦卡住会**静默关掉整条线**）；例外是 SOP、任务内会话、价格期望表三处。
  - ⚠️ **沙盒是任务线的另一半**。五个事实：
    - **响应顶层 `executeCmd`**（接口文档 L208/210）="沙盒环境中执行的命令，**仅在执行任务期间才能使用**"，判题器**本回合**执行、限时 15 秒，**沙盒故障与命令超时不计入异常次数** ⇒ 这条通道**不会吃红线**，可以放心用。**没有昼夜限制**（任务跨夜是常态，别加 `is_day` 门）。
    - **工具协议是我们自己定的**（文档只写了沙盒"能执行基础的 shell 指令与 python 指令、**无法访问外部网络**"（任务书 L439），**没规定 LLM 该用什么形状要命令**）。协议形状是最大的单点风险，只能靠实盘检验。两个形状：
      - **调工具**：`<tool><tool_name>工具名</tool_name><tool_param><参数名>参数值</参数名></tool_param></tool>` —— 参数是 `<tool_param>` 里的**具名元素**（可一个可多个、也可拆进多块；`\w+` 认中文参数名）；无参数的工具不写 `<tool_param>`。**严格只认这一种形状**（用户拍板）：属性式 `<tool_param name=…>`、裸 `<tool_param>值</tool_param>`、裸 `<tool>cmd</tool>` **全部不再是工具调用** ⇒ 落重问（丢一回合、不碰红线）。**值过五实体反转义**（`&amp;` 最后换，`&amp;lt;` 只还原一层；LLM 没转义时是空操作）—— prompt 教了转义，解析就得接住；答案不走这条。
      - **直接作答**：`<answer>答案本身</answer>`
      - **两者同回合**：`SOP2Prompt` 不产出命令，所以"沉淀 + 作答"是**两块并列** —— `<tool>…</tool>` 之后跟一个**块外**的 `<answer>`。⚠️ **`sop` 正文里不许出现 `<answer>` 与 `</answer>` 这对串**（用户口径）：`chat.strip_answers` 在入库前把**成对的**挖掉（挖了几处进 `【SOP 更新】` 日志），规则本身也写在 `prompt.py` 的输出约定段里。反过来，答案**落在工具块内**一律不算 —— `answer_of` 先把整个工具块挖掉再扫（分不清那是真答案还是 SOP 里的示例，就宁可不交）。
    - **三个谓词分居宽严两端**（`agent/chat.py`，同住一个模块）：`tool_of` **严格**（⇒ `(名, [(参数名, 原文), …])`，**参数名永远不是 `None`** —— 嵌套形状的参数名就是标签名；**任何一块 `<tool_param>` 里没有具名元素 ⇒ 整次调用 `None`**；**只有参数没名字 ⇒ `None`**，绝不猜；**只有名字没参数是合法形状**——可能是无参数工具的调用，放行与否由 `tool_call` 按参数声明判）；`looks_like_tool` **宽**（只认**前缀** `<tool` 出现 —— ⚠️ 它是模块里唯一不要求成对的地方，别顺手改成 `_TOOL_RE`：那会把"想调但没凑对"的回复从重问变成"原文即答案"；**不再解析的旧形状也靠它接住** ⇒ 落重问、不被当答案）；`answer_of` **三级**（先把**第一个工具块整段挖掉**再扫 —— 有 `<answer` 标记 ⇒ 只认成对块内容，空/半截 ⇒ `""`；**否则用原文判**"像工具回复" ⇒ `""`；否则**原文即答案**）。⚠️ **"先挖掉工具块"是那个污染缺陷的正面修法**：`SOP2Prompt` 沉淀的正文里几乎必然出现字面量 `<answer>…</answer>`（它讲的就是"答案要用 `<answer>` 包"），不挖就会把 **SOP 里那一段**当成答案交上去（真实缺陷，日志上看不出来）。`chat.py` **不认识任何参数名**（参数名是 LLM 写的标签、由 `tool_call` 对注册表认）。与它同住一处的是 `strip_answers(text) -> (正文, 挖掉几处)`：**唯一的调用者是 `SOP2Prompt`**（入库前清洗），住在 `chat.py` 是因为它认的就是 `_ANSWER_RE` 那一个模式 —— 分家等于把标签语法抄成两份。⚠️ **`answer_of` 是「该提交什么」与「该骂什么」的同一个谓词** —— `_answer_task` 提交它、判据 ④ 用它当纠错内容、判据 ⑤ 用它判"已经有答案了"。三者一旦分家，纠错段就会把**带 `<answer>` 标签的原文**喂回去，LLM 看到自己上次的标记被原文骂回来，行为是未定义的。
    - **注册表是唯一真相**：`Agent.__init__` 里那张 `self._tools`（名 → (实现, 描述, **参数表** `((参数名, 用途), …)`）—— 参数表**一表两用**：`prompt.gen_all_tool_prompt` 生成 `## ToolName - 名` / `- Description: …` / `- Params:` + `    - 名: 用途` 的**块**（无参数打 `- Params: （无参数）`、块间空一行；**由表生成、不手写第二份** —— `Agent.tool_desc` 已删，职责并入生成函数）、`tool_call` 按它**调度**（**只收具名参数**：带名按名对、认不出的名字**不进 resolved**、声明参数一个不少且非空才 `impl(**resolved)`；位置填充机制随严格解析一起删除）⇒ 加工具只改一处、描述与调度不会分家。⚠️ **`SOP2Prompt` 的参数表是两个参数** `(("name", …), ("sop", …))`（多流程口径：`name` 是条目名，存储是**流程表** —— 同名覆盖、异名追加、超 `SOP_FLOWS_MAX`(5) 丢最旧且日志点名；`SOP_MAX`(1000) 收窄成**单条**的上限；空文本 = 删掉那条），沉淀**流程与环境知识**（用户口径放宽：探索到的接口描述等环境事实也要存，**类型由 `name` 约定区分**（知识条目如「接口-查询」），**零新机制** —— 不加第三个参数（"声明参数一个不少"的闸门会把旧形状调用整单作废、丢沉淀）、不开第二个工具；ROLE 段有对应提示，**沉淀时机** =【沉淀的SOP】段里还没有这条经验就存、存过的不要重复存——"值不值得"不再是门槛）—— 作答统一走块外 `<answer>`，与沉淀的唯一交汇点是"同一条回复里各写各的"。⚠️ **表必须由实例构造**：`SOP2Prompt` 写的是 `self._sop`，只能是**绑定方法**，模块级常量表达不了这件事。**铁律：`tool_call` 的返回值就是那条要进 `executeCmd` 的命令**，`""` = 不产出命令（`SOP2Prompt`）/调用不成立（未知工具、形状不对、缺参数、空白值），**下游不需要区分**。`AGENT.tool_call` 是**副作用唯一发生点**，写在判据链之前 ⇒ 走"回灌结果"那一轮 SOP 照样生效（有意为之：LLM 不该因为"结果刚好回来了"就白调一次工具）。
    - **`planner.task_channel(turn) -> (prompt, executeCmd)`** 是这两条通道**唯一**的产出点，从上往下先命中先定夺（②–⑥ 各算出 `(prompt, cmd)`，**链尾另有回合末尾的压缩闸门**，见下面"两条通道的契约"）：① 没任务**或名册里没有开拓者** ⇒ 两个都不发（**在闸门之前** ⇒ 场外绝不压缩）；② `cmd_result` 非空 ⇒ 回灌结果、**这轮绝不发命令**；③ 工具给了命令 ⇒ 发命令，prompt 槽留给回合末尾的压缩闸门；③′ *（隐式子路径）* **完整调用但拿不到命令**（`SOP2Prompt` / `python_exec`（本地执行、产出当回合进会话）/ 未知工具 / 空参数）⇒ **不早返回、继续往下走**：有答案 ⇒ ⑤ 不发模型请求、没答案才落到 ⑥ 重问（把 ③′ 从边角料变成了**主路径** —— prompt 现在要求 `SOP2Prompt` 与答案写在同一条回复里，走的正是这一支：省掉的那一回合是实打实的分数）；④ 判题器报 `code 2` ⇒ 带"上次答错了"重问（**连它的 `description` 原话一起回灌**：那是黑盒里唯一能回答"错在哪一项"的东西，之前只进日志；**拿不到答案原文也照样纠错**——只把反馈装进提示词，绝不落「请继续。」）；⑤ 是答案 ⇒ **只交答案**：不发模型请求、**也不压缩**（压缩与 `<answer>` 互斥）；⑥ 否则把问题问出去（首问 = 题目，重问轮 = 一句「请继续。」）。
      **判据 ② 必须压在 ③ 前面**：文档给 `lastCmdResult` 专门写了"**未发命令时为空字符串**"（L33）、对 `llmResp` **一个字没写**（L31）—— 所以 `cmd_result` 按**不粘**设计、`llm_resp` 必须按**可能粘住**设计，否则同一条命令会被反复丢进沙盒。
      **纠错（④）是"修饰符"不是"分支"**：`errors` 说的是**本轮产生**的错误，与我们上轮发了什么**不同步**（发命令那轮没有 prompt，下一轮却可能同时拿到沙盒结果和更早那次 `submitAnswer` 的判决）—— 做成独立分支就会把纠错整段吞掉。触发条件是 `code == 2`。
      **畸形工具回复**（`<tool` 有开无闭 / 只有名字没参数）落到判据 ⑥ 重问，**不能**当成答案 —— `looks_like_tool` 判宽正是为了这个：两边判据一旦分家，就会出现"提问与提交同时哑火、永久空转且日志上什么都看不出来"。
      **③′ 不活锁**：任务期间 prompt 不限量、不计数 ⇒ 不吃额度不碰红线；出口有三个 —— LLM 自己改口给答案、`code 2` 带来的纠错段、下一回合 prompt 里**它自己刚写进去的 SOP 段**（那就是"调用成功了"的回执）。**升级触发条件**：首场日志若出现「同一任务连续 ≥3 回合 `llm_resp` 都是 `SOP2Prompt` 调用、且始终没有 `submitAnswer`」⇒ 升级成"显式 ack"编排。**未实测。**
      **`executeCmd` 的闸门要显式补**：`submitAnswer` 走 `roleCommandMap`，阵亡的人不在 `roles` 里 ⇒ 白送一道闸门；而 `executeCmd` 是响应**顶层字段**，不经过角色循环也不经过 `Action` 的权限闸门 ⇒ 判据 ① 里那句"名册里得有开拓者"必须手写。
      **两条通道的契约**：**任务提问不与命令同轮**（同一轮既提问又发命令 ⇒ LLM 拿着过期结果作答 ⇒ 活锁）。**压缩判定 = 回合最末尾的闸门，但只剩 ③ 命令轮会到那里**（用户拍板"压缩与 `<answer>` 互斥"——⑤ 答案轮提前返回 `("", "")`：压缩回复会占住下一轮 `llmResp` 槽，答案被判错时纠错分支就拿不到答案原文）：判据链算完 `prompt` 仍空 ⇒ 填上**压缩请求**；②/④/⑥ 的模型请求永远优先（压缩是这条链上优先级最低的租客）。压缩回复（裸 `<summary>`）**内容路由进摘要、永不当任务材料**，活锁的成因对它不成立；该轮判据按"没回复"落 ⑥（nudge）⇒ **命令轮与压缩轮交替**，互不阻塞。裸摘要判别 = `chat.is_summary_reply`（挖掉摘要块后一个字不剩），在 `hear` **之前**分流：进摘要、不进会话表。回退点 = 删闸门三行（`if prompt == "": prompt = AGENT.compression_request()`）。
      **不做流水线**（同一轮既提问又发命令）：回合数确实值钱（积分 `5 × 标准回合数 / (完成回合 − 接取回合)`），但 `llmResp` 滞后结果一回合 ⇒ 每条命令会被请求两遍、LLM 永远拿着过期结果作答，沙盒里若有带副作用的命令，答案直接被污染。
  - **回灌给 LLM 的是全文**（用户拍板），`app._log` 那一侧才 `_clip` —— 两个下游要的东西不同：一个要正确性，一个要人眼看得下。
  - **任务线的回合成本 = 一切任务线取舍的分母**：一次 `executeCmd` 之后**下一步动作要等 2 回合**（发命令那一轮拿不到回执，回执下一轮才进 prompt、回复再下一轮才到），`python_exec`（本地纯计算）是 **1 回合**（产出当回合进会话、同一回合就把话问出去），交一次答案 1 回合（对了就结束）。积分 = `5 × 标准回合数 / (完成回合 − 接取回合)` ⇒ **回合数就是分**。⚠️ 因此"一个参数一个参数地试"是协议下最贵的策略（5 个候选 = 10 回合，压进一条命令 = 2 回合）。**【注意事项】那段措辞就是这个成本模型的落地** —— 它讲资源约束（一次调用 = 一个回合），不写"遇到 A 就做 B"的流程（**写流程 = 过拟合，用户口径**）。
  - **开拓者一旦接任务就被钉死**：离开己方任务点周围一格内、超时、开拓者死亡、完成 —— 四种情形任务都结束（任务书 L377-380）。所以**它连夜里都不回炮位**，这一支写在 `plan()` 循环的**最前面**（全局最早的判据，与昼夜无关）。代价：**夜里三座炮只有两个人操，火力打折 1/3**，由任务积分覆盖。
    ⚠️ **唯一例外（第 65 步，用户口径"生存第一"）**：**夜里**且 `_short_handed(turn)` 为真 ⇒ 弃任务回炮位。判据 = **没被任务钉住的角色数 < 武器组数**（一人只能操一组）—— 工人阵亡（只可能在夜里）后 3 人变 2 人、阵形两组 ⇒ 火力缺口比任务积分要紧。**白天恒假**（白天不能开火、回炮位只发 `move`），所以两支都不带跨回合状态、工人白天复活即自愈。代价 = 任务当场作废（离开任务点一格），用户明确要的取舍。⚠️ **没有"离开后重新入环还能续上"这回事**。"周围一格"按**切比雪夫**（含对角，文档没写，与全局距离度量一致）。**"答案格式"文档完全没写**（只有"通过率 = 正确字段数 / 全量字段数" ⇒ 是多字段结构化答案），`agent/prompt.py` 的段模板措辞是唯一的杠杆 —— 这是最大的单点风险（也是 `SOP2Prompt` 存在的理由：让 LLM 自己把摸索出的答法沉淀下来）。
- **任务内的会话上下文 = `Context`（`agent/context.py`）**：判题器的 LLM 每回合只看到 `prompt` 一段字符串（接口文档标 String）⇒ "同一个 task 同一个上下文"的落地形态 = **每回合把这道题的会话渲染进 prompt**（`AGENT._context`）。**prompt = 标准 messages JSON**：`[{"role": "system"/"user"/"assistant", "content": …}]`（紧凑 `json.dumps` 串）—— 结构由 role 表达，正文就是原文；自造文本版式（`# 对话记录` + 标题块）**用户实测效果非常差，已弃**。事实与取舍：
  - **身份 = 题目原文**（`phaseTask`）：换题即换新会话；**冷却后同文再现 ⇒ 续上旧会话**（它记得试过什么，文档化行为）；任务结束不清（死会话，下场换题自然被替）。
  - **assistant 消息记原文**：`task_channel` 每回合把 `llm_resp` 交给 `AGENT.hear()` —— **发命令（③）/交答案（⑤）那两轮没有 prompt，回复照样得记**，否则回灌那轮 LLM 看见的是"题目 → 莫名其妙的结果"，它自己要的命令凭空消失。**粘住去重**：与**最后一条** assistant 相同 ⇒ 跳过（`llmResp` 按可能粘住设计、`lastCmdResult` 不粘 —— 判据 ② 那条结论的再利用）；中间隔了别的消息再来同文 ⇒ 记（真的又说了）。
  - **user 消息只在真发了 prompt 的轮次进表**：首问 = 题目**原文**（构造即问，**不加**`【题目】`包装）；回灌轮 = 结果块(+纠错块)（两个标题留在 content 里当**内容标签**）；**无新内容的重问轮（畸形回复/③′）= 一句「请继续。」**（`Context.NUDGE`，拍的、实盘可调）—— 判题器 LLM 是黑盒，会话停在它自己的输出上是个含糊指令。
  - **system（`prompt.py` 的段模板）每轮现刷**（`Agent.chat` 里 `gen_system_prompt(self._tools, self._sop)`）：SOP 是活的，任务进行中沉淀的下一轮就得看得见 —— 那是 ③′ "调用成功"的回执。**别冻在构造时**。
  - **存储全量、渲染有窗**：消息逐字进表、`json.dumps` 逐字序列化，但 `render()` 只输出 **题目 + `【历史摘要】` + 最近 `_WINDOW`(2) 轮** ⇒ 任务 prompt 从 O(全部历史) 变 O(窗口)。**摘要的来源**：**回合末尾的压缩闸门**在 ③ 命令轮发**压缩请求**（`AGENT.compression_request()` = 独立指令 + **原始上下文全文** —— 用户拍板"原文永久保留、压缩总从原文重来"，消息表**永不截断**；任务 prompt 专注任务、system 里没有压缩教学），它的回复（**裸 `<summary>`**，`chat.is_summary_reply` 判别）经 `AGENT.adopt_summary` 进摘要、**不进会话表**；判题器不答就保旧摘要（best-effort，绝不重问）。`answer_of` **先挖摘要块再扫**（字面量 `<answer>` 不是答案）。**时序**：压缩在 ③ 命令轮发、下一回合初摘要已就位（结果在窗口里）。**日志侧有界**：`【本轮提问】` 行是单行 JSON，`_clip` 钉在 `LOG_PROMPT_MAX`。
- **`describe()` 读 `targetPos` 必须容缺**（已从根上拆掉）：它跑在 `app.handle` 的 `try` 里，硬读 `cmd["targetPos"][0]` 遇到 `acceptTask` / `submitAnswer`（**报文里没有这个字段**）会 `IndexError` ⇒ **整回合退化成空指令**。现在是**通用摊开**（`action` 之外的字段有什么打什么）⇒ 这类动作天然踩不到雷。⚠️ **别回头去手写一份字段清单**：那是第二份会跟报文漂移的真相，而**漏字段不会有人发现**（用例 `test_describe_*` 三条是守门员）。
- **`render()` 的图例里 `1`-`4` 是「阵营」的任务点，不是「我方/敌方」**：`1`/`2` 来自 `zones` 的 `challengerTaskPoint*`、`3`/`4` 来自 `defenderTaskPoint*`，两队**同时存在**。样例里 `teamOur.type == "challenger"` 两套恰好重合 ⇒ **写"我方任务点"是一个在样例上永远测不出来的错**。我方可接的那两个点是 `Turn.task_points`。**别把 `mapInfo.zones` 的这 4 个字符和 `playerTasks` 混起来。**
- **地图字符表：空地是「空格」、我方围墙 `#`、敌方围墙 `%`**。"这张图有多大、边界在哪"由**一圈 `—` 边框**负责（不是靠填满）。两条不变量：**① `_char` 恒返回 1 个字符**（全角字符占 2 个显示列，一引入就整图错位）；**② 一个字符只能代表一样东西** —— 守门员 `assertEqual(len(set(chars)), len(chars))` 是**通用断言**（钉的是"不重复"，不是具体字符值），字符怎么改它都守着。⚠️ 图例里那一项写的是字面量 `空格=空地` 而不是 `f"{_char('')}=空地"`：空地就是一个空格，直接拼出来是 `" =空地"`，看着像少打了一个字符。字符变化是 1 字节对 1 字节 ⇒ 地图自身的体量不变。
- **判题器的回执有两个字段，"为什么"与"哪一条"分家**：
  - **`errors`（顶层，本轮）** —— `[{errorCode, description}]`，**这是"任务为什么一直失败"的唯一答案来源**。码的含义（接口文档 L181-198）：**0 未知 / 1 任务超时 / 2 答案错误 / 3 网络错误 / 4 指令错误 / 5 LLM 额度超限**；**2 专指 `submitAnswer` 交的答案"不正确或者不完全正确"**，**5 只在每个游戏日的 3 次额度用尽时出现**（任务存续期间不占额度、也不报这个错）。**这段含义不写进代码** —— 写进去就是一份会跟文档漂移的第二真相，码表只活在 `CLAUDE.md` 这一行里给读日志的人看。
  - **`lastRoundRoleActionResults`（顶层，上回合）** —— `Map<int, boolean>`，key 是**角色/武器 id**、value = **动作合法性**。它答的是"**哪一条**没通过"：撞墙、打空这类**执行失败**不计异常、`errors` 里一个字都没有，只有这里会翻 `false`。解析时**原样保留**（含我们不操控的基地格），**排序是打印时才做的事**。⚠️ **`"false"` 是个非空字符串**，`bool("false") == True` —— 只能认真正的 JSON 布尔，猜一次就会把"不合法"读成"合法"。
  - **`lastCmdResult` 是任务线唯一的沙盒窗口**：它是**上回合 `executeCmd` 的执行结果**，格式 `[exitCode:N]\n<输出>`（超时 `[TIMEOUT]`、判题器异常 `[JUDGER_ERROR]`、超 64KB 尾加 `[TRUNCATED]`）。**那行状态不解析** —— 非空即**原文**回灌给 LLM，让 LLM 自己读；解析它就是又一份会跟判题器漂移的真相。
  - **`worldNews` 不读**：它是宝藏线（`summonTreasure`）的线索（样例那段的"石门需三钥"就是），与任务线无关。
- **复盘日志的体量与格式**：`Turn.summary()` 的每个列表**必须有上界**（`SUMMARY_MAX_ITEMS`）—— `Turn.robots` 是逐回合全量、没有上界，摘要的长度不能是"数据相关的量"。摘要里 **-1 是"字段缺失"不是 0**（金币/回合号打成 `?`）。**局面 = 摘要单条**（图例与整张地图退出日志，`render()`/`LEGEND` 仍在代码里、只剩用例在用）。
  **摘要四个块、各占一行、块间不留空行**（`【回合】`/`【我方】`/`【机器】`/`【可接任务点】`），
  我方那一行还打出 `石N铁N铜N`（"工人有铜却不去卖"只有背包明细能回答）；
  ⚠️ 首行前面那个 `\n` 是留给 `logging` 前缀的（**空行也是物理行** ⇒ 数记录里的物理行时要把它算上）。
  **记录数不固定，每条都"有事才吭声"**：局面、动作每回合各一条；**判题器报错 / 回执名单 / 任务行 / 沙盒行 / 提问**五条各自只在有内容时出现（干净的白天回合 2 条，样例那种带假错误的局面 4 条）。`errors` / `action_results` / `cmd_result` / `prompt` 的空值是**天然的安全值**，所以正常局面一个字节都不花。
  ⚠️ **`handle` 在 `_log` 之前还打一条 banner**（`###第N回合###`）—— 它**不受 `_log` 上限管**，数"这一回合打了几条记录"时要单独加上（干净回合 3 条不是 2 条）。
  ⚠️ **日志的实际排列是 banner → 任务 → 沙盒 → 局面 → 动作 → 回执 → 提问**：任务行与沙盒行由 **`planner.task_channel` 自己打**（那两个字段只在它的作用域里），而 `task_channel` 先于 `_log` 被调用。⚠️ 它们因此**不在 `app` 名下**，只盯 `coregeek.app` 的守卫看不见（见硬约束 5）。
  ⚠️ **"任务行 / 沙盒行"曾被误删过一次**：那两块是黑盒下**唯一**能看见沙盒回执的地方（本地 e2e 的"LLM"是我们自己写的，只证明解析自洽）⇒ 动它们时别顺手删。
  **沙盒行只记结果、不记发出去的命令**：发命令那一回合 `llm_resp` 就是那次工具调用（新旧形状都算），已经印在任务行的"上一轮模型回复"里了，再抄一遍是**同一回合、同一条字符串抄第二遍**；而且"沙盒行数 = 实际跑过的命令数"这个对账关系更干净。
  **截断留痕**：`_clip(text, limit)` 超长时打 `…（共 N 字）`。原来那版 `text[:120]` 是**静默**截断 —— 任务一长，日志里就是一段没头没尾的文字，看不出后面还有没有内容，于是"任务一直失败"根本无从查起。**上限的单位是字不是字节**（中文 1 字 = 3 字节）。**截断规则只有 `_clip` 一份**，在叶子模块 `src/coregeek/utils.py`（与 `LOG_TEXT_MAX` 一起）。⚠️ **但 `describe` 那条边照旧是注入、不是 import**：`describe` 的契约是"不认识日志"，给它塞一个日志默认值就等于让 `protocol` 认识日志层。那个文件里**不放状态、不放 logger、不放格式化函数**，只放"上限"与"截断"。
  **`prompt` 打出来**：它 = 模板 + 这道题的会话往来，工具清单长什么样、SOP 那个槽填进去没有、会话接没接上，实盘上只有这一行能回答（本地 e2e 的"LLM"是我们自己写的）。它**单独一行**（`【本轮提问】：…`，由 `_log` 打 —— `planner` 那边拿到的还是"返回值"），题目原文与 LLM 回复留在任务行里。
  **回执那一行列全部实体**：`上回合合法性：10010=False 10011=True …`，按 id 升序。判题器只回它**收到**的那几条 ⇒ "这条没发指令"与"发了但没过"在只列未通过名单时长得一样，而两件事下一步的做法完全相反。触发条件是"**有回执**"（全 `True` 也打 —— 那正是"发出去的都合法"的唯一证据），不是"有未通过"。
  **SOP 更新那一行**（`【SOP 更新】：流程「名」存 N 字（…）｜ 前 80 字：…`，带流程名；括号里按需出现 `剔除 N 处 <answer> 段` / `收到 M 字，超上限截断` / `超 5 条，丢最旧：「名」`）由 `agent/tools/sop.py` 自己打，**只在内容变化时出现**（LLM 把同一段反复喂进来时静默）。换行转义成 `\n` ⇒ 一条记录恒为**一行**（时间戳前缀只加在第一条物理行上）。**任务行的触发条件带 `llm_resp`**：任务刚结束那一回合 `phase_task` 已经空了，而那是唯一一次能看见"判题器最后答了什么"的机会 —— ⚠️ **所以任务行必须写在 `task_channel` 那道早返回之前**（插在早返回之后那一格就永远打不出来了）。

## 文档地图

| 文档 | 用途 |
|---|---|
| `docs/任务书.md`、`docs/接口文档.md` | **权威规则与协议**。冲突时以这两份为准 |
| `docs/design/code-task.md` | **迭代留痕**：每一步的目标/产出/**仍生效的已知不确定性**，加文末一张「当前仍悬着的事」。动手前先看这里知道走到哪了 |
| `docs/design/task-analysis.md` | 规则整理笔记（标注了"文档明示 / 我的推断 / 矛盾点"三类，以及待实测清单） |
| `docs/design/strategy.md` | **当前策略导读**：打什么 / 每回合的判据链 / 旋钮在哪。⚠️ **§1 = 角色决策状态机**（状态 / 闸门 / 转移：①–⑦ · 夜里 N1–N3 · 工人 W1–W6）—— **读 planner 之前先看它**。**代码是唯一权威**，本文只讲"为什么"；逐条规则不重抄，未了结项只指向 `code-task.md` 文末那张表 |
| `docs/design/worker.md` | **角色运行逻辑全景**：三个角色每回合怎么决策——`plan()` 骨架 / 黑板五本账 / 白天优先级阶梯 / 夜里操炮 / 常量旋钮表。现状梳理，与 `strategy.md`（为什么）互补。⚠️ **状态机的权威在 `strategy.md` §1**，本文是同一件事的"阶梯"切法，改判据顺序时两处都要改 |
| `docs/design/code-design.md` | 上一版重写的设计稿。**正文已在工作区删除**，需要时 `git show HEAD:docs/design/code-design.md` 取；其中 §19「实盘接入踩坑」仍然有效，但**代码架构部分已被本次重写取代** |
| `docs/request.txt` / `docs/response.txt` | **只能当字段形状参考**。`response.txt` 不是合法 JSON（缺逗号 + 同 id 重复），不能 `json.load`；`request.txt` 是合法 JSON 但几何是手工示意数据（挑战者那两座墙不在环上），**不可用于校准几何**，`lastRoundRoleActionResults` 的值也别当真实信号解读 |
| `example/CoreGeek/CoreGeek/` | 官方 demo（**双层嵌套目录**）。入口形态照抄；`brain.py` 的 `_wall_order` 等已验证实现可参考，但整体策略是玩具级，不继承 |

## 当前工作区状态

本次是**推倒重写**：`main3.py` / `src/` 等已按第 1 步重写（旧版本在 git 历史里，`git show 5b4dfcf^:<path>` 可取回）。
`tools/`（selfcheck / smoke / decrypt_log）、`README.md` 目前**不存在**——按需再加，别凭惯性建。
进度、每一步的产出与**仍生效的已知不确定性**全在 `docs/design/code-task.md`（文末「当前仍悬着的事」是唯一一张未了结项的表）。**本文件不记步骤流水**。

`tests/` **按 src 拆分**（改哪个模块只读/只跑对应文件），**不建自研测试框架**：标准库 `unittest` 够用。
测试文件名与 `src/coregeek/` 一一对应（`test_<模块>.py` ⇒ 用例类名 `grep "^class" tests/*.py` 就列全）。
只有 `_fixtures.py` 是共用夹具（**≥2 个文件共用**的才进这里；discover 的 `test*.py` 不收它，也不会当用例跑）。

⚠️ **类名与拆分前一字不改** ⇒ 文档与注释里"见 `XxxTest.yyy`"的引用照旧成立；单文件可直跑
`py tests/test_xxx.py`（文件头 bootstrap 自带 sys.path，`_fixtures` 靠它找到）。

**常用命令**：

```bash
bash run.sh 18085                                          # 起服务（自动挑 python3/py 并验版本）
curl -s -X POST --data-binary @docs/request.txt http://127.0.0.1:18085/
PYTHONUTF8=1 py -m unittest discover -s tests -v           # 跑用例；不加 PYTHONUTF8 中文会乱码
```

⚠️ **起服务前先确认端口上只有一个监听者**：`netstat -ano | grep LISTENING | grep 18085`。
Windows 允许 `SO_REUSEADDR` 并存，上一轮遗留的进程会继续吃请求 —— **症状是"改了代码没反应"，
本地怎么调都是旧行为**（真踩过：合成局面上 `build` 一条不发，其实是旧进程在答）。
杀干净再起：`netstat -ano | grep 18085 | awk '{print $5}' | xargs -n1 taskkill //F //PID`。

本地 **`python` 是 3.7.1**（Anaconda），必须用 `py`（3.13）；`run.sh` 已处理这个坑。
