# 第一天折返复现

`day1-r51-public.json` 与 `day1-r51-memory.json` 来自源码37b73f3、本地observed-seven-days模型、seed1/defender的第51轮；模拟生成，非公司日志。仅公开观测和由此前公开观测形成的Planner记忆，没有模拟器私有答案。

worker20012：第50轮(34,4)→(35,5)，第51轮业务计划去矿点(25,13)，却被共同炮位避让改成回(34,4)。两次移动都成功，因此不是动作失败；绕开共同炮位的合法通路存在。

独立预期：仍朝同一个矿点的合法邻格前进，完整剩余路径变短，不返回刚离开的(34,4)，不拆墙。测试不锁定多个等长通路中的任意一种。
