# Competition_HW 开发合同

本项目规则基线是 `docs/任务书.md`、`docs/接口文档.md` v1.0（2026-09-09），
通过 `Tymonxiong/Competition` 提供。先读 `docs/DEVELOPMENT_RULES.md`，再读相关官方章节。

## 必须遵守

1. 官方协议、游戏规则、数值、资源限制、计分与胜负条件不是调参项。不得为提高本地成绩而改动。
2. 官方原文与样例只作为来源维护，禁止顺手修正文案、枚举拼写、重复 JSON 键或样例数值。
   官方更新时单独记录来源版本、差异及迁移影响；无法判定的冲突登记为待确认，不静默选边。
3. 可自由优化策略选择、寻路、任务求解、内部架构、性能与调试界面；不得扩大官方观测、权限或动作额度。
4. `_demo`、种子、未公开敌方信息、未来波次和答案只属于模拟环境，不能进入决策策略或正式输出。
5. 已知规则未实现叫“实现缺口”；明确规则的近似实现叫“非合规近似”；官方尚未给出的细节叫“假设/待确认”。三者不得混用。
6. 示例代码不是规则权威；本地模拟、动作审计和测试通过均不是官方认证。所有验证结论标明覆盖范围。
7. 策略不得依赖本地扩展字段才能运行。比赛入口保留 `bash run.sh <port>`、监听 `0.0.0.0`；
   根路径 POST 是当前兼容入口，网页 GET 和 `/debug/*` 是本地扩展，不强加给判题器。
8. 修改前给变更归类：官方实现修复 / 策略优化 / 本地假设 / UI工具。涉及规则的变更引用
   `docs/DEVELOPMENT_RULES.md` 中的规则编号与原文节号；写清行为例子和验证方式。
9. 规则修复需要独立预期结果的回归测试，不能只用同一实现生成答案并证明自己正确。
   策略变更跑相关测试及代表性左右换边/留出种子；纯文档或样式变更不要求跑整场基准。
10. 提交前执行 `git diff --check`。涉及协议/结算/策略时运行 `python -m unittest discover -s tests -v`。
    规则变化后旧报告标为旧版本证据，不覆盖为“新版本已验证”；报告登记代码版本或文件哈希、种子与假设。

## CodeGraph

存在 `.codegraph/` 时，在定位或理解代码前优先使用 `codegraph_explore` 或
`codegraph explore "文件/符号/问题"`；不存在时不自动创建索引。

## 完成说明

交付说明列明变更类别、规则依据、相关测试和仍未覆盖的规则。
不要把保护源码原件的约束解释为禁止修复参赛程序；符合既定规则的修复和日常策略优化可直接进行。

## 执行分工与额度（用户要求，2026-09-13）

主要实现任务优先交给全局 DSH，使用 deepseek-flash / max；Codex 负责顶层设计、简短 Spec、
审核与必要验证。范围明确的前端、工具和实现修复避免由 Codex 重复实现。困难问题或执行故障
确需接管时说明原因。减少重复读取、大段日志、重复测试和无变化轮询。
同类任务复用 DSH 原生 session，由 DSH 自行管理上下文，不改写或手动重建历史。
当前可用前端会话池为 `.workflow/dsh-sessions-v2`（frontend）；旧池 frontend 已超时失败，保留记录。
继续工作前检查对应 identity/status；会话失效时明确说明，不能将新会话冒充原会话恢复。
等待调用达到时限不等于DSH执行失败，不能据此接管实现、重发提示词或终止DSH。
应对同一任务输出目录继续执行wait，直到收到原生完成/失败事件；无进展时先检查进程与心跳。
如果监督程序确实终止了执行，应如实说明，优先采用DSH原生恢复；不得伪造上下文恢复。
原生恢复使用 `workflow/dsh_sessions.py resume`，绑定原会话ID、原运行证据和新的审核后续任务；
恢复确认见新输出目录的 `native-resume.json`，必须由 `ctx.agents.resume` 返回且 `created:false`。
2026-09-13迁移状态：v2/strategy已原生恢复并使用无执行时限监督器，v3/qa-tooling也使用新监督器。
v2/frontend与v2/qa-tooling仍是旧监督器进程；文件更新不会热更新进程。下次派单前，仅在确认其空闲
且已有持久日志时显式stop，再用原ID原生resume迁移；不得终止正在执行的任务来更新监督器。
