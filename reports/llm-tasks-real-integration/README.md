# 真实模型与模拟器Agent集成验证

原执行于f060d10源码完成，文件哈希见report.json，stable_sources=true。2026-09-15，OpenRouter deepseek/deepseek-v4.1-flash，max映射API xhigh。凭据只从用户本机已有凭据库读入内存，没有加入状态或仓库。

三组任务全部通过，共14次请求；已返回usage合计17175 tokens（失败请求未返回的用量不推算）：

| 用例 | 阵营 | 调用 | 游戏回合 | 结果 |
|---|---|---:|---:|---|
| 文档路径v1/北京天气 | challenger | 5 | 13 | PASS |
| 路径与参数改变v2/上海天气 | defender | 5 | 12 | PASS |
| 精确十进制账目汇总 | challenger | 4 | 10 | PASS |

链路使用实际LocalLLM异步适配器、simulator.step、brain/TeamAgent/shared router、虚拟沙盒和taskworld结算。模型返回前暂停本地游戏回合，没有重启或重复派发等待中的请求。每轮保存/恢复JSON记忆；模型必须完成真实文档读取、数据查询，最终答案再与独立手工标准比较。

这轮驱动Python为本机3.11.14；同一集成代码另通过Python3.11.10的661项全套测试，不把两个解释器的验证混称。模型调用次数上限24，仅用于这次独立试验，没有修改用户页面服务的费用保护。

第一组实际耗时400.75秒，第二组89.89秒，第三组23.328秒。包括真实API等待和控制流程；不是比赛HTTP回包耗时，也不是官方LLM时延保证。完整trace包含实际输入、最终模型答复、动作、工具结果和本地反馈；不包含隐藏推理链。

第一组首个模型回答等待368.72秒，随后另一次请求记录provider_timeout_or_network；控制器经过有界等待重新规划，仍完成同一任务。保留失败记录，不将这次运行描述为每次网络请求都成功。

结论仅覆盖三组自进化任务的实际模型集成。不是三种LLM任务类型都完成，也不是完整对局或内网PASS。新闻/宝藏推理、长题检索、方法复用效率、完整对局收益与后续重试修复仍需独立验证。
