# 中央商贩模拟地图对齐

2026-09-16；类别：用户补充对齐 / 本地几何假设 / UI工具。源码 `bebd99f6696af2e279f8fc3ea56debb612332b73`。

用户此前在对话中确认小贩在正中央、武器商店在其右上方。核对本地官方基线 `docs/request.txt` 后，样例位置分别为 `(20,16)` 与 `(25,20)`。官方正文（任务书 §4.1/§4.6、接口 §1.2）未定义所有地图通用的精确位置，所以默认模拟地图采用样例格子，记录为 `central-sample-v1`；精确坐标仍登记待核验（S05 / project_contract.yaml V-026）。本轮 GitHub 的“小贩/中央/右上”检索未找到更强的通用坐标规定，未拿其他 Issue 的模型/日志内容推导坐标。

此前生成器将商贩与矿点等一起随机摆放。现在新场景固定这两处位置，尽量保留其余种子布局；只搬动相冲突的中立点，初始放置会考虑任务点2的双格足迹。该处理不宣称修复了任务点2的所有移动/编码缺口。

保留显式 `legacy-random-v0` 用于历史随机地图复现；旧录像和导入请求不搬动已有位置。元数据放在 `_demo.market_layout`，正式观测去除私有元数据；参赛策略、交易价格、商品效果与动作权限未改，也不在策略中硬编码中央坐标。过去随机商贩布局的成绩仍是旧地图证据，不能直接比较为策略提升。

验证：

- 独立坐标、双方/种子/波次配置、100组初始地图中立点足迹和建造区不冲突、冲突重定位、旧布局/导入兼容、双队共用地图：7项定向测试通过。
- Python3.11.10 Linux 独立克隆全量 **983项，316.022秒，OK，无跳过**（full-suite.log / validation.json）。
- 实际 `/debug/scenario` 六组HTTP请求均返回小贩20,16与商店25,20（scenario-http.json）。
- 浏览器实际新建并暂停对局，点击小贩与武器商店，详情分别显示“位置 (20,16)”与“位置 (25,20)”；来源提示显示中央样例布局，画面中小贩中央、商店右上。JS语法检查通过。
- 四组双方/留出种子260轮，本地基地均1500HP，无动作审计错误或执行失败；积分为550/1209/909/873，见cases.json。仅为新地图冒烟，不是内网或完整十天认证。
- 原版入口的实际 tar.gz 10次HTTP请求通过，含第四夜全员防守等既有行为；最长55.288ms。包SHA256 `86cc678e48ec3f12505fada3c6a3c3e52386cbfd608a998661a0ef5332e99cb8`（package-build.json / package-http.json）。
- 官方原文、原始请求/响应和原始 Demo 归档未改；`git diff --check` 通过。

使用：更新后重启服务、新建对局查看；已有录像/已开始的旧地图保持原位置。相同种子在新旧地图几何下不代表相同实验条件。真实地图若返回不同坐标，以实际请求为准，继续校准模拟器。
