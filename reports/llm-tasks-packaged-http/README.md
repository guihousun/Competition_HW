# 实际压缩包的连续 HTTP 对局验证

2026-09-15，Python3.11.10，Windows。包源码be94ed4c3ccb820c5e26ee19e838023da9a0d230，
压缩包SHA256 ade1f698865265b7d88a449be1e942f8016fc41d8fcde9db58e87ab141ae0d91。
这是内部验证产物，尚未更新仓库根目录的正式交付包。

实际解压运行官方原样main3.py，独立HTTP进程返回动作与模型/工具请求；
另一进程只通过外部响应模式结算环境，禁止运行第二套本地策略。
脚本模型只读取实际prompt，工具由本地虚拟沙盒执行；全程没有真实API调用。
本次显式设置TaskAgent/WorldAgent为on，**不代表参赛包默认开关已完成推广审核**。

| 场景 | 回合 | 任务完成 | 基地HP | HTTP p95 | p99 | 最大 |
|---|---:|---:|---:|---:|---:|---:|
| seed1 challenger 冒烟 | 130 | 2 | 1500 | 46.61ms | 62.82ms | 87.74ms |
| seed90317 defender 完整 | 1300 | 23 | 1500 | 199.34ms | 221.92ms | 297.61ms |

完整场80个prompt、49个沙盒命令、100次攻击，无prompt/executeCmd同轮冲突，总分2142。
所有HTTP响应在5秒内，且此样本p99低于工程目标1秒。计时包括客户端请求构建、传输和JSON回包读取，
不包含脚本模型与环境结算；该口径接近比赛的请求响应边界。
它不是内网PASS，不能外推成任意机器/任意请求的延迟保证；Linux及最终默认配置还需独立验证。
此驱动的脚本模型与早期完整对局报告中的脚本求解器不同，分数不能当成完全相同条件的对照。
