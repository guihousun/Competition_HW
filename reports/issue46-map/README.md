# Issue46：attack_map 局部对齐交付

运行源码 `b1765d507a0b438252cfd9f6b92048d9bb038bfa`，最终测试提交 `dce65cfb4146388c7656af7c2132e3295ac01c4c`，只相差两份测试，运行/配置/打包输入完全一致，见runtime-equivalence.json。交付提交另含本报告与包。原始来源为[Issue46](https://github.com/guihousun/Competition_HW/issues/46)五场首帧文字摘录，未获得完整 replay.json。类别为本地地图证据校准和 UI 修复，R02/R07；正式策略、动作协议、数值、金币与胜负条件不变。

## 已实施

- 网页默认 attack-map-observed-v1，固定已知双方基地/市场/任务点，原 seeded-local-v1 随机地图可选；底层旧 scenario 调用仍保留历史默认。
- 市场原坐标本已正确；修正的是基地/任务布局及前端2×2左上锚、任务点2横2×1。绘制、拾取、聚焦、选择框和特效中心一致。显式两条任务zone仅显示层合并，导入状态不搬动。
- 调试新建/录制/LLM入口携带地图档；缓存键和来源元数据区分地图。页面明确本地重模拟不是官方逐帧回放。
- 数字23/24/25未解析，不擅自增加阻挡；矿点、完整defender出生、地形/建造区域/刷怪点仍待补证。任务演示快捷移动另有元数据说明。其他地图未套用。

说明见[地图对齐文档](../../docs/ATTACK_MAP_ALIGNMENT.md)、[Spec](../../specs/ISSUE_46_ATTACK_MAP_ALIGNMENT.md)。前一版中央两墙优先、从首日起整排后墙不建的策略完整保留。

## 实际验证

- Linux/Python3.12.3全量 **1352项通过**，实际退出0，耗时495.8秒；日志哈希见tests-receipt.json。
- 新地图双方各130轮（1昼夜），基地均1500，失败指令/异常均0；本地分数challenger386、defender498，不是内网成绩。完整逐轮JSON留在本机，day-night-report中登记哈希。
- 旧源4cb0a83与候选源码的seed1/7/19双边初始公共观测哈希逐一相同，保留历史压力实验输入。
- 浏览器实际运行/暂停/回退，任务点二(16,17)显示2×1，切换红方、切换随机地图及来源提示正常，错误级console为空。详见browser-check.json。自动几何JS检查由unittest包装器纳入全量。
- Python3.11.10实际包200轮HTTP通过，最慢197.3ms；脚本模型/虚拟沙盒仅验通道，不代表真实LLM成功率。该HTTP测试仍用旧seeded环境，固定地图双方验证单独列出。

首次Linux启动因Windows工作树git指针无法由WSL解析，在clone预检阶段结束，未执行测试；改用主仓库克隆后正常执行。首次完整回归发现两项旧测试：把默认随机场景与默认固定场景比较、基地中心预期向上偏移；测试提交显式比较同地图并覆盖两种布局，按官方左上锚修正像素预期，未改运行代码。最终全量结果如上。

## 参赛包和边界

根目录 CoreGeek.tar.gz，SHA256 `7d85d7f43a572a91221ffe8d91d929abb198113f7d064e127ee09b5c2eed831f`；原官方main3.py入口按competition-flat-v1保留。codex/sgh拉取即可取包。新页面需重启服务并刷新，再创建新对局；旧录像保留旧位置。

本次没有新十天胜率结论。此前 reports/center-rear 的5/6存活是旧seeded地图实验，不能改称attack_map成绩。缺完整官方首帧和数字图例，未声明全图逐格一致、真实LLM通过或内网PASS；来源Issue保持开放。

用户随后指出的空转已复现：清场后仍被防御角色锁定；蓝方开拓者还会因双格任务点第二格未进入寻路阻挡而被末端合法性过滤拦下。它们正在独立clear-night工作中修复，不在本地图候选中冒充已解决；本次1352项通过不代表不存在策略缺口。
