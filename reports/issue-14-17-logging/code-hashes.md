# Final reviewed source fingerprints

See ../issue-14-17-architecture/source-hashes.json. These are actual checkout bytes; Git normalizes text line endings in the committed package. The archive has its own full manifest.
