# 真实模型新闻与宝藏解释：未通过

2026-09-15，源码5ebfd5a71154cfcf13a72113753d7bc1d5e25a7e，Python3.11.10，
OpenRouter deepseek/deepseek-v4.1-flash、max→xhigh。三个日期的公开合成快照，经brain/router/WorldAgent调度。
不是完整对局，也不是官方数据或内网PASS。来源哈希运行前后一致。

共9次实际请求；3次宝藏回答完整返回，逐日累积得到地点、物品、窗口，提前不召唤。
6次新闻请求没有返回可用答案。该批只保存RuntimeError类型，不能断言六次都是网络错误。
成功回答返回的usage合计5372 token；失败请求的usage当时未保存，因此**这不是总消耗**。
耗时约2896秒不是游戏HTTP耗时。每日普通额度均为3，未超额或伪造豁免。

原始report.json passed=false。第1天news_today=true只是检查铁矿尚未停工，空建议也会通过；
该检查过弱，不能视作新闻解析成功。第2、3天的停矿判断失败，新闻状态均retry_limit。

随后一次独立同新闻重放加入安全诊断：finish_reason=length，completion_tokens=4096，
提示词386 token，总计4482，约130.7秒。它证明**该次重放被输出上限截断**，
不能反推之前六次失败的具体原因。截断内容没有交给Agent当有效答案。
诊断版本只保存固定原因枚举、结束类型和整数计数，不保存隐藏推理、HTTP正文或凭据。
后续提高输出上限的对照调用应另行登记，不覆盖本批失败。

第二次独立重放保持模型、max强度及完全相同prompt哈希，将单次上限显式改为16384：
返回完整JSON，completion_tokens=5199、total_tokens=5585，约141.6秒。
回答指出第1天可采、第2—3天停矿且涨价、第4天恢复，附公开原文引用。
这只是单次提供方对照返回；尚未证明整个四日调度通道、重复成功率或完整对局通过。
两次诊断结果分文件保留，不将第一次截断替换成PASS。
