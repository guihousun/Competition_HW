# Agent 演示与原文查看器审核

2026-09-15；类别：本地模拟器适配 / UI工具。协议及原文权限依据 R01/R07。
Python3.11.10完整699项测试通过，265.186秒，验证器确认运行前后agent及tests源码哈希一致。
validation.json登记文件哈希；base_sha=f208210是这些未提交变更的父版本，不能冒称本次源码SHA。

演示入口支持三类综合、自进化文档查询、长文档检索、新闻与多日宝藏，脚本/真实模型明确分开。
脚本模型只有prompt文本权限；真实调用累计上限可调但不清零计数与请求去重。
新测试覆盖长文档经过debug JSON请求、读取、检索、查询、提交与独立结算，全程实际API计数为0。
前端稳定性检查覆盖原文文字转义、选择保留、滚动位置、阶段与额度、任务结束提示覆盖旧阶段。

浏览器实际访问8083：长文档演示拿到本地100%通过率；原文查看器读取到14541字符且包含中部“接口入口”；
面板显示4次模型、2次沙盒、1次原文检索；暂停后可切换到已收到的文档。浏览器error日志为空。
检查时发现结算与“等待判题”阶段不一致，已修复，并添加结束报告不覆盖新任务的检查。
此为脚本模型流程验证，不是长文档真实模型能力证明。真实模型模式本轮未在浏览器付费运行。

本地serve默认开启Agent预览，显式off仍有效；正式submission入口不因此开启全部能力。
真实提供方失败新增固定枚举与整数usage诊断；截断回答不进入Agent，不保存隐藏推理或HTTP正文。
相关9项提供方/通道测试包含独立截断响应与敏感异常测试。

尚未完成：正式包构建、实际压缩包HTTP时延及Linux探测、默认策略推广、内网验证。
