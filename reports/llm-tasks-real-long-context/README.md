# 一次真实模型长文档任务验证

2026-09-15；冻结源码44d31fa4736a10713320ca999f4fd816c3ca6f35；Python3.11.10。
OpenRouter deepseek/deepseek-v4.1-flash，max→xhigh，输出上限16384，实际调用上限8。
类别：Agent/本地模拟器验证；依据R01/R07。不是官方题目、内网PASS或统计成功率。

通过debug JSON往返→LocalLLM→真实模型→TaskAgent→只读虚拟沙盒→模拟器独立判题的完整链路。
长文档14541字符；接口位于原文中段，不能仅从头部摘要读到。
模型先尝试cat目录、列目录、grep、cat文档，再使用inspect内存检索定位，最后执行天气查询。
未在宿主机执行模型命令；不支持的命令返回虚拟沙盒错误，随后由模型调整计划。

最终答案是北京、temperature=23，与独立夹具值一致；本地通过率100%，积分58、金币30。
结束状态roundNo16；7次实际模型调用全部返回，5次沙盒命令、1次原文检索。
模型返回usage总计15961 token，试验墙钟约216.4秒；这不是HTTP请求耗时。
前后agent源码哈希一致；原始最终内容、简短计划、工具命令、结算和哈希保存在JSON。
没有记录模型隐藏推理、API凭据或HTTP响应原文。

第一次驱动脚本在领取任务后误把尚为空的teamAgent当对象，首个模型请求前失败；
已用无凭据、零调用预算复现此驱动错误，原记录保留为initial-harness-failure.json。
修正驱动后才运行上述试验，不把驱动失败记作模型失败，也不擦除记录。

范围限制：只有一道、一次长文档真模型成功样本；仍需换表达/换数据/重复试验，
且普通新闻/传闻的长原文证据处理需要独立审计，不能由本任务的inspect成功替代。
