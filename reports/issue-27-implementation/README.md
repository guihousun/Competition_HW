# Issue27、升级采购与混合防守交付

日期：2026-09-16。包内源码 `32b1c609e94b70ce87f6c30020c89fa587f9294e`。类别：策略优化、日志字段实现修复；依据R01/R02/R03/R04/R06/R07/R08，未改官方源文、数值、动作额度或结算规则。

## 结果

- 用户指定默认2 rocket+1 railgun，startup identity会反映新阵容。
- 修复旧采购要求工人同时临近商店与待升级建筑的问题。按实际地图/商品价格派一名工人购券、返回、使用，留一名工人在炮位附近；支持武器、基地和墙券，期限、路径、背包、团队预算和已持有券均参与约束。直接观察字段驱动，不依赖本地任务种子或隐藏数据。
- 夜间任务/道具分配完成后，为尚未开火的就绪武器匹配空闲相邻操控者；可取消普通走位来立即合法开火，但不抢用道具或任务的动作。已有攻击操控者不重复分配。
- 控制台增加去重的upgrade_itinerary和weapon_readiness诊断，解释没购物/没开火的原因。用户已确认实机存在武器商店，策略不硬编码其位于小贩右上的相对坐标。
- 金币日志改读官方goldNum，保留输出键gold。新增冲突字段/缺失值测试，旧null不是零收益。未确认不是官方失败，不能用19个false训练成19个失败标签。
- 原题Markdown“提交形式”等章节现在提取明确JSON对象结构；示例值不当答案。独立check命令在能确定绝对路径时走既有CRLF兼容工具，复杂shell不改写。

## 验证

| 范围 | 结果 |
|---|---|
| Windows Python3.11.10全量tests | 956项，341.546秒，OK；5项平台相关跳过 |
| Linux Python3.11.10相关测试 | 36项，无跳过；覆盖上述5项，含双方API/工程12个变体 |
| 实际参赛包Windows check/提交日志 | 12次POST，最长98.333ms，goldNum的+80/积分+80仍不推断官方通过 |
| 实际包Windows HTTP恢复/围栏 | 10次POST，最长117.585ms |
| 实际包Linux check/提交日志 | 12次POST，最长80.119ms |
| 实际包双方远商店购券→返程→使用 | 32次POST，最长249.071ms；两侧均r20购买、r28使用，金币200→100，武器一级→二级、恢复1500HP |
| 两阵容配对短局 | 种子1及留出90601、双方、RRR/RRE，共8×260轮；全部基地存活，无执行失败及动作审计错误 |
| diff、实际归档清单和SHA256 | 通过 |

开发阶段全量出现一次旧identity测试仍断言三火箭的失败；按用户明确的新阵容改成2R+1E后重新运行上述完整全量，失败日志仍保留为full-suite-development.log。其他早期定向fixture失败未作为通过证据。

配对实验中，两火箭一电磁炮并非各场都更强：种子1防守方积分1097对1094；留出90601挑战方混合阵容HP1340/分782，三火箭HP1500/分909。两种阵容都使用本次新版其他策略，因此这不是“旧包对新包”整体收益估计，更不是官方胜率。按用户指定交付混合阵容，继续真实场景测试，保留失败与差异证据。

## Issue证据范围

#27本次读取正文与全部评论（当时0条）。17场/19个提交回执属于用户提供摘要，完整603xxx原始日志尚未在远端出现，不宣称逐行重放。标题“0次成功”不能由official_success_confirmed=false推出；明确value_mismatch仍是有效错误证据。

#26新上传的6份过滤评论已经读取；其中7个完整“提交形式”章节用新版解析器均得到对象契约（issue26-filtered-evidence.json）。它们是历史1831136d记录，不是新包成绩。过滤版丢掉prompt等上下文；596420未保留我方结构化任务事件，不能以这份过滤版核验全部36个episode。

独立合成题、局部模拟结算、脚本化模型与实际HTTP传输均不代表实机PASS。真实API分页/鉴权/数据聚合仍可能产生错误；原始401/空结果与字段值不足以推断正确答案。此轮没有新增交战中双阈值机动修墙调度。

## 轮询恢复

旧本机poller已退出，最后心跳为2026-09-15 23:40 CST，未找到可证明退出根因的异常栈。恢复时保留原游标/快照；新增本机Windows任务CompetitionHW-IssuePoller，以当前用户权限运行隐藏监督入口，2分钟触发、实例互斥、尊重STOP。验证了已有进程时不重复启动、原进程退出后由计划任务接管并成功读取GitHub。网络/server故障及恢复仍仅记本地。没有模型参与空轮询；这项部署不会随git clone自动安装，也不承诺关机/登出期间继续运行。

交付文件为根目录CoreGeek.tar.gz及两个sidecar；包SHA256 `37708c1630761ac8c9b369c4cd24e176776122e3f72b2c752c103971de0aa2ce`。[回测说明](../../docs/INTRANET_ISSUE27_CANDIDATE.md) · [Spec](../../specs/ISSUE_27_ECONOMY_DEFENCE.md)。Issue保持打开。
