# 防御策略最终整合审核

变更类别：策略优化、测试与诊断工具。规则依据 R02/R03：官方任务书 §4.1–4.5，接口 §1.2、§1.3、§2。不改机器人属性、资源价格、伤害、动作额度或胜负条件。

## 交付身份

- 包内源码 SHA：`07f24a2cdcfef01b571b781ac308a59b78edfa95`。
- 根目录 `CoreGeek.tar.gz`：137753 字节。
- 包 SHA256：`74580c13fc5fe78b8a50d82bcfd8ed2f09093a1e6117a8fcbec9cc58bfab04ba`。
- 固定测试分支 `codex/sgh`，上传包与清单/旁车一起进入 Git；包含产物的交付提交可以晚于包内源码提交。

## 审核结果与修正

DSH 原会话原任务已正常完成，未因等待器超时重派。Codex 阅读最终 diff、核对原始报告文件哈希，并用独立坐标/通行检查复核全部 9 个案例。
核心变化：基于公开地图和基地几何选择预期来敌方向；炮塔朝向该侧，正面优先砌墙，后方/可达侧保留明确出口；炮位组合保留独立操控位和内部通路；缓存有界且随相关地形、既有墙/炮变化失效。

修正三处交付证据问题：

- 原报告“没有比基线慢”不成立。四场记录中部分场景变慢、部分变快，不能宣称普遍提速；四组候选峰值约 47–134 ms，仅是本机测量。
- 官方机器人有 `roleType`。原 35 行夹具的无类型、自定义血量不能作为战斗压力证据；改用 smallRobot / 40 HP，仅验证可见怪群不改变几何先验，不推断颜色与类型的关系。
- 修正了一个永远成立的矿工位置断言，增加地形/既有炮变化后的缓存检查。

DSH 的旧 392 项记录与 metadata 绑定其原始证据提交，不能替代本次整合验证。

## 当前版本验证

- **Windows Python 3.11.10 完整回归 418 项通过**，274.822 秒；`git diff --check` 通过。
- **模拟器实际 HTTP 接口**：GET 页面正常，左右两侧分别运行 70 回合的 `/debug/step`；三座炮全部在预期来敌侧，第一处墙位于正面。不是只调用内部布局函数。
- 独立布局检查 9/9：左/右/纵向、已有炮、水面阻挡、边界、后方外侧封堵、旧围墙等；见 `codex-independent-audit.json`。
- 同一源码提交两次生成的参赛包字节相同。原版 main3.py / pyproject.toml、平铺 src/agent 启动结构继续保留。
- **实际最终包**：Windows 和 Ubuntu 的 Python 3.11.10 各 4 项入口/日志配置检查通过；GET/POST 200，官方样例 3 条指令，正常、日志不可写、日志关闭均可响应。详见两份 package 报告。

## 模拟器同步与覆盖边界

本地模拟器与正式程序共用同一 `brain.py` / `defense_layout.py`；本次更新的是策略，不是重新定义模拟器规则。旧进程需重启，旧录像仍代表旧代码，需要新建对局。
已有本地首夜对照为种子 1、90317 × 左右两侧，双方基地均满血，分数相同。这证明布局改变且这些案例未出现额外缺口/被困/执行失败，**不能证明实战收益提高、35 怪防守通过或整场胜率提升**。
来敌方向仍是几何先验，不会根据真实波次自适应。官方刷新点、颜色/兵种映射、平台上传 failure 的具体原因和内网对局效果仍待实际平台证据。未宣称内网 PASS，不自动关闭来源 Issue。
