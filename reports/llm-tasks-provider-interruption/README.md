# 追加真实测试的网络中断与降级验证

2026-09-15。所有失败保留，不能用先前成功或后续恢复覆盖。
新闻/宝藏与长文档各已有一次真实成功；再各执行两次时遇到了提供方网络/超时错误：

| 追加实验 | 实际请求 | 结果 |
|---|---:|---|
| real-interpretation-3 | 9 | 前3天检查通过；第4天3次网络/超时，最新新闻未解释，整组FAIL |
| real-interpretation-4 | 12 | 所有请求网络/超时，没有模型结果，FAIL |
| real-long-3 | 8 | 所有请求网络/超时，无工具执行，任务超时，FAIL |
| real-long-4 | 8 | 所有请求网络/超时，无工具执行，任务超时，FAIL |

因此截至这些记录，四日流程与长文档各为1/3完整通过，不能写成3/3稳定通过。
这些追加失败没有可用模型答案；不等同于模型返回了错误答案，也不能算任务正确性通过。
适配器当前将网络与读取超时合并分类，原记录不足以进一步区分DNS、代理或连接中断原因。
先前9/9固定查询任务及独立本地回归仍是对应覆盖范围的有效证据。

随后无凭据GET模型目录返回200，一次真实最小JSON请求成功，回传71 token，约1.9秒。
它只确认当时连接与小请求恢复；恢复后的完整四日/长文档实验正在独立新目录运行，不重放旧在途任务。

## 完整模型不可用时的参赛包行为

实际ddca27e压缩包、Windows/Python3.11.10、seed90317 defender，默认配置。
HTTP验证宿主将每个模型回复设为空，整场不调用API、不运行第二套本地策略。
完整1300轮，144个模型请求意图、0个工具命令、0个完成任务、101次攻击，总分604，基地1500。
HTTP p99约310.35ms、最大342.77ms，无通道冲突或5秒超时。
这证明该本地样本在模型完全不可用时仍推进和防守；**不是任务成功或模型可用性PASS**。
原报告的scope是早期模板文字；实际模式由model_mode=unavailable明确标识。

可用 `tools/verify_agent_http.py ... --model-mode unavailable` 复现。判定目标为持续响应与基地存活，
不会要求不可用模型“完成任务”，也不会生成替代答案。未知官方模拟差异仍保留，不是内网PASS。
