# 本地录制与回放验证（2026-09-12）

变更类别：UI工具 / 本地模拟器调试设施。规则边界：R01 / R02 / R08；
没有修改官方协议、数值、策略或结算实现。使用说明：[REPLAY.md](../docs/REPLAY.md)。

## 自动验证

- `python -m unittest discover -s tests -v`：**213 项通过**，约 93 秒。
- 最后针对导入失败状态恢复与前端校验的改动，再次运行 `test_replay.py`：通过。
- `test_recordings.py` 新增 7 项：取消与保留完成帧、重复启动拒绝、过期 ID 隔离、失败恢复、
  参数拒绝、左右两侧 JSON 单步结果与后台帧对齐、HTTP 状态与结果获取、立即取消。
- `test_replay.py` 在 Node 中验证完整状态/事件往返、校验失败与版本拒绝、缺帧与回合错配、
  实时指令保留、关键事件筛选跳转、终局录像从头连续播放、清除终局横幅、导入失败保留录像。
- JS 语法检查和 `git diff --check` 通过；当前应用代码在工作区原本未跟踪，另以修改前备份
  对前端执行 `git diff --no-index --check`，无空白错误（退出 1 表示存在差异）。

## 浏览器链路

验证服务为独立启动的 `http://localhost:8081/`，保留原有 8080 服务。

| 场景 | 实际结果 |
|---|---|
| seed 1 / challenger / pressure 1，录制到终局 | 1300 个事件帧、1301 个状态；初始回合 1，末状态回合 1300，done=true |
| 从初始帧播放终局录像 | 正常连续前进，未因整场已完成而在早期帧暂停 |
| 筛选昼夜切换并向后跳转 | 第 70 帧 / 观测回合 71，第一夜 |
| 保存文件，刷新，再通过文件选择器导入 | 完整 1300 回合恢复、校验通过；无须重新模拟 |
| 跳到末帧再重置 | 显示本地终局；重置恢复第 0 帧并清除终局横幅 |
| 录制过程中刷新，再取消 | 恢复同一后台任务追踪；保留 31 回合，显示“已取消”，done 不伪装为终局 |
| 900×720 窄屏 | 修复 canvas 固定像素宽度撑开网格的问题；文档宽度 885 ≤ 900，无横向溢出 |
| 浏览器控制台 | 本轮功能验证未观察到 error 日志 |

完整对局录像的源码快照哈希：
`efdfd34f16685f340202ba9ba93ae3a15b025ed8c73dab6288458a24cf6f1506`。
哈希算法见 `debug.recording_metadata()`；涵盖 agent Python 源文件与两份官方文档，
不包含前端。最终前端和新增测试的单文件哈希另外存于 `replay-source-hashes.json`。

验证范围是本地单队录像及调试传输。取消测试回合数取决于操作时机，不是确定性规则预期。
新增左右对齐测试各覆盖 seed 7 / pressure 2 的前 5 回合；整局浏览器验证覆盖上述一个配置，
不代表泛化性能基准，也未进行公司内网官方平台测试。
本地建造区域、波次、任务夹具与部分弹道等假设以及官方双队胜负覆盖缺口仍然存在。
