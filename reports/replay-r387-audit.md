# 本地回放 `futurewar-local-seed1-challenger-r387` 审计

来源：用户提供的 `futurewar-local-seed1-challenger-r387.json`，386 回合，`local=true`，地图 `official_certified=false`。这份回放不能证明官方判题器结论，但可以直接暴露本地策略和模拟器组合问题。

## 已确认的异常

- 第 323–329 回合没有任何 `commands`、`executed` 或 `actions`；不能用一回合执行延迟解释。
- 第 350 回合关键前墙 40000 已降到约 35 点血，仍没有使用已购买的 `WallFixer`，也没有形成替代防线。
- 第 357 回合炮手工人 10010 死亡；第 361 回合备用工人 10012 死亡。两个工人都在死亡前承担炮位或处在机器人射程内，开拓者没有提前接管三炮。
- 第 365–386 回合再次连续 22 回合没有任何命令；基地从约 1440 点持续掉到 60 点。此时仍有 230 金币、三座炮台，失败不是单纯缺钱，而是应急调度没有接管。
- 第 279 回合才购买 2 个维修包，维修包一直留在 10012 背包，直到该工人死亡，购买与实际使用之间没有闭环。

## 命令延迟边界

回放中大量 `commands` 与 `executed` 错位，约 455 个动作可在下一帧找到对应执行，主要像本地录制的一回合延迟。第 323–329 和 365–386 的空帧同时缺少命令和动作，属于 planner/调度器空转，不能归因于延迟。

## 已采取的修正

- 维修 lease 从第 3 天启用，允许第三夜使用已购买维修包。
- 存活工人低血量时，夜间允许空闲开拓者紧急接管三座火炮，即使处于“全员夜防”阶段。
- 回放分析纳入发布验收；单元测试继续负责局部规则，不能代替完整回放。

## 测试分层决定

当前约 131 个测试文件、1426 个测试方法。暂不删除历史 Issue 测试；它们是回归证据。后续新增快速门禁，只运行协议、核心规则、经济预算、导航/维修/炮位和回放验收；任务代理、前端、工作流与历史专项按改动触发；完整套件只在候选包发布或规则实现修复时运行。

