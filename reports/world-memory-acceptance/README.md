# WorldMemory 与夜间准备：ac0b8cb 验证记录

2026-09-15。变更类别：Agent 内部架构、策略优化、模拟调试工具；依据开发规则 R01/R02/R06/R07。未改变官方规则、数值或额度。本文记录一个冻结候选的实际结果，**不是整个三任务目标验收完成，也不是内网 PASS**。

候选源码 `ac0b8cbeca815e2298543852d254d161d1723b94`；对照 `8d7c2792dd4f7e994721b7f1649bd4f2db6d9aa1`。报告保存逐文件哈希。当前 DSH 新闻账本修订未包含在该源码中，下面的结果不能证明后续修订通过。

## 功能与证据

- 每主题公开原文归档、按需读取、实际发送范围登记、带出处的字段候选；未知发布日期不能直接用首次看见的日期替代。
- 只有材料与地点已知、唯一缺项为开启时间时才能准备；夜间还要求没有机器人威胁、基地健康、其他守卫到岗，并限制准备距离与返程。堵住己方岗位时允许有条件地拆一面己方墙恢复通路。
- 假想后续路段不再把移动者的原位置当成自身障碍；同轮行动经统一仲裁。任务终止及奖励字段可 JSON 往返保存。
- 实际模型同一来源的引文标点变化不再造成重复候选。修复前真实失败记录仍保留，未用脚本答案替换真实结果。

[完整回归日志](full-suite-ac0b8cb.log)：Python 3.11.10，`python -m unittest discover -s tests -v`，745 项通过，405.498 秒。

## 真实模型长传闻

OpenRouter `deepseek/deepseek-v4.1-flash`，max（API xhigh），输出上限 16384。同一份 4308 字符公开长资料包含分散线索；经过实际 planner/router/JSON 路径。

| 试验 | 阵营 | 调用数 | 结果 | 原始证据 |
|---|---|---:|---|---|
| 7 | challenger | 3 | 地点、物品、时间及原文覆盖检查通过 | [报告](real-7/report.json)、[交互](real-7/trace.json) |
| 8 | defender | 3 | 同上 | [报告](real-8/report.json)、[交互](real-8/trace.json) |
| 9 | challenger | 3 | 同上 | [报告](real-9/report.json)、[交互](real-9/trace.json) |

这是冻结样本上的 3/3，不能外推任意长题成功率。该试验验证模型解释，不包含完整比赛结算；整局结算另见下表。模型请求耗时与参赛 HTTP 回包耗时是两项不同指标。

## 完整对局对照

[games.json](games.json)：4 个种子 × 双方 × 两版本，共 16 局，每局 1300 轮。三类认知任务共同启用，模型为只读取 prompt 的确定性替身，工具为虚拟沙盒，不是真实模型整局。种子 45289 是这次新增留出样本，其他三个为 Spec 指定样本。

| 种子 | 己方 | 对照总分 | 候选总分 | 差值 |
|---|---|---:|---:|---:|
| 1 | challenger | 1828 | 1828 | 0 |
| 1 | defender | 2165 | 2301 | +136 |
| 90317 | challenger | 2257 | 2257 | 0 |
| 90317 | defender | 2520 | 2688 | +168 |
| 90601 | challenger | 3017 | 3023 | +6 |
| 90601 | defender | 1551 | 1545 | −6 |
| 45289 | challenger | 2132 | 2132 | 0 |
| 45289 | defender | 2196 | 2196 | 0 |

8 个候选样本合计 17666 → 17970（+304）。所有 16 局基地最低 HP 均为 1500，错误回合、认知通道冲突、损坏 Agent 状态均为零。此表没有证明所有地图都增分。

90601 defender 的 −6 分已单项消融：任务奖励总和均为 926，生存分均为 550，差在击杀分 75 → 69。[关闭夜间准备](90601-defender-staging-off/report.json)仍是 1545；[仅恢复旧版未来路径估计](90601-defender-legacy-route-estimate/report.json)恢复 1551，攻击次数 101 → 107。因此差异归于路径估计修正改变行动位置和射击机会，并非夜间准备降低基地防守。保留正确的路径语义，明确这一机会成本，不为追回单个样本的 6 分恢复错误阻挡。两项消融都是报告中声明的运行时评估开关，不是默认候选。

## 实际压缩包与 HTTP

包源码为 ac0b8cb；SHA256 `1cdfb813caae587f0f6d8df13298f5cab8082f3e75c1c22cec4c61b1cd4ed825`。215329 字节，57 个归档成员、51 个文件，根目录 CoreGeek，正式入口 main3.py。候选包在本地验证目录，尚未替换仓库根部交付包。

| 实际包运行环境 | 场景 | HTTP p95 | p99 | 最大值 | 证据 |
|---|---|---:|---:|---:|---|
| Windows / Python 3.11.10 | seed 1 challenger，1300 轮 | 333.50 ms | 365.94 ms | 494.04 ms | [报告](http-ac0b8cb-windows/report.json) |
| Linux / Python 3.11.10 | seed 90317 defender，1300 轮 | 129.00 ms | 189.57 ms | 322.83 ms | [报告](http-ac0b8cb-linux/report.json) |

参赛程序来自解压包，独立 HTTP 进程运行；外部模拟器只负责观测和结算，使用脚本模型与虚拟沙盒，两局基地 HP 均为 1500、通道冲突零。达到本地 p99≤1秒、每次响应<5秒要求；未测量公司平台的模型/网络时延。整局工具中的 simulation_step_p99 包含模拟环境与预览开销，不能冒充此处的 HTTP 时延。

[Windows 入口检查](package-smoke-windows-ac0b8cb/report.json)覆盖 main3、main、日志目录不可用、manifest 缺失，共 4 类 × 5 POST；[Linux 检查](package-smoke-linux-ac0b8cb/report.json)额外覆盖实际 `bash run.sh`，5 类 × 5 POST。均通过。复现脚本为 [smoke_package.py](smoke_package.py)。此候选没有重新运行全程模型不可用的包对局，旧源码的历史降级证据不能改标为 ac0 的验证。

## 前端与剩余工作

实际浏览器选择 seed 90317、红方、长传闻场景、脚本模型并运行到 572 帧；回放看到第 231 轮“已就位，等待公开开启时间”，第 288 轮实际献祭后得到宝藏 300 分/150 金币，金币 240 → 390。控制台没有错误或警告。该项为人工浏览器检查记录，未保存可独立复验的截图文件，不代替自动化断言。

P3 新闻量价、更正/撤回及冲突账本仍在审核。父级 10 个独立反例暴露金额容差、未知恢复日、历史日期覆盖、容量淘汰及引用恢复等问题，已交回原 DSH 会话修复。待修正版审核后，重新验证相关运行路径及交付包，再同步固定 `codex/sgh` 和最终 Spec 状态。官方内网与尚未公开的规则细节继续待真实证据。
