# 四日真实新闻与宝藏解释复测

2026-09-15，源码44d31fa4736a10713320ca999f4fd816c3ca6f35，Python3.11.10。
OpenRouter deepseek/deepseek-v4.1-flash / max→xhigh，max_tokens16384，调用预算12。
通过真实brain/router/WorldAgent处理四个日期的公开合成快照；没有把未来消息传给策略或模型。
没有在完整连续对局中调用模型，不能将本报告称为完整真实模型对局或内网PASS。

9次实际请求，8次完整回答；第2天一次finish_reason=error，有限重试后恢复。
每日普通额度2、3、2、2，不借自进化任务豁免；实际总墙钟1360.156秒，报告回传usage30227 token
（包括该次失败的1698 token），不等于官方HTTP耗时或平台允许的模型时延。

逐日严格检查全部通过：新闻状态必须为本版本interpreted；D1可采、D2/D3停矿、D4恢复；
历史相对日期不因重复读取滑动。D1地点、D2精确物品、D3窗口261—290被逐步组合，
D1/D2不提前召唤、D4不把过期窗口显示为开放。所有来源与引用经真实控制器验证。
这次不再使用旧实验中“空建议也可通过”的单独news_today检查作为解析成功证据。
原始trace、检查、返回答案与安全故障原因保留；运行前后源码哈希一致。

与前批相比，本地输出预算从4096增至16384后通过了这一组复测；
单组通过不能证明每次提供方请求均成功，也不能证明任意新闻题或长篇传闻的正确率。
还需重复/不同表述样本、完整综合审核、HTTP延迟以及正式包与内网验证。
