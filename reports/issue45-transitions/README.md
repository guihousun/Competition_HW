# Issue #45：任务切换旧结果污染修复

用户在本对话明确确认#45使用刚交付b0e837b，按该版本/包内源码043918d调查；不再以缺少manifest行阻断执行。日志状态名与当前源码不同仍作为需要对应的摘要差异，未否定用户的版本确认。

本次源码 `9f0e6984f1ad9961ff3fddd4095ea81fd18ceab4`；包SHA256 `ba833ff8d1b9686c40d8036f9273f0de0850cdb58c958bb5e532941c38a685af`；官方原始main3.py入口和competition-flat-v1打包方案不变。交付提交另包含报告和产物，固定codex/sgh。

## 复现与修复

依据摘要构造真实brain/planner/router、逐轮JSON恢复的手定观测：beta有工具待返回→空题轮隔离beta结果→nanjing新题及新工具命令→beta旧结果重复出现。旧版双阵营均将重复旧结果误认为新命令的single_inflight回执，进入新题evidence和prompt，见[修复前探针](before-fix/summary.json)与完整手定观测。不是公司原始请求重放，不能据此确定公司所有失败都由此导致。

修复只拒绝可证明存在歧义的旧结果：无nonce的新任务工具回执，与当前命令发出前因旧任务失效/代际变化隔离的结果哈希相同，且保留的请求记录证明不同generation时，记录ambiguous_prior_task_output，保持当前pending及原期限等待新结果。正常成功读取相同内容不全局去重；旧回合不能回滚新任务，活动题不重复领取。

N轮发出命令，非空合法结果在N+3仍可接收，N+4过期；反复旧结果不推迟sent_round。既有空回执N+3结束等待的语义未改，未增加官方动作/调用额度，也未改写复杂Shell。

基地与全队角色消失的诊断新增our_roster_state：明确空列表记observed_empty，空动作原因记observed_empty_team_roster；缺失/非法列表与真HP=0区别记录。空列表不被臆断为死亡，更不凭空生成已不存在角色的动作。手定r236空角色输入产生空roleCommandMap，符合无法给不存在角色下指令的约束；没有声称修复基地失守原因。

类别：R01/R07内部回执关联修复与工程诊断；官方接口§1.1/§1.7/§2、任务书§五，[实施Spec](../../specs/ISSUE_45_BUILD_AND_TASK_TRANSITIONS.md)。三火炮、升级、前六墙及模拟器结算均未修改。

## 验证

- 新增8项任务切换测试通过，含双阵营、JSON恢复、旧回合、题目持续/消失、r7或末轮r8正常新结果、r9过期、合法相同输出跨任务。测试见[独立回归](../../tests/test_issue45_task_transitions.py)。另有角色空/缺失/非法列表的独立诊断回归。
- 冻结源码Linux/Python3.12.3全量1298项，真实退出0；[回执](tests-receipt.json)与原始tests.log哈希匹配。
- Windows/Python3.11.10真实压缩包独立HTTP进程200轮通过，最大311.1ms；[报告](http-report.json)。模型为脚本回复，这不是真实LLM成功率或内网PASS。

## 仍需实战核对

无nonce回执没有携带真正请求ID：从未见过的迟到输出、有界历史已经淘汰的旧证据仍可能无法区分；内容碰巧与旧隔离输出相同的合法新结果也会先被标记歧义等待，可能超时。这是明确的保守取舍，不声称全面解决工具回执关联。全面输出nonce包装另需验证Shell语义、退出码和2000字符预算。

请从已有记录补r47–53的原始phaseTask（区分缺失/null/空字符串）、实际命令/回执、任务点状态、角色位置和动作结果，以确认真实序列。r234–237补完整己方角色/HP、robot及官方终局字段，用来定位防线崩溃，不能只凭“基地HP=无”推导伤害数值。版本已确认，不要求再提供SHA才能继续。

#45实际列了6个不同任务、4次提交与2次未提交；提交不等于明确成功，425积分邻近增量也不能独自证明全是任务收益。来源Issue不关闭。本次修复、已有保护通过、基地败因未定三项分别记录。

拉取codex/sgh后上传根CoreGeek.tar.gz。复测需对应本包源码/包SHA；旧报告保持原版本证据，不覆盖成新版官方通过。
