# 世界记忆接入：验证与保留的失败

范围：本地 Agent 与模拟器验证，不是内网或官方 PASS。目标尚未完成。

## 冻结版本 750ca5a

- `full-suite-750ca5a.log`：Python 3.11.10，728 项测试通过，271.426 秒。
- `initial-real-1` / `2` / `3`：OpenRouter `deepseek/deepseek-v4.1-flash`，max/xhigh，输出上限 16384；同一 4308 字符公开长传闻，双方交替。三次分别调用 2/3/2 次，完整解题 **0/3**。全部报告与原始 prompt/回答已保留。
- 真实回复揭示 inspect 与 hypothesis 同时出现、重复/内嵌 draft、超过 2000 字符的读取请求。严格旧解析器拒绝这些回复，未形成危险行动。不能把“守住校验边界”算作“解题成功”。

## 后续修复与当前缺口

后续代码只在值完全一致时归一化草稿的几种位置；不同草稿继续拒绝。实际读取仍最多 2000 字符，超长读取意图拆段而非扩大内存权限。进一步验证排除的物品不能混入更大的献祭集合，排除时间窗不能与允许时间重叠。

新增可选 `world-long` 场景，在首日公开消息中加入长背景与尾部排除条件，第二日公布物品，第三日公布原有 261—290 窗口。默认场景与原有规则数值保持不变。脚本模型只处理已发出的 prompt、检索片段和草稿，不读取模拟器私有条件。

新增 `test_long_world_simulator` 的 90317 蓝方完成实际购买、移动和召唤；红方正确整理条件、备齐物品，但未在窗口内到达，当前该回归仍失败，**不作为可交付绿灯**。路径诊断显示第二夜末红方到祭坛需要 30 次移动，261 轮出发也最早 291 轮才能召唤，超出窗口。不能靠缩短移动、延长窗口或从未来消息提前获知日期消除这条失败。

`rejected-post-staging.patch` 是已撤回的试验：尝试在安静夜晚保持同一炮台操控距离并改善站位。独立边界测试通过，但原红方场景仍未成功，因此没有保留在运行代码中。

仍需：修复后的真实模型重复试验；定位与改善完整行动调度，并保留有证据的不可达/机会成本判断；新版本全套回归、留出地图/换边、实际参赛包验证。用户的 8084 服务与公开 tar.gz 继续使用此前已交付版本。
