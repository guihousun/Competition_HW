# LLM 通道补缺审核

2026-09-12。类别：官方接口修复（R01/R07）/ 本地适配 / UI工具。
实现基线 fef12156c9057c1585284da817f9197763a174ec，隔离工作树 codex/gap-llm。

- 完整回归：222 项通过（74.7 秒）；最后的前端回执文案调整另跑 viewer/replay 回归通过。
- 根 POST 由 respond 保留 prompt / executeCmd，正式路径不直接请求 DeepSeek。
- 请求去重、等待不推进、预算耗尽、提供方错误脱敏、存档恢复、延后消费及字段透传均有回归。
- 真实 API 连通探针一次；完整合成任务验证一次（1 调用，160 token，100%本地通过率）；
  网页再实际跑通同一类型演示任务一次。无自动重试，无真实公司数据上传。
- 密钥未出现在源码、网页、测试和报告；仅保存在仓库外、当前 Windows 用户加密文件。
- 集成前核对原工作目录每个待修改文件的原始哈希，未覆盖其他并发修改。旧文件备份留在本机忽略目录。
- 最终相关文件哈希在 llm-source-hashes.json；LLM_VALIDATION.json 保留真实检查当时的源码快照，
  不能把其旧哈希改写成后续文件哈希。浏览器核验回答已返回且任务奖励累计一次。

局限：官方沙盒未接入，executeCmd 返回明确错误而不执行宿主机命令；真实官方任务判题、固定
刷新点坐标、完整双队规则差异未解决。无公司内网 PASS，不自动发布/合并比赛行为变更。
