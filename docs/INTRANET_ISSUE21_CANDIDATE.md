# #21/#22 任务流水线候选测试说明

本次已经修改运行代码，并重新生成参赛包。固定测试分支仍为 `codex/sgh`。

- 包内源码：`66a4d9a6c704aa0425d995cff29ab812ba6cc98e`。
- 文件：仓库根目录 `CoreGeek.tar.gz`。
- SHA256：`676e42ab26837599479944f1dcadb6187de07b01f7b1376e34addbc3038165c7`。
- 交付提交晚于源码提交，`CoreGeek.manifest.json` 的commit代表包内源码，不代表二进制交付提交。

## 已实现

1. phaseTask明确引用一个md/txt文件时，确认任务后首个可用沙盒回合直接定位并读取。优先明确绝对路径，否则在有界候选目录搜索；输出绝对路径、题文及同目录API_DOCS/spec/check等材料。重名不任意选择，未找到/权限错误/截断均报告。
2. 上下文加入真实接受回合、截止回合、剩余轮数；保留提交缓冲，最后一个有效回合只提交现成答案，不再启动无法返回的模型/命令调用。未知时限仍为未知。
3. Agent、router、存档和虚拟沙盒统一命令上限2000字符（内部工程限制，不是官方规则）。2001字符等超限计划会得到明确反馈并允许重新规划，不再被下游拒绝后停住。
4. 更充分地直接提供已收到的中等长度文档和最新工具回执，保留原文索引、哈希与截断标记；可选的简短模型工作摘要搭载普通回复，不额外调用，也不能代替真实证据。
5. 提示词补充API文档读取、工程spec/修改/check闭环、批量操作和得出答案及时提交。继续使用原有run/answer到executeCmd/submitAnswer映射，不引入第二套官方接口。
6. 模拟器同步探测命令的确定性解释，并新增可选的嵌套目录15轮任务。时限15、奖励80+80仅为该合成场景元数据；默认场景原有数值和官方规则未改。旧脚本模型适配新的原文上下文标签。

## 公司测试

```powershell
git switch codex/sgh
git pull --ff-only
Get-FileHash .\CoreGeek.tar.gz -Algorithm SHA256
```

直接上传根目录的 `CoreGeek.tar.gz`，不要再上传旧Demo目录里的官方原始示例。

优先复测有任务文件的API题与工程题，双方换位。日志中预期看到：
- 首次确认题目后出现以 `# task-workspace/1` 开头的executeCmd；
- 下一轮有题文/路径或明确探测失败，不先空跑一次相对路径cat；
- 模型收到剩余轮数、资料和退出码；
- 有答案后发出submitAnswer/taskAnswer，并检查真实判题回执。

回复#21或#22，附包内源码SHA、地图、任务数/提交数/通过数、失败episode和对应请求/响应/判题回执。仍可直接提交现有日志，无需先自行诊断。

## 已验证与限制

详细本地结果见 [验证报告](../reports/issue-21-implementation/README.md)。脚本化模型、隔离Linux工具fixture和实际tar.gz HTTP检查用于验证工程链；不代表真实LLM一定能在5轮解完未知题，也不代表比赛胜率提升。

这批交付覆盖#21 P0主要缺口与#22上下文/批量操作建议，并补充API/工程的执行链和模拟fixture。完整泛化API/工程专用求解器、额外SOP候选晋升、真实模型重复效果评估、官方v7.0隔离复现及既有防御升级任务仍未全量完成。后续继续基于真实反馈分阶段改进，不将来源Issue标为完成或自动关闭。
