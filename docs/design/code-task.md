# 代码迭代任务留痕

> 重写工程（基于 `example/` 的形态）的**逐步记录**。编码规则见 `CLAUDE.md` §编码规则。
> 每节三段：**目标 / 产出**（含验证结果）**/ 仍生效的已知不确定性**。**仍未了结的事**
> （未完成的下一步、以及跨步重复的已知不确定性）统一收在文末「**当前仍悬着的事**」。
>
> ⚠️ **压缩过三次**。本次（第 23 步之后，2026-09-14）**换了口径**：只留**目的与结果** ——
> 决策理由、已失效的「不做什么」、粘贴的逐条命令输出全部删掉；
> **仍生效的已知不确定性一条没删**（已经兑现或被后续步骤推翻的已删，正文里就地写明）。
> - 本次压缩前的全文：`git show 75ceed5:docs/design/code-task.md`
> - 上一次（第 20 步之后，旧口径）：`git show 15c43e2:docs/design/code-task.md`
> - 更早一次（第 12 步之后）：`git show 53dec12:docs/design/code-task.md`

---

## 第 1 步：能跑的空框架（HTTP 进出 + 空动作）

**目标** 把"平台 ↔ 我们"这条链路焊死：能收请求、能回合法响应，**不含任何策略**。
入口形态照抄 `example/CoreGeek/CoreGeek/`（**双层嵌套目录**）。

**产出** `main3.py`（读端口 → `chdir` → `src/` 进 `sys.path` → 起服务）/ `run.sh` /
`pyproject.toml`（`dependencies = []`）/ `web/server.py` / `app.py` / `game/planner.py`。
验证：`curl` 打样例 → 200 + 合法三字段、3ms（红线 5000ms）；
空 body / 坏 JSON / `GET /`（501，判题器只发 POST）/ 样例四种边界都不抛异常。
`run.sh` 加 `PYTHONUTF8=1` —— Windows 控制台默认 GBK，中文与 `→` 会乱码，
而日志是本地唯一的观测手段（判题机是 Linux，本就 UTF-8）。

---

## 第 2 步：看见局面，第一次发出真指令（解析 + 移动）

**目标** 把"解析 → 决策 → 编码 → 响应"整条管线跑通。行为刻意简单：**所有角色朝我方基地走一格**。

**产出** `protocol/model.py`（payload → `Turn`，**容错解析**：字段缺失/类型不对就丢掉那一条或退化，
绝不抛异常）+ `commands.py`（唯一知道线上指令形状的地方，第 3 步并进 `actions.py` 后删除）。
只解析这一步真会用到的字段；阻挡格 = 我方单位 + 敌方可见单位 + 中立元素 + 机器人，
外加**基地 2×2 的另外三格**（`pos` 是左上角）—— 只标一格会让角色**一头撞进基地里白扔一个回合**。

验证：三个角色各一条 `move`，**建筑没有出现在指令里**；7 条边界用例全过，
最值钱的两条是"贴着基地 ⇒ `{}`"与"两个工人相邻且目标同向 ⇒ 各自认领不同格"。
**踩到的坑**：`collections.abc.AbstractSet` **在 Python 3.13 已被移除** ⇒ `ImportError`，
进程直接起不来，而局部调用走不到那行 import ⇒ **只有起真服务才会暴露**
（这就是"每一步都必须真的把服务跑起来打一次"的来源）。

---

## 重构：按层归位（第 2 步之后，无行为变化）

**目标** 5 个模块平铺，看不出"谁能依赖谁" —— 按层归位。**只搬位置、不改行为。**

**产出** `app.py` / `web/server.py` / `protocol/{model,commands}.py` / `game/{grid,world,planner}.py`。
`grid` 从 `model.py` 里拆出来（`Pos`/方向/距离是**几何基元**，建墙、寻路、算矿区距离都要用）。
依赖方向：`app → web / protocol / game`、`protocol → game`、
`game/planner → protocol/commands`（唯一一条"由内往外"）。

验证：`curl` 响应与重构前**逐字节一致**；坏 JSON 退化正常；第 2 步的 7 条边界用例全过。

---

## 第 3 步：Action 基类（创建即校验）+ 两个角色类

**目标** 把 §4.4 表**最右列**（可用角色）变成可执行的东西，
让**角色无权执行的动作根本构造不出来**（§8 没说"角色没权限"算异常还是算执行失败，口径不明就让它发不出去）。

**产出** `protocol/actions.py`（`commands.py` 并入后**删除** —— 留着就是两处写线上格式）
+ `game/roles.py`（`Pioneer`/`Worker`，**只认角色、建筑返回 `None`**）
+ `tests/test_actions.py`（**`tests/` 首次出现**，8 条）。

验证：8 条全过（含"测试内部定义 `roles = WORKER` 的子类"来钉住**校验机制本身**）；
逐字节回归 213 字节、三个 `move` 坐标不变 ⇒ **闸门对合法指令零副作用**。
⚠️ 如实记下当时的局限：`Move` 对全部角色合法 ⇒ 这一步的校验在真实运行中**拦不到任何东西**，
第一次真正拦住东西要等 `build`/`collect`（第 7/8 步已兑现）。

---

## 第 4 步：解析地图中立元素，工人走向石矿（跑通全流程）

**目标** 第 2 步发的指令**不读地图**，管线只通了一半。让工人奔一个真实存在于地图上的目标（石矿）。

**产出** `Turn.mines`（矿点 → 矿种；只有 `protocol/` 知道字段名）
+ `model._MINE_KINDS`/`_mines` + `planner` 改成"工人各自朝最近的石矿走一格"。
**走位与站位是同一件事**：矿格本身在 `blocked` 里，`step_toward` 走到**贴着矿的那一格**就自然返回
`None`，而那正好是 `collect` 的站位 ⇒ 不用写两段逻辑。`claimed` 防"两个人冲同一个空格"；
**不认领矿点**（两个工人挤同一座矿的不同邻格**都能采**）。
⚠️ 字段名是 **`neutralType`**，不是 `zoneType`；同一张表里的小贩/武器商店/任务点**不是矿但一样挡路**。

验证：单测 12 条。最值钱的是**收敛用例**（回合串起来跑 40 轮，最终 `dist == 1` 且此后不再产生动作）
—— **单帧"目标格算得对"证明不了走得过去、停得下来**，走歪、绕圈、贴住后抖动只有串起来跑才看得见。
起真服务 + 手工核对：`10010` **"什么都没做"正是正确行为**。

---

## 第 5 步：寻路换成 BFS 最短路

**目标** `step_toward` 从"贪心挪一格"换成 **BFS**：贪心只挑"确实更近"的格子，
遇凹形障碍会**停在原地不动**（不是走错，是**卡死**）。

**产出** 四条契约：**终点是"贴着 `goal` 的一格"而不是 `goal` 本身**（`goal` 通常挡路，
BFS 只在可通行格上展开 ⇒ 不需要特判）；**返回从起点迈出的第一步**；
`pos.dist(goal) <= 1` 提前返回 `None`（否则会被带到**另一个**贴着 `goal` 的格子白扔一条指令）；
`Turn.size` 缺失给 -1 ⇒ **单位不动，这是故意的**。

验证：单测 15 条，含"绕得过且**恰好 5 步**"（用步数钉最短）与"界外一步都不走"。
反向验证：把旧贪心抄回来 ⇒ 第二步就 `None`（卡死）；去掉边界检查 ⇒ 走出 `Pos(x=-1, y=4)`。

**仍生效的已知不确定性**
- **地图边界不在任务书 L85 的阻挡清单里**（那一列只有建筑/角色/机器人/中立单位/任务点/矿区）。
  贪心几乎撞不到，**BFS 会绕到图外去** ⇒ 取保守解：界外一律不走。
  越界算"指令非法"还是"执行失败"**文档没写 —— 不走一定安全**。
- **并列最短时取哪条取决于 `STEPS` 的探索顺序**：样例里 `(9,17)`/`(10,17)`/`(11,17)` 都是等长首步，
  测试钉的只是其中一条。**等长不影响正确性，只影响逐字节回归。**

---

## 第 6 步：把地图建成一个格子矩阵（`Turn.map`）

**目标** `Turn.blocked` 是扁平集合，看不出那一格是什么；且 `model.load` 对同一份 `mapInfo`
**解析了两遍**（两份真相迟早对不上，而症状是"以为能走、其实撞墙"）。改成每格只装一个**类别**。

**产出** `Map(size, entries)`：`.size` / `.cells`（`[y][x]`，**y 向上**）/ `.blocked` / `.stones` / `.render()`。
类别**原样取自 payload**，不查白名单 —— **未知类别照旧挡路**（判错方向只会多挡、不会放行）。
**不变量：非空即挡路**（任务书 L85）。构造收**稀疏** `entries`，稠密矩阵由 `Map` 自己铺。
`render()` 的字符表**是有损的**（机器人不分体型），`cells` 才是真相。
`Turn` 从 6 个字段瘦到 3 个（`station` 直接删，它只用来把基地展成 2×2）；
`model.py` 四趟分头扫 → **一趟铺 `entries`**（写入顺序即覆盖顺序：中立元素先写、单位后写）；
`grid.py` 一行未动。**顺带修掉一个真 bug**：基地 2×2 而 `pos` 只给左上角（接口文档明说"**双方**"），
旧实现只展开了我方基地 ⇒ 敌方基地只挡 1 格（`blocked` 32 → 35 格，方向是**多挡**）。

验证：单测 19 条。最关键的是 `test_blocking_is_exactly_non_empty`：**独立按任务书 L85 从 payload
重算一遍**阻挡集合（四路来源 + 基地 2×2），**完全不走 `Map` 的代码**，再与 `map.blocked` 比对 ——
它防的是"迁移中漏掉某一类挡路物"。**肉眼对一次图是唯一能发现 y 翻转的办法**
（图上一切"看着像张地图"，单测和实现可能一起错）。真服务响应与第 5 步逐字节一致。

---

## 第 7 步：工人建武器（`build` 落地，优先建在基地后方）

**目标** 第一条经济动作：**工人先建武器**，位置优先放在基地后方，让基地的 2×2 实体挡在武器与机器人之间。
顺序：加特林 → 电磁狙击炮 → 火箭发射台；上限取"武器数 < 总角色数"。
**顺带把第 3 步欠的账还上**：`build` 是**闸门第一次真的挡住东西**。

**产出** `Build`（`roles = WORKER`）；`grid.weapon_cells(base)` / `back_weapon_cells(base, width)`；
基地 2×2 的展开从 `model` 搬进 `Map.__init__`；`Turn.gold` + `is_day`
（`round_no` 缺失给 -1 ⇒ 判成夜里 ⇒ 不建造）。
`planner` 每回合按优先级择一：① 建武器（已在邻格就发 `build`，否则走一格 —— **复用 `step_toward`，
天然停在邻格**，即"站位即建造位"）；② 否则朝最近石矿走一格。
**金币按递减预算扣**，不写成 `gold >= 25 * 待建数`（那样金币只剩 25 时会一座都不建）。
`sites`（已认领建造格）与 `claimed`（本回合落脚格）**分成两个集合**。新增 `_emit` 统一收口越权。

验证：单测 31 条。合成白天开局串着打 ⇒ **恰好 3 座**、顺序 `gatling/railgun/rocket`、
**全部在 `x=9`**、75 金归零。**踩到的坑**：合成局面上 `build` 一条没发 —— 实际是端口上挂着
一个上一轮遗留的服务进程（Windows 允许 `SO_REUSEADDR` 并存），请求打到了**旧代码**上
⇒ **起服务前先确认端口上只有一个监听者**。

**仍生效的已知不确定性**
1. **可建造区公式来自图片**（`docs/pic/build_map.png`），任务书正文没有。算错 ⇒ 建造失败；
   失败大概率算"执行失败"不计异常，但**未确认**。
2. **武器 `name` 的取值只对 `wall` 有实证**，武器名是从 `roleType` 表推的。
3. **机器人刷新方向只来自示意图**，正文未写。判据封装在 `_front_back` 一处，首场后按实测翻转。
4. **武器上限口径原文冲突**（表格"每种 ≤3"合计 9 座 vs 补充说明"全局最多 3 座"）。取保守的全局 ≤3。

---

## 第 8 步：工人采石、砌围墙（`collect` 落地 + 回合预算）

**目标** 补完策略指导第一天主线的后半句（最近的石矿、必须在晚上前把墙砌好、墙面向机器人进攻的方向）。
用户定的三条：**攒多少石头不写死**（每回合实时算，留 5 回合容错）；
**背离机器人那一侧的中间留缺口**；**夜里回基地走到武器旁边待命**。

**产出** `grid.wall_cells(base, width)`（**16 格，按建造优先级排**：正面列 → 顶行 → 底行 → 背面两角）；
`Collect`（`roles = WORKER`，`targetPos` 是**矿石的坐标**而不是站位）；
`BaseRole` 收窄成带 `stone`（`backpack` 是物品名数组、重复即计数，**不读 `backPackCapability`**）；
`within = (roundNo-1) % 130 + 1` + `day_rounds_left`（**含本回合**）；
`planner._stones_to_mine`（`d_mine + d_wall + 2s − 1 + 3k ≤ 白天还剩的回合 − TIME_MARGIN(5)`，
每多采一块**净**花 3 回合，再与"还差几格墙"取小。距离用**切比雪夫**）；
`_to_weapon`（夜里按距离取最近的一座**没被本回合别人认领**的武器，贴着就停）。

验证：单测 45 条（含**开拓者构造 `Collect` 抛 `PermissionError`**）。合成白天端到端：
回合 4/6/7 三座武器 → 采石 30 次 → 16 格墙 → 回合 66 空指令（环满了，手里还剩 3 块留给第二天）。

**踩到的坑（这一步最值钱的一条）：两个工人 26 回合来回踱步，一格墙都没砌**
根因是一行过滤：`_ring` 把"工人脚下那一格"也当挡路的格划掉了。`model._entries` 会把
`teamOur.roles` **写进网格**，所以"谁站在哪"也进 `blocked`；工人恰好站在一个**还没砌的墙格**上时，
那一格就从候选表里消失 ⇒ 另一个工人的 `free[0]` 整体后移、掉头去砌更靠后的墙；
等前一个工人一挪窝目标又变回来 —— **两人对着改目标，永远在原地互指**。
修法：**自己人站的那一格不算挡路**（只是路过，下一回合就走）；已砌的墙/中立元素/机器人照旧排除。
教训两条：① **手搓 `Map` 的合成局面里角色不在 `blocked`**，只有 `model._entries` 会把他们写进网格
—— 新用例第一版就是这么写的，于是它**连旧代码都放过去了**；
② 这类"多人互指"的死循环单帧断言看不见，得把回合串起来跑，**并且反向验证一次**。

**仍生效的已知不确定性**
1. **`collect` 没有实证报文**（`docs/response.txt` 里没有），形状是从接口文档推的。
   **它是不是真的没有昼夜限制，也没实测过。**
2. **回合预算里的距离是切比雪夫**，绕障时低估 —— 靠 5 回合余量吸收，**未实测**。
3. **夜里机器人打不打野外的工人** —— 决定缺口会不会变成"机器人进环"。留缺口是用户既定的取舍：
   正面完整、背面开口，而武器列（`x=9`）与基地紧贴缺口内侧。（第 17 步把缺口从 4 格改成整列 6 格。）

---

## 第 9 步：每回合复盘日志（先地图、后动作）

**目标** 判题器是黑盒、**不给任何别的视角**：出了事故手边只有一条 `move`，看不出当时场上是什么局面。

**产出** `app._log(turn, cmds)`（放"红线所在"的文件，日志的开关与降级必须和报文格式同一处管）
+ `protocol/actions.describe(cmds)`（字段名只有 `protocol` 知道，与 `to_wire` 一写一读）
+ 一条 `assertLogs` 用例钉住**顺序与内容**。
**关键设计**：`_log` 写在 **`try` 里面** —— 日志代码再不起眼也是代码，逃出去的话
`web/server.py` 的 `do_POST` **不接异常** ⇒ 连接直接断掉 = 判题器那边"响应超时"（红线第一条）。
**退化成空指令只是丢一个回合，断连接是整个资格。**

验证：单测 46 条；新用例按**逐字**断言抬头、行数、动作串，钉住"先地图后动作"的顺序。
起真服务顺手对图：`w`/`#`/`p` 的位置与 `docs/request.txt` 一致 ⇒ **行序没翻反**。

**仍生效的已知不确定性**
- **stdout 会不会把管道写满** —— Windows 管道缓冲 64KB，判题器若**不读**子进程 stdout
  就会阻塞在 write 上 ⇒ 响应超时 ⇒ 直通红线。**本地判题环境没跑过，这是唯一没被实测过的实质风险**
  （历次字节数变化见 `CLAUDE.md` 硬约束 5 与文末总表）。

---

## 第 10 步：夜里操炮防守（`attack` 落地，三个角色各认领一座武器）

**目标** 第 8 步把工人夜里"走到武器旁边待命"做完了，但**待命本身不产生任何效果**。
用户要求"**所有角色**（上限三个）回来操作武器"。两条策略：**开火优先级 = 射程内血最少的机器人**
（并列时打近的）；**机器人还没走进射程时站在炮位待命**（不发指令，空指令合法）。

**产出** `world.py` 新增 `Weapon` / `Robot` 两个 `NamedTuple`，`Turn` 多两栏（**带默认值 `()`**）；
`map.py` 删掉 `Map.weapons` 与 `WEAPON_KINDS`（它没有 id/射程/冷却，留着就是两份真相）；
`model._weapons` / `_robots` / `_destroyed`（**没有 `teamOur.weapons` 这个 key**，
武器和角色混在同一张表里靠 `roleType` 分流）；`Attack`（`roles = ALL`），`describe` 补 `controllerId`；
`planner`：昼夜分岔、`_to_weapon` 改名 `_defend`、新增 `_fire`、`_emit` 开 keyword-only 的 `key`。
`tests` 46 → 66 条，新增 `_records` / `_terrain` 两个夹具（**记录是唯一真相，地形由它推**）。

**四个坑（都是"发得出、判题器说不"那种）**：① **`attack` 的 key 是武器 id**，操控角色在
`controllerId` 里 —— 与其它动作**反着来**；② **`round_no` 缺失时一条都不发**（`is_day` 把缺失的
回合号判成夜里，那个降级方向对"白天不许建造"安全，对 `attack` 就反了）；
③ **先按射程过滤、再取血最少的**（顺序写反就成了"拿全场最弱但打不着的那个当目标"）；
④ **已毁的炮 / 阵亡的角色不发指令**（`health == 0` ⇒ 丢；用 `== 0` 而不是 `<= 0`，
因为字段缺失是 -1）。`attackRange` / `cooldown` 的缺省值方向**故意相反**：
射程 -1 ⇒ 够不着 ⇒ 不开火；冷却 -1 ⇒ 照打。

验证：单测 66 条。**反向验证 4 条**（优先级换序 / 射程 `<=` 改 `<` / 删掉冷却判断 / `_emit` 的 key
换回角色 id）。**逐字节回归：样例这一次必须变（变了才是对的）** —— 开拓者现在也发指令，2 条变 3 条。
**合成夜战端到端**：三件事一次看全 —— **key 是武器 id**（`20000`/`20001`/`20002`）、
`controllerId` 是角色 id、三座炮**集火打同一只**。

**仍生效的已知不确定性**
1. **`cooldown` 字段样例里没有**（三座炮都缺）。真机 payload 若也没有，"冷却期不开火"永远不生效。
   接口文档与官方 demo 两处独立确认了这个字段，只能按"该有"来写。
2. **`attackRange` 是 3/6/10 还是样例的 4/7/INT_MAX** —— 以 payload 为准。
3. **L1 加特林/火箭只传 1 个目标是否合法** —— 文档写"数量与等级数相同"，自洽；
   "少传算不算非法"没写。
4. **白天发 `attack` 到底算不算异常** —— 文档只说"非法/不可用"，没列进 §8 的异常清单。我们按"算"来防。
5. **已知代价**：**角色数 < 武器数时火力打折**（开拓者被任务钉住 ⇒ 夜里三座炮只有两人操，
   打折 1/3）。由任务积分覆盖，首场赛后调。

---

## 第 11 步：开拓者做任务（`acceptTask` → 问判题器 LLM → `submitAnswer`）

**目标** 第 1~10 步白天只有工人在动。任务书 §5.3：开拓者到**己方任务点**周围一格内 `acceptTask`，
任务原文出现在下一回合 payload 顶层的 `phaseTask`，答案通过 `submitAnswer` 交回。
收益量级完全不同：任务是 `积分奖励 + 5 × 标准回合数 / (实际完成回合 − 接取回合)`
（**越快交完奖励越高**，以乘法计；样例两个点各 50 分 + 30 金），而杀一台机器人只有 1~10 分。
唯一确定存在的智能通道就是响应里那个至今恒为空串的 `prompt` 字段 —— 发给判题器的 LLM，
下一回合在 `llmResp` 里回来。

用户定的四条：范围 = 接任务 + LLM 求解闭环（**不做沙盒**）；**不发 `executeCmd`**；
**无状态**；提示词 = 让题目自己说话。

**产出** `Turn` 加三栏，全带默认值（既有 66 条用例零改动）：`task_points`（本回合**可接取**的己方任务点）
/ `phase_task`（任务**原文**，**非空 = 手上有任务**，这是"任务进行中"的唯一判据）/ `llm_resp`（**要提交的答案原文**）。
`model._tasks` / `_text`，`_pos` 加 `key` 参数（任务点是**唯一**叫 `taskPosition` 的）。
**`playerTasks` 是任务点的权威来源**（只含我方 2 个点、只认**锚点格**就够 ⇒ 不必去 `zones` 里认
`challengerTaskPoint*`、也不必读 `teamOur.type`）。
字段缺失的降级方向**故意不是"少做"**（与 `_gold`/`_size`/`_stone` 相反）：误接一个冷却中的点只是
**指令执行失败**（不计异常），而误判成"永远接不了"会让整条任务线**静默作废** ⇒ `isValid` **明确为 `False`**
才排除，`coldDownRounds` 缺失给 -1、按 `<= 0` 也算就绪。
`AcceptTask` / `SubmitAnswer`（**都仅开拓者**，**报文里都没有 `targetPos`**，领哪个点由**站位**决定）。
`planner.plan()` 的循环分四支，**"钉死"那一支放在最前面**（全局最早的判据，与昼夜无关；判据用
**载荷事实** `phase_task` 而不是自己记的"谁领了任务"）；新增 `_take_task` / `_answer_task` / `prompt_for` + `TASK_PROMPT`。
`app.py`：`prompt_for(turn)` 接进响应顶层，`_log` 在任务存续期间多打一行。

验证：单测 94 条，**反向验证 7 条**，最值钱的两条：把"钉死"挪到夜间分支**之后** ⇒ 开拓者真的被拉去
开炮、**任务当场作废**；`describe` 改回硬读 `cmd["targetPos"][0]` ⇒ **两条用例一起炸**（含端到端那条）。
**合成端到端 6 个局面**（真 HTTP）：接任务时报文**没有 `targetPos`**；`phaseTask` 非空时开拓者
一条指令都没有且 `prompt` = 模板 + 任务原文；有 `llmResp` 时发 `submitAnswer`；
同一份"有 `llmResp`"的 payload 连打三次都是 `submitAnswer` —— **无状态设计的预期行为，不是 bug**。

**仍生效的已知不确定性**
1. **答案格式完全未知** —— 本步最大的单点风险。通过率按"字段个数"算 ⇒ 是多字段结构化答案，
   但字段名、分隔符、是否要 JSON，文档一个字都没有。`PROMPT` 的措辞是**唯一的杠杆**。
2. **`llmResp` 的生命周期未知**（"没发 prompt 时是空串"还是"保留上一次"）。设计对两种都成立，但没实证。
3. **跨任务时会多交一次上一个任务的答案**（`llmResp` 若仍是旧的），一回合后自愈。
   代价一个回合 + 一次 errorCode 2。
4. **`timeoutRounds` 样例里没有** ⇒ 不做任何"值不值得接"的时间预算。
5. **白天发 `acceptTask` 是否合法**（§4.4 那格没写昼夜限制）。我们只白天接；
   若实测证明夜里也能接，是另一条策略（早接早完成，速度奖励是**乘法**的）。
6. **"周围一格"是否含对角**没写。按切比雪夫（含对角）—— 与全局的距离度量一致。

---

## 第 12 步：地图可视化（坐标标尺 + 关键事实摘要 + 图例）

**目标** 用户反馈"地图可视化效果比较差"。最大痛点是**没法定位** —— 而策略里**一切都是坐标**
（任务点、建造环、炮位），复盘时数格子是每分钟都在付的成本。用户拍板：**紧凑 + 坐标标尺**；
**摘要 + 每回合图例**。

**产出** `game/map.py`：`render()` 改成返回**整个可打印块**（标尺放这里而不是让 `app._log` 自己拼：
列宽与缩进由 `size` 推导，那是地图自己的知识）+ `_NAMES` + `_legend()` + `LEGEND`。
`game/world.py`：`Turn.summary()` + `_listed()` + 两个常量（**领域对象自己格式化自己**）。
`app.py`：`_log` 把三块拼成**一条**记录，顺序 = 摘要 → 图例 → 图
（摘要第一行 = 回合号，`logging` 的时间戳前缀只加在**第一条物理行**上，按时间翻日志时正是这一行要定位）。
`tests` 94 → 102 条。

**图例由 `_NAMES` 生成，不是手写**（手写一份 + 一条字符串断言 = 两份真相，写错的症状是**复盘时看错阵营**）。
⚠️ **四个任务点按「阵营」标注，不是「我方/敌方」**：`1`/`2` 来自 `challengerTaskPoint*`、
`3`/`4` 来自 `defenderTaskPoint*`，两队**同时存在**；样例里 `teamOur.type == "challenger"`
两套恰好重合 ⇒ **写错也测不出来**。我方可接的那两个点是 `Turn.task_points`，所以摘要里的标签写
「**可接任务点**」。摘要的三条格式化约定（**缺失 ≠ 0**、**冷却只在 `> 0` 时出现**、**每个列表都有上界**）见 `CLAUDE.md`。

验证：单测 102 条，**反向验证 6 条**。**逐字节回归**：这一步只动日志，响应一个字节都不该变。
**实测字节**：摘要 294 B / 3 行 + 图例 305 B / 3 行 + 地图 1661 B / 34 行 = **2262 B / 40 行**
（改前 1374 B / 33 行）。⚠️ **字节数一律以实测为准**：估算两次都偏低（漏掉 `│` 是 3 字节）。

**仍生效的已知不确定性**
- **`│` 的显示宽度**：在 UTF-8 终端占 1 列，但在 cp936 终端是 2 列，那时标尺会差一列。
- **宽度按字符数算而不是显示列数** —— 前提是 **`_char` 恒返回 1 字符**且图里没有中文。
  这个不变量已写进 `_char` 的 docstring，但**没有断言守着**（"一个字符只能代表一样东西"那条守的是
  "不重复"，不是"长度为 1"）。

---

## 第 13 步：武器落点改成"后列 1 格 + 前排两角"

**目标** 把三座武器从"全挤在基地后方那一列"改成用户拍板的阵形 —— **一台在基地后方、两台在基地前方的两角**。
理由不是审美，是**够得着**：任务书 **L292**（武器/基地/围墙升级券均需在**目标建筑周围一格内**使用并指定
目标位置）、**L314**（围墙修复包要站在待修复围墙一格范围内），而 `attack` 也要求角色站在炮旁。

**产出** `grid.py`：抽出 **`_front_back(base, width) -> (d, far, near)`**
（那个判据原本在 `wall_cells` 与 `back_weapon_cells` 里各写了一遍，而 `CLAUDE.md` 早就写成"只此一处"
—— **本来就是假的**；加第三个使用者之前先抽出来，让那句话变成真的）；
**删 `back_weapon_cells`、换成 `weapon_sites(base, width)`**（恰好 3 格，顺序即建造顺序）。
`planner.py`：`WEAPON_ORDER` 改名 **`WEAPONS_BY_SITE = ("rocket", "gatling", "railgun")`**
（新名字直说"下标与 `weapon_sites()` 一一对应"；**配对按射程**）；
**`_slots` 改成"先按种类配对、再滤"**（旧写法会在"后列那格被占"时把 `zip` 整体前移、
**每个种类都挪到别人家的落点上**）。

验证：单测 103 条。`WallRingTest` 四条**一条都没改**就全绿 —— 它们是 `_front_back` 那次重构的
等价性证明。**反向验证 4 条**：`_front_back` 判据写成 `>=` ⇒ 8 条挂；`weapon_sites` 后列挪到围墙环上
⇒ 5 条挂；`WEAPONS_BY_SITE` 换回旧顺序 ⇒ 2 条挂；`_slots` 改回旧写法 ⇒ **恰好 1 条挂**，
而报文本身完全合法（**每个种类都挪到了别人家的落点上**）。
⚠️ `test_every_site_touches_the_base` 对"方向判反"**不敏感**（武器环 12 格全都贴着基地），
它的价值在"落点必须落在武器环上"这一半 —— 方向归另外两条。

**仍生效的已知不确定性**
1. **阵形的收益完全押在"券要在目标周围一格内"这一条上** —— 而**买券那条线还没做**。
   所以这一步目前只兑现了**一半**：位置摆对了，用券的动作还没有。首场后要回头核 L292/L314 有没有读错。
2. **"前排两角"是 `by-2` 与 `by+1`** —— 判据是"与基地的角格切比雪夫距离 1"，而 12 格武器环里
   有 8 格都贴着基地 ⇒ 不只一组解。选这两个是因为它们**离机器人最近、在火线上**。**没有实证**。
3. **配对按射程，而射程本身就没定论**（样例 4/7/∞ vs 表格 3/6/10）。好在两套数据下火箭都是最长，
   结论不变；但**若实测发现火箭射程其实是三者里最短**，这个配对要翻。
4. **后列那格到底该贴哪一条边** —— 用户从三个候选里选了"贴基地下沿 `(bx-1, by-1)`"，没有实测数据支撑。
5. **`_front_back` 的"只此一处"目前只在 `grid.py` 内部成立**：`planner` 不直接算方向，
   但若以后有人在别处再写一次 `base.x * 2 < width`，那句话就又变成假的了（没有 lint 守着，也不该加）。

---

## 第 14 步：把判题器的回执记进日志（`errors` / `lastRoundRoleActionResults`）

**目标** 用户要求"把任务信息打印出来，看看是什么情况"。查下来发现两处：任务那一行是 `phase_task[:120]`
的**静默**截断（看不出后面还有没有内容）；**判题器对任务的判决从来没被读进 `Turn`、更没进过日志**
—— 日志里只有"我们发了什么"，没有"判题器认不认"。所以这一步不是"多打点字"，
是补上整条诊断链最关键的一环。

**产出** `game/world.py`：`Error` NamedTuple（`__str__` = `f"{code}：{description}"`）
+ `Turn.errors` / `Turn.action_results`。**`errorCode` 不翻译成文字**（那 5 个码是**协议知识**，
写进代码就是一份会跟接口文档漂移的第二真相；码表只活在 `CLAUDE.md` 的领域事实里给人读日志用）。
**`action_results` 原样保留、不剪枝**（含我们不操控的基地格）。
`protocol/model.py`：`_errors` / `_action_results`，两条解析各有一处**故意的严格** ——
`errorCode` 解析不出来 ⇒ **整条丢掉**（`-1：xxx` 会被读日志的人当成"未知错误 0"去查一个不存在的问题）；
`value` **只认真正的 JSON 布尔**（JSON 里的 `"false"` 是**非空字符串**，`bool("false") == True`）；
key 转不成 `int` 的丢掉。
`app.py`：`_log` 加三块（判题器报错 / 上回合未通过 / 任务行），`_clip` 替掉静默截断
（超长打 `…（共 N 字）`，**上限单位是字不是字节**）。

验证：单测 113 条，**反向验证 5 条**全中。真服务端到端：样例响应逐字节不变；
合成的"任务在身 + 判题器报错"回合里**截断标记与真实字数对上了**（1066 = 66 + 200×5）——
这正是"静默截断"修好的证据。

**仍生效的已知不确定性**
1. **`errors` 是"本轮"的**（接口文档 §1.7 原文）：它报的其实是**上一回合**那条指令的结果。
   这一点文档没有直说（"本轮产生的错误信息数组"里的"本轮"指哪一回合），
   实测时要用"我们发了什么 / 它报了什么"对齐一次才能确定。
2. **任务原文会有多长完全不知道**。`LOG_TEXT_MAX = 400` 是个**估的**数。真跑起来若发现任务普遍超过
   400 字，截断标记会直接告诉我们"共 N 字"，照那个数调这一个常量 —— 这就是把截断**说在脸上**的用处。
3. **"任务期间每回合打同一段任务原文"是这一步最大的字节浪费**（占任务期间日志的一半以上）。
   一个不改策略、不加状态的省法是把 `LOG_TEXT_MAX` 调小；要既省又不截断就得给 `app` 加跨回合状态，
   **代价是日志不再自明**。现在的取舍是**日志优先**：诊断工具的可预测性比几个字节重要。

---

## 第 15 步：采矿落实（"保持够砌墙的石头量，然后选价格最高的矿"）

**目标** 兑现 `docs/策略指导.md`（用户手改）里的第二条。
**"然后"是顺序，不是并列** —— 墙只吃石头，若一上来就挑最贵的铜，围墙永远砌不成。
所以：**墙上还缺石头** ⇒ 只认石矿、取最近的；**墙砌完了** ⇒ 才按**收购价**挑最值钱的矿。
"保持够砌墙的量"那一半本来就有（`_stones_to_mine` 拿"还差几格墙"当上限），
本步的新行为只有后半句：**砌完之后工人原本原地闲置，现在转去采矿**。

**产出** `Map.stones: frozenset[Pos]` → **`Map.ores: Mapping[Pos, str]`**（矿点 → 矿种，
新增 `ORE_KINDS` 与 `STONE`；**小贩/武器商店/任务点照旧只进 `blocked`**）；
`Turn.vendor_prices`（`vendorShopList` → 矿种→收购价，只收 `price >= 0`）；
`_nearest_stone` → **`_pick_ore(pos, ores, prices, *, want_stone)`** + 新增 **`_mine_spare_ore()`**；
`tests` 113 → 119 条。
**`ores` 带矿种、价格从载荷读** —— 同一条理由（"铜 > 铁 > 石"**不是常量**，写死的排序在事件期间
恰好是错的），见 `CLAUDE.md`；价目缺失/矿种不在表里 ⇒ 按 0 算 ⇒ **不为它多走一步**。
`_mine_spare_ore` 的往返预算参照点换成**基地**（不是工地）—— 墙砌完后没有工地可回了，而夜里的炮位就在基地四周。

验证：单测 119 条。**反向验证 5 条全中**。
⚠️ **`test_the_ring_decides_whether_price_gets_a_vote` 的第一版是假通过的**：它原本让工人"朝矿走一格"，
而 BFS 的第一格常常**同时靠近两座矿** ⇒ 改坏 `want_stone` 也照样过。改成"两座矿各贴在工人一边、
断言 `collect` 瞄的是哪一座"之后才真的挂了那一次。**"走一格"这种断言测不出方向，只有坐标级别的答案测得出。**
**合成端到端**：样例价目（石 1 / 铁 3 / **铜 5**）⇒ 两人都朝**铜矿**；**只把价目翻成 铁 9 / 铜 5**，
同一份局面重发 ⇒ 两人双双掉头朝**铁矿**。**这才是"价格来自载荷"的端到端证据。**

**仍生效的已知不确定性**
1. **"墙砌完了"是常态还是例外完全不知道。** 若 16 格一般在第 40 回合就砌完，那每天能多出 30 个回合的
   劳作；若拖到第 68 回合才砌完，它基本不会触发（往返预算会把远矿全否掉）。
2. **`vendorShopList` 是"收购价"这件事是**推断**（接口文档 §1.1 只给了元素形状 `{name, price}`）。
   若它其实是**售价**，那"按价排序选矿"仍然成立，但"价 0 的矿不去采"这条会误伤。
   **下一场实测时对一下金币数。**
3. **挖矿点的"采满 10 次就消失"没有计数**（payload 没有剩余次数字段）。真跑起来若发现工人对着空矿位
   一直 `collect`，症状是 `action_results` 里翻 `false` —— 那时再想跨回合计数的办法，现在加就是预先抽象。
4. **同价矿的取舍只按坐标排**：两个工人可能挑中同一座矿的相邻格，这是允许的（矿不认领），
   但会让两个人整天黏在一起 —— 若实测发现"两个工人总是同进同出"，再考虑按工人 id 错开。

---

## 第 16 步：给任务线接上沙盒（`executeCmd` + `lastCmdResult`）

**目标** **用户从日志里找到了任务一直失败的真因**：响应里那个 `executeCmd` 是"让判题器执行的 shell 命令，
由 LLM 回复生成"，所以拿到任务后要和 LLM 交互、给它一个 `executeCmd` 工具、把生成的命令放进响应顶层，
**拿到执行结果之后才知道任务要什么**。核对文档判断完全正确，而这是一条**从来没有接过的通道**：
`src/` 里 `lastCmdResult` **零出现**、`executeCmd` 恒为 `""` ⇒ 答案只能靠 LLM 硬猜
⇒ 判题器只能报 `errorCode 2`。**这就是任务线 0 分的根因。**

**产出** `Turn.cmd_result: str = ""`；`model.load()` 接上 `lastCmdResult`；
**本步主体在 `planner`**：`prompt_for` 删除 → **`task_channel(turn) -> (prompt, executeCmd)`**
+ `_asked` / `_is_tool_reply` / `_tool_command` / `_retry_note`；`TASK_PROMPT` 重写成三段式；
`app.handle` 改成写响应顶层的 `executeCmd`，`_log` 新增**沙盒行**；`tests` 119 → 135 条。

**六条判据的状态机**（从上往下先命中先返回）：① 没任务或名册里没有开拓者 ⇒ 两个都不发；
② `cmd_result` 非空 ⇒ 回灌结果、**这轮绝不发命令**；③ 工具给了命令 ⇒ 发命令、不提问；
④ 判题器报 `code 2` ⇒ 带"上次答错了"重问；⑤ 是答案 ⇒ 都不发；⑥ 否则只把题目问出去。
核心不变量 = **两条通道互斥**（同一轮既提问又发命令 ⇒ LLM 拿着过期结果作答 ⇒ 活锁），
这也是把它合成一个函数而不是 `prompt_for` + `execute_for` 的全部理由。
② 必须压在 ③ 前面、纠错做成修饰符而非分支、`executeCmd` 的闸门要显式补 —— **三条理由都在 `CLAUDE.md`**。

验证：单测 135 条，**反向验证 8 条全中、无假绿**。
⚠️ 这一轮**反向验证抓出了一处真冗余**：`_retry_note` 里原本写的是
`return reply if reply and not _is_tool_reply(reply) else ""`，但去掉 `reply and` 行为**完全相同**
⇒ 按"禁止冗余设计"删掉了那个 `and`。
⚠️ **还原只用备份文件，绝不用 `git checkout`** —— 那会退到 HEAD、把本步所有未提交的改动一起抹掉。
**合成一整条任务链路**（逐回合只改 `phaseTask` / `llmResp` / `lastCmdResult`）：
提问 → `executeCmd == 'python -c "print(1+1)"'` → prompt 含 `[exitCode:0]\n2` → `submitAnswer: 晴 26 度`。

**仍生效的已知不确定性**
1. **`<tool>…</tool>` 是我们自己定的协议，判题器认不认完全不知道**（任务书只写了沙盒"能跑基础的
   shell 与 python 指令"，**没规定 LLM 该用什么形状要命令**）—— 这是**最大的单点风险**。
   若不认，症状是 `executeCmd` 一直发出去、`lastCmdResult` 一直是空 ⇒ 退化成第 15 步的状态。
2. **`llmResp` 粘不粘，文档一个字没写**。自检判据：日志里"沙盒：回「…」"连续两回合出现同一条
   ⇒ 要么粘住了，要么判题器真的重复执行 —— 两种都说明这条链要重新想。
3. **`lastCmdResult` 会不会粘住**同样没实测过。它若也粘，判据 ② 会**每回合都命中** ⇒ 任务线永远停在
   "回灌结果"、再也不发新命令 —— 症状同样在日志上。
4. **沙盒执行若延迟超过一回合，判据 ② / ③ 的互斥就失准**：`cmd_result` 与 `llmResp` 会错开一回合到达，
   我们会在等结果的回合里把那条命令**再发一遍**。判据 ② 只挡住了"同回合"。
5. **任务切换那一回合，`cmd_result` 里装的是旧任务的输出**（判据 ② 只认"非空"）。**已知、暂不修**
   —— 要修就得引入跨回合状态。
6. **回灌全文可能撑爆判题器 LLM 的 prompt**（沙盒输出上限 64KB，全文回灌等于把 64KB 塞进下一轮）。
   用户拍板"全文回灌"，若实测发现 LLM 因此答不出来，这里的取舍要重新谈 —— **`_clip` 早就在手边**。
7. **判题器执行 `executeCmd` 的 15 秒限时与"不计异常"这两条没实测过**（L210 是文档承诺）。
   若实际会计异常，那这条通道的每一次使用都在赌红线的 5 次 —— 那是**必须立刻回退**的事。

---

## 第 17 步：围墙只砌三面（背面整列留成"门"）+ "不能把工人关起来"的闸门

**目标** 用户原话："墙只建立前面的，最后一排都不建立，建墙有一个原则，**不能把工人关起来**，
如果可能出现，需要工人先走出来"。两条拍板：围墙范围 = **14 格**（正面列 6 + 顶行 4 + 底行 4，
**背面整列 6 格含上下两角，一格都不砌**）；"不能把工人关起来"做成**运行时的闸门**，不靠几何自动保证。
⚠️ **门那 6 格里没有任何建筑**（后列炮位在**武器环**的后列，不在围墙环的背面列）⇒ 能堵门的只有**单位**。

**产出** `grid.py`：`wall_cells` **16 → 14 格**（顺序逐格不变）+ 新增 **`box_cells(base)`**（36 格）
与 **`step_outside(pos, box, blocked, size)`**（BFS 与 `step_toward` 同构，只把终止条件从"贴着 goal"
换成 **`nxt not in box`**）。`tests` 135 → 151 条。

**闸门两半**（都在 `planner.plan` 的一次调用里）：**(1) 会关人就一格都不砌** —— `_trapped` 逐角色问
"**假设墙砌满**（`_walled`）之后还出不出去"，返回要救的 id 集合 `leaving`，`gated` 传进 `_build_walls`；
⚠️ 那一处 `if gated: return` 的位置在 `if not free:` **之后** —— "环上无可砌（⇒ 去采最值的矿）"与
"闸门挡住了"都会让 `free` 空掉，含义却相反（前者是**砌完了**，后者是**还没砌完、这一回合不许砌**）。
**(2) 要被关住的人先走出来**（`plan` 的角色循环里，任务钉死那一支之后）：
⚠️ **两半用的是两套障碍**（`leaving` 按"假设砌满"判、迈出去那一步按"**现在**"判，
两套之差恰好是那 14 格里还没砌的部分）；⚠️ **`turn.is_day` 不能省**（夜里那一支只会把人从炮位上拉走）；
⚠️ **`| claimed` 不能省**（两个要走出来的人会被指到同一格 ⇒ §4.5.4 目标争夺 ⇒ 两人都不动）；
⚠️ **`_walled` 把自己人算成障碍，`_ring` 把自己人算路过 —— 这两处故意相反**。

**`_step_aside`（"站在目标格上先挪开"）是 e2e 才揪出来的**：`model._entries` 把单位铺在**最后**，
于是人站在墙上时网格里只剩 `worker`、**墙被盖掉了**；而 `_ring` 的"自己人算路过"又把它复活成候选
⇒ `free[0]` 永远是脚下这一格 ⇒ **每回合对同一格 `build`，永远轮不到下一格、石头白花，`free` 也永不为空**
（连"砌完了去采最值钱的矿"那一支都进不去）。修法：`role.pos == target` ⇒ 这一回合只 `move`、
**下一回合再砌**；两个方向都收敛。
**`_dead_ends` 实现过又删掉**：穷举 69,132 个局面，**用与不用的踪迹完全相同**，而它还会把"现在走得通"
的格当障碍、拉长或否决合法路径 ⇒ 按规则 1 删除。

验证：单测 151 条。**反向验证 11 条改坏全部被抓住**。
⚠️ **还原只用备份，且每次改完代码要重新 `cp` 一份**（旧备份会把新改动一起抹掉）。
**60 回合驱动**：14 格全部砌上、与 `wall_cells` 顺序一致、正面列最先；第 37 回合有 `movePos`
（站在刚砌好的格上 ⇒ 先挪开），第 38 回合从邻格砌上。
⚠️ **驱动脚本第一版是错的，差点得出错误结论**：它只跟踪了 1 号工人，另外两个角色的指令被丢掉，
于是"只砌了 13 格、工人掉头去采铜"看起来像策略 bug —— 实际是**另一个工人已经认领了最后一格**。
教训：**模拟判题器必须跟踪全部角色**，且 `acceptTask` / `submitAnswer` **没有 `targetPos`**。

**仍生效的已知不确定性**
1. **闸门 (1)/(2) 在今天这条几何下几乎打不着**（要门那 6 格**同时**被单位堵满，而我方至多 3 个角色）。
   它的价值是兜底。**下次复盘若发现日志里从没触发过，这是设计如此，不是坏了。**
2. **闸门 (2) 有时会被 `_mine_spare_ore` 抢功**：盒外有矿、且该工人这回合本来就要去采，
   那条路径也会把人带出盒子。(2) 是**保证**而不是常见路径。**未实测。**
3. **闸门 (1) 是"全有或全无"**：门被堵时哪怕只剩最后一格墙也**整回合不砌**。副作用是墙可能停在 13/14 格。
4. **"假设墙砌满"把我们的角色也算成障碍格**：光靠自己人仍关不住人，但**叠加机器人**时会提前触发
   （少砌一回合墙）。**未实测**，症状是"墙莫名停在 13 格"。
5. **门开在背面，机器人从正面进来后仍可从门绕出去**。本步把缺口从 4 格放宽到 6 格且**挪到了整列**，
   等于把"绕出去"的通道挪离了正面火力线。**影响未实测**，首场若发现机器人绕圈不挨打，第一嫌疑是它。
6. **"站在目标格上先挪开"白花 1 回合**。比它更省的做法是"把脚下那一格从自己的候选里剔掉"，
   但那会漏掉"脚下正好是**最后一格**没砌的墙" ⇒ 工人站在上面干等、墙永远停在 13/14（静默停摆）。

---

## 第 18 步：Agent 系统（`agent/` + `tools/`）+ 任务线切到新工具协议

**目标** 用户原话（要点）：独立 `agent/` 文件夹 + 其中的 `tools/`；提供 `executeCmd(cmd)` 工具；
提供 `SOP2Prompt(SOP)` 让 PromptSOP 自进化；接到任务时触发 Agent 的 `chat` 组装上下文；
解析 `llmResp` 里的 `<tool><tool_name>…</tool_name><tool_param>…</tool_param></tool>`；
提供顶层的 `tool_call(tool_name, tool_param)`；无需工具时用 `<answer>…</answer>` 包裹答案；
Prompt 模板 = `# Agent定位 / # 可使用的工具 {{tool_desc}} / # 输出格式 / # 沉淀的 SOP {{SOP}}`。
第 16 步的 `<tool>…</tool>` 形状是**我们自己定的**，且当时只有"一条裸命令"、没有"工具"这个概念 ——
这一步把工具**注册化**。四条拍板：位置 = `src/coregeek/agent/`；SOP = 跨回合状态（全项目**唯一**一处）；
**同一步把任务线切过去**（新形状**取代**旧形状，不建"暂时没人调用"的子系统）；
**全覆盖兼容**（旧形状照旧当 `executeCmd`；没有 `<answer>` 就**原文当答案交** —— 这是新协议不被认账时唯一的退路）。

**产出** `agent/__init__.py`（新建，0 字节）/ `agent/chat.py`（`PROMPT` 四段模板 + `chat()` +
三个谓词 `tool_of` / `looks_like_tool` / `answer_of` + 共用的 `_block`）/
`agent/tools/__init__.py`（`TOOLS` 注册表 + `tool_call` + `tool_desc`）/
`agent/tools/cmd.py`（`executeCmd(cmd)` —— **原样返回**，一个赋值都不做）/
`agent/tools/sop.py`（`SOP2Prompt` / `current` / `reset` / `SOP_MAX=1000` + 内容变化时一行日志）；
`planner.task_channel` 换料（判据链顺序不变）；`tests` 151 → 187 条。
依赖方向新增 **`game/planner.py → agent`**，机械保证是 **`chat(request: str, …)` 收字符串、不收 `Turn`**。

**三个谓词的分工**（宽严两端，细节见 `CLAUDE.md`）：`tool_of` **严格**（名字与参数**成对**才给
`(名, 参数)`；**两个都没有 ⇒ 整块正文当一条 `executeCmd` 命令** = 旧形状兼容层）；`looks_like_tool`
**宽**（`"<tool" in reply`）；`answer_of` **三级兜底**（有标记只认块内容 / 像工具回复给空 / 否则原文即答案）。
⚠️ **`answer_of` 是「该提交什么」与「该骂什么」的同一个谓词**（提交 / 纠错内容 / 判"已有答案"三处共用）
—— 一旦分家，纠错段会把**带标签的原文**喂回去，LLM 看到自己上次的标记被原文骂回来，行为未定义。
**铁律：`tool_call` 的返回值就是那条要进 `executeCmd` 的命令**（`""` = 不产出命令 / 调用不成立，**下游不需要区分**）；
它是**副作用唯一发生点**，写在判据链之前 ⇒ 走"回灌结果"那一轮 SOP 照样生效。**`executeCmd` 不执行任何东西**
（执行者是**判题器的沙盒**）。**`SOP2Prompt` 是整段替换不是追加**（追加没有遗忘机制）；内容没变就不吭声。

验证：单测 187 条。**反向验证 21 处改坏，21/21 全中**。
⚠️ **第 12 条一开始是假绿**：改坏把描述写死成一个**仍然包含两个工具名**的字面量 ⇒ 行为等价；
修的是**用例**，不是改坏脚本。
**字节预算**：最坏局面实测 **44 行 / 6180 字节**（任务在身 + 沙盒结果顶格 + SOP 刚更新），上限钉在 6900。
**真服务九轮 e2e**（每一条都验了）：② `SOP2Prompt` 不发命令**且存下的 SOP 立刻出现在同一轮的 prompt 里**；
⑤ **交上去的是块内容 `晴 26 度`，不是 `<answer>` 原文**；⑥ 纠错段含 `晴 26 度` 且**不含**标签；
⑨ 畸形回复**不发命令、不提交、prompt 非空**（两条通道都不哑火）。
**全局断言**：任何一回合都没有同时出现非空 `prompt` 与非空 `executeCmd`。
⚠️ **这份 e2e 里的"LLM"是我们自己写的** ⇒ 它只证明"我们的解析与编排自洽"，**不证明判题器认这个形状**。

**仍生效的已知不确定性**
1. **最大单点风险：判题器的 LLM 认不认这个形状。** 兼容层把"不认账"的后果压到"退化回第 16 步"，
   但"LLM 用第三种形状"仍是敞口。**首场自检判据**：任务行"提交："里若**不是** `<tool_name>` 形状
   ⇒ 形状没被采纳 ⇒ 回退只需改 `tool_of` 一处。
2. **SOP 一旦存错就是整场的事**（没有重置机制、没有遗忘）。最坏 = LLM 往 SOP 里灌了一段有害的"套路"，
   此后每回合 prompt 都带着它。后果是**答得差，不是异常**。**未实测**。
3. **SOP 不按任务分区**：任务 A 沉淀的 SOP 会灌进任务 B 的 prompt。用户拍板的取舍，记录、不修。
4. **`SOP2Prompt` 那一轮走判据 ⑥ 重问**：LLM 若反复调它（`llm_resp` 粘住），会退化成"每回合重问、
   永不提交"。**升级触发条件**：首场日志若出现「同一任务连续 ≥3 回合 `llm_resp` 都是 `SOP2Prompt` 调用、
   且始终没有 `submitAnswer`」⇒ 升级成"显式 ack"编排。**未实测。**
5. **`<answer>` 被解包后提交，等于改了交上去的字节**（第 16 步是原文进原文出）。**未实测**。
   裸文本兜底保证"没有 `<answer>` 时行为与第 16 步一致"。
6. **畸形 `<answer>`（空块 / 半截）改成了"不提交 + 重问"**：代价是那一回合没有提交
   （判题器取"通过率最高"的那份，所以不提交不扣分）。**未实测。**
7. **`planner → agent` 是新的依赖边**：若将来 `agent` 需要 `Turn`/`Pos`，这条边会变成双向 ——
   那时应该把"什么时候说话"也搬进 agent，而不是让 agent 认识 `game`。
8. **`SOP_MAX = 1000` 是拍的**，没有实盘依据。首场看 `SOP 更新：存 N 字` 那一行的 N 分布。

---

## 第 19 步：Agent 从"模块级散函数"改成"单实例 + 状态在实例上"

**目标** 用户原话："这个 Agent 是一个单实例的，每次开拓者来的时候，注入这个实例"。
第 18 步落地的是**模块级散函数**，SOP 状态是 `tools/sop.py` 里的模块级 `_current`。
用户要的是**一个显式的 Agent 对象**：一个进程一个实例，SOP 这个跨回合记忆**长在它身上**。
两条拍板：单实例住在 `agent/__init__.py`（`AGENT = Agent()`，`planner` 直接
`from ..agent import AGENT` 取它）；状态搬到实例上。
⚠️ **这一步不动协议、不动策略**：形状、判据链六条、"不本地执行"一个字节都不改。
**判据 = 第 18 步 e2e 的输出表逐字重现**。

**产出** `agent/agent.py`（新建 `class Agent`：`_sop` / `_tools` / `chat` / `tool_call` / `tool_desc` /
`SOP2Prompt` / `sop`(property) / `reset`）；`agent/__init__.py` **不再是 0 字节**；
`chat.py` 改成 `chat(request, *, sop, tool_desc, result="", retry="")`（**纯函数**，删两个包内 import）；
`tools/__init__.py` 只剩一段 docstring；`tools/sop.py` 保留 `LOGGER`（**名字不变**）+ `SOP_MAX`
+ 新的 `store(current, sop)` / `describe(received, stored)`；`tests` 187 → 195 条。
⚠️ **`LOGGER` 留在 `tools/sop.py` 是有意的**：logger 名 `coregeek.agent.tools.sop` 因此**不变**
⇒ `app._log` 的字节表、那条"唯一一条不在 `app` 名下的日志"的守卫用例、`CLAUDE.md` 硬约束 5 三处都不用改。
**`chat` 的 `sop` / `tool_desc` 没有默认值**：为了让"忘了传"在**调用点**就炸，
而不是静默发一份空槽的 prompt 出去（实盘表现是"LLM 完全不知道有什么工具"，本地一片安静）。
**三个谓词仍是 `chat.py` 的自由函数**：它们与状态无关，而两个调用点**必须共用同一个**。

验证：单测 195 条。**行为等价（最重要的验证）**：第 18 步的九轮驱动**一个字节没改**重跑
⇒ 与第 18 步那张表**逐字一致**（597 / 630 / 0 / 663 / 0 / 664 / 0 / 0 / 630）——
每一个字数同时证明"模板没动""SOP 段照旧填进去""两条通道互斥还在"。
**反向验证 14 处，每处都挂住了用例**。
⚠️ **第一版脚本 14 条全报"没挂住"**：`FAIL_LINE` 正则漏了 `re.M` ⇒ `^` 只匹配字符串开头
⇒ **一条 FAIL 都没匹配上**。**报告"全没抓住"和"全抓住"一样可疑 —— 先怀疑测量工具。**

**仍生效的已知不确定性**
1. **`from ..agent import AGENT` 让"注入点"退化成全局查找**：`planner` 手里绑的是**对象**
   ⇒ 测试**换不掉**它，只能靠 `Agent.reset()` 隔离。若将来需要"同一进程里同时跑两个 Agent"
   （现在没有这个需求），这条边得改成 `from .. import agent` + `agent.AGENT`，或者把实例当参数注入。
2. **`reset()` 是生产代码里唯一一个只为用例存在的公开方法**：没有它，走 `task_channel` / `app.handle`
   的用例会跨用例串味。**接受**：它是实例上的一个赋值，不碰红线。
3. **`AGENT` 在 import 期构造** ⇒ `agent/__init__.py` 有 import 期副作用（一个空串赋值，无 I/O、不会抛）。
   若将来 `Agent.__init__` 要做重活（读文件、起线程），这条副作用会变成踩雷点 —— 那时应改成惰性构造。
4. **`test_the_logger_name_does_not_depend_on_the_agent` 断的是 `LOGGER.name` 与一个字面量**：
   若将来真的把 logger 改名，这条用例会挂 —— 那是**有意的**（改名要连带改 `app._log` 的字节表、
   `CLAUDE.md` 硬约束 5、以及本条用例三处）。它防的不是"名字写错"，是"改名字时漏改"。

---

## 第 20 步：日志打详细（动作打全字段 / prompt 打全文 / 回执列全部）+ 重新钉字节上限

**目标** 判题器是黑盒、**没有"打一局试试"这回事** —— 日志是唯一的观察窗，而第 18/19 步落地的任务线
**一次实盘都还没跑过**。这一步把三块看着最要紧的信息补齐，每条都有明确的"要回答什么问题"：
**动作打全**（`10010 move (6,22)` / `10012 build name=wall (13,23)`）；
**`prompt` 打全文**（原来只有"有（630 字）"）；
**回执列全部实体**（`上回合合法性：10010=False 10011=True …`，触发条件是"**有回执**"而不是"有未通过"
—— 全 `True` 也要打，那正是"发出去的都合法"的唯一证据）。

**产出** `protocol/actions.py`：`describe(cmds, *, clip)` 改成**通用摊开**（`action` 之外的每个字段一律
`名=值`，`targetPos` 折成 `(x,y)`，字符串值过 `clip`）。
`app.py`：新增 `LOG_PROMPT_MAX = 1000`；`_clip(text, limit=LOG_TEXT_MAX)` 多一个上限参数。
`tests` 195 → 198 条。
**三条取舍**（理由见 `CLAUDE.md`）：通用摊开（手写字段清单 = 又一份会漂移的真相，且顺手拆掉
"硬读 `cmd["targetPos"]`"这颗老雷）；截断规则以 `clip` 递进去（`protocol` **不能 import `app`**）；
`prompt` 用 1000 而不是 400（**光模板就 591 字**）。

验证：单测 198 条。**响应零变化**：第 18 步的九轮驱动**一个字节不改**重跑，九轮输出**逐字相同**。
**日志真的变详细了**：那一屏就是这一步的全部意义 —— 工具清单、SOP 槽、沙盒结果段、题目原文**一次看全**。
**反向验证 9/9 全中**。⚠️ **其中一条第一次一条都没挂住 —— 那是真的覆盖缺口**：
触发条件改坏之后只有"全 `True` 的回执"会表现不同，而当时没有任何用例喂过全 `True` 的回执。
**两次教训正好相反，所以两条都要记住**：报告"全没抓住"时先怀疑工具（第 19 步），
确认工具没问题之后，"没抓住"就是用例真的没覆盖（这一步）。

**仍生效的已知不确定性**
1. **`LOG_PROMPT_MAX = 1000` 是个估计**：SOP 长到 ~400 字之后 prompt 必被截（保头），
   那时"题目原文在不在里面"就看不见了 —— 但题目另有「任务：」那一格兜着（**有意的重叠**），
   而保头留下的正是模板 + 工具清单 + SOP 段这几样**别处看不见**的东西。
2. **`prompt` 的换行没转义**（与 `cmd_result` 同一条做法）。若将来"一条记录 = 一行"变成硬需求
   （比如日志要机器解析），得在 `_log` 里统一转义 —— 那会同时改变 `cmd_result` / `phaseTask` 的形态。
3. **`describe` 现在把所有字符串字段一律过 `clip`**：将来若某个字段需要完整（不该截），
   得在这儿开例外 —— 现在没有这样的字段（`taskAnswer` 恰恰是**最该**截的那个）。
4. **最坏那一格用的是"回灌轮 + SOP 调用"的合成 fixture**（`llmResp` 粘住 + `cmdResult` 顶格）。
   现实里两个撞在一起的概率不高，但**这价格表就是按最坏钉的**。

---

## 第 21 步：地图铺满 —— 空地打 `#`，围墙让位给 `=`

**目标** 用户原话："打印地图的时候，打满整个矩阵，空地用 `#` 符号，这样比较显著"。
动机是**可读性**：空地打成 `.` 时几乎融进终端的深色背景 —— 看得见内容、看不出**这张图有多大、
边界在哪**。用户自己先改了一行（`_char` 的空串分支 `"." → "#"`），这一步把它做完。
**碰撞的后果（实测）**：`_char("")` 与 `_char("wall")` 都返回 `#` ⇒ 我方那一圈墙
**沉进空地背景里整圈看不见**，而图例会同时出现 `#=围墙` 与 `#=空地`（看上去只是重复一项）。

**产出** `_RENDER_SIDED["wall"]`：`("#", "%")` → **`("=", "%")`**；`_char` 的空串分支 → `#`；
`_legend()` 的 `".=空地"` → `"#=空地"`；`tests` 改 3 处断言 + **新增 1 条**
"一个字符只能代表一样东西"（`assertEqual(len(set(chars)), len(chars))`）——
既有那几条 `assertIn(f"{_char(kind)}=", LEGEND)` 在碰撞发生时**一条都不会挂**。

验证：单测 198 条全绿。**反向验证 1 条**：把 `_RENDER_SIDED["wall"]` 改回 `("#", "%")` ⇒
`AssertionError: 18 != 19`，**恰好那一条挂**，其余 197 条照过 —— 这就是"既有断言全都抓不住"的实证。
**响应逐字节不变**。

⚠️ **整步已被第 22 步撤销**（见下），这里只留两条仍然成立的结论：
- **`_char` 恒返回 1 字符这条不变量仍然没有断言守着**（"一个字符只能代表一样东西"守的是"不重复"，
  不是"长度为 1"）。
- **满屏 `#` 之后敌我围墙的对比度都下降了**（`#` 在视觉上比 `=`/`%` 更"重"，背景反而比结构显眼）
  —— 这条"已知不确定性"**当天就兑现了**，用户于是把空地直接改成**空格**、`#` 收回给墙。

---

## 第 22 步：卖矿线（`sell`）—— 矿第一次变成金币

**目标** 用户原话："买矿产的逻辑"。"买矿"这个动作**文档里没有**（§4.4 只有 `sell` 卖矿换金币、
`buy` 在武器商店买商品/券），按上下文与已实现的采矿线取 **`sell`**：**把背包里的矿石背到小贩跟前
批量卖掉换金币**。这一步之前这条线**整条都不存在**，而且断在三处：没有动作类（`grep '"num"'` 在 `src`
下零命中）；不知道小贩在哪（`Map` 只建了 `blocked` / `ores` 两张索引）；不知道背包里有几块铜
（`model._stone` 只数 `stone`）。
第 15 步采回来的铜/铁**兑现不了**，而**价格波动的玩法本来就要靠卖矿才成立**。

**产出** `Sell`（`code = "sell"`、**`roles = ALL`**、`__init__(role_type, name, num)`、
`to_wire()` → `{"action","name","num"}`）；`model._stone` 升级成 **`_bag`**（`{物品名: 件数}`）；
`BaseRole.stone` 从字段变成**派生属性** `bag.get(STONE, 0)`；`Map.vendors: frozenset[Pos]`
+ 模块常量 `VENDOR`；`planner` 新增 `SELLABLE` / **`_sell_ore`** / **`_best_load`**；
`world.summary()` 的我方那行打出 `石N铁N铜N`；`tests` 198 → 214 条。
`describe()` **一行没动** —— 它是第 20 步的通用摊开，`name` / `num` 自己就打得出来。

**`_sell_ore` 的四条门**（任一条不成立 ⇒ 返回 `False`，调用方接着去采矿）：
① **有货**（`_best_load` 挑**收购价最高**的那种，同价取件数多的，价 ≤ 0 或件数 = 0 的跳过；
空价目表 ⇒ 一件都不卖）；② **有小贩**（`vendors` 为空 ⇒ `False`，`min(空集)` 会 `ValueError`）；
③ **够本**（**已经贴着小贩就跳过这一条**：`货值 < 2 × dist(role, 小贩)` ⇒ `False`）；
④ **回得来**（`dist(role,小贩) + dist(小贩,基地) > day_rounds_left − TIME_MARGIN` ⇒ `False`）。
然后 `dist <= 1` ⇒ `_emit(..., Sell, kind, num)`（**一次卖光这一堆**，一回合一个角色只有一条指令）；
否则 `_step`。
**调用点只有一处**（`_build_walls` 的 `if not free:`，即"14 格墙砌完了"那一支）改成**先卖后采**：
`free` 为空 = 墙砌完了；墙没砌完时工人手里的石头一律有用 ⇒ **`free` 非空时不存在"多余的石头"**，
那个不变量就是它的定义，不需要再写一遍计数。

**顺带四件事**：① **`_clip` 搬进新叶子模块 `src/coregeek/utils.py`**（原来住在组装根 `app.py`，
下游只能把它当参数递；⚠️ **但 `describe` 那条边仍然是注入、不是 import** —— 它的契约是"不认识日志"）；
② **摘要改成四块并打出 `石N铁N铜N`**；③ **订正第 21 步**（空地 = 空格、我方墙收回 `#`；图例那一项
写的是字面量 `空格=空地` 而不是 `f"{_char('')}=空地"` —— 直接拼出来是 `" =空地"`，看着像少打一个字符）；
④ **banner + 字节上限重钉**：用户给 `handle` 加了 `###第N回合###`（**唯一一条不受 `_log` 管辖的日志**，
数记录数时要单独算），最坏那一格抬到 **71 行 / 8723 字节**。

验证：**单测 214 条全绿**。**反向验证 3 条，三条门各自只有一条用例守着，且都不是假通过**
（⚠️ 第一次改 `SELLABLE` 时按字符串 `("stone", …)` 找没找到，实际写的是常量 `(STONE, …)`
—— **改坏也得先读一眼真代码**）。
**真服务端到端**：① 样例响应三个顶层字段齐全、四块摘要（含 `10010 worker(5,23)石1铁1铜1`）都在；
② **合成一个"工人手拿 4 块铜、站小贩正下方"的白天的局面** ⇒
`{"10010":{"action":"sell","name":"copper","num":4},…}` —— **`sell` 真的发出去了**；
同一局面里另一个工人（离小贩 10 格、只有 1 块铜 = 5 金币 < 阈值 20）**没有跟去卖**，转去朝矿走
⇒ **够本门在真服务上按预期分流**。

**仍生效的已知不确定性**
1. **「1 金币 ≈ 1 回合」（货值 ≥ 2 × 距离）是拍的**，没有任何文档依据。
   它是这一步**唯一的调参旋钮**，首场实测后按日志里的"小贩距离 / 卖出量"回头调。
2. **阈值可能让工人永远不动身**：样例小贩距离 10 ⇒ 阈值 20 金币 ⇒ 铜要攒 4 块。
   若实际矿区离小贩更远，阈值会高到当天攒不满 ⇒ **卖矿线静默失效**。
   **首场日志若出现"矿堆着却没卖过"，先看这一条。**
3. **「贴着小贩就跳过够本门」是策略选择，不是文档规定**：不这样写的话，工人会在小贩边上因为
   "只剩 1 块铜不值 20 金币"而**站着不卖**，而那 1 块铜本来不需要再走路。
4. **小贩坐标的真实数量未知**（样例只有 1 个）。`Map.vendors` 是集合、取切比雪夫最近的那个
   （并列按坐标排），**未实测**。
5. **`sell` 的报文形状没有实证**（`docs/response.txt` 里只有 `move`/`build`/`remove`）——
   形状从接口文档 §2.3 推的。
6. **`name` 用英文矿种名还是中文没实证**：取英文（与 `neutralType` / `vendorShopList.name` 同一套词）。
7. **`num` 超出手上件数会怎样**（报错还是按实际卖）文档没写；我们只发"手上确实有的件数"，绕开这一条。
8. **两个工人同时去卖**是否会撞在同一格，靠 `claimed` 分开，**未实测**。
9. **`sell` 的昼夜限制**：§4.4 那一格是空的 ⇒ planner 把它放在白天那一支，夜里**不卖**。
   与 `collect` 同源，**文档没明说，是策略侧的选择**。

---

## 第 23 步：日志措辞与版面（用户手改 + 收尾）

**目标** 用户手动改了日志的措辞与几处版面，本步把**改动的后果**补齐：让代码、日志、文档、用例
四边重新对齐。**策略与响应报文一个字节没动**。

**产出**
- **日志标签统一成 `【…】：`**（用户手改）：`【判题器报错】：` / `【SOP 更新】：`。
- **任务行与沙盒行搬到 `planner.task_channel`**（用户决定"留在 planner"）：它们打的三样字段
  （`phase_task` / `llm_resp` / `cmd_result`）**只在那个函数的作用域里**。三个后果逐一补齐：
  ⚠️ **必须写在早返回之前** —— 原来的改动插在 `if not turn.phase_task … return` **之后**，
  于是"任务刚结束、`phase_task` 已空但 `llm_resp` 还有值"那一回合（**唯一一次能看见判题器最后
  答了什么的机会**）永远打不出来；⚠️ **`assertLogs("coregeek.app")` 看不见它了** ——
  这是本项目**第二条不在 `app` 名下的日志**，三条用例改成收 root（与第 18 步 SOP 那条同一个坑）；
  **`prompt` 那一格留在 `app._log`**（`【本轮提问】：`）—— `task_channel` 拿到的是返回值，
  它当时还没有这个值，`LOG_PROMPT_MAX` 也就不用搬出 `app`（避免 `planner → app` 反向边）。
- **摘要去掉块间空行**（用户手改）：四块各占一行。⚠️ 手改时【可接任务点】被拼进了【机器】那一行
  （两个 f-string 隐式拼接）—— 已拆回独立一行。
- **地图去掉行号槽与两行数字标尺**（用户手改），换成上下各一行 `—` 边框、每行 `│`…`|`。
  两条守门用例跟着改：`test_render_shape` 的下标全部重算（列 −4、行 −1）；
  `test_render_labels_every_row_with_its_y` 失去行号这个抓手 ⇒ 换成**斜线地图**
  （第 y 行只有 `(y,y)` 是矿 ⇒ 矿的列位置就是 y），比数行号更强。
- **字节预算重测**（三处同步：`app._log` 的 docstring / `CLAUDE.md` 硬约束 5 / 用例）：
  干净回合 44 行 / 2555；有回执 46 / 2637；提问那一轮 69 / 6170；回灌那一轮 70 / 8572；
  **最坏 71 行 / 8861 字节**。**净减 2 行、字节反而 +138**（空行是 1 字节，而 `—` 是 3 字节/字）。
  上限仍是 9300，**余量只剩 439**。
  量法：`assertLogs(level="INFO")` 收 root，累加 `len(r.getMessage().encode("utf-8"))`，
  物理行 = 每条 `1 + 消息里的换行数`（首行带时间戳前缀）。

验证：214 条全绿。**同一局面在新旧两版上各量一遍**（`git worktree` 取 HEAD 的代码）：
最坏那一格旧版 8723 / 71 行、新版 8861 / 71 行；干净回合旧版 2481 / 46 行、新版 2555 / 44 行。
**旧版能量出 `CLAUDE.md` 里记的 8723 ⇒ 量法可信**，这不是"换个脚本就换个数"。
**反向验证**：把 `planner` 的任务行挪回早返回之后 ⇒ 实测挂在 `assertIn("【本轮任务】：无", task)`
（`AssertionError: '【本轮任务】：无' not found in ''`），即"任务刚结束那一回合"整块日志消失。
⚠️ **第一次写这个验证脚本时把新旧顺序写反了，脚本什么也没改、用例当然"过了"**
—— **反向验证本身也要证伪**（判据：脚本得打印出"真的换了"）。
真服务一局：响应与第 18 步逐字一致，日志里摘要四行、`【图例】：`、地图 34 行（`—` 边框）。

**仍生效的已知不确定性**
1. **地图右边框是 ASCII `|`、左边框是全角 `│`**（用户手改的原文如此）。**看着像笔误**，
   但本步不擅自改 —— 它不影响任何断言（用例只钉左边框、行长与标尺宽度）。若确认是笔误，
   改成 `│` 只是 1 字符的事。
2. **`prompt` 现在单占一条记录**（原来与题目、回复挤在同一条里）：多一条时间戳前缀，
   但换来"提问行可以单独 grep"。**没有实测过真机上哪种更顺手。**
3. **上限 9300 的余量只剩 439**：任何一次"顺手再多打一个字段"都可能顶破它。
   若本上限开始频繁报红，正确的动作是**重新钉上限并说明理由**，不是把某块日志删掉
   （第 22 步的教训：任务行 / 沙盒行是黑盒下唯一的观察窗）。

---

## 第 24 步：武器落点改成"后列上角 + 前排两角"（防操作者互堵）

**目标** 用户拍板：三座塔放在**基地前的两个角 + 基地后面的上角**，要求"不会出现操作者
堵了另一个操作者"。对照几何（`map.py`：**y 向上**，原点在左下）⇒ 前排两角不变
（`(12,22)/(12,25)`），**后列从贴基地下沿的 `(9,23)` 挪到上角 `(9,25)`**。

**产出** `grid.weapon_sites` 一行 diff；`WEAPONS_BY_SITE` 配对不动（后列仍最远离机器人
⇒ 火箭）。死锁的机理（逐格推过）：武器环 12 格被三座炮占掉 3 格后，剩下的 9 格是一条
**走廊**，门（背面不砌的那 6 格）是唯一入口；旧阵后列 `(9,23)` 的环内站位含
`(9,22)/(10,22)`——**正在底行路线**（门 → (9,22) → (10,22) → (11,22)，通往前排两角）
上，后列操作者站上去 + 前上角操作者站 `(12,24)` ⇒ 前下角的站位 `(11,22)/(12,23)`
成孤岛。新阵后列在 `(9,25)`：环内站位只剩 `(9,24)/(10,25)`（**全在顶线一侧**），
底行路线谁也压不着；且武器贴门柱 `(8,24)/(8,25)/(8,26)`，**从外面回来的操作者 BFS
停在门柱上、根本不进走廊**。新用例 `test_the_rear_sites_operator_stays_off_the_bottom_route`
钉住"环内站位 ∩ 底行路线 = ∅"+"门柱可操"两条；反向验证过：旧阵 `(9,23)` 在这条
用例上挂（站位 `{(9,22),(9,24),(10,22)}` 压住底行）。`BuildWeaponTest` 的夹具跟着挪
（工人改站 `(8,25)` 贴新后列、挡格用例的墙改放 `(9,25)`）。

验证：215 条全绿（新增 1 条；期望值用例改 2 处、夹具 2 处）。

**仍生效的已知不确定性**
1. **顶线一侧仍有一个理论死锁**：后列操作者若从**走廊里面**贴炮（白天被关在环内的工人
   夜里站 `(9,24)`），加上前下角操作者站 `(12,23)`（而非 `(11,22)`），前上角照样够不着。
   需要两个不常见的站位同时成立，概率比旧阵低得多；`_defend` 的"贴住就认领、不换炮"
   让它一旦发生就整夜不散。首场夜里若见某座炮整晚无人操，先查这两个站位。

---

## 第 25 步：任务内全量会话上下文（`Context`）

**目标** 用户拍板：agent 太薄弱，要做上下文管理 —— **同一个 task 同一个上下文**，新建 `Context`
类放独立文件，管理 system prompt / user / assistant 等完整会话信息，**先不压缩、全量**，
注入 Agent。设计先行呈报（两条决策点均按推荐落定：无新内容重问轮追加「请继续。」；
消息标题 `【题目】`/`【你的回复】`）。

**产出** 新叶子 `agent/context.py`（`Context`：`task` 身份 + 消息表 + `feed`/`nudge`/`hear`/`render`，
零包内 import）。`Agent.chat` 变**编排**：题目变了 ⇒ 新会话（构造即问：首条 user = `【题目】`）；
回灌轮 `feed`（两个标题逐字沿用旧模板）；无新内容的重问轮 `nudge`（`Context.NUDGE` =
「请继续。」—— 判题器 LLM 是黑盒，会话停在它自己的输出上是个含糊指令）；system（四段模板）
**每轮现刷**（SOP 是活的，③′ 的回执不能冻在构造时）。新增 `Agent.hear`：`task_channel`
每回合把 `llm_resp` 记进会话 —— **发命令（③）/交答案（⑤）那轮没有 prompt 也得记**，否则回灌那轮
LLM 的命令凭空消失；**粘住去重**（与最后一条 assistant 相同 ⇒ 跳过，`llmResp` 按粘住设计的再利用）。
`chat.py` 的自由函数 `chat()` 删除（无第二调用方），`PROMPT` 只剩四段 header，尾巴由
`Context.render` 接 `# 对话记录`；三个谓词一字未动。**判据链 ①–⑥ 与三个 `AGENT.chat` 调用点
一字未动**（planner 侧只多了 `AGENT.hear(reply)` 一行）。`reset` 清两处状态（SOP + 会话）。
渲染是**拼接不走 `str.format`** ⇒ 消息正文里的 `{}` 永远安全（原 `chat()` 的同一条铁律平移）。

**会话记原文、纠错块记解包件**：prompt 里从此有两份"它说过的话" —— `【你的回复】` 收整段原文
（它真说过的话），纠错块收 `answer_of` 解包后的（交上去的那份）；e2e 第 ⑤ 步的
`assertNotIn("<answer>…")` 相应翻成"两份各自钉"。

验证（TDD：先挂后写，两轮 RED）：第一轮 6 个行为用例对着旧代码挂（4 个 `hear` 缺失的
AttributeError + e2e/`task_channel` 的会话断言）；第二轮 `ModuleNotFoundError`（模块缺失）。
GREEN 后 **227 条全绿**（新增 `ContextTest` 7 条 + `ChatPromptTest` 5 条 + `task_channel` 1 条；
`chat()` 直调的 3 处改写到新 API；删 1 条对象消失的用例 `test_the_assembly_needs_sop_and_tool_desc`）。
**字节预算重测**（同一脚本在 HEAD worktree 的旧版上能量出第 23 步记的 6170/8861 ⇒ 量法可信）：
带提问的回合 +2~3 行（`# 对话记录` 标题占的物理行），最坏 **75 行 / 8944 字节**（上限 9300、
余量 356；字节反而略降）。`app._log` docstring / `CLAUDE.md` 硬约束 5 / 预算用例三处已同步。
真服务一局（`run.sh 18085` + 样例 payload 四连发）：① 首问 = 四段 header + `# 对话记录` +
`【题目】`；② 命令进 `executeCmd`、无 prompt；③ 回灌轮的 prompt 里**有它自己的工具调用**
（发命令那轮记下的）、回复只出现一次（粘住去重）、结果块全文在；④ 纠错轮**会话收原文**
（`【你的回复】<answer>晴 26 度</answer>`）、纠错块骂**解包件**（`晴 26 度\n请重新作答。`）、
`submitAnswer` 照发 —— 判据链与两条通道的互斥在会话化之后原样成立。

**仍生效的已知不确定性**
1. **prompt 无上界增长（用户拍板"先不压缩"）**：极端 = 多轮 64KB 沙盒回执全量进 prompt ⇒
   响应体巨大、判题器侧 LLM 推理变慢 ⇒ **响应超时是红线第一条**；判题器对 `prompt` 字段的
   体量上限未知。压缩是既定下一步（滑动窗口 / 摘要）。
2. **「请继续。」与「# 对话记录」的措辞都没实测过**：判题器 LLM 对"会话停在它自己的输出上
   + 一句请继续"会怎么接，纯推断。首轮实盘若见答非所问/复读，先改这两处措辞。
3. **同文任务续会话**：冷却后同一道题再现 ⇒ 续上旧会话（设计如此：它记得试过什么）。
   若实盘发现"同一文本其实是新的一道题"，会话会被旧尝试污染 —— 到时按 `taskPosition`
   或 `playerTasks` 的其他字段细分身份。
4. **`llmResp` 粘住只是推断**：去重靠"与最后一条 assistant 相同"。若判题器其实**每回合清空**
   `llmResp`（不粘），去重只是白设一道（无害）；若它粘住的时点与推断不同，极端情况下
   会漏记一条真回复（同文、且恰在粘住窗口内）。

---

## 第 26 步：日志策略大改（用户手改 + 收尾）

**目标** 用户手改并拍板：① 局面日志**只留摘要**（图例与整张地图退出）；② 两个上限一并放宽
（`LOG_TEXT_MAX` 400 → **40000**、`LOG_PROMPT_MAX` 1000 → **100000**，中途又改了一次）——
**日志基本不截**，"日志长度不能是数据相关的量"那条旧原则让位于"观察优先"。
本步把后果补齐：用例、三处字节表、文档。

**产出** `app.py`：去掉 `map_legend` 的死 import、`_log` docstring 重写（新表：干净 7 行/506B、
有回执 9/599、提问(400字) 34/4140、**顶格 ≈605000** —— 题目/回复/沙盒各 40000 字全量过，
提问行 100000 上限也基本不截）。用例五条重写：地图/图例的形状断言换成"确实不在了"
（`assertNotIn`）；9000 字答案与沙盒输出**原文全量**进日志 + 过 40000 才截留痕；提问行的
上限用例参数化（跟着 `LOG_PROMPT_MAX` 走，两度改值都没挂）；`test_the_worst_round_stays_under_the_budget`
（≤9300）撤销，换成 `test_a_clean_round_stays_small`（干净回合 < 1500B 的**结构性**守卫）。
`Map.render()`/`LEGEND` 保留（只剩用例在用，要不要删留给以后）。

验证：227 条全绿（重写 5 条，总数不变）。

**仍生效的已知不确定性**
1. **顶格回合一回合写满 64KB 管道**（≈605KB ≫ 64KB）：判题器若不读 stdout，一次大字段回合
   就阻塞到响应超时 = 红线第一条。**用户拍板接受**（观察优先）。首场若出现不明"响应超时"，
   第一嫌疑是它；回退动作 = 调小两个常量（`utils.LOG_TEXT_MAX` / `app.LOG_PROMPT_MAX`）。
2. **`render()`/`LEGEND` 成为纯测试代码**：生产路径不再调用。留着是给"地图要回日志"留门；
   若确认不要，删除时连带 `MapTest` 的形状用例。

（原第 3 条"`app.py` 顶部那对模块级空串"已了结：那是"全局变量传 prompt/executeCmd"的
半成品想法，讨论后拍板**维持返回值传递**并删除——互斥由 `task_channel` 单一返回点保证，
不引入隐式的"置空→写入→读取"时间协议；模块级可变量也是第 19 步收敛掉的东西。）

---

## 第 27 步：武器打击改"最大伤害落点"（撤掉补刀残血）

**目标** 用户改方针：武器落点按**最大伤害**算、不是挑残血敌人；目标是**打死所有机器人**
（第 10 步的"补刀优先 = 集火"就此作废）。

**产出** `planner`：L1 伤害常量（加特林 10/颗、电磁能量 10、火箭中心 20 + 溅射 10；
任务书 §4.5.1 表格 + §4.5.4 补充说明）。`_fire` 分派两种选法：**火箭** `_rocket_site` ——
导弹指哪打哪 ⇒ **全场算账**：候选 = 机器人格及其 8 邻格（越界格不进候选——红线不让赌）、
落点须在射程内（**溅射可以够到射程之外的机器人**），评分 = Σ min(伤害, 剩余血)，
并列取坐标序最小；**加特林/电磁** `_beam_site` —— 弹道武器 ⇒ 落点打在机器人身上
（终点必在弹道 ⇒ 必命中），取**有效伤害** = min(伤害, 剩余血) 最高的、并列打近的。
**`assigned` 记账**（`plan` 建表、逐炮传递）：同回合先开火的炮把估计伤害记在机器人身上、
后开的按剩余血算 ⇒ 两座炮不挤同一个将死的目标。机器人血量 40/60/500/800 全 ≥ 20
（§4.7.2）⇒ L1 对满血机器人不可能过量，伤害的差别只在被打残之后。

验证（TDD）：5 条新方针用例先在旧"血最少"代码上挂、实现后绿；重写 2 条旧方针用例
（"挑血最少"→"伤害等额打近的"；"先射程过滤"那条只改措辞）。**231 条全绿**。
真服务一局：样例（夜里、三炮、场上有机器人）—— 每座有人的炮各发一条 `attack`，
落点全在机器人身上/簇里，key 是武器 id、`controllerId` 是角色 id，报文形状与实证一致。

**仍生效的已知不确定性**
1. **弹道是否被墙挡没有明写**：任务书只说火箭"不会被阻挡"，对加特林/电磁只说"会产生
   攻击路径"。若实测子弹/能量被围墙挡住，武器根本打不出盒子 —— 第 24 步的落点、
   第 17 步的墙整个防守阵形都要重想。首场夜里看 `attack` 落点与机器人掉血对不对得上。
2. **加特林的实际命中 = 弹道上最近的那台**，记账按"终点那台"估 —— 有偏差，代价是
   次优（不会非法）；弹道画法（Bresenham 变体）也没建模，只影响"途中谁先接住"的估计。
3. **L2/L3 的多目标没做**（加特林 90° 锥、多枚导弹叠加、能量 20/30 的穿透）：升级线
   没做 ⇒ 恒 L1；做升级线时这里要跟着扩。

---

## 第 28 步：prompt 换成标准 messages JSON

**目标** 用户实测：第 25 步那版自造的文本版式（四段 header + `# 对话记录` + 标题块）
**效果非常差**。改用**标准 chat 格式**：`prompt` 字段（接口文档标 String）装
`[{"role":"system"/"user"/"assistant","content":…}]` 的紧凑 `json.dumps` 串。

**产出** 只动 `Context.render`：消息模型、判据链、粘住去重、system 每轮现刷全部不变。
首条 user = 题目原文（**删掉 `【题目】` 包装**——结构由 role 表达）、assistant = 回复原文
（**删掉 `【你的回复】` 包装**）；回灌轮的两个标题留在 content 里当内容标签（沙盒输出是
任意文本，没标签分不清哪段是什么）；`json.dumps`/`loads` 负责转义与还原（`{}` 天然无害）。
`chat.py` 的 `PROMPT` 从"整份 prompt"降为 **system 消息的 content**。

验证（TDD）：15 条格式用例先挂（14 个 `json.loads` 解析错误 + 1 个前缀失败）后绿；
断言全部改为**解析后比结构**（role 序列、content 原文），比数文本子串更强。
**231 条全绿**。提问轮日志 34 行 → **9 行**（prompt 是单行 JSON，换行全被转义），
顶格 605072 → 605150 字节 / 11 行；`app._log` docstring 与 `CLAUDE.md` 硬约束 5 已同步。
真服务一局见下。

**仍生效的已知不确定性**
1. **判题器认不认 messages JSON 是第 28 步的赌注**：文档只说 `prompt` 是 String
   （"提交给 LLM 的 prompt 内容"），没规定格式。用户的判断依据是"文本版式实测效果
   非常差" ⇒ 标准格式大概率被解析成 chat messages。若实盘仍答非所问，下一个候选是
   纯自然语言模板（无结构标记）。
2. **content 里的两个回灌标题（`【上一条命令的执行结果（原文）】`/`【你上一次提交的
   答案被判定为不正确】`）沿用了文本版式的措辞**，在 chat 格式下是否最优未实测。

---

## 第 29 步：经济线四件套（修闲置 / 顺路卖 / 开拓者卖 / 升级券）

**目标** 用户四条指令：① 工人挖好石头后闲置 —— 夜晚前**就近挖矿**，机器人到进攻范围前
回得到武器前就行；② 规划路线靠近小贩就**顺路卖矿**；③ 任务点全空时**开拓者卖矿**；
④ 买得起**武器升级券就优先升级**，优先级由我判断（群体打击口径）。

**产出**
- **修闲置**：`_mine_spare_ore` 重构 —— 先按"走得到 + 回得来"筛**可行矿**（回程 = 最近
  武器位，没有武器才用基地），再按价挑。旧版按价挑了最贵的、发现走不回来就整段放弃，
  这正是"挖好石头在家等着"的根源。
- **顺路卖矿 `_detour_sell`**：去矿路上绕去小贩 ≤ `DETOUR_MAX`(2) 格 ⇒ 先绕；贴上小贩
  的那回合链路里更早的 `_sell_ore` 自然出手（贴着跳过够本门）。
- **开拓者卖矿**：白天任务点全空（冷却/做完）⇒ `_sell_ore`（`sell` 可用角色是全部，
  `_sell_ore` 的 role 类型放宽到 BaseRole）。⚠️ **规则限制**：`collect` 仅工人、没有转移
  物品的指令 ⇒ 开拓者背包通常没矿 —— 结构留位，等任务/宝藏给可卖物才跑得起来。
- **升级券线**：`actions.Buy`（name+num，全部角色）/`actions.Use`（name+targetPos，券须贴
  目标建筑，L292；已满级不生效不消耗 L293-294 ⇒ 发错不碰红线）；`Weapon.level`（缺失按
  1，别给 -1）、`Turn.shop_prices`（顶层 `weaponShopList`，样例实证 100/150，不写死）、
  `Map.shops`（zones 的 weaponShop 格子）。`_upgrade_line` **无状态**编排：拿没拿券看背包
  （跨夜保留），跑腿者 = 持券工人 / 名册第一个工人，整趟（商店→武器+两动作）来得及才
  出发，只在白天跑（夜里 `_defend` 接管）；持券阶段终点即炮位。
- **优先链（我判的，用户授权）**：`UPGRADE_CHAIN` = gatling→2 → rocket→2 → gatling→3 →
  railgun→2 → rocket→3 → railgun→3。**加特林 > 火箭 > 电磁**：加特林 +1 弹 = 每回合 +10、
  无冷却、弹道必命中、双弹分打两台（90° 锥）、射程 +2 更早接敌；火箭 +1 枚对簇 +~30/齐射
  但 3 回合冷却一夜只 ~20 轮齐射、依赖扎堆；电磁单目标、能量 10→20 对满血机器人（≥40）
  不穿透。
- **顺序**（`_build_walls` 砌完那一支）：先卖（卖来的钱补券的差价）→ 再升级 → 后采矿。
- 摘要的武器行打 `L{level}`（"升级成没成"只有日志能回答）。

验证（TDD）：RED 21 条 → **249 条全绿**（+18：动作 3、解析 3、SpareOre 2、SellOre 1、
`UpgradeLineTest` 9）。夹具两坑自记：① 工人站在墙格上会被 `_ring` 的"自己人算路过"
例外复活成待砌格（走进砌墙支）—— 夹具别把角色放墙上；② 盒内的工人去东侧目标第一步
朝西（穿门绕行是正确行为），断言"靠近目标"得把角色放盒外。**另：金币在
`teamOur.goldNum`（不是顶层 `gold`）—— 冒烟脚本设错键、行为其实全对**。
真服务一局（墙补齐 + goldNum=100）：贴商店 ⇒ `{"action":"buy","name":
"WeaponUpgradeVoucher1","num":1}`；持券贴加特林 ⇒ `{"action":"use","name":…,
"targetPos":[武器位]}` —— 两个报文出口实证。
日志预算重测（摘要 +`L1`×3）：干净 512B / 提问 4322B / 顶格 ≈605KB，三处已同步。

**顺带（用户手改 `chat.py` 的 system 模板与 `agent.py` 的工具描述，本步收尾）**：
`# Agent定位` 措辞改成"自主任务执行Agent…将经验沉淀为SOP"；`<tool>` 的输出示例改成
**多行块**（单行 `executeCmd` 示例保留）；两个工具的描述精简、加换行。`tool_of` 对
两种写法都解析得了（`strip` 去首尾空白）；`test_both_output_shapes_are_shown_verbatim`
改成逐字钉新形状，提问行用例的前缀断言跟着模板开头的换行改。日志预算重测（模板变更后）：
提问 4288B / 顶格 ≈605KB。

**仍生效的已知不确定性**
1. **`buy`/`use` 没有实证报文**（`docs/response.txt` 只有 move/build/remove），形状从接口
   文档 §2.2 推 —— 与 `sell` 同一类风险：写错 = 一次"指令非法"（红线）。回退只需改
   `actions.Buy/Use.to_wire`。
2. **升级优先级是我的判断，未实测**：判据是群体打击的理论账（每回合期望伤害），实盘
   若发现火箭的溅射在真实机器人密度下更值钱（或电磁的穿透在残血局更强），调
   `UPGRADE_CHAIN` 一处即可。
3. **跑腿期间工人不采矿**（用户拍板"优先升级"）：商店离基地远时（样例 13 格）一趟
   ~28 回合，经济上是否值得换 +10/回合 —— 首场看金币曲线再定。
4. **开拓者卖矿线今天基本空转**（规则限制，见上）—— 若实盘确认它永远没货，可删掉
   那一支（一处分支）。

---

## 第 30 步：工具协议参数泛化（0 参 / 1 参 / 多参）

**目标** 用户指出：工具可能**没有参数**、也可能有**多个参数** —— 而 `tool_desc`/`tool_call`
硬编码了"恰好一个字符串参数"。要求想个方案修正。

**方案（要点：值永远是原文，不上 JSON）**——`executeCmd` 的参数是任意 shell/python 文本，
JSON 化会让 LLM 背上转义地狱（`python -c "print(1+1)"` 要写成
`{"cmd":"python -c \"print(1+1)\""}`），那是主路径的倒退。所以扩展现有 XML 形状：

- **协议**：`<tool_param>` 可重复，每块可带 `name="参数名"`（单双引号都认）；多参数工具
  每块带 name；无参数工具不写 `tool_param`。prompt 的「输出格式」加一句规则，
  示例（单参数无名形状，用户手调过的）原样保留。
- **解析 `tool_of`**：返回 `(名, [(参数名|None, 原文), …])` —— 带名按名收，无名留 `None`；
  **旧裸形状 `<tool>cmd</tool>` 落成 `(None, 正文)` 的位置参数**，与无名参数同一个机制
  （兼容层不再是一条单独规则）；**只有参数没名字 ⇒ None（不猜）**；**只有名字没参数
  从"畸形"改成合法形状** —— 那可能是无参数工具的调用，放行与否由调度按声明判。
- **调度 `tool_call`**：注册表给每个工具声明**参数表** `((参数名, 用途), …)` —— **一表两用**：
  `tool_desc` 生成 `参数 名：用途` 行（无参数打 `（无参数）`），`tool_call` 按它调度：
  无名参数**位置填充**（单参工具的无名形状、旧裸形状都是特例）、带名按名对、认不出的
  名字忽略（宽容）、声明的参数**一个不少且非空**才 `impl(**resolved)`。参数说明从描述
  散文挪进结构 —— 加工具只改注册表一处。
- **分界**：`chat.py` 只管语法（块与属性），**不认识工具的声明**（参数表在 Agent 上）
  —— 位置填充因此放在 `tool_call`。

验证（TDD）：RED 22 条 → **260 条全绿**（解析组重写 + 调度组重写 + 新增 0 参/双参/
编造名/形状坏损用例；SOP 保全断言保留——空白参数在闸门处挡下、清不掉沉淀）。
真服务四形状全链路：带名参数 ⇒ `executeCmd:'ls -la'`；无名（位置填充）⇒ `'pwd'`；
旧裸形状 ⇒ `'whoami'`；编造参数名 ⇒ 命令空 + 重问。日志预算重测（prompt +1 句、
工具表重构）：提问 4541B / 顶格 ≈605KB，三处已同步。

**仍生效的已知不确定性**
1. **判题器 LLM 会不会按带 `name=` 的形状回**仍是黑盒（与整个工具协议同一条风险）——
   但解析对带名/无名/旧裸全兼容，LLM 选哪种都能走通；最坏情形只是多参数工具
   （现在还没有）需要 prompt 措辞再调。

---

## 第 31 步：把当前对战策略梳理成导读（`docs/design/strategy.md`）

**目标** 用户要求：把 `src/coregeek/game/` 里**现在真正在跑**的策略逻辑梳理出来，放在
`docs/design/strategy.md`（该文件此前是上一版策略稿，正文已从工作区删除）。

**产出** 新建 `docs/design/strategy.md`（8 节）：0 目标与红线 / 1 每回合决策骨架（五条判据链
+ 四个记账集合）/ 2 白天工人（建武器 → 砌墙 → 卖 → 升级 → 采，含 `_sell_ore` 四道门与
`_upgrade_line` 的券线）/ 3 白天开拓者 / 4 任务线（判据链 ①–⑥ + 会话 Context）/ 5 夜里
（`_defend` + 最大伤害落点）/ 6 几何与静态阵形 / 7 旋钮表 / 8 薄弱处。

定位写死：**代码是唯一权威**，本文只做导读与"为什么"；不与 `code-task.md`（迭代留痕）、
`task-analysis.md`（规则整理）、`策略指导.md`（用户方向）三份重复。凡"仍生效的已知不确定性"
只指向文末那张表，不另抄一份。

验证：逐条对着代码核常量与判据 —— `WEAPONS_BY_SITE` 与 `weapon_sites()` 的下标对应、
`_sell_ore` 四道门的边界（够本门含"贴着就免"）、`_stones_to_mine` 的算式、
`_upgrade_target` 的 `level == want - 1`、`_beam_site` / `_rocket_site` 的取极值口径、
`_trapped` 里 `r.pos in box` 那一半。纯文档，无代码改动、无用例改动。

**仍生效的已知不确定性**
1. **打击侧没跟上等级** —— 第 29 步把 L2/L3 变成可达之后留下的缺口，梳理时才显形：
   `Attack.to_wire` 恒发**一个** `targetPos`，而任务书约定"长度 = 武器当前等级"
   ⇒ L2/L3 的炮只报一个落点；**发少了算"指令非法"（红线）还是"执行失败"文档没写**。
   同一条缺口还有一半：`GATLING_SHOT` / `RAILGUN_ENERGY` / `ROCKET_CENTER` / `ROCKET_SPLASH`
   没跟 `Weapon.level` 联动（那半只影响次优）。见下表第 23 项。

---

## 第 32 步：任务 Agent 两处优化（省一回合 / 工具描述换块格式）

**目标** 用户指出 `src/coregeek/agent/` 的两个问题：① **沉淀了 SOP 之后还要再跟 LLM 交互一次
才能拿到答案**，白花一回合（任务得分 `5 × 标准回合数 / (完成回合 − 接取回合)`，分母就是回合数）；
② **工具描述太简陋**，每个工具要写成 `## 名 / Description: / Params: / - 名: 用途` 的块。

**产出**

① **省一回合 = prompt 措辞改动，代码零改动。** 诊断先做实：`AGENT.reset()` 后拿两个 `Turn`
跑对照 —— `llmResp` 只调 `SOP2Prompt` ⇒ `answer_of=''`、走判据 ⑥ 重问、`plan={}`（**这一回合白花**）；
`llmResp` = SOP 调用 + `<answer>晴</answer>` ⇒ `answer_of='晴'`、判据 ⑤、**不重问**，同时
`plan` 里开拓者已经 `submitAnswer`。**代码侧早就支持同回合既沉淀又作答**：`answer_of` 先认
`<answer>`（它落在 `<tool>` 块之外，不干扰 `tool_of` 只取第一块），判据 ⑤ 与 `_answer_task`
用的是同一个谓词（第 11 步"该提交什么与该骂什么是同一个谓词"顺带带来的能力）。
拦路的只有 `PROMPT` 里那句"一回合只输出一样东西"和一句病句（"在完成任务的前一不沉淀成SOP"）。
改法：定位段改成"用 SOP2Prompt 沉淀下来，并与最终答案写在同一条回复里"+ 一条说明
（"它也占一回合就是白花一回合"），输出格式段补一条**同回合形状的示例**
（LLM 照抄示例的概率远高于照抄规则）。**范围仅 `SOP2Prompt`** —— 不给 `executeCmd` 开口子
（那会鼓励 LLM 在看结果之前先交一份猜测）。判据链、`tool_call`、`Context` 一个字节没动。

② **`tool_desc()` 换成块格式**（`agent.py`）：`## 名` / `Description: …` / `Params:` +
`    - 名: 用途`；无参数打 `Params: （无参数）`（不用 `- （无参数）`，那看着像多了一个叫
"（无参数）"的参数）；块间空一行。**仍由 `_tools` 那张表生成、不手写第二份** —— 手写第二份
迟早出现"prompt 里写了、代码里没有"，而那种不一致**只有实盘上 LLM 报错才看得出来**。
顺手去掉 `SOP2Prompt` 描述里的 `**整段替换**`（markdown 粗体在纯文本行里是噪音），
并在它末尾补一句"调用它的那一回合必须把答案一起写上"。

**验证**
- 用例 3 改 2 增 ⇒ **262 条全绿**。新增 `test_sinking_the_sop_rides_along_with_the_answer`
  （钉住省回合那条链路：`task_channel` 返回 `("", "")` + `AGENT.sop` 已更新 + `plan` 里
  `submitAnswer`）与 `test_the_sop_round_must_carry_the_answer`（prompt 里有那条规则与示例）。
  **反向验证**：把 `answer_of` 里的 `looks_like_tool` 判据提到最前 ⇒ 新用例挂（说明它真的在守）。
- **真服务两局面打穿**（`bash run.sh 18085` + `curl`）：同轮沉淀+作答那局响应里
  `roleCommandMap` 有 `10011 submitAnswer`、顶层 `prompt` 是 `""`；另一局返回的 `prompt`
  字符串里 `# 可使用的工具` 段是新的块格式。
- **日志预算重测**（改 prompt 会动提问轮的字节）：提问那轮（题 400 字）**4541 → 5630 字节**
  （+1089 = system 段长大的那部分），行数不变 9；顶格那行 **≈605000 → ≈603391**
  （11 行不变）—— 提问行钉在 `LOG_PROMPT_MAX`=100000 **字**的裁剪上，system 段多占的字把
  尾部等量挤掉，字节数基本持平。**量法与两个坑写进了 `app._log` 的 docstring**：
  顶格那行的 `llmResp` 必须是**工具形状**，写成答案形状会被 `_answer_task` 抄进
  `submitAnswer` 的 `taskAnswer` ⇒ 动作行凭空多 120KB（量出来 ≈723000，不是顶格的真实值）。
  ⚠️ 顺带**改正**了表里第 7 项的 `≈363KB`（与表不符，表为准）。

**仍生效的已知不确定性**
1. **"沉淀与作答同轮"只靠 prompt 措辞，实盘未验** —— 措辞历来是用户手改的地方，也是
   第 2 项（答案格式未知）那类风险的同一形态。**坏方向是安全的**：LLM 若照旧只调
   `SOP2Prompt`，走 ③′ → ⑥ 重问，白花的那一回合与改动前一样，**不碰红线**（不比现状更差）。
2. **顶格那行仍 ≈603KB** ⇒ 悬着表第 7 项（判题器不读 stdout 则一回合写满 64KB 管道 ⇒
   响应超时）**没有被这次改动缓解**，仍是"已知并接受"。
3. **块格式对调用正确率的影响未实测** —— `Params:` 块比旧的一行 `参数 名：用途` 长，
   换来的是结构清楚；判题器的 LLM 认不认这种版式只有实盘能答（协议形状本身是第 1 项那类风险）。

---

## 第 33 步：门收窄到 2 格 + 正面昼开夜封 + 全部距离换 BFS + 收工闸门

**目标** 用户从**实盘日志**定位的三个夜间防守问题（第 33 步的三条里 ③ 本步不做）：

| # | 现象 | 代码根因（已定位到行） | 用户决定 |
|---|---|---|---|
| ① | 只做上下前防御 ⇒ 机器人从**后方涌入**、快速破防而入 | 盒子唯一开口 = **背面整列 6 格**（`wall_cells` 只砌 14 格），6 路并行入口 | 门收窄到背面**中间 2 格**；正面开一个口 `(13,24)` **白天通行、天黑前砌上封死**；每工人保底留 1 块石头 |
| ② | 机器人已到跟前，角色还没走到武器旁 ⇒ 白丢开局几回合 | 所有**回合预算**用切比雪夫直线（`_stones_to_mine` / `_sell_ore` / `_mine_spare_ore` / `_upgrade_line`），而回炮位必须绕到背面门再横穿盒子 —— 直线 3~4 格、真实 10+ 格 | **所有距离计算改 BFS**；并加一道「白天末尾回炮位」闸门 |
| ③ | 机器人清完后角色站桩到天亮 | `_defend` 贴上炮就永不离开，夜里没有「没敌人 ⇒ 去干活」的分支 | **本步不做**（见下"不做"） |

**产出**

① **围墙 14 → 18 格，顺序改成「沿环走一圈」**（`grid.wall_cells`）：正面列 6（含封口格）+ 两侧行各 5
（含背面两角）+ 背面 2；背面中间 2 格 `(bx-2, by)` / `(bx-2, by-1)` 永不砌，新增
**`grid.door_cells`** 给的就是它们（存在的唯一理由是给"自己人算不算障碍"一个判据，见 ⑥）。
顺序 = 正面列（一端扫到另一端）→ 一侧行 → 背面（**先砌中间那 2 格把门收窄**、穿过门）→ 另一侧行
→ **封口格**。⚠️ **走一圈不是顺手排的**：同一份合成局面（`BuildWallTest`，矿在 9 步外）实测，
旧的"正面列 → 顶行 → 底行 → 背面 → 封口格"跑完 70 回合**只砌上 17 格、封口格当天没砌上**
（正面整晚开着口）；走一圈 **18 格全砌上，第 65 回合收官、手里正好剩 0 块石头**。差的就是那些
横穿盒子再折回来的来回。

② **封口格 = 正面列正中 `(front_x, by)`（左半基地 `(13,24)`），排 `wall_cells` 最后一个** ——
这就是"白天开着方便通行、天黑前砌上封死"的落地方式，**零新机制**：靠建造优先级表达
（`_ring` 保序、`_build_walls` 取 `free[0]`）。用户原话："**开了前面的口行动会方便，
但是防御的时候必须保证前面是完全封死的**"。

③ **`grid.steps_between`（新）** = 走到"贴着 goal 的一格"的 **BFS 真实步数**（绕障），与
`step_toward` 同一套终点约定与边界裁剪，差别只在返回步数而非第一步；**唯一新增的失败态是
`-1`（不可达）** —— `Pos.dist` 永远不会返回它，所以每个调用点都必须自己接住（一律按
"这趟不去了"处理：安全方向，宁可少干一件事，不可把回程预算算成负数、把人留在墙外过夜）。
模块 docstring 写明**两个距离口径**：`Pos.dist` = 切比雪夫直线（**选点**用，如挑最近的矿/炮），
`steps_between` = BFS 真实步数（**回合预算**用）。

④ **`planner` 五处距离换 BFS**：`_stones_to_mine`（赶路到矿 + 从矿回工地）、`_sell_ore`
（选小贩、到小贩、小贩到基地）、`_mine_spare_ore`（到矿 + 矿到最近炮位，**每个炮位的 BFS 提到
循环外算一次**，避免"每颗矿 × 每座炮"重复跑）、`_detour_sell`（绕路量）、`_upgrade_line`
（商店→武器那段）。`TIME_MARGIN(5)` **保留**，含义从"吸收低估的补丁"变成**真余量**。

⑤ **每工人背包保底 1 块石头**（`_best_load`）：卖石头时 `num = max(0, 石头数 − 1)` —— 收工时手里
得有货才能封 `(13,24)`（另一层用处：隔夜墙被机器人拆了，第二天立刻有料能补，不必再去矿上）。
为 0 就不发石头那条指令。`WALL_COST` 与 `_stones_to_mine` 的"还差几格墙"口径**不动**
（石头照旧只用于砌墙）。⚠️ 顺带把 **`_detour_sell` 的"有没有可卖的货"也改成问 `_best_load`**
（原来自己抄了一遍"价 > 0 且件数 > 0"）：不统一就会出现"**绕到小贩旁边才发现自己不肯卖那 1 块
石头**"—— 白绕一趟，而且同一件事的第二份口径迟早分家。守门员
`test_the_reserved_stone_is_not_worth_a_detour`（**反向验证**：把那句抄回去，用例立刻挂）。

⑥ **白天收工闸门 `_leave_for_the_post`（`plan` 白天分支最前）**：**环砌完了**（`_ring` 为空）
且回炮位的 BFS 步数 `>= day_rounds_left - 1`（或不可达）⇒ 这一天不再干活、**只发一条 `move`**
往最近的空闲炮位挪。三件事：**"环砌完了才收工"是有意的**（补墙 > 收工，与用户"防御时必须保证
前面完全封死"一致 ⇒ 第 1 天墙没砌完时闸门不生效、第 2 天起环通常是满的、全天生效）；
⚠️ **这一支绝不能复用 `_defend`** —— 它贴近炮位会调 `_fire` 发 `attack`，而 `attack` 仅黑夜
⇒ 白天发就是**非法指令**（红线）；贴住炮后 `steps == 0` ⇒ 条件不成立 ⇒ 剩余白天自然空指令待命。

⑦ ⚠️ **`_walled` 改口径 —— 对批准计划的一处偏离（必须记）**：原来"自己人**一律**算障碍"，
现在**只在门那 2 格**算。理由是那 18 格墙里**没有任何建筑** ⇒ 能堵门的只有单位，
所以"自己人站在待砌的格子上"这个问题**只在门口有意义**；环内那 16 格（基地 + 武器环）站着的
自己人是**过路的**。**触发这次偏离的是实测出来的活锁**：门从 6 格收到 2 格之后，环一闭合进出
只剩环内那几条走廊 ⇒"一个工人正好站在走廊格上"变成常事 ⇒ 走廊另一头的工人被判成"砌满就出不去"、
闸门把他送出去一格 ⇒ 下一回合他回来砌墙、两人**来回踱步、一整天不砌墙**
（合成局面实测 **16 回合 0 座墙**；用 `_emit`/`_build_walls` 打点追到 `(12,24) ↔ (13,25)` 的往复）。
**反向验证**：把 `_walled` 退回旧口径，`TwoWallBuildersTest` 立刻挂回 0 座。
已砌 4 处 "14 格" 旧字面量、`planner`/`grid` 的 docstring 一并同步。

**验证**
- 用例 **262 → 272 条全绿**。新增 `StepsBetweenTest`（4 条：贴着一格 ⇒ 0；goal 被围死 ⇒ -1；
  绕障步数 > `Pos.dist`；**门是唯一入口**——整圈含门都砌上时两个方向都 -1，只砌那 18 格时 > 0）
  与 `DayEndGateTest`（3 条：闸门恰好在 `round_no = DAY_ROUNDS - steps` 打开、早一回合关着；
  **`test_a_day_round_never_fires` = 红线守门员**，扫 `round_no ∈ {1,35,60,66,69,70}` ×
  `at ∈ {(20,24),(14,24),(10,25),(9,24),(9,26)}`，断言**一条 `attack` 都没有**；
  `test_the_gate_never_preempts_the_wall`；`SpareOreTest.test_the_reserved_stone_is_not_worth_a_detour`
  钉住 ⑤ 那处口径统一）。`SpareOreTest` / `UpgradeLineTest` 两条
  "来不及就别去"用例改成断言"不发那件事 + 这一步更靠近炮位"（闸门接管了，正是 ② 要的修法）。
  **反向验证**：关掉 `_leave_for_the_post` ⇒ 两条 `DayEndGateTest` 立刻报错。
- **真服务两个局面打穿**（`bash run.sh 18085` + `curl`，先 `netstat` 确认端口干净）：
  `roundNo=1`（白天）⇒ `10010 collect (4,24)` / `10012 move (9,17)` / `10011 move (11,11)`，
  **一条 `attack` 都没有**；`roundNo=85`（夜里）⇒ 移动向量与文档里记的 `EXPECTED_MOVES` **逐字相同**。
  响应体 217 / 213 字节。
- **三个风险的实测值**（计划要求量化，全部记在这里）：
  - **18 格砌得完吗** ⇒ **砌得完**：`BuildWallTest.test_day_one_mines_then_walls_the_whole_ring_in_order`
    的 70 回合串跑，**18/18 全砌上、最后一格在第 65 回合、石头剩 0**，且砌的顺序与 `wall_cells` 逐字一致。
  - **BFS 预算的每回合耗时** ⇒ **0.47 ms/回合**（N=200，环砌满、强制走最贵的"绕门"路径），
    红线是 5000 ms ⇒ **不做距离场缓存**（禁止冗余设计）。
  - **日志体量** ⇒ 干净回合 **512 字节 / 3 条记录**；用 `git archive HEAD` 在**改动前**的代码上量
    同一份数据，**一模一样** ⇒ 本步不碰日志（`app._log` docstring 那张表记的 514 是权威口径，
    前两格的 ±几字节随 `docs/request.txt` 当天内容漂移，是它自己写明的）。

**仍生效的已知不确定性**
1. ⚠️ **`remove`（拆墙）没实现 ⇒ "白天开口"只在第 1 天生效**：`(13,24)` 砌上就拆不掉，
   第 2 天起正面永久封死、工人一律走背面那 2 格门。**这是有意的**（通行口的主要价值在前 1 天：
   墙还在砌、人频繁进出盒子；之后人基本在盒外干活、天黑才回），但**用户需要知道**。
   要每天重开就得先做 `remove`。
2. **收工闸门完全没实测** —— `- 1` 那个余量是**拍的**（可调），"环砌完了才收工"这个取舍也没验过。
   坏方向是安全的：闸门只在白天发 `move`，走不动就是空指令，**不碰红线**。
3. **`steps_between` 的 `-1` 分支实盘没见过** —— 合成局面里造得出来（目标被围死），
   真实地图上会不会出现（例：工人被机器人堵在盒子里）不知道。所有调用点都按"这趟不去"处理，
   症状是"该干的活突然不干了"，日志上看不出来 —— **要在实盘日志里留意"某天工人完全不采石"**。
4. **门只剩 2 格 ⇒ "机器人堵在门口"的后果放大了**（与文末表第 12 项同源，但现在更尖锐）：
   早上工人出不去 / 晚上回不来都只堵在这 2 格上。夜里机器人会不会站在门格上、堵多久，**没数据**。
5. **封口格砌上的时机靠建造顺序保证、没有任何断言守"必须在白天结束前砌上"** ——
   第 65 回合是**这份合成局面**（矿 9 步外）的数字；真实地图上矿更远、或第一个白天被打断
   （工人阵亡 / 机器人挡路），封口格就可能当天砌不上、**正面整晚开着口**。
   下一场要盯日志里"第一个白天结束时砌了几格"。

---

## 第 34 步：`remove`（拆墙）—— 绕路 ≥5 或出不来就拆，当天当临时门、天黑前补上

**目标** 兑现第 33 步留下的那条死角（本文件表第 24 项）：`remove` 没实现 ⇒ 封口格 `(13,24)`
砌上就拆不掉 ⇒ **第 2 天起正面永久封死**；万一有人被关在盒子里（门被机器人堵死），**整场出不来** ——
`_trapped` 闸门是**预防**，缺的是**补救**。用户原话："提供一个 remove，当有角色出不来，
或者需要多绕 5 格以上，就拆墙重建"。四条口径用户逐一拍板：

| 问题 | 决定 |
|---|---|
| 什么时候拆 | **开洞能省下的步数 ≥ 5**（`HOLE_MIN_SAVING`） |
| 拆哪一格 | **开洞后步数最少的那一格**（逐格试算，并列取坐标序） |
| 什么时候补 | **同一天当临时门，工人天黑前补上**（窗口两个，见下） |
| 开拓者被挡 | **工人去救**（开着任务被钉死的开拓者自己动不了） |

⚠️ **探索阶段揪出一个会让整套机制静默失效的硬错误，留痕在此**：我最初想用"环上恰好一个缺口、
且它不是封口格"识别"我们开的洞"。**这条判据是错的** —— 「17 格砌好 + 1 格空着」这一个状态
**既是"刚挖的洞"也是"还差一格没砌"**，地图上逐字节同形，任何无状态判据都分不开。
它今天就会挂掉既有用例 `test_a_worker_on_the_last_cell_still_finishes_the_ring`，
真实后果更重：**那格墙白天再也不补**。修法 = **首日豁免 `_after_first_day`**
（第 1 天环在建，缺口是真的没砌；**第 2 天起环开局必然是满的 ⇒ 环上一切缺口只可能来自我们自己**）。
区分只能靠**时间**。这条豁免顺带让既有用例全部免疫（它们的 `round_no` 只有 1..70 与 85）。

**产出**

① **`protocol/actions.py` 新增 `Remove`**（`code="remove"`、`roles=WORKER`、
`to_wire → {"action","targetPos":[{x,y}]}`，**没有 `name`**）—— 照抄 `Build` 去掉 `name`。
四件权威事实：§4.4 **仅工人**、"需指定与自身距离一格内的围墙位置"（**与 `build` 同一站位契约**，
切比雪夫 ≤1）、拆后那格变**可穿越空地**（L73）、**不回收石头**（L209）⇒ 拆墙不是免费的。
**报文有实证**（`docs/response.txt:42-50`，全项目只有 `move`/`build`/`remove` 三个动作有）⇒
用例专门钉了 `"name" not in wire`（多打一个字段就是赌"指令非法"）。
⚠️ **昼夜**：任务书那一格**没写**（`build` 写了"仅白天"），项目按**保守口径只在白天发** ——
夜里不发最多少拆一次，反过来若实际禁夜就是指令非法，红线优先。昼夜门与 `build` 同路：
**不进闸门**（那是"什么时候"不是"谁"），由 `planner` 把关。

② **三个常量**（全部标注"拍的、可调"）：`HOLE_MIN_SAVING = 5`（唯一的口径旋钮）、
`HOLE_MIN_LEFT = 30`（白天剩这么多回合以上才允许拆）、`HOLE_PATCH_LEFT = 15`（剩这么多回合以内必须补）。
⚠️ **`HOLE_MIN_LEFT > HOLE_PATCH_LEFT` ⇒ 两个窗口不相交 ⇒ 一天最多拆一次** ——
这才是防"反复拆墙净亏"的真正机制（**不是**门槛 5：门槛 5 只防"开了洞也不划算"，防不住来回拆）。

③ **触发 (b)「绕路 ≥5」= `_dig`，钩在 `_step` 里**（`walk = blocked | claimed | avoid` 之后、
`step_toward` 之前）—— 一处覆盖全部差事（采矿 / 卖矿 / 顺路卖 / 升级 / 砌墙 / 回炮位），
**8~9 个调用点一个字没改**。闸门：白天 ∧ 工人 ∧ `_after_first_day` ∧ **环已砌满**
（`_ring` 为空 ⇒ 同时最多一个洞）∧ 本回合没人拆过（`claimed & set(wall)` 为空）∧
有石头 ∧ `day_rounds_left > HOLE_MIN_LEFT`。候选 = **已经贴着自身的那几格墙**（`remove` 的站位要求）。
**刻意不做"走到最优的那一格再拆"**：那会引入"在路上"的中间态（目标每回合重算 ⇒ 来回抖），
而且省下的步数没扣掉走过去的回合 ⇒ **系统性高估收益**；沿墙走的过程里这种机会很多，不必提前跑。
`now < 0`（现在压根到不了）而 `after ≥ 0` ⇒ 更该拆 —— "出不来"与"绕太远"在这一个函数里合流。

④ **触发 (a)「出不来」= `_stuck_inside` + `_rescue`**：`_stuck_inside` 用**"现在"的障碍**
（与 `_trapped` 的"假设砌满"相反：一个问将来/预防，一个问眼下/补救），且
**自己人一律不算障碍**（同事站在门格上只是路过，算成障碍会**白拆一次** —— 与第 33 步改 `_walled`
口径治的是同一个病）。`_rescue` 只让**工人**动手，开哪一格由"**开了真能让某个被困的人迈出去**、
且离救援者最近"决定。⚠️ **实测订正一处 docstring**：被关住的**工人**也走这里，而且**先**走这里
（`_rescue` 在 `plan` 里排在白天所有差事之前）；`_dig` 的 `now < 0` 是**第二道**，不是唯一一道。
**被任务钉死的开拓者**才非靠别人救不可。

⑤ **临时门 = `_build_walls` 里一行**：`if free and _door_open(turn): free = []`。
`_door_open = _after_first_day ∧ day_rounds_left > HOLE_PATCH_LEFT`。`free` 空掉之后**自然落进**
既有的"卖 → 升级 → 采闲矿"那一支（工人去干别的）⇒ **不需要单独一个"别补墙"的分支**。
补墙也不用新函数：`day_rounds_left <= HOLE_PATCH_LEFT` 时 `_door_open` 自然失效、
`_build_walls` 取 `free[0]`（= 那个洞）砌回去。**语义与封口格 `(13,24)` 完全相同**
（"白天开着通行、天黑前封死"），只是从"排在建墙顺序最后"换成了"排到时间窗最后"。
`_stones_to_mine` 的"还差几格"口径**不动** —— 那块补墙的石头当天必须照样采得到。

⑥ **`plan` 只加一支**（`leaving` 那道闸门之后、夜里分支之前）：
`if turn.is_day and _rescue(role, turn, cmds, claimed, box): continue`。
⚠️ 顺序要紧：**放人比回炮位要紧**，所以排在收工闸门之前（`_rescue` 自己带时间门）。
`_trapped` / `_walled` / `wall_cells` / `door_cells` / 封口格顺序 / 武器落点 **一字未改** ——
**闸门是预防（零成本、砌墙那一刻就避开），`remove` 是补救（1 回合 + 1 块不退的石头 + 一整天的洞），
两者并存，各管各的**（不许因为"反正能救"就把闸门拆了）。

⑦ **`grid.py` 只改注释**：`wall_cells` 里那句"`remove` 没实现 / 砌上就拆不掉"改成
"第 2 天起环开局就是满的，开口改由 `planner` 的**临时门**机制重开"。**几何与函数一个字没动。**

**验证**

- 用例 **272 → 295 条全绿**（新增 23 = `DigTest` 10 + `DoorTest` 4 + `RescueTest` 6 +
  `HoleLifecycleTest` 1 + `MoveWireTest.test_remove_wire_shape` + `GateTest.test_pioneer_cannot_remove`）。
  夹具数值全部**用一次性探针实测钉死**后才写进用例（探针已删）。
- **反向验证 8/8 全被抓住**：① `_after_first_day` 恒 False ⇒ 11 条挂；② `_door_open` 恒 False ⇒ 3 条；
  ③ `_dig` 恒 False ⇒ 6 条；④ `_rescue` 恒 False ⇒ 3 条；⑤ `_stuck_inside` 把同事算成障碍 ⇒ 1 条；
  ⑥ `HOLE_MIN_SAVING = 0` ⇒ 1 条；⑦ `HOLE_MIN_LEFT = 10` ⇒ 2 条；⑧ `HOLE_PATCH_LEFT = 69` ⇒ 2 条。
  ⚠️ 其中 **⑤ 第一版没被抓住**：用例只堵了一格门 ⇒ 盒内的人本来就走得出去 ⇒ **用例空转**。
  改成**两格门都堵**（`colleagues=self.DOOR`）后才抓住 —— 这就是"写完可疑用例要反向验证"那一条
  第 N 次救人。
- **真服务五个局面打穿**（`bash run.sh 18085`，先 `netstat` 确认端口只有一个监听者）：

  | 局面 | 结果 |
  |---|---|
  | 第 2 天白天(131) + 环满 + 工人 `(12,24)` 要出盒外采石 | `10010 remove (13,23)`；两台同事各 `move`；**一条 `attack` 都没有** |
  | 第 2 天白天 + **两格后门各被一台机器人堵死** + 工人在盒内 | `10010 remove (13,23)` **自救**（`_rescue`，没轮到差事） |
  | 第 2 天白天 + 门被堵 + **被钉在任务上的开拓者**在盒内、工人在盒外 `(7,24)` | `10010 remove (8,25)`，**这一回合工人连 `move` 都没有**（用户口径"开拓者被挡 ⇒ 工人去救"） |
  | 第 1 天白天(1) | 只有 `move`，**零 `remove`** |
  | 夜里(85) | `attack` + `move`，**零 `remove`** |

- **每回合耗时**：真服务**往返**（含 HTTP + 全部日志）30 次 ⇒ **min 14.2 / p50 33.1 / max 56.4 ms**，
  红线 5000 ms ⇒ 触发那一回多跑的 ≤8×2 次 BFS 完全在噪声里，**不做距离场缓存**。
- **日志体量不变**：本步**一行日志都没加**（`remove` 走既有的 `【动作】` 行，
  `describe` 是通用摊开、天然打得出来）。同一局面干净回合实测 **532 字节 / 7 个物理行**
  （含 banner 那条）。`test_a_clean_round_stays_small` 照旧守着。
- **第 2 天串跑**（`HoleLifecycleTest`，131..200 全 70 回合、环满、矿 `(18,24)`、工人起于 `(12,24)`）：
  **`remove` 恰好 1 条、落在 `(13,23)`**；**下一回合真的从那格里穿过去**（`log[i+1]` = `move (13,23)`）；
  第 189 回合 `build (13,23)` 补回（在 `_left ≤ HOLE_PATCH_LEFT` 内）；收工时
  **18 格环上无一缺口**；全程无 `attack`。

**仍生效的已知不确定性**

1. ⚠️ **补墙失败的路径**：工人阵亡 / `gated`（有人按"砌满"算出不去）/ 没石头 ⇒
   **夜里墙上有我们自己的一个洞**，而且**没有任何机制会在当天补救**（夜里不能 `build`）。
   日志上的症状是"**一条 `remove` 找不到对应的 `build`**"，收工时环上缺一格。
   `gated` 那道拦截是**有意保留**的（补洞可能把人封死）；白天末段它只在"有同事正站在门那 2 格上"
   时成立，而那一刻大家正在 `_leave_for_the_post` 回炮位 ⇒ 罕见但**不是不可能**。
2. ⚠️ **首日豁免的残留误判**：第 1 天**没砌完**的残局（17/18，缺口不是封口格）⇒ 次日起那个缺口
   被当成"门"、**推迟到当天末尾才补**（末尾一定会补，但**整个白天正面/侧面少一格**）。
   触发条件是"第 1 天被打断"（工人阵亡 / 机器人挡路 / 矿太远），**没数据**。
3. **三个旋钮全是拍的**：`HOLE_MIN_SAVING = 5`（用户给的口径，但它与"1 回合 ≈ 几格步"的换算没验过）、
   `HOLE_MIN_LEFT = 30`、`HOLE_PATCH_LEFT = 15`（15 回合够不够走回洞口再砌，没验过 ——
   洞口在正面、收工目标在盒内，通常顺路）。
4. **候选格不限制**（正面列也允许拆，包括封口格 `(13,24)`）：用户拍的是"逐格试算取最省的那一格"，
   限制候选等于推翻它，而且封口格本身就是用户定的"白天开口"位置。风险由补墙窗口兜底
   （天黑前封死），**残余风险 = 拆了正面列而当天没补上**（见不确定性 1）。
5. **`_dig` 的 `now < 0` 分支今天进不去，但仍然保留**：四处差事的 `_step` 之前都已经把"走不到的目标"
   滤掉了（`-1 ⇒ 这趟不去`），而"人在盒里出不来"会被 `_rescue` 先接管。留下的理由是
   **`_step` 的 `walk` 比各差事自己算的那份多了 `claimed` / `avoid` 两样** ⇒
   "差事算得通、走到 `_step` 时却不通"是可能的。删掉的代价是"工人安静地不动"（正是本步要治的症状），
   留着的代价只是一次条件判断。**别当死代码顺手删。**

---

## 第 35 步：任务 Agent 三处修正（答案走参数 / 判决回灌 / 工作流措辞）

**目标** 第 34 步收尾后我给了 Agent 系统三处优化的讨论，用户**逐条拍板**（含否掉一个方案）：

| 用户原话 | 落地口径 |
|---|---|
| 讨论 1：**还是不做独立压缩**，扩展 sop 工具一个参数 `answer`，这样能有效避免 sop 工具里面出现 `<answer></answer>` 触发错误匹配 | `SOP2Prompt(sop, answer)`；答案走**工具参数**通道，`answer_of` 参数优先。**独立压缩轮不做** |
| 讨论 2：按照你的想法修正 | ① 纠错回灌带上判题器的 `description` 原话；② `# 注意事项` → 编号 `# 工作流`（先复述规格再动手） |
| 讨论 3：**删除工具描述**，保留 prompt 中的描述 | 删掉 `executeCmd` 描述里「若无法判定文件的位置，先找到文件的位置」那句；prompt 的那一条**附上可照抄的命令范式**（用户选定） |

**① 治的是一个真实缺陷**（`answer_of` 的旧三级判据）：第 32 步让"同轮沉淀 + 作答"走
`<tool>…SOP2Prompt…</tool><answer>答案</answer>` 两个**并列**的块，而旧判据的第一级是
「回复里出现 `<answer` ⇒ 只认成对块的内容」—— **SOP 正文里几乎必然出现字面量
`<answer>…</answer>`**（它讲的就是"答案要用 `<answer>` 包"），于是扫到的是 **SOP 里那一段**，
**错答案被当成答案交上去**，而日志上完全看不出来（任务行只打原文）。把答案挪进
`<tool_param name="answer">` 之后，参数在**工具块内部**、按名字认 ⇒ 这类污染从根上消失。

**② 治的是"知道错了、不知道错在哪"**：`errorCode 2` 的 `description` 是判题器自己的原话
（黑盒里唯一能回答"哪一项不对"的东西），此前**只进日志**（`_clip(turn.errors)` 那行 INFO）。
另一处：「遵守任务书」原本只是 `# 注意事项` 里一句散文式提醒，位于 system 最末、离调用点最远。

**③ 治的是两句话打架**：`executeCmd` 的描述说"先找到文件的位置"，`# 注意事项` 说"能一条就合并"
—— **描述更具体、就在调用点上，所以它赢**，这正是用户观察到的"基本上都采用了两条命令"。

**产出**

① **`agent/chat.py`：`answer_of` 升成四级**（判据顺序是这个函数的全部内容）：

1. **完整的工具调用、且参数里有 `name="answer"` 且非空 ⇒ 返回它**（第 35 步新增，**必须压在
   `<answer>` 块扫描之前**）；
2. 否则把**第一个工具块整段挖掉**得到 `rest`，扫 `<answer` 标记（成对才作数，空/半截 ⇒ `""`）；
3. 否则**用原文 `reply` 判**"像工具调用" ⇒ `""`（⚠️ **不能用挖过的 `rest`** —— 挖完就不像了，
   会误放行 `<tool>ls</tool>` 后面那句话）；
4. 否则**原文即答案**。
   **无工具块的回复 ⇒ 第 2~4 级与第 34 步之前逐字一致**（`AnswerParseTest` 五条一条没改）。
   配套：`_block` 拆出 `_span(text, open, close) -> (起, 止) | None`（判据原文照搬，`_block` 变成薄壳）；
   新增模块常量 `_ANSWER_PARAM = "answer"`（**`chat.py` 唯一一处知道参数名的地方**，与注册表
   那条参数同名是**硬约定**，docstring 里写明了）。
   模板同步：`# 沉淀的 SOP` 之后的示例换成双参数形状；`# 注意事项` 换成 **`# 工作流`** 三条编号步骤
   （① 一条命令把任务书找出来并读完 ② 读完先把"要交什么、什么格式"抄进回复 ③ 提交前逐条核对），
   附用户选定的范式 `f=$(find / -maxdepth 4 -name '*任务书*' -print -quit 2>/dev/null); echo "FILE=$f"; cat "$f"`
   （`-maxdepth`/`2>/dev/null` 兜住沙盒 15 秒，`head` 兜住 64KB 截断）。
   ⚠️ **新措辞里一个裸 `{}` 都没有**（`format` 会把 `{}` 当占位符 ⇒ 运行期 `KeyError`
   ⇒ 整回合空指令），这条规矩因此写进了 `PROMPT` 的注释块。

② **`agent/agent.py`**：`SOP2Prompt(self, sop: str, answer: str)` —— **必需、不给默认值**
（"沉淀必须同轮作答"是**协议层的事实**，由 `tool_call` 的"声明参数一个不少且非空"兜底，
不是靠 prompt 求它一句）；参数表加 `("answer", "答案本身")`。
⚠️ **`answer` 不进 `_sop`、函数体一个字节都不用** —— 它只是"同轮作答"那条通道的**载体**，
读者是 `chat.answer_of`（从**回复原文**的参数里读）；docstring 写清了这一点，免得后来者以为漏传。
`executeCmd` 的描述**删掉第二句**，按用户口径**不补新描述**（那条要求改由 `# 工作流` 承担）。

③ **`game/planner.py`**：`retry` 从"只有答案"变成"答案 + 判题器原话"：

```python
rejected = any(e.code == 2 for e in turn.errors)          # 判据 ④ 的触发条件
why = "；".join(e.description for e in turn.errors if e.code == 2 and e.description)
retry = ""
if rejected and answer:
    retry = f"{answer}\n【判题器反馈】：{why}" if why else answer
```

⚠️ **没答案就不纠错照旧**（判据 ④ 的定义，`test_a_rejection_needs_an_answer_to_blame` 守着）；
⚠️ **判题器没给描述时退回第 34 步的行为（只骂答案）** —— 不为"少了一句话"把整段纠错吞掉：
那是判题器侧的信息缺失，不是我们该丢的信心。`Context.feed` 的模板**一个字没改**，
标签（`【判题器反馈】：`）由 planner 侧拼 —— 两个 `answer_of` 调用点仍然**同源**。

**验证**

- 用例 **295 → 301 条全绿**。改的是 5 条既有用例（`SopStateTest` 18 处直接调用补第二个实参、
  参数表那条加 `- answer: 答案本身`、SOP 示例形状、`SOP2Prompt` 走工具表那条、
  纠错块逐字断言、`HandleTest` 端到端那条），新增 6 条：`AnswerParseTest`
  （参数优先于字面量 / 半截与空参数不当内容用 / **块外 `<answer>` 兼容层不退化**）、
  `AgentToolCallTest`（**漏 `answer` ⇒ 整次调用作废且 SOP 一个字节没变**）、
  `ChatPromptTest`（`# 工作流` 与范式在、**被删的那句工具描述不在**）、
  `TaskChannelTest`（**判决原话进回灌段**）。
  ⚠️ 用例里**三处**形状钉了"参数优先"：`TaskChannelTest.test_sinking_the_sop_rides_along_with_the_answer`
  换成**两种形状各一条**（新形状 = 答案在参数里；两种形状混在一份回复里 ⇒ 认参数那份）。
- **反向验证 4/4 全被抓住**（每次改一处生产代码、跑全量、再还原，`diff` 核对还原干净）：
  ① 删掉参数优先那一级 ⇒ 3 条挂；② `answer` 改成可选（给默认值、从参数表里去掉）⇒ 3 条挂；
  ③ `retry` 不回灌判决 ⇒ 7 条挂；④ 恢复那句被删的工具描述 ⇒ 1 条挂。
  **没有出现"改了实现用例照样过"**（那种情况说明用例空转）。
- **真服务三局面打穿**（`bash run.sh 18085`，先 `netstat` 确认端口只有一个监听者；完事杀干净）：

  | 局面 | 结果 |
  |---|---|
  | 新形状沉淀轮，**SOP 正文里塞了 `<answer>假答案</answer>`** | `10011 submitAnswer 真答案`（**参数那份**）、`prompt=""`、`executeCmd=""` |
  | 同上但**漏了 `answer`** | **没有任何 `submitAnswer`**、`executeCmd=""`、`prompt` 非空（重问），且那个 SOP **没落库**（prompt 里找不到它） |
  | `llmResp="晴 26 度"` + `code 2` + description | 回灌段逐字为 `【你上一次提交的答案被判定为不正确】\n晴 26 度\n【判题器反馈】：第 3 项应为整数\n请重新作答。` |

- **日志预算重测**（`app._log` 的 docstring 那张表 + `CLAUDE.md` 硬约束 5 两处同步）：
  提问那轮 **5733 → 6020**、顶格 **603375 → ≈603662**，**两行同为 +287**（= system 段长大的
  那部分；提问行没到 `LOG_PROMPT_MAX`=100000 字的裁剪线，长多少就多多少）。干净回合 512、
  有回执 606 **两行在改动前后逐字节相同**（它们没有 prompt 行 ⇒ 模板改不到）。
  ⚠️ 量法这次做**了新旧对照**：同一脚本（`logs/measure_bytes.py`，一次性、不入库）
  在 `git worktree` 取的 `f2ef150` 代码上跑出 5733 / 603375 ⇒ 上面那两处数字可信；
  与文档旧记的 5630 / ≈603000 差的那点属于 `request.txt` 当天的内容漂移与第 33 步的改动。

**仍生效的已知不确定性**

1. ~~⚠️ **`answer` 必需 ⇒ LLM 漏写就把整次调用作废**（SOP 也落库不了…）~~ ⇒ **已被第 36 步兑现**：
   用户审阅后判定"SOP 工具应当**也是答案的生产者**"，`answer` 退出参数表（只声明 `sop`）、
   沉淀与作答分家。事后看这一版的**真实代价比这里写的更重** —— 不是"漏写 `answer`"（那还会重问），
   而是**答案写在块外 `<answer>` 时**：`answer_of` 照样取到答案 ⇒ 判据 ⑤ 命中 ⇒ **SOP 静默不落库、
   且此后不再提问**（`tool_call` 忽略认不出的参数名 ⇒ 带 `answer` 参数反而没事）。
2. **`# 工作流` 的三条与那段命令范式都是拍的措辞**，效果只能靠实盘：判据是任务行里
   **"一条 shell 被拆成两个回合"**（连续两回合各一条 `executeCmd`）有没有变少。
   范式里的路径（`-maxdepth 4` 从 `/` 开始、按名字找）也可能与真实沙盒的目录结构不符 ——
   它只是**范例**，LLM 可以自己改。
3. **判决回灌的字数没有上界**（`description` 是判题器给的、可长可短）：它进 `Context` 的
   `user` 消息 ⇒ 与第 25 步"prompt 无上界增长"同一笔账（本文件表第 19 项），不单独设限。
4. ~~**`_ANSWER_PARAM` 与注册表的参数名是"两个文件里的同一个字符串"**~~ ⇒ **已被第 36 步撤销**：
   那个常量随参数方案一起删掉了，`chat.py` **从此不认识任何参数名** —— 这条"两份字符串要一起改"
   的一致性问题**从根上消失**（代价是它也不再能替 `SOP2Prompt` 认答案，那部分改由块外
   `<answer>` 承担）。

---

## 第 36 步：SOP 工具退回单参数 + 答案只走 `<answer>`（用户作废第 35 步的方案）

**目标** 用户中途叫停第 35 步的方案，原话：

> 作废SOP参数增加的废案，我们强制要求SOP工具只有一个参数，只做流程沉淀，提交统一的用
> \<answer\>\</answer\>管理，但是SOP强制不允许里面包含 \<answer\>\</answer\> 这对串

「强制不允许」的落地方式（挖掉 / 作废整次调用）我确认过一次，用户选定 **"挖掉那一对再存"**。
三条要求各自对应一处改动：**只有一个参数** ⇒ 参数表只留 `sop`；**只做流程沉淀** ⇒ 答案不再有
任何工具侧通道；**不许含这对串** ⇒ 入库前清洗。

**为什么第 35 步那条路是死的**（事后复盘，比第 35 步当时写的更重）：`answer` 一旦进了参数表就是
声明参数 ⇒ **必需**。而 LLM 最自然的写法是块外 `<answer>`（prompt 里本来就这么教）—— 那种回复下
`answer_of` 照样取到答案 ⇒ 判据 ⑤ 命中 ⇒ **SOP 静默不落库、而且此后不再提问**（用户永远等不到
纠正的机会）。反过来，"带 `answer` 参数"那一支又要求 LLM 同时记住两套语法。**一个参数既想当
必需项、又想当可选的作答通道，这两件事互斥** —— 作废它是对的。

**产出**

① **`agent/chat.py`：`_ANSWER_PARAM` 删除**，新增 `strip_answers(text) -> (text, n)`
（`_ANSWER_RE.subn("", text)`，唯一的调用者是 `SOP2Prompt`）；`answer_of` 从四级退回**三级**
（挖掉第一个工具块 ⇒ 块外成对 `<answer>` ⇒ 原文判"像工具调用" ⇒ 原文即答案）。
**"先挖掉工具块"这一条留下来并成为主路径** —— 它本来就是那个污染缺陷的正面修法（第 35 步的
参数只是绕过它，不是解决它）。`chat.py` 从此**不认识任何参数名**，模块 docstring 那句
"五段模板 + 三个谓词 + 一处清洗"重写。
⚠️ **同一步把 `<…>` 的解析全部换成正则**：七个模块级 `re.compile`
（`_TOOL_RE` / `_NAME_RE` / `_PARAM_RE` / `_ANSWER_RE` / `_ANSWER_MARK_RE` / `_TOOL_MARK_RE` /
`_PARAM_ATTR_RE`），旧的 `_span` 与 `_block` 删除。动机是这一步要动的正是这几种标签的边界，
`str.find` + 下标算术把判据散在切片里、改一处要看三行；换成正则后"成对才作数"由模式自带
（每个模式都含 `</…>`），非贪婪停在第一对上，开标签一律 `<tool\b[^>]*>`（按 `>` 定位 ⇒
`<tool >`、带属性都不作废）。
⚠️ **换法做了差分验证**：36 个回复样本 × 三个谓词，新旧实现逐条对比输出，**2 处分歧**
（都出在开标签带空格/属性的形状上，旧实现判 None），两处都按新语义补了用例 —— 没有"顺手改语义"
被漏过去。

② **`agent/agent.py`：`SOP2Prompt(self, sop: str)`** —— 参数表退回 `(("sop", "SOP 全文"),)`，
函数体两行：

```python
sop, stripped = strip_answers(sop)
self._sop = store(self._sop, sop, stripped=stripped)
return ""
```

描述重写（"它只沉淀、不产出命令、当回合也没有回执""它不影响你作答：答案照旧写在工具块外的
`<answer>` 里，两者写在同一条回复里即可"）。⚠️ **漏写答案不再影响沉淀** —— 沉淀与作答彻底分家：
这一回合算不算作答由 `answer_of` 单独判，两条都走不通才落到判据 ⑥ 重问，而**沉淀已经落库了**。

③ **`agent/tools/sop.py`：`store(current, sop, *, stripped=0)`** —— 新增的关键字参数**只进日志**：
`describe` 在有挖掉时追加 `剔除 N 处 <answer> 段`。这一行是**唯一**能看见"LLM 又把答案格式写进
SOP"的信号；否则"收到 N 字、存了 M 字"是一笔没头没尾的账。`sop` 传进来时**已经是挖过的那份**
（`strip_answers` 在 `Agent.SOP2Prompt` 里）。

④ **`chat.py` 的 `PROMPT`**：示例从"答案塞进 `answer` 参数"换成**"工具块 + 块外 `<answer>`"两块**
（逐字给全、并说明漏了会怎样），另加一句 `sop` 的正文里**不要出现 `<answer>` 与 `</answer>` 这对
标签（讲答案格式时换个说法）。`定位`段与`例外`段同步改成"调用它的那一回合照样是你的作答回合"。

⑤ **`game/planner.py`** 只改 docstring（`_answer_task` 的判据说明改成三级），逻辑一个字节没动。

**验证**

- 用例 **301 条全绿**。删掉 3 条（参数优先 / 参数必需 / 块外兼容那三条已无对象）、新增/重写 5 条：
  `AnswerParseTest` 换成"SOP 里的字面量不是答案""块外才是唯一作数的地方"，
  `AgentToolCallTest` 换成"SOP 里含这对标签 ⇒ 入库时挖掉"（多对 / 空对都挖、**半截标记不挖**、
  日志里有 `剔除 1 处 <answer> 段`），`TaskChannelTest` 加一条端到端
  （SOP 正文塞假答案 ⇒ 交上去的是块外那份，且存下来的 SOP 是挖过的），
  `SopStateTest` 撤掉第二个实参。
- **反向验证 3/3 全被抓住**（每次退一处生产代码、跑全量、再还原，`diff` 核对还原干净）：
  ① `answer_of` 不挖工具块（退回第 34 步顺序）⇒ 污染用例挂；② `SOP2Prompt` 不清洗 ⇒ 2 条挂；
  ③ `store` 不报 `stripped` ⇒ 1 条挂。
- **真服务三局面打穿**（`logs/scenario36.py`，一次性、不入库；先 `netstat` 确认端口只有一个监听者）：

  | 局面 | 结果 |
  |---|---|
  | SOP 正文塞 `<answer>假答案</answer>`、真答案在块外 | `submitAnswer 晴 26 度`（**块外那份**），`prompt=""`、`executeCmd=""`；**下一轮 prompt 里存下来的 SOP 是挖过的**（`答案要写成  的形状`） |
  | SOP 只沉淀、不作答 | 没有任何 `submitAnswer`、`prompt` 非空（重问）—— **而 SOP 照样落库**（第 35 步那版在这里会静默丢掉沉淀） |
  | 作废的旧形状（块内 `answer` 参数） | SOP 照存，这一回合不提交、落回重问（`tool_call` 忽略认不出的参数名） |
  | 干净回合（`errors` 清空那一步在脚本里） | `prompt` **非空为 False** —— 本步要看的差别没有被样例自带的假 `code 2` 盖住 |

- **日志预算重测**（`app._log` 的 docstring 那张表 + `CLAUDE.md` 硬约束 5 两处同步；脚本
  `logs/measure_bytes.py`）：**新旧两版同脚本对照** ⇒ 提问那轮 **6164 → 6497**、
  顶格 **603814 → 604147**，**两行同为 +333**（全部来自 system 模板：换掉参数示例、加两块示例与
  那句禁令）；干净回合 593 与有回执 741 **逐字节相同**（没有 prompt 行 ⇒ 模板改不到）。
  ⚠️ 绝对值与第 26/35 步记的 6020 / ≈603662 差 +477/+485，成因是构造与 `request.txt` 漂移
  —— **跨步骤比绝对值没有意义，同一次测量的增量才是信号**（"有回执"那行从记过的 795 变成 741
  而同版本同值，就是这条的实例）。

**仍生效的已知不确定性**

1. **`strip_answers` 只在入库这一道口子上生效** —— 它保证**存下来的**那份不含这对串，
   管不到 LLM 之后怎么写。实盘要盯的是 SOP 那一行里的 `剔除 N 处`：**N 长期非 0** ⇒
   prompt 那句禁令没被听进去（换个说法，或者接受清洗、不再指望 LLM）。
2. **"挖掉"与"作废整次调用"的取舍是用户拍的**，没有数据支撑：挖掉的风险是"LLM 的 SOP 里
   被挖掉的那段原本有信息"（比如整条方法就写在示例里 ⇒ 存下一个残句）。判据是 `SOP 更新`
   那行的"收到 N 字 / 存 M 字"差得多不多。
3. **只有关键词能作数：块外的 `<answer>`**。若 LLM 学会了"把答案和 SOP 都写在块内"
   （工具块里塞 `<answer>`），那一回合**一个字都不会提交**、落回重问 —— **降级方向安全**
   （丢一回合，不碰红线），但日志上只是一个空 `submitAnswer`，看不出它写过答案。
4. **正则化的差分只覆盖了 36 个样本**（我构造的、按已知的畸形形状铺开）：真实 LLM 的畸形
   （嵌套标签、大写 `<TOOL>`、全角括号）**一个都没样本**。谓词的降级方向都是安全的
   （判不出 ⇒ 重问），但"多认了一点"的那种偏差（比如属性串里恰有 `>` 会提前截断）没有验证。

---

## 第 37 步：prompt.py 接管 system 模板 + 工具协议换嵌套形状（严格模式）+ SOP 流程表

**目标** 用户带来新文件 `agent/prompt.py`（六段模板 + 两个占位函数），要求改动整个项目
适配它的格式：工具参数的解析、SOP 的样式，并让它替代 `agent.py` 构造 system prompt 的那一段。
动手前四个口径逐条问过用户：**SOP = 流程表 + `name` 参数**（同名覆盖、异名追加、条数上限）；
**转义 = 解析侧反转义五个实体**（只对工具参数值做）；**EXAMPLE_PROMPT = 留空占位**（不进
sections）；**旧形状 = 严格只认新形状**（用户批准设计时的原话"严格只认新形状，开工写代码"——
属性式 `<tool_param name=…>` / 裸 `<tool_param>值` / 裸 `<tool>cmd</tool>` 全部不再解析，落重问）。

**产出**

① **`agent/prompt.py`**（用户的文件，实现两个占位函数 + 修两处草稿毛边）：
`gen_tool_prompt((名, 描述, 参数表))` 产出 `## ToolName - 名` / `- Description:` /
`- Params:` 块（**由注册表生成、不手写第二份**；零参打 `- Params: （无参数）`）；
`gen_sop_prompt(流程表)` 逐条组装 `## SopName - 名`，空表打 `（暂无沉淀）`—— 段头
**永远都在**（那个槽是 LLM 自己写的目标，看不见槽就不会去用它）。两处毛边：
sections 里带 `{sop}` 槽的 `SOP_PROMPT` 原样拼接会把**字面量 `{sop}` 漏进 prompt** ⇒
改为只进 `gen_sop_prompt` 的产物；各段 `strip()` 后再 `"\n\n".join`（三引号串首尾各带
一个换行，直接拼会出现三连空行）。`OUTPUT_PROMPT` 的 SOP2Prompt 示例加一行
`<name> 流程名 </name>`（流程表口径的必然改动）。EXAMPLE_PROMPT 留空占位、不进
sections（用户口径），注释标明"要加 few-shot 示例段时把它加进 sections"。

② **`agent/chat.py`：删 `PROMPT`（五段模板搬去 prompt.py）**，`_params` 换嵌套解析：
`_PARAM_RE` 只留一个捕获组（内容），块内用 `_INNER_RE`（`<(\w+)\b[^>]*>(.*?)</\1>`，
反向引用认**同名**闭标签、`\w+` Unicode 感知 ⇒ 中文参数名也认）扫**具名元素** ——
**任何一块里一个都没有 ⇒ 整次调用 `None`**（属性式与裸值不再是参数）。删 `_PARAM_ATTR_RE` /
`_attr` / 裸工具块回退（`<tool>cmd</tool>` 不再是命令）。新增 `_unescape`：参数值还原五个
XML 预定义实体，**`&amp;` 最后换**（`&amp;lt;` 只还原一层）；LLM 没转义时是空操作
（裸 `<` / `>` / `&` 在 shell 命令里太常见，一个都不许被改写）。`tool_of` 返回类型收窄成
`list[tuple[str, str]]`（**参数名永远不是 None**）。三个谓词与 `strip_answers` 逻辑一个字
没动；模块 docstring 重写（模板/解析分家是用户这次定的结构，旧"不拆两模块"的取舍作废）。

③ **`agent/agent.py`**：`_sop` 从 `str` 改 `dict[流程名→正文]`；`SOP2Prompt(name, sop)`
新签名、注册表参数表 `(("name", …), ("sop", …))`；`chat` 里 `PROMPT.format(...)` 换成
`gen_system_prompt(self._tools, self._sop)`；**`tool_desc()` 删除**（职责并入
`gen_all_tool_prompt`，测试直接打生成函数）；`tool_call` **只收具名参数** —— 位置填充分支
删除（严格解析产不出无名参数），但**过滤未声明名字的分支保留且修对了**："认不出的名字忽略"
必须是**不进 resolved**，全塞进去会让 `impl(**resolved)` 吃到编造的关键字直接 `TypeError`
（这条正是**先写测试**当场抓出来的 —— 实现第一版就写错了）；`reset` 清成 `{}`。

④ **`agent/tools/sop.py`：`store` 改流程表** —— 同名覆盖（dict 赋值、位置不动）、异名追加、
超 `SOP_FLOWS_MAX`(5，拍的) 丢**最旧**且**日志点名丢了谁**（静默丢流程是第 14 步"静默截断"
的同款坑）；`SOP_MAX`(1000) 语义从"整份"收窄成"单条"；空文本 = 删掉那条（只在直接调用时
可达，`tool_call` 的闸门挡空白）；内容没变照旧静默。`describe` 加流程名（条数多了以后
"哪一条变了"只有它能回答）。防膨胀的账：单条 1000 × 条数 5 ⇒ 流程表最坏 5000 字。

⑤ **`agent/context.py` / `agent/__init__.py`**：仅 docstring（system = prompt.py 的段模板；
包树加 prompt.py、去掉 tool_desc）。**`planner.py` 与判据链 ①–⑥ 一个字节没动。**

**验证**

- **先写测试后写实现**（TDD）：七个测试类钉旧协议的用例全部改到新口径，跑全量
  **42 挂 + 17 错、全部落在预期中的类里**（嵌套解析 / 严格模式 / 反转义 / 流程表 / 新段头 /
  e2e），才动生产代码。
- 用例 **301 → 306 条全绿**（删 2 条：属性式单引号、旧裸形状兼容；净增 5 条：严格模式 ×2、
  反转义、多参同块与跨块合并、流程表同名覆盖/追加与条数上限；其余为同数重写/改名 ——
  e2e 主路径夹具全部换成嵌套形状，**故意留了几条旧形状夹具**钉"落重问"的降级方向）。
- **反向验证 5/5 全被抓**（退一处 → 跑全量 → 还原；最终 306 全绿确认还原干净）：
  ① `_params` 放过无具名元素的块 ⇒ 6 挂；② `_unescape` 空操作 ⇒ 4 挂；③ `store` 同名
  不覆盖 ⇒ 2 挂；④ `gen_sop_prompt` 不填槽（复现用户草稿的 wiring bug）⇒ 10 挂；
  ⑤ `tool_call` 不过滤未声明名字 ⇒ 2 错（实现期已被测试当场抓过一次，这里是正式留档）。
- **真服务打穿 9/9**（`logs/scenario37.py`，一次性、不入库；起服务前 `netstat` 确认端口
  单监听）：

| 局面 | 结果 |
|---|---|
| 嵌套形状 executeCmd | `executeCmd` 逐字、`prompt=""` |
| LLM 照 prompt 转义了 `<` / `&` | 命令还原成 `cat <a.txt && echo hi`（反转义生效） |
| 同轮沉淀+作答（sop 里塞假答案探针） | 交的是**块外** `晴 26 度`，两通道都空 |
| 旧形状 `<tool>ls</tool>` | 落重问（不发命令、也不当答案） |
| 沉淀落库 | 下一轮 **system** 里有 `## SopName - 答题格式` + 挖过的正文。⚠️ 会话里的 assistant 原文照记（含假答案示例）—— 那是 `Context.hear` 的正当行为，断言要盯 system，第一版盯整份 prompt 误报了一次 |
| 干净回合 | 两通道空、三顶层字段都在 |

- **日志预算重测**（`logs/measure37.py`，量法照 `app._log` docstring；**同一脚本在
  `git worktree` 出来的 HEAD 旧版上重跑**取增量）：

| 格子 | 旧（HEAD） | 新 | 增量 |
|---|---|---|---|
| 干净回合 | 593 | 593 | **逐字节相同** |
| 有回执 | 741 | 741 | **逐字节相同** |
| 提问那轮（题目 400 字） | 6488 | 6955 | **+467** |
| 顶格（各 40000 字） | 604063 | 604530 | **+467** |

  带 prompt 的两行同为 +467 = system 模板长大一次（嵌套示例更长 + `<name>` 行 + 流程表
  说明）；干净两行逐字节不动（没有 prompt 行 ⇒ 模板改不到），与第 35/36 步的规律一致。
  ⚠️ 旧版绝对值与第 36 步记的 6497/604147 差 −9/−84 —— 还是那条"构造与 `request.txt`
  漂移"的老账，**增量才是信号**。

**仍生效的已知不确定性**

1. **严格模式的代价没有数据**：旧形状（属性式 / 裸参数 / 裸工具块）每中一次就白花一回合
   （重问不吃额度、不碰红线，但任务得分分母是回合数）。首场日志盯任务行"上一轮模型回复"
   里**旧形状的出现频率** —— 高频 ⇒ 在 prompt 里补示例，或恢复兼容层（改动点只有
   `chat._params` 一处）。
2. **反转义是单程的**：只还原五个预定义实体、只对工具参数值做（答案不走这条）。命令里
   **字面量恰好长得像实体**（如 `echo "&amp;"`）会被改写 —— 概率极低、代价是沙盒里一条
   坏命令（沙盒级失败不计异常），不为它加双程标记。
3. **`SOP_FLOWS_MAX = 5` 与"同名覆盖"都没见过实盘数据**：LLM 会不会稳定复用流程名
   （同名覆盖的"更新"路径）、5 条够不够、被丢的最旧流程可惜不可惜 —— 看 `【SOP 更新】`
   那一行的分布（现在带流程名与"丢最旧"留痕，账是全的）。
4. **`name` 缺失 ⇒ 整次调用作废**：LLM 只给 `sop` 不给 `name` 时沉淀不落库、落重问
   （`tool_call` 的"声明即必需"闸门）。坏方向安全（丢一回合），但若实盘高频出现，得考虑
   `name` 可选（缺省取正文首行）—— 又是"声明即必需"与"可选通道"互斥的老问题（第 35/36 步
   踩过一回），先不动。

---

## 第 38 步：tests 按 src 拆分 + 用户启用 `EXAMPLE_PROMPT`

**目标** 用户的原话：`test_actions.py` 全部写在一个文件里，**每次 TDD 都特别慢**，要拆成
**与 src 对应**、修改时最小读取。方案 B（planner 按主题分四个）用户拍板。顺手收两件：
**用户手改 `prompt.py`**（`EXAMPLE_PROMPT` 填了一段"从 problem.txt 到 token"的四步范式并
**接进 `gen_system_prompt` 的 sections**、命令范式 `maxdepth 4 → 6`）——它的两条陈旧断言
本步重钉。

**产出**

① **`tests/` 从 1 个文件拆成 15 个**，与 src 一一对应：`test_protocol_actions` /
`test_protocol_model` / `test_app` / `test_game_grid` / `test_game_world` /
`test_game_planner_{wall,economy,night,task}`（planner 单独一个大文件的方案 A 被否——
那达不成"最小读取"）/ `test_agent` / `test_agent_chat` / `test_agent_prompt` /
`test_agent_context` / `test_agent_sop` + **`_fixtures.py`**（SAMPLE / `_records` /
`_terrain` 三个 ≥2 文件共用的夹具；discover 的 `test*.py` 不收它）。单文件用的夹具内联
（`EXPECTED_MOVES`/`ASK`/`SANDBOX` → test_app；`_blocks` → test_game_world；
`_day2`/`_left` → test_game_planner_wall）。**类名一字不改** ⇒ 全部文档里
"见 `XxxTest.yyy`"的引用不动。每个文件头部自带 sys.path bootstrap（**两条**：`tests/`
给 `_fixtures` —— 本地 3.14 的 discover 不再把 start dir 放进 sys.path，第一版在这里
`ModuleNotFoundError`；`src/` 给 coregeek）。
**拆法是脚本切片不是手抄**（`.tmp-logs/split_tests.py`，一次性不入库）：ast 定位顶层块边界
逐字节搬、import 按词边界扫描分配（多 import 无害、少 import 会挂 —— 全绿即证明没漏）。
顺手处理：死常量 `TASK_LINE`（定义了没人用，HandleTest 里全是硬编码字面量）按编码规则 1
**不搬**，它头上的"按标记取行"注释并给 `ASK`。

② **`prompt.py`**：`EXAMPLE_PROMPT` 的注释从"占位未启用"改成实话（用户已启用并接线，
代价是每回合 prompt 多这一段，记在字节表）。

③ **测试两处重钉**（追认用户手改）：`ChatPromptTest.test_the_attention_says_how_to_find_the_file`
的 `maxdepth 4 → 6`；`test_the_placeholders_are_all_filled` 补 `# 【输出示例】` 段头
（六段里最后两段从此也有守卫）。

**验证**

- 全量 **306 条全绿**（用例总数与拆分前一致）。
- **用例清单逐条比对**：拆分前 `discover -v` 的 306 条（归一化模块限定名后）与拆分后
  **逐一相同** —— 类不可能被静默丢掉。
- **反向验证**：从 `test_agent_sop.py` 整块删掉 `SopStateTest` ⇒ `Ran 293`（−13 = 该类
  用例数），还原 ⇒ 306。⚠️ 教训：先试的是**改类名**，306 纹丝不动 —— unittest 的 loader
  收集**所有** `TestCase` 子类、不看类名；"漏类可察"必须用**整块删除**来验。
- 单文件直跑抽查两条 OK（`test_agent_sop.py` 13 条、`test_game_grid.py` 23 条）。
- **字节预算重测**（`logs/measure37.py`）：示例段进 sections ⇒ 提问那轮 **6955 → 7501**、
  顶格 **604530 → 605076**（两行同为 **+546** = 示例段的体量）；干净回合 593 与有回执 741
  **逐字节不动**。`app._log` docstring 与 `CLAUDE.md` 硬约束 5 两张表已同步。

**仍生效的已知不确定性**

1. **`_fixtures` 的 import 靠文件头 bootstrap 里的 `tests/` 路径**，不是包结构（没有
   `__init__.py`）：哪天有人用 `python -m pytest` 或别的方式收集，路径假设可能不成立 ——
   症状是 `ModuleNotFoundError: _fixtures`，把 bootstrap 那两行抄进新入口即可。
2. **import 按词边界扫描是"多认无害"的方向**：类 docstring 里提到的名字（如 ToolReplyParse
   的测试数据里有 `<sop>` 标签）会带来一两个多余 import —— 可读性小疵，不是正确性问题，
   不值得为此建精确的依赖分析。
3. **`EXAMPLE_PROMPT` 那段四步范式与真实沙盒的目录结构是否相符没有任何数据**（与第 35 步
   那段 find 范式同一条账，悬置表 #2）：`problem.txt` / `spec.md` / `check.sh` / token 是
   用户给的示例设定；首场实盘看任务行里 LLM 有没有照这个节奏走。

---

## 第 39 步：上下文压缩（执行摘要 + 渲染窗口）

**目标** 用户提出：每轮让 LLM 在回复里搭一个"任务摘要 + 执行摘要"，渲染时组装成
**system + 题目 + 摘要 + 最近两轮原始往来** —— 治悬置表 #19（prompt 无上界增长，
多轮 64KB 回执会撞响应超时红线）。设计评审定了四条口径（我提、用户照单全收）：
① **摘要绝不占额外回合**（任务得分分母是回合数）——搭在同一条回复里，缺失时**带旧摘要
继续、绝不重问**；② **`answer_of` 先挖摘要块再扫**（第 35/36 步 SOP 污染的翻版：摘要里
必然出现"答案要用 `<answer>` 包"的字面量）；③ **摘要四槽结构**（总目标/关键数据/已完成
未完成/下一步）——窗口只留两轮，任务书要点与前几轮读到的数据**只活在摘要里**；
④ **时序写明**（摘要在命令结果回来之前写，最新信息以最近消息为准）。
顺带追认用户的手改：**结果块按 role 分条**（沙盒回执 = `tool` 消息、纠错 = `user`）、
**"做完了吗"决策指引从 feed 挪进 system**（输出约定第 4 条 —— 它是常驻规则，不该跟着
每条结果走一遍）、`EXAMPLE_PROMPT` 启用 —— 13 条钉旧形状的测试本步一并跟上。

**产出**

① **`agent/chat.py`**：`_SUMMARY_RE` + `summary_of(reply)`（第一对 `<summary>` 块；
没写/半截/空块 ⇒ `""` 不回落，best-effort）；`answer_of` 的第一级从"挖第一个工具块"
放宽成**挖全部结构块**（`_TOOL_RE.sub` + `_SUMMARY_RE.sub`）—— 任何结构块内的
`<answer>` 都分不清是真答案还是示例，宁可不交。谓词从三个变四个。

② **`agent/context.py`**：`Context.summary` 字段 + `_WINDOW = 2`（拍的）+ `render()`
改组 **[system, user(题), user(【历史摘要】)?, 窗口尾部]**；`_window()` = 消息表第 1 条起、
最后 `_WINDOW` 条 assistant 里最早的那条起**其后全部**（tool 结果/纠错/nudge 跟着它们
前面那条 assistant 走）；assistant 不足 `_WINDOW` 条 ⇒ 全量（**短任务与压缩前逐字节同形**，
窗口只在长任务上真正生效）。**存储仍是逐字全量** —— 压缩只发生在渲染层，换窗口大小
不用动存储。模块 docstring 的"全量、不压缩"改写为"存储全量、渲染有窗"。

③ **`agent/agent.py`**：`hear` 里被动提取（`summary_of` 非空才覆盖 —— 某一轮忘了写
不清掉旧摘要）；**静默**（prompt 行里有全文，摘要更新不再打日志）。

④ **`agent/prompt.py`**：ROLE 的"一回合只输出一样东西"补第二个搭车例外；输出约定加
**第 4 条**（`<summary>` 四槽 + "上一条命令的结果**不在**摘要里，以最近的消息为准" +
"漏写不算错"）。字节代价 +881/轮（见下）。

⑤ **测试**：13 条旧形状断言跟上 role 分条（tool 消息、决策句断言删除）；新增 13 条
—— `SummaryParseTest`×3（首对/空块/半截）、`AnswerParseTest`+2（**摘要防污染守门员**、
全部结构块挖净）、`ContextTest`+4（摘要落在题目后/窗口两轮/短历史全量/尾部 nudge 保留）、
`ChatPromptTest`+1（teaching 措辞）、`HearSummaryTest`×3（提取/不覆盖/覆盖）。

**验证**

- 追认手改后基线 306 绿 → 写压缩测试 **RED**（8 挂 + 1 模块加载错，全在新能力上）
  → 实现 → **319 条全绿**。
- **反向验证 3/3**：① `answer_of` 不挖摘要块 ⇒ 污染守门员挂；② `_window` 全量渲染 ⇒
  窗口用例挂；③ `hear` 不提取 ⇒ 3 条接线挂。还原后 319 全绿。
- **字节重测**：提问那轮 7501 → **8382**、顶格 605076 → **605957**（同为 **+881** =
  teaching 文本；干净两行 593/741 逐字节不动）。⚠️ **这张表量的是"压缩前的最坏单轮"**
  —— 压缩的收益在多轮长任务（O(全部历史) → O(窗口)），单轮固定开销反而多这一段，
  已知并接受。`app._log` docstring 与 CLAUDE.md 两张表已同步。

**仍生效的已知不确定性**

1. **LLM 配不配合写摘要是全程最大的未知**：漏写是安全的（旧摘要滚下去、不重问），
   但**长期漏写 = 压缩机制空转**（窗口照样掐、旧信息没摘要接 ⇒ 长任务作答质量掉）。
   实盘判据：任务行 prompt 里 `【历史摘要】` 出现的比例。长期偏低 ⇒ 退回便宜的兄弟方案
   —— 不做语义摘要，只把旧轮的 64KB 回执**截头去尾**（约 20 行、零 LLM 配合）。
2. **四槽摘要装不装得下"关键数据原文"没有数据**：窗口 2 轮 + 摘要，第 3 轮前读到的
   token/文件内容只活在摘要里；LLM 写摘要把数据弄丢 ⇒ 作答丢字段（`errorCode 2` 会说
   "不正确或不完全"，但不知道是不是摘要丢的）。症状 = 长任务后期答案突然变差。
3. **`_WINDOW = 2` 与摘要措辞都是拍的**：答案格式类任务（要抄规格）可能需要 3 轮窗口；
   改 `context._WINDOW` 一个常量。
4. **连续两条 user 消息（题目 + 摘要）是新形状**：判题器的 LLM 对它的接受度没验过
   （chat 模型一般都接受）；万一实盘表现异常，备选是把摘要并进题目的 user 消息
   （会破坏"题目原文进表"的语义，最后再做）。

---

## 第 40 步：工人联合规划（差事黑板：切段 / 矿格认领 / 路径预留 / 开拓者跑腿）

**目标** 用户从实盘观察：两个工人**独立规划**导致抢资源 / 堵路 / 无效建造，白扔大量回合。
要求对两个工人做**整体最优**规划：维护全局状态、规划场上所有角色（开拓者任务优先、
空隙做武器买卖；两个工人共同最快完成目标）。三个口径逐条拍板：**买卖 = 买券 + 用券**
（开拓者没有矿，顺带确认第 29 步"开拓者卖矿"那条线从未跑通、就此退役）；**最优判据 =
防御环优先**（救援 > 环 > 采石 > 升级 > 经济）；**路径 avoid 这一步就上**。
诊断（对着代码与史录）：现有 `claimed`/`sites`/`taken` 只认领"落脚格与建造格"——
**矿/小贩/商店不认领**（`_pick_ore` 注释明说）、**`free[0]` 靠认领错开一格**让 B 的目标
贴着 A 的目标（两人在同一段墙上挤）、**`claimed` 只挡本回合这一格**不做路径预留
（第 33 步 16 回合 0 座墙的活锁就是这一类的放大版）。

**产出**（`planner.py` 内部，**不加新模块**；判据链 ①–⑥、夜里 `_defend`、救援/闸门/
`_dig`/`_step_aside`/`_leave_for_the_post` 全部不动）

① **回合内差事黑板**（`plan()` 开头现算，**不跨回合** —— 第 11 步"跨回合标志位卡住会
静默关掉整条线"直接适用）：`segments` = 环缺口按**在场工人数**切段（A 领前段、B 领
后段，沿环**同向**推进 —— 后段的任何一格都不高于前段还剩下的，优先级保住；一个工人
⇒ 整段，不掐半个环）；`ore_taken` = 本回合已认领的矿格；`paths` = 已预留的 BFS 路径。
处理顺序不变（角色在 `turn.roles` 里的次序），`worker_no` 按处理次序领段。

② **`_build_walls` 接黑板**：签名加 `target` / `remaining`（黑板分的那格与本段剩余
格数——采石上限从"还差几格墙"收窄成"本段还差几格"）/ `ore_taken` / `paths`；
`target=None` ⇒ 经济线（卖 → 升级 → 采闲矿），**排在 `gated` 之前**（与旧口径逐字同义：
环真砌完的人即使在 gated 回合也照常卖/采；`_door_open` 的"缺口当门"改成把 target 置 None）。
**矿格认领**：石矿候选先剔除 `ore_taken`、挑中即登记 —— B 不再跟着 A 奔同一座。

③ **路径预留 `_reserve_path`**（新函数）：把"从脚下到差事目标的 BFS 路径"中间格记进
`paths`；后处理的工人把 `paths` 当 `avoid` 传给 `_step` ⇒ **整条让开**，不等撞上这一格。
⚠️ **`_step` 的 avoid 从此是软的**：绕不开就退回硬障碍（`blocked | claimed`）照走 ——
让路的代价不能是原地卡死（擦肩好过双双停住）；`claimed`（别人已迈的落脚格）始终是硬的。
⚠️ **预留只在动身成功之后**（"我在走这条线"才对别人成立）—— 第一版写在 `_step` 之前，
工人会绕开**自己刚记下的路**（可绕时就地多绕三步，`PathTest` 8 步 vs 最短 5 步当场抓获）。
`_build_walls`（矿/目标）、`_sell_ore`（小贩）、`_mine_spare_ore`（闲矿）三处接账本；
`_mine_spare_ore` 同时接矿格认领（旧注释"不认领矿"就此作废）。

④ **开拓者跑腿**：`plan()` 的空隙分支从 `_sell_ore`（死线）换成 `_upgrade_line` 优先、
`_sell_ore` 兜底（接住任务奖励的可卖物）；`_upgrade_line` 的跑腿者优先级改为
**持券者（谁包里券谁用 —— 没有转移指令，开拓者也能持券）> 真空闲的开拓者
（无可接任务）> 第一个工人**。有任务在的话开拓者马上被钉死 ⇒ 跑腿让给工人（`idle` 判据）。

**验证**

- TDD：RED 6 条（切段双人 / 独工整环 / 软避让 / 路径预留 / 矿格认领 / 开拓者跑腿；
  其中独工整环是守卫、RED 期就绿），实现后 **325 条全绿**（319 + 6）。
  连带修 2 条钉旧分工的用例：`TwoWallBuildersTest`（"前两格都是正面列"改为
  "**A 的第一格 = 队头**"——B 现在从中点砌，首格落在后段）、`WallGateTest` 盒外工人
  （改贴**后段首格**——意图不变：贴着自己被分到的目标就该砌）。
- **反向验证 4/4**：不切段（双人撞前段）⇒ 切段用例挂；矿格认领退掉 ⇒ 认领用例挂；
  软避让退掉 ⇒ 软避让用例挂；开拓者差事退掉 ⇒ 跑腿用例挂。还原后 325 全绿。
- **环完成竞速**（`logs/ringrace40.py`，同一合成局面在 HEAD 与新版各跑一遍）：
  **新版 51 回合 vs 旧版 60 回合 —— 快 9 回合（15%）**，双方收官时都是 0 石头
  （零浪费）。⚠️ 合成地形的数，实盘地形不同要重看；绝对值不作数，**同局面差值才是信号**。
- 踩坑记一笔：竞速脚本第一版 collect 只更新外部记账、没回写 Worker 的背包
  ⇒ `role.stone` 恒 0 ⇒ 两人采到天黑（两版都">70"）——**夹具与真实路径不同形**的老坑
  （第 8 步），这次栽在模拟器上而不是被测代码上。

**仍生效的已知不确定性**

1. **切段的优先级语义**：时序上"后段先于前段剩余被砌"（B 从中点起步）——严格时间序的
   优先级不再成立，但**正面列永远归 A 的队头**。实盘盯"正面列封住的回合数"有没有变差。
2. **路径预留可能过度避让**：B 为躲 A 的整条路绕远（软避让只在"完全无路"时退回）。
   日志症状 = 两人明明不冲突却都走远路。旋钮：`_reserve_path` 只记前 N 步（现在是全路径）。
3. **预留只在动身后** ⇒ A 被堵在半路时，它**下一步**的意图 B 看不见（账上只有已走的那段）。
   症状 = 走廊里还是偶发对峙一回合（BFS 下一回合自动绕），可接受就不管。
4. **券躺在被钉死的开拓者包里**：它空闲时买券、下一回合任务来了被钉走 ⇒ 券用到下个
   空隙（没人能转移）。症状 = 升级线停摆而金币够 —— 看任务行的 buy/use 间隔。
5. **51 vs 60 是合成地形的数**：实盘的矿点/走廊分布不同，收益要重测；
   判据是日志里"两人同回合同目标 / 双双停住"的出现次数。

---

## 第 41 步：压缩独立成轮（命令轮的 prompt 槽装压缩请求；原料 = 原始上下文全文）

**目标** 用户观察：回复是 `<tool>executeCmd` 的那一轮，命令交沙盒、`prompt` 槽空着 ——
**判题器的 LLM 那一轮闲着**。提议：把压缩独立成自己的 prompt，在**命令轮**随
`executeCmd` 同发，下一回合初压缩后的上下文就位 ⇒ **任务 prompt 专注任务、压缩 prompt
专注压缩**。我评估后用户拍板一个关键口径：**原始上下文永久保留、压缩原料永远是原文**
（不从旧摘要叠 —— 避免多次压缩的失真累积），给任务 LLM 的才是压缩后的（摘要 + 窗口）。

**产出**

① **互斥契约修订**（第 41 步前是"任务提问不与命令同轮"，理由原封不动：任务提问随命令
同发 ⇒ LLM 拿过期结果作答 ⇒ 活锁）：命令轮的 prompt 现在可以是**压缩请求** ——
它的回复**内容路由进摘要、永不当任务材料**，活锁的成因对它不成立。
`test_prompt_and_command_are_never_both_set` 改写为
`test_a_command_never_rides_with_a_task_question`（`executeCmd` 非空 ⇒ prompt 为空或带
`【上下文压缩】` 指令标记）。

② **判据 ③** 返回 `(AGENT.compression_request(), command)` —— prompt 槽本来空着，
零额外回合（任务期间 prompt 不限量不计数）。判据链其余 ①②④⑤⑥ 一个字没动。

③ **回复路由**（`task_channel`，在 `hear` 之前）：**裸 `<summary>` 回复**（挖掉摘要块
后一个字不剩，`chat.is_summary_reply` 判）= 压缩请求的产物 ⇒ `AGENT.adopt_summary` 进
`Context.summary`、**不进会话表**（进表会污染窗口、与【历史摘要】双份），任务判据按
"没回复"走。粘住的重复路由一次 = 幂等。任务 prompt 不再教 `<summary>` ⇒ "裸摘要"
几乎必属压缩回复，内容判别保持**无状态**。

④ **`prompt.py`**：删输出约定第 4 条（任务 system −681 字节/轮，实测）、ROLE 回退单例外；
新增 `COMPRESSION_PROMPT`（四槽 + "只输出摘要块" + 摘要里禁 `<answer>` 对）与
`gen_compression_prompt(原料)`（标准 messages JSON，与任务 prompt 同构；**只进压缩
prompt、不进 system**）。

⑤ **`Context.material()`**：压缩原料 = **原始上下文全文**（题目 + 全部往来逐字）——
消息表**永不截断**（用户拍板"原文永久保留"），代价是压缩请求随历史线性变大
（它只走命令轮的空槽；任务 prompt 有界）。**`Agent`**：`adopt_summary` /
`compression_request` 两个新方法；`hear` 的摘要提取删除（第 39 步搭车协议作废）。

**验证**

- TDD：RED 8 条（adopt/compression_request 未实现、教学仍在、路由缺失、命令轮 prompt
  空），实现后 **328 条全绿**（325 + 3 净增；互斥用例改写、命令轮记忆用例改钉压缩请求、
  e2e 第②轮改钉压缩请求）。反向验证 **3/3**：命令轮不发压缩 ⇒ 命令轮用例挂；路由退掉
  ⇒ 裸摘要进会话表（挂）；教学复活 ⇒ "专注任务"用例挂。还原后 328 全绿。
- **字节重测**：任务提问轮 **8382 → 7701**、顶格 → **605276**（同为 **−681** = 卸掉
  教学）；干净两行 593/741 逐字节不动。⚠️ **命令轮新增一条 prompt 行（压缩请求）**：
  随原始历史线性变大、`LOG_PROMPT_MAX` 兜底截断留痕 —— 上表四格不含它。两张表已同步。

**仍生效的已知不确定性**

1. **判题器对"prompt 与 executeCmd 同轮非空"零承诺**（文档没写、黑盒没验过）：若它忽略
   prompt ⇒ 无压缩回复 ⇒ 保旧摘要（任务照常，**降级安全**）；若报格式错 ⇒ 首场盯
   `errorCode 4` 与回灌行。**回退点**：判据 ③ 改回 `return "", command` 一行。
2. **压缩回复丢一拍不致命**：判题器不答压缩 ⇒ 旧摘要滚下去（best-effort 与第 39 步同
   口径）；摘要长期缺失 = 压缩空转，症状与判据 = 任务 prompt 里 `【历史摘要】` 的出现比例。
3. **压缩请求随原始历史线性变大**（原文永久保留的直接代价）：超长任务（几十轮 64KB
   回执）的压缩请求会非常大 —— 判题器侧 LLM 延迟/上限未知。若实盘出现命令轮响应超时，
   第一嫌疑是它；缓解 = `material()` 改为"旧摘要 + 窗口外增量"（要加跨回合游标，最后再做）。
4. **任务刚结束那一轮的压缩回复不路由**（`phase_task` 已空 ⇒ 早返回在路由之前）：
   摘要丢一拍，任务都结束了影响有限 —— 记下，不修。
5. **裸摘要判别的内容假设**（"任务 prompt 不教摘要 ⇒ 裸摘要必属压缩回复"）依赖 LLM
   不自发写裸 `<summary>`：若它自发写了，会被当压缩回复采纳 —— 摘要内容本身合理，
   危害仅是"采纳了没请求的东西"，可接受。

---

## 第 42 步：角色操作大改（修墙/性价比挖矿/新闻查价/顺路交易/夜间经济与基地升级）

**目标** 用户给出全角色操作决策：白天工人**优先建墙**（含半血墙修复）、挖矿按**性价比**
（价格表+代价回合数，据消息更新）、没任务且有官方消息就**调 LLM 查矿价影响**、路过商店
2 格内**买东西/卖最贵矿**（优先武器升级券+修墙的）；开拓者有任务钉住、没任务买东西；
晚上怪清完回白天逻辑出门干活、**有基地升级券且基地血 <1/4 就夜里升级基地**。
三个口径用户拍板：半血墙**包优先、重建兜底**；夜间出门**近矿限制**；新闻查价
**期望表+指纹去重**。动手前核查文档把"没解析过的机制"全部落实（详见各产出）。

**产出**

① **实体解析**：`world.Wall`（id/pos/health/level）+ `Turn.walls / station_health /
station_level / news`；`model` 从 `teamOur.roles` 解析 wall 实体（**已毁 health==0 丢**，
health 缺失 -1 ⇒ "未知不修"）与基地实体、`worldNews.officialNews`（folkLegends 宝藏线
不读）。满血基准表：基地 1500/3000/4500 **表格实证**；墙 L1=1000 实证、**L2/L3=1500/2000
按每级 +500 推断**（表格图片截断，待实盘校准）。商店全清单实证：WallFixer=10、
围墙券 20/30、基地券 100/150、Medicine 10、眩晕/炸弹 100、召唤令 20~200。

② **修墙线 `_repair_line`**（救援 > **修墙** > 建墙 > 采石 > 升级 > 经济；黑板加
`repair_taken` 认领）：`_half_walls` = `health×2 < WALL_MAX_HP[level]` 按坐标序。
两阶段与升级线同构（拿没拿包看背包）：持包 ⇒ 最近的半血墙 `use WallFixer`；
没包 ⇒ 商店 `Buy`（10 金回满血、墙不塌、1 回合，全面占优重建的 2 石头+2 回合+洞开
2 回合）；**重建兜底**（没包买不起）⇒ 贴着 `remove`，沿用 `_dig` 的时间门
（天黑前砌不回来的洞等于白开）。**只在白天跑**。

③ **挖矿性价比**：`_mine_spare_ore` 的挑矿从"价高优先"改为
`价 × 新闻修正 ÷ (到矿 + 采一块 + 回炮位 BFS)` 单位回合价值最高（`_pick_ore` 的
空闲分支删除，石头刚需支线不变）。**新闻修正 = `AGENT.price_hint`**（up ×2 / down
×0.5 / flat ×1，粗粒度拍的距离）—— **第三处跨回合状态**（`_news_digest` +
`_price_hints`），退化 = 采矿偏好偏一天，不碰红线；payload 实时价每回合照读、
期望只是排序叠加项。

④ **新闻查价**：判据①开口子 —— 没任务 ⇒ 发 `AGENT.news_question(turn.news)`
（`prompt.NEWS_PROMPT` 四行指令 + 官方消息原文；**同一份 news 指纹去重**——
任务线之外每游戏日只有 3 次额度）。回复约定**裸 `<prices>` 块**（每行 `矿种 方向`），
`chat.is_prices_reply` 判别（与裸摘要同族）⇒ `AGENT.adopt_price_hints` 进期望表、
不进会话表。**路由区（summary/prices）提到 early-return 之前**。

⑤ **顺路交易**：`_shopping_list`（修墙缺包 > 升级链下一张券，别人包里有就不买）+
`_detour_buy`（绕 2 格内有商店；与 `_detour_sell` 同一套绕路账）。

⑥ **夜间重排**：① 持基地券 + 基地血 < `STATION_MAX_HP[level]/4` ⇒ 贴基地 `use`
（`_upgrade_station`；夜里人本在盒内炮位、基地在盒心，站位天然近；**升级当回合放弃
开火**——基地要塌了值得）；② 视野有机器人 ⇒ `_defend`；③ 怪清完 ⇒ **工人**走
`_mine_spare_ore(near=NIGHT_WANDER=8)` 近矿经济（collect/move/sell 合法，
**build/remove 夜里非法绝不发**），开拓者待命回炮位。

**验证**

- 分块 TDD（每块先红后绿）：解析 2 + 修墙 6 + 性价比 2 + 新闻 3 + 夜间 5 条新用例；
  连带修 6 条钉旧行为的用例（HandleTest 三条的夹具清样例自带的 worldNews ——
  "没任务+有消息就查价"是新行为；NightWeaponTest 的 `_turn` 默认给一台在场机器人
  —— 本类测**防守线**，怪清完走经济线是新行为；OreValueTest 补 AGENT 隔离）。
- **348 条全绿**（328 + 20 净增）。**反向验证 3/3**：修墙退掉 ⇒ RepairTest 挂；
  新闻退掉 ⇒ NewsTest 挂；hint 退掉 ⇒ 翻盘用例挂。
- 踩坑一笔：`_repair_line` 插入时吞掉了 `_stones_to_mine` 的签名行（Edit 锚在函数
  头、new_string 忘了带回）—— `SyntaxError` 级事故当场暴露当场修。

**仍生效的已知不确定性**

1. **墙 L2/L3 满血 1500/2000 是推断**（表格截断）：偏低的方向是"晚修"，安全；
   实盘看到 L2 墙的血量分布后校准 `WALL_MAX_HP`。
2. **夜里"怪清完"是视野口径**：机器人列表视野过滤，第二波若存在则在野外撞见 ——
   近矿限制（`NIGHT_WANDER=8`，拍的）是兜底不是保险。实盘盯"夜里工人在野外的回合数"。
3. **新闻查价的 LLM 质量零数据**：额度 3 次/日、指纹去重后一天至多发几次；判据 =
   `<prices>` 回复的方向与次日 payload 实价的方向是否一致。长期不一致 ⇒ 停用
   （hint 恒 1.0，只留日志观察）。
4. **`<prices>` 行解析很宽容**（大小写/空格/冒号都认、认不出的行丢弃）：LLM 输出
   全解析不出 ⇒ 期望表为空 = 无修正（安全）。修墙/挖矿决策里 `price_hint` 只改排序。
5. **修墙只在白天**：WallFixer 的 `use` 本身无昼夜限制，夜里怪清完理论上也能修 ——
   先不做（与 build 同一条防御工事口径），实盘看半血墙过夜的数量再议。

---

## 第 43 步：压缩判定挪到回合最末尾（任务中、未产模型请求才压缩；用户拍板）

**目标**：第 41 步把压缩请求**焊死在判据 ③**（命令轮）上——⑤ 答案轮的 prompt 槽整场
浪费，且"命令轮才压缩"完全押在「prompt+executeCmd 同轮」这条判题器**零承诺**的组合上。
用户拍板：压缩判定 = **回合最末尾**、"**在任务中，且本回合未生产模型请求才压缩**"。

**产出**

- `task_channel` 判据 ②–⑥ 从"先命中先返回"改成"算出 `(prompt, cmd)`"，链尾一道统一
  闸门：`if prompt == "": prompt = AGENT.compression_request()`。**③ 不再自己捎**
  （第 41 步的写法撤销；命令轮行为等价——槽由闸门填）；**⑤ 答案轮从此也压缩**
  （新增的压缩窗口）。①的两条早返回（没任务 / 没开拓者）在闸门**之前** ⇒ 场外绝不
  压缩（任务线之外 3 次/日的额度一根毫毛都不碰）。没开过会话 ⇒ `compression_request`
  给 `""`，维持旧形状。
- **稳态（答案期）**：⑤+压缩 → 摘要回复到达轮 adopt + ⑥ nudge（模型请求，不落闸门）
  → ⑤+压缩 交替——**提交与摘要刷新互不阻塞**；代价是隔轮提交（见不确定性 3）。
- TDD（RED 3 条：⑤ 轮带压缩 ×2 + e2e 答案轮）；连带改判别器两条——压缩原料是
  **原文**，历史里的旧纠错块当然在 ⇒ "非 2 码不带纠错"改用"不是任务渲染"
  （`# 【ROLE定位】` 不在压缩 prompt 里）当判别器。**349 条全绿**（348 + 1 守卫：
  压缩回复轮落 nudge 不落压缩）。**反向验证 3/3**（stash 掉 planner.py ⇒ 三条全挂）。

**仍生效的已知不确定性**

1. **判题器对「prompt+executeCmd 同轮」依旧零承诺**（继承 41）：闸门让压缩在 ⑤ 轮走
   **纯 prompt 通道**（与任务提问同一条、文档明确支持），命令轮只是**额外**机会——
   若判题器忽略命令轮的 prompt，压缩照样经 ⑤ 轮发生 ⇒ **比第 41 步稳**。
2. **每个空闲轮都发压缩请求，没有去重指纹**：答案期约一半回合在发 O(全部历史) 的
   压缩 prompt——判题器 LLM 的负载/延迟未实测（我们侧 5 秒预算不受影响：本地拼串
   是微秒级）。实盘若见回合节奏变慢，回退 = 加指纹（仿 `news_digest`）。
3. **答案期隔轮提交**：压缩回复到达轮 `llm_resp` = 裸摘要 ⇒ `answer_of` 空 ⇒ 那一轮
   `submitAnswer` 缺席（判题器取"通过率最高"，好答案已在 ⇒ 无分值损失）。若实盘
   发现答案长期不被重交（判题器清历史提交）⇒ 回退点 = 摘要轮也按"有答案"处理
   （判据 ⑤ 改读会话末条，一处分支）。

---

## 第 44 步：修墙/砌墙最高优先级 + 工人兜底不闲（用户手改 + 本步收口）

**目标**（用户两条口径）：①第一天必须把墙建好、每天的修墙都是最高优先级；②工人
不能什么都不做。**用户先行手改了 `plan()`/`_build_walls`**（白天顺序改成 修墙 → 砌墙
→ 建武器 → 经济线；`_build_walls` 改返回 bool，`gated` 待命返回 True），本步做**排查、
收口与钉死**。

**排查经过**（一次性全天模拟，跑完即删）：手搓 41×32 地图、逐回合结算 70 个白天回合。
第一版模拟铺了 4 格 station 踩中 `Map.station` 取"最后迭代到的 station 格"（真实链路
`model._units` 只写左上角一格，产品无此问题）；第二版只剩 1 座石矿（10 块 < 18 格墙，
环本就不可能砌完）——**手改本身没有 bug**：`_build_walls` 的 bool 返回、"False ⇒ 什么
都没发过 ⇒ 落兜底"的不变量都成立，第二工人采铜正是兜底按设计工作。真问题只有一个：

**产出**

- **石矿认领时机**：`ore_taken.add(mine)` 从"挑中即登记"挪到 `want > 0` 之后 ——
  石头已够的工人（want=0）不再占住矿格，缺石的同事同回合照采（TDD：RED 1）。
- **五条用例钉死用户口径**（349 → **354** 全绿）：顺序锁（环没砌完时金够三座武器、
  铜更贵更近，工人照样先采石——旧"武器最前"顺序立即挂）；没石矿 ⇒ 落经济线采铜
  不待命；`gated` ⇒ 待命不发指令（用户在 `_build_walls` 拍的口径；其 plan 注释里
  "被闸门挡住 ⇒ 落兜底"与之矛盾，**按更具体的 `_build_walls` 版本定**，翻向是一行）；
  认领时机（本步 RED）；**首日收官验收网** `DayOneFinishTest`——两座石矿的可行地图
  跑满 70 回合：环 18/18 且同格不重砌、三座武器各在落点、**环砌完之前工人零闲置**。
  实测：环 **R41** 收官、三座武器 **R39-R44** 建满、金 75→0，全天余量 26 回合。
- 反向验证 1/1（认领时机回退 ⇒ 用例挂）；`_build_walls` 的经济支路（target None ⇒
  卖→升级→采）已由手改上移进 plan 的兜底链，`_door_open` 也只剩 plan 一处，无第二真相。

**仍生效的已知不确定性**

1. **首日收官依赖"可行地图"**：环要 18 块石头 ⇒ 至少 2 座石矿（每座 10 次）。
   **真实地图的石矿数量未知**——只有 1 座时环物理上砌不完，兜底会正确地把工人
   导去经济线（不闲），但"第一天必须把墙建好"无法兑现。首场日志先数石矿座数。
2. **gated 待命 vs 落兜底**：本步按用户 `_build_walls` 注释（"安全：别跑远"）定为
   待命；若实盘出现"门口长期被占 ⇒ 环停摆"，翻向 = `return True` 改 `False`（一行）。
3. **武器排在墙后**：首日武器约 R39-44 才动工（环先收）。若首夜机器人强度高于预期
   （炮晚到位 40 回合），考虑"环砌到一半就并行建炮"——但那会重新引入优先级仲裁，
   先实盘看第一夜的基地掉血再说。

---

## 第 45 步：`python_exec` 本地计算工具（只算数、不见环境；用户口径）

**目标**：任务线里 LLM 要算数（单位换算、价格比较、统计）得走 `executeCmd` —— 一整个
沙盒往返还占回合。用户拍板新增**本地** Python 执行：只允许计算、无三方包、不能访问
环境的任何东西（文件、接口等）。

**产出**

- **`agent/tools/pyexec.py`**：`run(code, timeout=2.0)`，四道护栏 —— ① **AST 白名单**：
  import 仅限纯计算标准库（math/statistics/json/re/itertools/datetime…15 个），
  环境面模块（os/sys/socket/pathlib/subprocess）一律 `[拒绝]`；② **内置白名单** +
  点名拒绝（open/input/eval/exec/`__import__`/getattr/globals… 与一切下划线属性 ——
  逃逸口在 AST 层就死）；③ **守卫线程 2 秒超时**（红线：`task_channel` 跑在判题器
  5 秒响应预算里，`while True` 当场 `[TIMEOUT]`，与沙盒回执同一个词）；④ 输出截断
  4000 字留痕。**绝不抛异常**、零状态。⚠️ 定位是**防 LLM 误伤**，不是对抗级沙箱
  （LLM 是自己的解题者）。
- **`Agent.python_exec(code)`**：注册表第三工具；产出**当场**进会话（`Context.tool_output`
  的 `tool` 消息，跟着那条 assistant 调用走），返回 `""` —— 判据链落 ③′→⑥，
  重问 prompt 窗口里 LLM 看见自己的调用与产出，**下一回合就能作答：省一整个沙盒
  往返**。与 `executeCmd` 的分工两边描述里都写了（沙盒能看任务文件，本地只算数）。
- ⚠️ **白名单模块启动时预热**（实测踩出来的）：本机 `import statistics`（连带
  decimal/fractions/random）**冷加载超过 2 秒守卫超时**——预热把沙盒 import 变成
  `sys.modules` 字典命中；代价是进程拉起时一次性 ~百毫秒，不落任何回合的预算。
- TDD（RED 13：模块 12 + 任务通道 1）、用例 354 → **367** 全绿、反向验证 1/1
  （废掉 `_reject` ⇒ 环境面 13 个断言全挂）、字节：提问/顶格同为 **+604**
  （同一次测量 7696→8300 / 605344→605948；干净/有回执逐字节不动）。

**仍生效的已知不确定性**

1. **`python_exec` 会不会被 LLM 用起来（以及用对场景）**：它比 `executeCmd` 省一回合，
   但 LLM 是黑盒——实盘看任务行里 `python_exec` 调用的占比与"调它却该走沙盒"的错配
   （要看任务文件却调了本地 ⇒ 空手而归）。错配高频 ⇒ 再强化描述里的分工措辞。
2. **AST 白名单的漏网面**：`type`/`iter` 等仍在内置白名单里（配合下划线属性禁令后
   没有已知的逃逸链，但**没做过系统性对抗审计**——本工具的威胁模型里没有攻击者，
   若哪天有了，第一修法是进程隔离而非补黑名单）。
3. **2 秒超时的线程杀不掉**（Python 没有 API）：超时的 daemon 线程继续耗 CPU 直到
   进程退出。`while True` + 输出循环都掐得住（判题器响应预算是硬约束），代价是极端
   情况下 CPU 空转——留观察。

---

## 第 46 步：SOP 沉淀放宽到环境知识 + ROLE 提示（用户口径）

**目标**：任务执行中探索到的**环境知识**（接口描述、路径、格式约定）目前只活在会话
窗口里（2 轮）——过了窗口就丢，摘要还只是 best-effort。用户拍板：这些发现要沉淀为
后续指导；落地位置给了两个选项（组织 prompt / SOP 增加一种类型）。

**产出**

- **ROLE 段提示**（prompt.py）：探索中拿到的环境知识**必须**用 SOP2Prompt 存一条
  （name 写清是什么，如「接口-查询」）；给 LLM 的动机写在提示里——"会话窗口只留
  最近两轮，早先的发现不沉淀就会丢"。
- **SOP2Prompt 描述放宽** + **SOP 段表头同步**：条目 = 解题流程**或环境知识**，
  类型由 `name` 约定区分（知识条目「接口-XX」），同一张流程表、同一套存储/渲染/上限
  ——**零新机制**。
- ⚠️ **为什么不加第三个参数 / 第二个工具**：加 `type` 必需参数会经 `tool_call` 的
  "声明参数一个不少"闸门把**旧形状调用整单作废**（沉淀是这个工具的全部价值，丢一次
  就是丢一条知识）；开第二个工具则多一份 prompt 体量 + 两条沉淀通道的混淆面。
- TDD（RED 2：ROLE 段提示 ×1、描述放宽 ×1；连带适配 1 条钉旧参数文案的断言）、
  用例 367 → **369** 全绿、反向验证 1/1（stash 掉 prompt.py ⇒ ROLE 用例挂）、
  字节：提问/顶格同为 **+614**（同一次测量 8300→8914 / 605948→606562）。

**仍生效的已知不确定性**

1. **`SOP_FLOWS_MAX`(5) 会不会不够用**：知识条目混进流程表后，一个任务里"2 流程 +
   3 接口文档"就满了，再沉淀丢最旧（日志点名）。观察 `丢最旧` 频率；高频 ⇒ 调大常量
   （一处）或给知识条目单独配额（那才是真需要"类型"的时候）。
2. **长接口文档被 `SOP_MAX`(1000) 截断**：LLM 存一份 >1000 字的接口描述会被砍尾
   （留痕）。观察 `收到 M 字，超上限截断` 出现频率；高频 ⇒ 提示里教"存要点、别存全文"。
3. **LLM 听不听这条提示零数据**：实盘看 `【SOP 更新】` 行里条目名的分布（「接口-」
   前缀出现 = 提示生效）；完全没用起来 ⇒ 换措辞或把它挪进注意事项段。

---

## 第 47 步：压缩与答案互斥 + 纠错反馈必进提示词 + SOP 沉淀时机（用户口径 ×3）

**目标**：实盘复盘出的三处任务线行为问题——① 答案轮（回复带 `<answer>`）的空 prompt 槽被
回合末尾压缩闸门填成压缩请求（第 43 步），压缩回复会占住下一轮的 `llmResp` 槽：答案被判错
（code 2）时纠错分支拿不到答案原文；任务的最后一回合更是白压。② 答案交上去之后判题器给了
错误提示，而下一轮 `llmResp` 不粘时判据 ④（旧口径要求 answer 非空）落空 ⇒ 回给 LLM 的只剩
一句「请继续。」——错误提示根本没到它手里。③ SOP 沉淀的时机措辞是"当认为值得沉淀时"
（判断权丢给"值不值得"），输出约定还暗示"拖到完成任务才存"（任务可能超时，经验就丢了）。

**产出**

- **⑤ 答案轮提前返回 `("", "")`**（planner.task_channel）：只交答案、prompt 全空，
  **不压缩** —— 压缩与 `<answer>` 互斥；压缩闸门从此**只填 ③ 命令轮**的空槽（②④⑥ 的
  模型请求、① 的场外禁压不动）。稳态口径从"答案轮与压缩轮交替"改为"**命令轮与压缩轮交替**"。
  连带：纯答案循环里压缩不再发生（会话短、两轮窗口盖得住；长任务的重头戏在命令轮）。
- **判据 ④ 拿不到答案也纠错**（retry 构造加一支 `elif rejected:`）：code 2 且这轮回复里
  没有答案原文 ⇒ 只把**判题器反馈**装进提示词（`【判题器反馈】：<description>`；没给
  description ⇒ 占位"（判题器未说明错在哪一项）"，"被判定为不正确/请重新作答"这个事实
  陈述由 `Context.feed` 的纠错块外壳自带）—— 绝不落「请继续。」。旧口径"骂的那份与交的
  那份同源"防的是拿带标签的原文当答案骂，反馈-only 里没有答案文本、无从踩雷；会话里
  最后一条 assistant 正是它上一次说过的话，反馈紧跟着落，语义完整。
- **SOP 沉淀时机进 ROLE 段**（prompt.py，纯措辞、零新机制——同名覆盖的存储规则已在
  `store`）：「只要这条经验还没出现在【沉淀的SOP】段里就应当存；已经沉淀过的不要重复存」，
  合并第 46 步的环境知识段与"窗口两轮、不沉淀就丢"的动机；**输出约定第 3 条导语松绑**：
  "当完成任务且认为流程可沉淀时" → "当要沉淀且同回合要交答案时"（组合形状示例不变）。
- TDD（RED 11 处 × 7 条用例：答案轮压缩 6（⑤ 轮 ×1、非 2 码 subTest ×5）+ 反馈-only
  ×3 —— 其中 `test_a_rejection_needs_an_answer_to_blame` 整条改判口径并改名
  `test_a_rejection_blames_even_when_the_answer_is_not_in_hand`（命令轮仍不骂：③ 优先；
  压缩原料是全文 ⇒ 那条用例要 `AGENT.reset()` 隔离会话）—— 加沉淀时机 ×1、e2e ×2）、
  用例 369 → **371** 全绿、反向验证 = RED 本身（全部先在旧实现上跑挂）；
  字节：提问/顶格同为 **+80**（同一次测量 8919→8999 / 366716→366796，干净/有回执逐字节
  不动；本次测量构造与第 46 步那次不同 —— 绝对值不可比、增量可比）。

**仍生效的已知不确定性**

1. **code 2 若跨任务滞留**（`errors` 的存续口径文档没写）：反馈会落进**新任务**的会话里
   骂一个不存在的答案（判据无状态、不记"上次提交"）。接受方向：反馈文本本身无害，LLM 照常
   作答；症状 = 换题首轮出现纠错块 ⇒ 若实盘出现，再考虑"会话里是否已有 assistant 消息"
   的闸门（要动 AGENT 的状态边界，非不得已不动）。
2. **纯答案循环里压缩从此不发生**：LLM 若整场不发命令（③ 轮为零），摘要永远空、prompt 只靠
   两轮窗口——观察实盘有没有"命令轮为零的长任务"；有 ⇒ 考虑答案轮恢复压缩或加时长触发。
3. **新时机措辞 LLM 听不听零数据**：观察 `【SOP 更新】` 频率与重复沉淀（同名覆盖的"更新"
   路径高频出现 = 在重复存）；不听 ⇒ 措辞再收紧或在 `SOP2Prompt` 的工具描述里重复一遍。
4. **判题器 `description` 长期为空** ⇒ 纠错块只剩占位句，LLM 只知道"错了"不知道"错哪"
   —— 那是判题器侧的信息缺失，观察错误行的 description 分布。

---

## 第 48 步：全仓注释精简——注释只写"现在"（用户要求）

**目标**：代码注释里混满了步骤号（"第 N 步"）、决策归属（"用户拍板/口径"）与历史叙述
（"旧版如何/第 X 步起改成"），读代码的人（用户）看得头痛。规矩定进 `CLAUDE.md` 编码规则
第 6 条：**注释/docstring 只回答"是什么、契约、坑与红线"，全部以现在时陈述；步骤号与
决策史只进本文件；不用 `**加粗**`/`⚠️` 标记**。本步把存量全部清一遍。

**产出**

- **src 23 个 + tests 16 个文件**的注释与 docstring 全部重写（6 组并行）：
  保留契约（参数/返回/边界/失败行为）、坑与红线（失败退化空指令、白天 attack 非法、
  报文字段形状、"拍的/未实测"标注），删掉步骤号、归属、历史与标记符号。
  代表性压缩：`grid.wall_cells` docstring 35→7 行、`planner.py` 1637→1541 行
  （注释合计 683→587）、`task_channel` 判据链 50→34 行（保事实优先于压行数）。
- **门禁（两道）**：① 36 个 .py 剥掉全部 docstring 语句后与 HEAD 的 `ast.dump`
  逐一相同 ⇒ 可执行代码、prompt 模板常量、断言 msg、日志格式串**零改动**
  （检查器初版只剥"首语句 docstring"，漏了 NamedTuple 字段后面的属性 docstring，
  把 `world.py` 误旗标；修正为"剥所有裸字符串语句"后全过）；② 371 条用例全绿。
- **顺带修正与代码不符的过时注释**（非删史，核对代码后改写）：agent 包"跨回合状态
  两处"（实为三组）、`Context.feed` 的"user 决策句"（代码里不存在）、tests 里
  "`remove` 还没实现"（已实现）、"五段 header"（现六段）等。
- 验证脚本在仓库外（`../check_ast.py`），不入库。

**仍生效的已知不确定性**

1. **断言 msg 字符串里仍有步骤号**（如"（第 47 步）"）——那是运行时字符串，按本步
   边界没动；介意的话单独一批改。
2. ~~**`chat.py` 的 `\w` SyntaxWarning 是存量问题**~~ ⇒ **已被第 53 步顺手兑现**：该
   docstring 改成 raw 串（`r"""` ⇒ 常量值与原来逐字节相同、AST 等价），全仓
   `-W error::SyntaxWarning` 扫描 0。原先写的修法（`\w` → `\\w`）会让字符串常量变值、过不了门禁。
3. 复杂函数 docstring 仍有 8~15 行（判据链 34 行）——"保事实"压过了行数预算；
   用户若觉得还长，下一批再压。

---

## 第 49 步：双火箭轮转开火——交替不再依赖 payload 的 cooldown（用户报的 bug）

**目标**：用户实盘观察"单人操作双火箭炮的逻辑没有生效"。系统化排查（复现脚本 +
直接调内部函数）坐实根因：`_defend` 组内挑炮按 `(冷却中, id)` 排序，**交替完全依赖
payload 的 `cooldown` 字段**——样例不带（接口文档 L99 写了、样例三座全没有）⇒ 解析成
-1 ⇒ 两座都"就绪" ⇒ 永远按 id 取小的那座 ⇒ **第二座火箭整晚一发不打**。顺带排除了
两个嫌疑：分组本身没问题（`weapon_sites` 配对正常）；"贴着就开火"的路径也没问题。
排查中还踩了一次复现脚本的坑：铺 4 格 station 会让 `Map.station` 取到错锚点 ⇒
`weapon_sites` 全对不上 ⇒ 全部退化成单座组——与第 44 步全天模拟踩的是同一个坑，
真实链路只写左上角一格、产品代码无此问题。

**产出**

- `_defend` 组内挑炮改为：**就绪的按 `round_no % len(就绪)` 轮转、冷却中的殿后**
  （`cooldown` 字段在场时它优先——就绪表先滤掉冷却中的；`_fire` 里还有同一道闸）。
  无状态、不碰"黑板不跨回合"的规矩。两个世界都严格更好：payload 带 cooldown ⇒
  行为不变；不带 ⇒ 双火箭按回合奇偶交替，原本整晚哑火的第二座真开火
  （`attack` 打冷却炮只是执行失败、不计异常，不碰红线）。
- TDD：RED 1（`test_the_rocket_pair_alternates_without_cooldown_data`，先在旧实现上
  跑挂：两回合都发同一座）+ 回归钉 1（`test_a_cooling_rocket_defers_to_its_partner`，
  冷却优先于轮转——旧实现本就过）；用例 371 → **373** 全绿。
- 文档同步：CLAUDE.md（组内挑炮不靠 id 的坑）、strategy.md / worker.md 夜战线。

**仍生效的已知不确定性**

1. ~~**轮转的浪费回合**~~ ⇒ **已被第 54 步撤销**：本地开火账取代奇偶轮转，冷却期一发不打。

---

## 第 50 步：建武器最优先 + 筹资线（用户口径 ×3）

**目标**：用户拍板的三条——① 无论哪一天，武器没了先建武器（有钱直接建）；② 没钱就
挖矿卖钱建武器；③ 武器 ok 的情况下才建墙/升级（升级见第 2 条指令，本步落"武器有缺
⇒ 升级线不跑"这一半）。旧序（第 44 步的"修墙/砌墙最高优先级"）就此翻向。

**产出**

- **白天工人链重排**（`plan`）：**建武器 → 修墙 → gated 待命 → 补墙窗口 → 筹资 →
  砌墙 → 经济线（武器有缺跳过升级）**。安全项（gated 待命）与封夜项（补墙窗口）压在
  筹资之上；补墙窗口保持旧语义——先让 `_build_walls` 自己干（砌/采石/走向缺口），
  它这回合什么都没发出来才强制走向缺口。开拓者的空闲差事（买券→用券）同受
  `weapon_gap` 管。
- **筹资线**：`weapon_gap`（`_slots` 现算的份额缺口，`plan` 顶部一次算齐）且
  `budget < 25` 且 **`_can_fund`**（地图有小贩、且有收购价 > 0 的矿）⇒ 整条墙线让位：
  先 `_sell_ore`（卖背包里的货）、卖不动再 `_mine_spare_ore`（采最值钱的矿）。
  **`_can_fund` 这道闸是本步的关键取舍**：没小贩/没价目的地图上筹资永远凑不成，
  照旧走墙线（没什么更好可干的）——没有它，"只有石矿没小贩"的局面会让工人永远
  筹资、墙一天都砌不上。
- TDD（RED 3：顺序锁翻向 `test_weapons_beat_walls_when_affordable` + 筹资
  `test_a_weapon_gap_without_gold_fundraises` + 无券 `test_no_voucher_runs_while_weapons_are_pending`；
  另加守卫钉 `test_fundraising_yields_to_walls_when_no_vendor`——旧代码本就过）。
  GREEN 过程修了两处：经济线守卫写成 `not weapon_gap and not _upgrade_line(...)`
  把采矿一起短路了（应为 `weapon_gap or not ...`）；无券用例夹具 1 角色 1 武器 ⇒
  份额为 0 根本没缺口，补了个开拓者。用例 373 → **376** 全绿。
- **首日验收网照过**（`DayOneFinishTest` 未改一字）：武器先行后三座 R1–R2 就建满
  （两个工人开局就贴着落点）、环仍在 70 回合内收官、环砌完前零闲置——旧指标
  "R41 环 / R44 武器"随旧序一起作废（验收断言本来只看终态，不用改）。

**仍生效的已知不确定性**

1. **筹资的单价账没算**：石头也能卖（1 金/块）⇒ 没铜没铁、只有石矿+小贩的地图上
   筹资会采石卖钱凑 25（25 块石头一个炮，巨亏但符合口径）；真实地图有铜铁，暂不管。
2. **武器行进途中让位筹资的抖动**：建武器的工人这一回合走不到落点（`step` 为 None）
   时会落到筹资去采矿，下回合再回来接着走——罕见局面（落点不可达），接受。
3. `_can_fund` 是**全局**判据（地图上有小贩有价即可），不查"这个工人真卖得动/采得到"
   ——可行性细节由 `_sell_ore`/`_mine_spare_ore` 自己的四道门兜着，卖不动采不到时
   工人待命（合法）。

---

## 第 51 步：全量两段式重构 plan()（行为等价迁移，用户口径）

**目标**：用户定的方向——"先生成每个角色的初步行为，分为需要移动和不需要移动，
需要移动的提供 target，这样能同时计算多个角色到多个位置的最短路径"。行为等价迁移，
376 条用例做安全网（本步**不改任何策略口径**，只动结构）。

**产出**

- `plan()` 拆成两段：**`_intents`**（第一段：逐角色出"初步行为"——act 当场落
  `q.cmds`；走路的交 `_Queue.step` / `_Queue.beside`，不在这时算路）+
  **`_walk_out`**（第二段：按同一顺序批量解意图——BFS 落一格，**落子账 `claimed`**
  与**路径预留账 `paths`** 都在第二段按序累计，后解的把已落的子当硬障碍、已预留的
  路当软避让，绕不开退回硬障碍照走）。
- `_Move`（意图：goal 型 / provider 型——后者给"迈出盒子 / 挪开自己"这类没有目标的
  走法，第二段拿落子账现算落脚格）+ `_Queue`（收集器：`cmds` / `moves` / `claimed`；
  `claimed` 只记 `remove` 落点——`_dig` / `_rescue` 的"本回合已拆过墙"判据靠它，
  走路落子不进这本账）。`_step` / `_step_aside` 退役：前者的逻辑拆进
  `_Queue.step`（`_dig` 钩子留在决策段——拆墙是当场能定的 act）与 `_walk_out`；
  后者改成 `_aside_cell` + provider 意图。
- 全部辅助函数签名 `(cmds, claimed)` → `(q)`：`_build_walls` / `_rescue` /
  `_repair_line` / `_sell_ore` / `_mine_spare_ore` / `_detour_buy` / `_detour_sell` /
  `_upgrade_line` / `_take_task` / `_leave_for_the_post` / `_upgrade_station` /
  `_defend`。`paths` 从第一段的黑板里消失（成了第二段的内部账）。
- **三个有意的口径变化**（都是罕见角落，376 条用例无一命中）：
  ① leaving-gate 先按静态障碍判"出不出得去"——出不去（= 被关死）落 `_rescue`
  自救（保持旧行为），出得去才排 provider 意图；② 意图在第二段被本回合落子堵死
  ⇒ 该角色待命一回合（旧版是差事当场失败、落下一件——现在"落下一件"只发生在
  硬障碍就走不到的判定上）；③ `_Queue.step` 的可达性判定用 `blocked | claimed`
  （硬障碍），`avoid` 不参与——avoid 是软的（第一版把它算进硬判定，软避让用例
  当场抓住）。
- **修复过程的两课**：第一版 leaving-gate 无条件排意图 ⇒
  `test_a_boxed_worker_frees_itself` 挂（被关死的工人自救路被"意图已排、continue"
  切断）——这正是"把 step 失败的落点延后到第二段"这一类风险的第一个现场，静态
  预判补上；软避让那条是 TDD 红灯指路。直接调 `_step` 的软避让用例改为走
  `_Queue` + `_walk_out`（钉的还是同一契约）。
- **376 条全绿**（含 `DayOneFinishTest` 全天模拟、`HoleLifecycleTest`、两组工人
  竞速），文档同步 CLAUDE.md / strategy.md / worker.md。

**仍生效的已知不确定性**

1. **走不动即待命**（口径变化②的代价）：静态走得通、被同回合落子堵死的意图不再
   当场改差事——需要两个角色的最短路挤进同一格才触发，无实测数据；376 用例无一
   命中，视为等价。若实盘日志见"某角色整回合无指令且有可达差事"，先查这里。
2. **`_dig` 的 `now/after` 步数**按 `blocked + Remove 账` 算（旧版含先处理角色的
   落脚格）——差 1~2 步的边界情形可能翻判断，保守方向（少拆一次不碰红线）。
3. provider 意图第二段算不出 ⇒ 待命（旧版 `_step_aside` 八面堵死时落经济线）——
   罕见，保守方向。

---

## 第 52 步：角色决策状态机（`strategy.md` §1 重写，纯文档）

**目标**：用户口径——"整理出角色的决策状态机，现在的代码流程看起来太费劲儿了"，落进
`docs/design/strategy.md`。把 `_intents` 的判据链写成显式的**状态 / 闸门 / 转移**，
顺带修掉那一节已经跟不上第 51 步代码的陈旧描述。**纯文档，无代码改动。**

**产出**

- `strategy.md` §1 由"一张 ①–⑥ 表"重写为**状态机**：§1.0 开篇讲清"`plan()` 里没有跨回合
  角色状态、状态是每回合从 `Turn` 现推"（并点出全场仅三处跨回合状态，都在任务线）；
  §1.1 两段式（第一段定行为 / 第二段解路径 —— **读某一支策略只看第一段**）；§1.2 判据链
  ①–⑦（钉死 / 出盒 / 救援 / 夜里 / 收工 / 开拓者 / 工人），每级列出**命中之后**与
  **什么时候不占这一级**（含"② 静态走不出去 ⇒ 让路给 ③ 的自救"）；§1.3 夜里三支 N1–N3；
  §1.4 工人白天阶梯 W1–W7（含 `target is None` 让 W3/W4/W6 同时失效这一实现要点、
  W6 的 `True`/`False` 契约、W4 与 W6 是同一函数的两处调用）；§1.5 共用记账（补了
  `worker_no` 只随"走到阶梯的工人"递增）。
- **修掉两处陈旧**（本步真正的收益，代码一行没动）：
  ① 旧表的 ⑤/⑥ 是**反的** —— 代码里收工闸门 `_leave_for_the_post` 在开拓者 / 工人分支
  **之前**，且带 `not _ring(turn)` 这道前门；夜间那一格只写了 `_defend`，**漏了先于它**的
  `_upgrade_station` 与"怪清完的近矿经济"（§5 的标题与首段同步改正）；
  ② §3 漏了开拓者真空闲那一支的开关（`weapon_gap` ⇒ 券线整个跳过、直接卖矿，第 50 步）。
- `worker.md` §0 与 `CLAUDE.md` 文档地图各加**一句指针**：状态机的权威在 `strategy.md` §1，
  `worker.md` 保留"优先级阶梯"的叙事口径 —— 两份文档写同一件事，明写谁为准，防漂移。

**仍生效的已知不确定性**

1. **W1 认领先于成功**（`budget -= 25` 与占建造格发生在判"贴没贴着 / 走不走得到"**之前**）⇒
   越权（bug）或落点当时不可达时，那一格本回合**既不建也无人补**。两个触发条件都极罕见；
   本步只把它写进文档、**没有改行为**（要改就是 `_intents` 里那几行挪个位置，单独一步）。
2. **文档与代码的一致性没有自动校验**：状态机是从代码读出来的，**代码是唯一权威**。
   改 `_intents` 的判据顺序 / 闸门时 §1.2–1.4 要同步改 —— 没有 lint 盯着这件事
   （`CLAUDE.md` 明写"不加分层 import-lint"，同理不为文档再建一套）。

---

## 第 53 步：注释格式统一到 `task_channel`（用户要求，全仓纯注释）

**目标**：用户口径——"注释格式按照 `task_channel` 这样写，将所有注释重构后提交"。
第 48 步已把**内容**清干净（步骤号、决策史、标记符号），但**格式**各写各的：同一种判据
在 A 处是一句标签、在 B 处是一段论证；`#:` 在函数体内外混用；docstring 篇幅从 1 行到 15 行
都有。本步把格式钉死并把存量全部对齐。**纯注释，一个字符的代码都不动。**

**产出**

- **格式规范（已写进 `CLAUDE.md` 编码规则第 6 条，含参照物名）**：docstring = **是什么 + 契约**
  （一两行，必要时补几行边界/坑）；内联 `#` = **一句话的"做什么"标签**；关键判据压成一句保留；
  `#:` **只留给模块级/类级常量**，函数体内一律收成 `#`。参照物 = `planner.task_channel`。
- **36 个 .py 全量重写**（19 `src` + 16 `tests` + `main3.py`）：`1264 插入 / 1574 删除` ⇒ **净 −310 行**，
  与"注释行 3704 → 3395"（docstring −263、`#` −46）对得上 —— **减掉的行全是注释，代码行数一行不变**。
  函数体内 `#:` 收成 `#`：`test_app.py` 24 处、`test_game_planner_task.py` 6 处、`wall.py` 6 处、
  `economy.py` 4 处、`sop.py` 2 处、`grid.py` 1 处；模块/类级常量上方的 `#:` 全部保留。
  块状判据**保编号语义**（例 `# ② 回灌结果、这轮绝不发命令（必须压在 ③ 前）`）——
  编号是判据链的优先级信息，不是步骤号，不在删除之列。
- **测试文件的专门口径**：测试的"契约"就是**这条用例守什么、夹具为什么非得这么搭** ⇒
  保留"挡住哪个缺陷 / 夹具前提（必须堵满 2 格门否则空转）/ 反向验证结论 / 红线理由 /
  守门用例指向"，删掉多段式现场复述与旧写法史。类级 docstring 8~14 行 → 6~8 行，用例 docstring ≤5 行。
- **门禁（三道，全过）**：① `logs/comment_check.py`（AST 挖掉**所有**裸字符串语句后与 `HEAD` 逐字节
  比对）对 36 个文件**逐一 `OK`** ⇒ 可执行代码、prompt 模板常量、断言 msg、报文 dict、
  日志格式串**零改动**（`wall.py` 另自查了 625 条非 docstring 字符串常量与 HEAD 完全相同）；
  ② **376 条用例全绿**；③ 全仓 `-W error::SyntaxWarning` 扫描 **0 个文件**。
- **顺带兑现第 48 步的不确定性 2**（`chat.py` 的 `\w` 噪音）：docstring 改 raw 串。

**仍生效的已知不确定性**

1. **断言 msg 里的步骤号仍在**（如 `"答案轮不提问也不压缩（第 47 步）"`）——那是**运行时字符串
   字面量**，改它会动 AST、过不了本步门禁。按第 48 步的边界延续；介意的话单独一批改
   （改了要同步改对应断言）。
2. **行尾风格在工作区里不齐**：`src/` 与部分 `tests/` 文件被脚本写成 LF，另一部分 `tests/`
   文件保住了 CRLF。`core.autocrlf=true` ⇒ **入库内容一致、`git diff` 无换行噪声**，
   但下次谁用文本模式读写这些文件时，会再翻一次盘。不是缺陷，是没人管这件事。
3. **"注释格式"没有自动门禁**：AST 等价守卫的是"没动代码"，管不着"注释写得对不对"——
   `#:` 是否落在类级、docstring 超没超 5 行、判据有没有被压没，全靠人看。
   `CLAUDE.md` 规则 6 已写明格式，但**规则不会自己生效**；再加 lint 属过度设计
   （与"不加分层 import-lint"同源）。
4. **压缩到边界的守门结论**：`wall.py` 几个守卫类 docstring 从 8~14 行压到 6~8 行，
   取舍是"保结论、删推导"——若日后读不懂某条夹具前提，回查本文件对应步骤。

---

## 第 54 步：双火箭交替改本地开火账（用户拍板的方案）

**目标**：第 49 步的奇偶轮转在"判题器真执行 3 回合冷却但不发字段"的世界里每 4 回合
白打 2 发（87/88 回合打到冷却中的炮 = 指令执行失败、白丢火力）。用户拍板：**本地记录
操作记录，在非冷却回合操作**。

**产出**

- `planner._fired`（模块级 dict `{武器 id: 发出 attack 的回合号}`，**planner 侧第一处
  跨回合状态**）+ `ROCKET_COOLDOWN = 3` + `_cooling(weapon, round_no)` 谓词：
  payload 的 `cooldown` 优先；火箭字段缺失（-1）时查本地账——发出后 3 回合内判冷却。
  `_fire` 在**真发出去**那笔才记账（`_emit` 成功才算），加特林/电磁不记（文档口径恒 0）。
- `_defend` 组内挑炮：就绪 = `not _cooling`（就绪的并列仍按 round_no 轮转，给夜初
  双就绪定序）；**都冷却 ⇒ 待命一发不打**——两座火箭 3 回合冷却的物理极限是每 4 回合
  2 发，现在打满、零浪费。
- TDD：先放空 `_fired` 让用例能跑（否则 setUp 直接 AttributeError），RED 干净地挂在
  87/88 两回合（旧轮转照发）；`test_the_rocket_pair_alternates_without_cooldown_data`
  重写为 `test_the_rocket_pair_alternates_by_local_record`（序列 85→201、86→200、
  87/88→空、89→201）。`NightWeaponTest.setUp` 清 `_fired`（模块状态跨用例串味——
  上一条用例打出的那发会把同一座炮判成冷却中）。**376 条全绿**。
- 状态过"加第四处跨回合状态"那道门（CLAUDE.md 架构节 ④）：观测性、按 id 键控、
  期满自动过期、武器毁/重建换 id 旧账自愈；卡住的最坏代价 = 某座火箭少打 ≤3 回合，
  不碰红线。第 49 步不确定性 1（轮转的浪费回合）就此撤销。

**仍生效的已知不确定性**

1. **attack 执行失败也记账**：目标选择已保证射程内，但若真有执行失败（如机器人那回合
   恰好移开），冷却账会白锁 3 回合——保守方向（少打不打错），接受。
2. **本地账与判题器冷却若不一致**（例如判题器冷却其实是 2 回合）：我们会少打（晚一回合
   复发），不会非法——首场日志对一下"相邻两次同座火箭的间隔"即可校准
   `ROCKET_COOLDOWN`（一处常量）。

---

## 第 55 步：夜里真几何实测——敌人一出范围就打、双火箭无缝交替（用户报的 bug ×2）

**目标**：用户报的两条夜间症状——① 敌人进了范围**不是马上就打**；② 一个角色操两座火箭时**没法
无缝切换**、两座炮不能连续运行。第 49/54 步的"交替"只在手搭地形上绿，真几何（基地 `(10,24)`、
火箭 `(12,24)/(12,25)`、加特林 `(12,22)`、18 格墙砌满）上整夜只有一座火箭偶尔响，大半回合
一发都没有。用户拍板：**夜间机制 + 白天收工闸门一起改**（收工就把人送到共用操作位上）。

**产出**

- **三条叠加的根因**（真几何复现出来的，缺一条都治不好）：
  - **D1 自己挡自己**：`model._entries` 把 `teamOur.roles` 写进网格 ⇒ `turn.map.blocked` 里混着
    我方角色的格子。整份拿去算 `_operator_spots`，站在共用操作位 `(11,25)` 上的操作者就**看不见
    自己的岗位**（那格被自己挡掉）⇒ `spots` 空 ⇒ 跳过整组火箭、转头去加特林、最后冻在 `(10,25)`。
  - **D2 走不上去**：`step_toward` 的契约是"贴着 goal 即到"（`dist(goal) <= 1 ⇒ None`）。共用操作位
    是**要踩上去**的格子，而它的唯一通路是 `(10,25)` ⇒ 角色停在 `(10,25)`、`q.step` 每回合返回
    `None`、**原地卡死**，同时还把整组火箭认领走了（另一个角色也别想用）。
  - **D3 只贴一座就不打**：`ready`/`cooling` 只从 `adjacent`（切比雪夫 ≤1 的炮）里建 ⇒ 只贴着
    `200` 时，`200` 一冷却就一发不发——哪怕 `201` 就绪。
- `grid.step_onto(pos, goal, blocked, size)`：与 `step_toward` 同构（BFS 最短路、跳障碍与越界、
  返回第一步），**只换终止条件与守卫**（`nxt == goal` 收；`pos == goal` ⇒ `None`）。文件里已有三个
  因终止条件分家的 BFS 变体，这是第四个。**两个函数不能互换**：`step_toward` 到不了这种格子，
  `step_onto` 停不进挡路的 goal。
- `planner`：
  - `_Move.onto` + `_Queue.step(..., onto=False)` 透传；`_walk_out` 里 `m.onto` 时主路与 `avoid`
    回退路都走 `step_onto`。
  - `_post_spots(group, turn, role)`（`_operator_spots` 的唯一调用者）：**自己人一律从障碍里剔掉**
    （`blocked - cells`），**别人占着的岗位排除掉**（那格被占就得换一组，否则一个走不进去、一个
    干等着）。自己那格保留 ⇒ 站在岗位上的人认得出自己的岗位。
  - `_defend` 重排成**先开火、打不了才挪岗**：贴着组内某座就先打它（只贴一座也打 ⇒ 治 D3）；
    贴着整组、又都打不了 ⇒ 原地待命（不换组）；只贴一部分、或压根没贴着 ⇒ 朝**多座组共用的
    那个操作位**走一格（`onto=len(group) > 1`）。
  - `_leave_for_the_post` 的目标从"最近的空闲炮位"改成"**那一组的岗位**"（同一个 `_post_spots`）：
    多座组站到共用操作位、单座组站在炮旁 ⇒ 天黑时人已经在岗、夜里第一回合就交替。新增
    `_steps_to_post` 统一步数口径（多座组的岗位是空地 ⇒ "+1 站上去"；单座组的岗位就是那座炮
    ⇒ `steps_between` 的口径就够）。照旧**只发 `move`** —— 白天发 `attack` 是非法指令。
- 用例 **+8 条**（376 → **384 全绿**）：
  - `test_game_grid.StepOntoTest` ×3：绕障走上去、已在上面 ⇒ `None`（并对照 `step_toward` 在这一格
    返回 `None`）、目标被围死 ⇒ `None`。
  - `test_game_planner_night.NightPostTest` ×4：**真几何**且**把角色铺进 `entries`**（现有
    `test_the_rocket_pair_alternates_by_local_record` 的手搭地形漏了这一步 ⇒ 它在旧代码上也是绿的，
    那正是 D1 在本仓潜伏这么久的原因）。站在 `(11,25)` 的操作者按本地开火账交替
    （`{85: 201, 86: 200, 87: 空, 88: 空, 89: 201}`）；站在 `(10,25)` ⇒ 这一回合 `move` 到 `(11,25)`；
    只贴着 `200`（`(12,23)`）⇒ 这一回合**先打 `200`**、下一回合才挪岗；队友站在 `(11,25)` ⇒
    站着的那位开火（`controllerId` 钉住是谁）、另一位不抢不撞、改奔加特林。
  - `test_game_planner_wall.DayEndGateTest._steps_home` 改成"到最近**岗位**的 BFS 步数"（火箭对
    `(11,25)` 且 +1、加特林 `(12,22)` 不加）+ 新增 `test_the_gate_aims_at_the_shared_operator_spot`。
    旧 helper 的口径与 planner 不一致却恰好没让边界用例挂掉 ⇒ 改它是为了让 docstring 那句
    "与 planner 同一个口径"成真。
- **验证**：① 全集 **384 全绿**；② **反向验证**：把 `_post_spots` 换回 `_operator_spots(group,
  turn.map.blocked, …)`、`q.step` 去掉 `onto`，上面 4 条夜间新用例**逐条挂掉**（不挂说明用例测的是
  别的东西）；③ **起服务打真请求**（第 54 步之后第一次动夜间路径）：`docs/request.txt` 起一发、
  再把工人手工挪到 `(11,25)` 连推 85→89 回合，日志里 `attack` 的 key 在两座火箭 id 之间交替
  （85→201、86→200、87/88→无、89→201）、`(10,25)` 那位的 `move` 目标正是 `(11,25)`。

**仍生效的已知不确定性**

1. **`(11,25)` 是"口袋里那格"**：唯一通路是 `x=9` 那条一格宽的走廊。同事正好停在那条走廊上时，
   BFS 判不可达 ⇒ 这一步不走（少走一步，不赌碰撞）。夜里两人各占一组时不会互相堵（站在岗位上就
   不挪），但白天收工会把人送进口袋、第二天早上要多走 6 步出来 —— **未实测**。
2. **夜里选组按切比雪夫 `dist`、收工闸门按 BFS 步数**：两处口径不同是**有意的**（选组问"离哪组近"，
   闸门问"来不来得及"，后者是回合预算问题）。并列时（同时贴着火箭与加特林）落到 `min(w.id)` 上
   —— 炮 id 由 payload 决定，这里只是**稳定排序、不是几何判据**。
3. **"先开火、打不了才挪岗"的代价**：挪岗那几回合角色在路上、火力空窗；换来的是一旦站上共用
   操作位就整夜不再移动。**未实测**（哪个更值只有实盘的两组日志能回答）。
4. **共用操作位只有 `(11,25)` 一格**（`(11,24)` 是基地、`(13,*)` 是墙）⇒ 双火箭**必然**共用一个
   操作者，"两人各操一座"在这个阵形下不存在。阵形若再改，"共享操作位还在不在环内、走不走得进去"
   要先在 `BuildGeometryTest.test_the_two_rockets_share_an_operator_spot` 那一层验。

---

## 第 56 步：白天的"估算距离"口径修正——自己人不算障碍 + 三处差一格（用户报的 bug）

**目标**：用户报"**工人白天一直卡在某个地方不动，检查是不是移动到障碍物 / 阻塞不动**"。第 55 步
治的是夜里"自己挡自己"，同源的那半个病白天还在：`_operator_spots` 那条边改了，**距离估算没改**。

**产出**

- **根因 ①：估算把同事当障碍**。`model._entries` 把我方角色写进网格 ⇒ `turn.map.blocked` 里混着
  队友和**自己**。盒子里本来就只有几条一格宽的走廊（第 34 步那张表 34），谁停在走廊上，同事的
  "矿 → 最近的炮位"就一律算出 **-1**（不可达）⇒ `_mine_spare_ore` 把每一座矿都判成不可行 ⇒
  整段静默放弃、**一回合一条指令都不发**。`_ring` / `_walled` / `_post_spots` / `_stuck_inside` 早
  就按"自己人算路过"处理，**只有距离估算漏了** —— 这就是半个病。
- 新增 `planner._passable(turn) -> set[Pos]`（`turn.map.blocked - {r.pos for r in turn.roles}`），
  **10 处**估算改走它：`_stuck_inside` / `_post_spots`（两处原样内联，等价搬迁）+
  `_repair_line` / `_stones_to_mine` / `_sell_ore` / `_mine_spare_ore` / `_detour_buy` /
  `_detour_sell` / `_upgrade_line` / `_leave_for_the_post`。
  ⚠️ **这是"问路有多远"，与"这一回合实际怎么走"是两套口径**：`_Queue.step` / `_walk_out` 那边
  **必须**把同事当硬障碍（撞上就是执行失败、双双停住），`_slots`（落点占没占）、`_walled`
  （门口那 2 格）、`_ring`（墙格候选）、`_rescue`（**仍然没改**，见不确定性 2）各自有各自的判据，
  一处都没跟着动。函数 docstring 把这条分界写在最前面。
- **根因 ②：三处把"差一格"当成了"贴着"**。`steps_between` 的口径是 **0 = 已经贴着目标**
  （"还要走几步才贴上"），而 `_sell_ore` / `_repair_line` / `_upgrade_line` 写的是 `<= 1` ——
  于是**离小贩/商店两格（切比雪夫 1）就发 `sell` / `buy`**：判题器那边是**非法指令**（红线），
  而且每回合重发一次；本地全是合法报文，只有判题器会说"不"。三处改成 `== 0`。
- **根因 ③：`_detour_buy` / `_detour_sell` 的空转**：已经贴着小贩/商店时 `via == 0`，绕一步 =
  原地不动，可函数 `return True`（声明"我处理了"）⇒ 第二段 `q.step` 走不了 ⇒ **这一回合空指令**。
  两处补 `if via == 0: return False`（把这一趟交回直路）。
- 用例 **+6 条**（384 → **390 全绿**）：
  - `test_game_planner_economy.SpareOreTest` ×2：真几何（火箭 `(12,24)/(12,25)`、加特林 `(12,22)`、
    18 格环、角色铺进网格）+ 同事站在口袋咽喉 `(11,25)` ⇒ 另一个工人**照样`move` 向石矿**
    （旧口径：每座矿 `back = -1` ⇒ 整段放弃）；站在商店贴着那格（`(24,27)`）不挡同事的采矿差事。
  - `SellOreTest` / `UpgradeLineTest` / `RepairTest` 各 ×1：离小贩/商店**两格**（切比雪夫 1）
    ⇒ 这一回合是 `move` 且**真的更近了**，不是隔着一格 `sell`/`buy`。
  - `test_game_planner_wall.DayOneFinishTest.test_the_workers_never_stall_a_whole_day`：**整网** ——
    同一局面连跑 **2 天 260 回合**（`MINE_CHARGES = 40`，免得"矿采光了"被当成停滞），数每个工人
    **白天**最长的一段连续空指令，**断言 < 30**。实测最长 12，且那 12 轮是合法的保守待命
    （第 2 天 R171-182：两人都在小贩边卖完货、背包空了，剩下两座矿来回 26/31 步 > 当天剩余预算
    ⇒ 蹲着等天黑）。旧口径下是 **70/70（一整天）、金币停在 0**。
- **验证**：① 全集 **390 全绿**（12.4 s）；② **反向验证**：把 `_passable` 改回 `turn.map.blocked`、
  三处 `== 0` 退回 `<= 1`、两处 `via == 0` 关掉 ⇒ **6 条新用例逐条挂掉**（2 条 ERROR + 4 条 FAIL，
  含整网那条；顺带 `RescueTest.test_colleagues_in_the_doorway_are_not_walls` 也挂 —— 它本来就守
  `_stuck_inside` 的同一口径，正好说明那次搬迁是等价的）；③ **3 天合成局**（`logs/day_sim.py`，
  每座矿 100 次）：**非法/撞障碍指令 0 条**、环 18/18、三座武器、**金币 142**（改前恒 0），
  白天最长空转 13 轮（第 1 天末尾，gated/预算待命，设计如此）；④ 起服务打真请求（见下）。

**仍生效的已知不确定性**

1. **估算乐观、走路悲观 ⇒ "估得到、这一步迈不出去"这一类还在**：`_passable` 把同事算成路过，
   而 `_Queue.step` 仍然撞他就停 ⇒ 估算说"矿能去"、这一回合却可能因为走廊被占而空转。它**自愈**
   （同事下一回合就挪窝；3 天合成局实测最长 12~13 轮），但**不是消除**。要真消除得让"预留 + 落子账"
   参与估算 —— 那会把 `_walk_out` 的记账渗进 `_intents`，判为过度设计，**不做**。
2. **`_rescue` 那条走路仍然把同事当障碍**（引用的 `walk` 没换 `_passable`）：同事站在门口时，
   "这个人出不去"会被多判一次 ⇒ 白拆一格墙（`HOLE_MIN_LEFT`/`HOLE_PATCH_LEFT` 两个窗口不相交 ⇒
   一天最多拆一次，代价有上界）。同上，为一致性改它反而会削弱"真被堵死"的判据，**留观**。
   对照：`_stuck_inside` 里那条**同口径的**闸门已经改了（它问的是"假设砌满之后出不出得去"，
   同事只是路过）—— 两处判据不同是有意的。
3. **`_pick_ore(want_stone=True)` 仍按切比雪夫选石矿、再让 BFS 去否**（切比雪夫挨着但绕远/不可达
   时会白挑一次 ⇒ 那一回合空转，下回合重挑）。第 33 步就记过的老账，本步没动。
4. **"预算拦住就不去"是保守方向、不是 bug**：本局面 12 轮的那一段、合成局 13 轮的那一段都是它。
   真要让工人"走一半也算动"，得引入"部分路程"的中途目标 —— 会跟"回得来"的约束打架，**不做**。
5. **真实地图的走廊宽度、矿/小贩分布都可能与合成局面不同** ⇒ 本步的收益（金币 0 → 142）只在
   合成局面上量过；实盘要看"工人有没有连续一整天不发指令"。
6. **手搓 `Map` 的用例必须把角色铺进 `entries`**（CLAUDE.md 已记）：这一步两条新用例都铺了，
   但这是纪律不是机制 —— 漏了的话用例连旧代码都放得过（第 55 步就栽过一次）。

## 第 57 步：围墙改 U 形（14/16 格）+ 删整套临时门机制（用户口径）

**目标**：用户给出一套新的工人决策链，其中"建墙逻辑：**上方、下方、面向敌人放建满，后方空出来作为
路径**"推翻了第 17/33/34 步那套"闭环 18 格 + 背面 2 格永久门 + 白天拆一格当临时门"的几何。几何一
换，`_dig` / `_door_open` 那套机制**失去前提**（后方常年敞开 ⇒ `_door_open` 恒真 ⇒ 墙被打掉也
不补），用户明示**整套删掉**。所以几何与机制必须同一步落地。

**产出**

- **`grid.wall_cells(base, width, *, sealed=False)`**（`grid.py:193`）—— 从"18 格闭环、封口格排
  最后"改成 **U 形 14 格**（正面列 6 + 上下两行各 4，背面整列 6 格不砌），`sealed=True` 再补背面
  **两个角格**变 16 格（后方通道 6 → 4 格）。顺序 = 沿环走一圈：**正面列（迎着机器人）→ 下行 →
  〔背面两角〕→ 上行**。左半基地 `(10,24)`、`width=41`：正面列 `x=13`、背面列 `x=8`。
- **`grid.door_cells`** 语义从"永远不砌的那 2 格门"变成**后方通道**：背面列里不在 `wall_cells` 的
  格（14 格时 6 个、16 格时 4 个）。它仍是盒子唯一的进出口 —— `test_the_door_is_the_only_way_in`
  照旧成立（砌满 20 格边框 ⇒ 两个方向都 -1）。
- **`planner._sealed_back(turn)`**（新增，一处）：`turn.round_no > 2 * ROUNDS_PER_DAY` ⇒ 第 3 天起
  补角格。判据**只能用回合号**（环上"没砌"与"砌了又被拆"在地图上逐字节同形）。
- `sealed` 透传三处：`_ring` / `_walled` / `_rescue`（**漏一处就是静默不一致**）。
- **删**：`planner._dig`（+ `_Queue.step` 里的钩子与 `dig` 形参、`_intents` 的 `dig=False` 调用点）、
  `_door_open`、常量 `HOLE_MIN_SAVING`(5)、`HOLE_PATCH_LEFT`(15)、`_intents` 里的补墙窗口那一整支。
  **留**：`HOLE_MIN_LEFT`(30)（现在是"拆墙放人"的时间门，`_rescue` 用）、`_after_first_day`（同上）。
  `_repair_line` 里那道时间门（原 `HOLE_PATCH_LEFT`）换成 `3 * TIME_MARGIN`（同一个量级，不引入新常量）。
- **注释同步**：`_trapped` / `_stuck_inside` / `_stones_to_mine` / `_sell_ore` / `_leave_for_the_post`
  / `_Queue` 类 docstring 里"背面那 2 格门"一律改成"后方通道"。测试侧 5 处"18 格"改成"14 格"。
- 用例 **+1 条**（376 → **377 全绿**）：新增
  `BuildWallTest.test_the_third_day_seals_the_back_corners_without_rebuilding` —— 拿第 1 天砌满的
  14 格当初始局面跑第 3 天：两个角格按 `_sealed_back` 的顺序补上、**已砌的 14 格一格都不重砌**、
  补完就收手。**反向验证**：把 `_sealed_back` 改回恒 `False` ⇒ 这条当场挂（`[] != [Pos(8,26), Pos(8,21)]`）。
- 删掉 15 条随机制消失的用例（`DigTest` 10 条、`DoorTest` 4 条、`HoleLifecycleTest` 1 条），
  `WallRingTest` / `WallGateTest` / `RescueTest` / `BuildWallTest` 按新几何重写。

**验证**

- ① 全集 **377 全绿**（12.3 s）。
- ② **起服务打真请求**：`docs/request.txt`（第 85 回合、夜里）⇒ 合法报文、三个顶层字段齐全、
  无异常；把同一份 payload 的 `roundNo` 改成 10（白天）再打一次 ⇒ worker 发 `collect (4,24)`
  （石矿）—— 白天线走通，无异常。

**仍生效的已知不确定性**

1. **新的 14/16 格建造顺序没有实测背书**。旧的"沿环走一圈"那个 18 格顺序是第 33 步实测出来的
   （别的排法一天砌不满）。新顺序我按"正面优先 + 绕行小"推，**只有 `DayOneFinishTest` 兜底**
   （第 1 天 70 回合必须砌满 14 格）—— 合成局面过了，真实地图没验过。首场看"第一个白天末尾砌了
   几格"，14/14 才算过。
2. **第 3 天补角格那一刻，环上凭空多出两个缺口**：此后 `_ring` 非空 ⇒ `_rescue` 整个不生效
   （它开头就判 `if _ring(turn): return False`）；那两格若被机器人/敌方建筑占住 ⇒ 永久补不上、
   环停在 14 格（无害，但"环砌完了"这个前提在整张网上是拿 14 格判的）。
3. **删 `_dig` 之后救援慢一回合**：`_Queue.step` 不再有拆墙钩子，`_rescue` 只能老老实实走到墙边
   （它本来就要求贴着才 `remove`）。**没有别的调用点** ⇒ `remove` 现在全天最多一条、只在第 2 天
   起的白天、只由 `_rescue` 发。
4. **`_trapped` / `_walled` 在新几何下更不容易触发**：后方敞口从 2 格变 6 格（第 3 天起 4 格），
   堵满它比堵满 2 格难得多。闸门保留（删它的风险比留它大），但**几乎不会真的合上** —— 日志里
   见不到它是设计如此，不是坏了。
5. **`docs/request.txt` 不是本几何的产物**（它是 `roundNo=85` 的中局快照、墙是手画的）：拿它打
   本地服务时，`_ring` 会把新几何下"还没砌"的格报成缺口。这**不是 bug**，但因此**不能用它判断
   建墙线对不对** —— 只能判断"跑得起来、报文合法"。

---

---

## 当前仍悬着的事

跨步重复、或不归属某一步的未了结项。**已实现的下一步不在此列。**

| # | 事项 | 出处 | 判据 / 症状 |
|---|---|---|---|
| 1 | **协议形状与沙盒链路一次实盘都没跑过** —— 判题器的 LLM 认不认嵌套形状是本项目**最大的单点风险**。⚠️ **第 37 步起严格只认新形状**（用户拍板）：属性式 / 裸参数 / 裸工具块全部落重问，每中一次白花一回合 | 第 16/18/30/**37** 步 | 日志里有没有 `沙盒：回「[exitCode:N]…」`；任务行"上一轮模型回复"里**旧形状出现频率**（高频 ⇒ 补示例或恢复兼容层，回退只需改 `chat._params` 一处） |
| 2 | **答案格式与 prompt 措辞**完全未知（通过率 = 正确字段数 / 全量字段数 ⇒ 是多字段结构化答案）。⚠️ 第 35 步加「工作流」、第 36 步换两块示例，**第 37 步整份模板搬进 `prompt.py`（六段：role定位/工具描述/输出格式/示例（占位）/沉淀的SOP/注意事项），工具形状换嵌套式 + `<name>` 行** —— 措辞全是拍的 | 第 11/35/36/**37** 步 | `errorCode 2` 出现 ⇒ 改措辞。纠错段带回了"你上次答错了"，日志里能看到 LLM 每次换没换答案；范式有没有让它省掉一回合看任务行的 `executeCmd` 条数；还要看"同轮沉淀 + 作答"的比例（`submitAnswer` 与 `prompt=""` 同时出现 = 省下了一回合） |
| 3 | **`sell` 发没发出去、铜涨没涨** | 第 22 步 | `【动作】` 里有几条 `sell`；`【我方】` 那行的 `铜N` 有没有在涨 |
| 4 | **卖矿阈值（货值 ≥ 2 × 距离）调参** —— 这一步唯一的旋钮 | 第 22 步 | "矿堆着却没卖过" = 阈值太高（样例距离 10 ⇒ 铜要 4 块） |
| 5 | **`buy` / `use` 券线的报文形状没有实证**（response.txt 只有 move/build/remove；线已实现，回退改 `to_wire` 一处） | 第 29 步 | 实盘若报"指令非法"先查这两个形状；升级优先级 `UPGRADE_CHAIN` 是我的判断未实测 |
| 6 | **`vendorShopList` 是"收购价"还是"售价"是推断** | 第 15 步 | 下一场实测对一下金币数。若其实是售价，"价 0 的矿不去采"会误伤 |
| 7 | **stdout 会不会把 64KB 管道写满** —— 判题器若**不读**子进程 stdout 就会阻塞在 write 上 ⇒ 响应超时 ⇒ 直通红线。**第 26 步起顶格回合 ≈603KB（第 36 步重测 604147）⇒ 一回合就满**（用户拍板接受） | 第 9/12/20/26/35/**36** 步 | **本地验不了**。首场若出现不明"响应超时"，第一嫌疑是它；回退 = 把 `LOG_TEXT_MAX` 调回去（一处常量）。⚠️ 第 32 步把这里的 `≈363KB`（与 `app._log` 的表不符）按表改正 |
| 8 | **可建造区公式来自图片**（`docs/pic/build_map.png`），任务书正文没有 | 第 7/8 步 | 算错 ⇒ `build` 落点非法；大概率不计异常但**未确认** |
| 9 | **武器射程口径矛盾**（样例 4/7/∞ vs 任务书表格 3/6/10）与 **`cooldown` 样例里没有** | 第 10/13 步 | 以 payload 为准。若实测发现火箭射程其实是三者里最短，第 13 步的配对要翻 |
| 10 | **机器人刷新方向只来自示意图**（正文没写） | 第 7 步 | 判据封装在 `grid._front_back` 一处，首场后按实测翻转 |
| 11 | **武器 `name` 取值只对 `wall` 有实证**；**武器上限口径原文冲突**（取保守的全局 ≤3） | 第 7 步 | 建武器落款错 ⇒ 25 金币白花 |
| 12 | **夜里机器人打不打野外的工人** —— ⚠️ **第 57 步把背面从"2 格门"改回"整列敞口"（14 格时 6 格、第 3 天起 4 格）⇒ 这道口子常年开着**，机器人若会进环，路径就是它（详见 26） | 第 8/10/17/33/**57** 步 | 首场若发现机器人绕圈不挨打、或直接摸到炮位，第一嫌疑是它 |
| 13 | **闸门 (1)/(2) 在新几何下几乎打不着**（第 33 步门收到 2 格时它反而过度触发，第 57 步把敞口放回 6/4 格 ⇒ 又难堵满了） | 第 17/33/**57** 步 | 日志里从没触发过 = **设计如此，不是坏了**；真要盯的是**误触发**（第 33 步那场 16 回合活锁就是它过度触发的产物） |
| 14 | **SOP 是否真的自进化**；**SOP 一旦存错就是整场的事**（没有重置、没有遗忘、不按任务分区）。⚠️ 第 36 步起入库前挖掉成对 `<answer>`；**第 37 步起是流程表**（`name` + `sop` 两参数：同名覆盖、异名追加、超 5 条丢最旧且留痕） | 第 18/19/36/**37** 步 | `【SOP 更新】：流程「名」…` 出现几次、流程名的分布（LLM 复不复用名字 = 同名覆盖的"更新"路径走不走得通）；同行有没有 `剔除 N 处 <answer> 段`（N 长期非 0 = 禁令没被听进去）；有没有 `丢最旧`（5 条不够的信号）。升级触发：同一任务连续 ≥3 回合 `llm_resp` 都是 `SOP2Prompt` 且始终没有 `submitAnswer` |
| 15 | **地图右边框是 ASCII `|`、左边框是全角 `│`** —— 看着像笔误 | 第 23 步 | 确认是笔误的话改 1 个字符即可（不影响任何断言） |
| 16 | **`_char` 恒返回 1 字符没有断言守着**（地图虽退出日志，`render()`/`LEGEND` 仍在、只剩用例在用） | 第 12/21/23/26 步 | 要回日志时先补那条断言再动字符表 |
| 17 | **`SOP_MAX = 1000`（第 37 步起是**单条流程**的上限）与 `SOP_FLOWS_MAX = 5`（条数上限，超了丢最旧）都是估的数**（`LOG_TEXT_MAX` 已放宽到 40000） | 第 12/14/18/26/**37** 步 | 截断标记会直接报"共 N 字"、`丢最旧` 会点名，照那两处调常量 |
| 18 | **新阵顶线一侧仍有理论死锁**：后列 op 从走廊里贴 `(9,24)` + 前下角 op 站 `(12,23)` ⇒ 前上角够不着 | 第 24 步 | 夜里若某座炮整晚无人操，先查这两个站位 |
| 19 | ~~**会话 prompt 无上界增长（先不压缩）**~~ ⇒ **已被第 39 步兑现**：渲染改组为"题目 + 执行摘要 + 最近 2 轮"，prompt 从 O(全部历史) 变 O(窗口)；存储仍全量。承接它的不确定性见第 39 步（LLM 写摘要的配合度、四槽装不装得下关键数据、`_WINDOW` 调参） | 第 25/39 步 | 长任务（≥10 轮命令）若开始报响应超时，第一嫌疑换成"摘要长期缺失 ⇒ 压缩空转"；看任务行 prompt 里 `【历史摘要】` 的出现比例 |
| 20 | **「请继续。」与「# 对话记录」措辞未实测**；同文任务续会话若实盘发现是"新题"会被旧尝试污染 | 第 25 步 | 实盘见答非所问/复读先改措辞；身份细分换 `taskPosition` 等字段 |
| 21 | **弹道是否被墙挡没有明写**（只明说火箭不被挡）—— 若被挡，武器打不出盒子，防守阵形整个要重想 | 第 27 步 | 首场夜里 `attack` 落点与机器人掉血对不对得上 |
| 22 | **加特林实际命中"弹道最近"与记账估计有偏差**；Bresenham 变体未建模；L2/L3 多目标（90° 锥/多导弹/穿透）没做 | 第 27 步 | 只影响次优不影响合法性；做升级线时一并精化 |
| 23 | ⚠️ **升级线（第 29 步）让 L2/L3 变得可达，而打击侧还按 L1 发包**：`Attack.to_wire` 恒发**一个** `targetPos`，任务书约定的是"长度 = 武器当前等级" ⇒ **发少了算"指令非法"（红线）还是"执行失败"文档没写**（第 22 项"不影响合法性"那句在这一半上不再成立）；伤害常量也没跟 `Weapon.level` 联动 | 第 29/31 步 | 实盘若出现 `attack` 相关的 `errorCode 4`（指令错误）先查这一条 —— **这是唯一可能直通红线的新缺口**。回退 = 券线只买到 L1→L2 之前停下（改 `UPGRADE_CHAIN` 一处），或让 `Attack` 按 `level` 补足落点 |
| 24 | ~~**`remove`（拆墙）没实现 ⇒ 正面开口只在第 1 天生效**~~ ⇒ 第 34 步兑现，**第 57 步又整套撤掉**：后方常年敞开 ⇒ 不再需要"白天开个洞当门"。`remove` 现在只有 `_rescue` 一个调用点 | 第 17/33/34/**57** 步 | 第 58 步会把 `_repair_line` 的 L1 墙也接到 `remove` 上 ⇒ 届时这条要再改一次 |
| 25 | **收工闸门（第 33 步）完全没实测**：`day_rounds_left − 1` 那个余量是**拍的**；"环砌完了才收工"这个取舍也没验过 | 第 33 步 | 白天末尾若出现"该采石却往回走"，先看它。坏方向安全（只发 `move`，走不动=空指令，不碰红线） |
| 26 | **后方通道比旧的门宽**（第 57 步起 6 格、第 3 天起 4 格）⇒ "被堵在门口"变难；反向的新问题是**这道口子常年开着**（与 12 同源） | 第 33/**57** 步 | 夜里机器人会不会站进通道、堵多久、会不会进来，**没数据**。若实盘见"工人整晚在盒外"或"机器人贴着炮位"，第一嫌疑是它 |
| 27 | **`steps_between` 的 `-1`（不可达）分支实盘没见过** —— 合成局面里造得出来（目标被围死），真实地图上会不会出现（例：工人被机器人堵在盒子里）不知道 | 第 33 步 | 症状是"**该干的活突然不干了**"，**日志上看不出来** ⇒ 留意"某天工人完全不采石"；回退 = 各调用点把 `-1` 当 0 处理（一处一个分支） |
| 28 | **`_rescue` 拆掉的那一格当天没补上 ⇒ 夜里正面留着一个洞**（没石头 / 工人阵亡 / 天亮前没轮到 `_ring`），而夜里不能 `build`、当天无法补救 | 第 34/**57** 步 | 日志症状 = "**一条 `remove` 找不到对应的 `build`**"、收工时环上缺一格 ⇒ 与"第一个白天末尾砌了几格"同一处看 |
| 29 | **首日豁免（`_after_first_day`）第 57 步起只管着 `_rescue`**：第 1 天被打断的残局从第 2 天起被当成"环上有缺口" ⇒ `_rescue` 整个不生效（它开头判 `_ring` 非空即退），要等环补满 | 第 34/**57** 步 | 触发条件是"第 1 天没砌完"，**没数据**。症状 = 那些天被关住的人没人救（`_stuck_inside` 那条"自己走出来"不受影响）⇒ 环越早补满越早自愈 |
| 30 | **拆墙只剩一个旋钮 `HOLE_MIN_LEFT`(30)**（剩不到 30 个白天回合就不拆）—— 另两个随临时门机制在第 57 步删除，它现在管的是"别在天黑前拆" | 第 34/**57** 步 | 症状 = 白天末尾拆了墙当天补不回来（第 28 条）；门槛太高 ⇒ 被关住的人迟迟没人救 |
| 31 | **压缩闸门（第 43 步）没实测**：判题器 LLM 对每空闲轮一次 O(历史) 压缩请求的负载/延迟；压缩回复若不是裸 `<summary>`（⇒ 进会话表当任务材料，污染窗口）。⚠️ **第 47 步起答案轮不再压缩（闸门只剩 ③ 命令轮）**——"答案期隔轮提交有没有分值影响"那条担忧随答案轮压缩一起消失；负载点收敛为"每命令轮一次" | 第 43/**47** 步 | 日志看【本轮提问】里【上下文压缩】的占比与回合节奏；回退点 = 删闸门三行（回到第 41 步的命令轮捎带）；污染观察 = 会话窗口里出现 `<summary>` 字样 |
| 32 | **真实地图石矿座数未知**——第 57 步起环要 **14 块**石头（第 3 天起 16 块），1 座石矿（10 次）仍砌不满第 1 天（第 44 步验收网的前提是 2 座） | 第 44/**57** 步 | 首场日志数 `Map.ores` 里 stone 的座数；只有 1 座 ⇒ 环策略整体要重议（缺口留哪面） |
| 33 | **注释格式没有自动门禁**：AST 等价只守"没动代码"，管不了"注释写得对不对"。第 53 步把格式写进 `CLAUDE.md` 规则 6 并全量对齐，但规则不会自己生效 —— 新增代码可能再漂 | 第 53 步 | 见 `code-task.md` 第 53 步不确定性 3。回退方向：若发现漂得厉害再谈 lint（当前判为过度设计） |
| 34 | **双火箭的共用操作位 `(11,25)` 是个"口袋"**：唯一通路是 `x=9` 那条一格宽的走廊 ⇒ 队友停在走廊上时 BFS 判不可达、这一夜它就不挪窝；白天收工会把人送进口袋，第二天早上要多走 6 步出来 | 第 55 步 | 症状 = "某个角色整夜站在同一格"。先看收工后它停在哪儿；坏方向安全（走不到 ⇒ 不发指令，不碰红线）。回退 = 收工闸门退回"最近炮位"口径（一处） |
| 35 | **估算口径（第 56 步）比实际走路乐观**：`_passable` 把同事算成路过，`_Queue.step` 撞上同事仍然停 ⇒ "估算说去得了、这一回合空转"这一类还在（同事下一回合挪窝即自愈，3 天合成局实测最长 12~13 轮）。同源的还有 `_rescue` 那条走路**仍旧**把同事当障碍（白拆一格墙的代价有上界，见第 56 步不确定性 2） | 第 56 步 | 症状 = 一次几回合的空转、且下一回合自己会动。要真消除得让落子账/预留账参与估算 ⇒ 判为过度设计 |
| 36 | **"预算拦住就不去"是保守方向**：当天剩的回合跑不完一个来回 ⇒ 工人原地待命（可能连续十几轮）。第 56 步的整网用例以 30 轮为天花板，实测 12 | 第 56 步 | 症状 = 白天末尾成片空指令、且人停在原地不动。真要做"走一半也算动"就得引入部分路程的中途目标，会跟"回得来"打架 |
