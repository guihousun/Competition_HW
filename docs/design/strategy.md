# 《未来战争》对战策略（当前实现）

> 本文梳理 **`src/coregeek/game/` 里已经写进代码**的对战策略：打什么、按什么顺序决策、每个判断的
> 判据与代价、旋钮在哪 —— 外加"按天看"这一层（§0.2），供作分析战略的起点。
>
> **代码是唯一权威** —— 本文只做导读与"为什么"。数值与判据一律以代码为准，常量集中在
> `game/core.py` 顶部（改一处就够）。逐步的迭代留痕、以及**仍生效的已知不确定性**见
> `docs/design/code-task.md`（文末那张表是未了结项的**唯一**出处）；规则来源与文档矛盾点的整理见
> `docs/design/task-analysis.md`；用户给的方向性要求见 `docs/策略指导.md`。本文不复制那三份的内容。
>
> 旧版策略稿（根目录那份"按天梳理"，正文已并入本文、文件已删）见 `git show 08512da:strategy.md`。
>
> ⚠️ **符号住址**（第 117/121 步按"处境"拆文件）：任务线 = `game/task.py`、白天 =
> `game/day.py`、夜里 = `game/night.py`、胶水（两段式组装 + `_intents` 按昼夜分派）=
> `game/planner.py`、两个时段共用的底座 = `game/core.py`。**下文的短名按这个归属读**
> （`_fire` / `_defend` / `_foe_robots` 在 `night`，`BackToPost` / `WallLine` / `voucher_errand`
> 在 `day`，`task_channel` 在 `task`，`_priciest_ore` / `_post_spots` / `_Ctx` 那一族在 `core`）——
> 短名没跟着加前缀是有意的：正文是"为什么"，加一遍前缀反而更难读。

---

## 0. 打什么：得分、日历与红线

### 0.1 得分构成（任务书 §6）

`总积分 = score1 + score2 + score3`：

| 项 | 算法 | 我们的现状 |
|---|---|---|
| `score1` **任务** | 完成 = 任务积分奖励 + `5 × 标准回合数 / (完成回合 − 接取回合)`；部分完成 = 奖励 × 通过率 | 白天开拓者接、接了就钉死；**分母就是回合数 ⇒ 省一回合直接加分** |
| `score2` **击杀** | `Σ 机器人击杀数 × 积分`（小 1 / 中 2 / 大 4 / BOSS 10，§4.7.2） | 夜里三座炮自动打 —— 当前**唯一被自动化的得分手段** |
| `score3` **生存** | `Σ_{day=1..10} 10 × day × 存活系数`（基地未被摧毁 = 1；**被摧毁当天及之后全为 0**） | 防御线就是为它 |

- **胜负判定（§7）**：基地先被摧毁的一方判负；半场结束双方基地都活着 ⇒ 比积分。防守不只是分数，**它就是胜负条件**（"基地被完全摧毁"同时也是结束条件之一）。
- **`score3` 的坡度很陡**：第 3 天丢基地 ⇒ 第 3..10 天的分全没（`10×(3+…+10)` = 520 分）。越靠后越值钱，**第 10 天单独值 100 分**。
- 金币（卖矿 / 任务奖励）不是分，是燃料：建武器 25/座；券 —— 武器 100/150、围墙 20/30、基地 100/150。
- **杀机器人是极大的得分手段**（策略指导原话）⇒ 第 27 步定的方针是**最大伤害落点、打死所有机器人**，而不是"补刀残血"。这条方针决定了 `_fire` 的全部形状。

### 0.2 十天怎么过（按天看策略）

日历：**130 回合 = 1 天（白 70 + 夜 60），10 天 1300 回合**（`within <= 70` 为白天）。
夜里残余机器人**第二天早上第一个回合自动清除**（§4.7.3）⇒ 每晚都是满血开局；机器人**一夜只有一波**、**数量随天数增加**（曲线文档没给）。机器人**全图可见**（任务书 §4.3 / L95；接口文档 L154），视野 4 那条只管敌方队伍。

**Day 1（回合 1–70 白 / 71–130 夜）—— 唯一"墙没砌完"的一天**

- 开局 = 3 角色（1 开拓者 + 2 工人）+ 基地 + **75 金币** + 0 武器。75 = 3 × 25 ⇒ 开局就锁定"**造满三座**"。⚠️ `docs/request.txt` 是 `roundNo=85` 的**中局快照**，别当开局读。
- 工人：第 1 级连建三座 → 第 2 级砌环上的 14 格（第 1/2 天**不封背角**，`_sealed_back` 第 3 天起才为真）→ 环也补齐了才轮到第 3 级挖矿买券。
- 开拓者：任务点开局**应当**都可接（冷却只在任务结束时才刷新，开局没有可冷却的东西 —— 推断，没验过）⇒ 它整天的活就是任务（§4）。
- 合成局面实测（2 座石矿的可行地图）：**环 R41 收官、三座武器 R44 建满**（`DayOneFinishTest`）。
- 夜：三座炮都是 L1。**2 个角色即可操满 3 座**（两座火箭共用一个操作位、交替开火；加特林单独一组）⇒ 开拓者被任务钉死**不影响火力**。第 1 夜机器人最少（曲线未给，未实测）。

**Day 2–9（常规日）—— 环是满的，钱开始变成等级**

- 早上环（14 格）通常已满 ⇒ `_ring` 为空 ⇒ 白天先补弱墙、再干经济（W3/W4 两级的闸门同时失效，这就是"墙砌完了"的落地）。**第 3 天起** `_sealed_back` 为真 ⇒ 补背面两个角格（16 格），后方通道从 6 格收到 4 格。
- 每个白天的**第一个回合出世界新闻**（§4.8）：官方消息 = 矿价事件原文（塌方 / 停工），我们拿它问 LLM 要价格方向（**每游戏日 3 次额度**，§4.3）；民间传闻 = 宝藏线索，**不读**。
- 任务点做完 / 超时 / 开拓者死亡 ⇒ 该点 `isValid: false`（冷却中），冷却走完再变回可接 —— 冷却期间开拓者改领"买券 → 用券"差事（§3）。
- 夜里被打死的角色**第二天白天开始后 20 回合内在基地复活、背包保留**（§4.5.2）⇒ 白天少一个人、夜里满员恢复。代码里**没有"复活"这回事**：`len(roles)` 变了，武器份额与炮手数跟着变，全是从 payload 现推。
- 升级链在这个窗口推进（`UPGRADE_CHAIN`）；⚠️ **武器有缺口时整条券线让位**（先筹资建武器，§2.1/§2.3）。
- 夜里：开火 → **打我方的那波**清完 ⇒ 工人转出门采矿（第 85/119 步，§1.3 的 N2）。

**Day 10（终局日）**

- `score3` 这天权重最大（100 分），且基地一丢当天就归零 ⇒ 全力防守。
- 任务窗口能开就开（哪怕只拿部分分）；开不了就别硬凑。
- 夜里是最强一波（含 BOSS，10 分/只）。
- ⚠️ 终局"消耗品救场"目前只是**没动用的牌**（§0.3）。

**跨天不变的三件事**：白天 70 / 夜里 60；残余机器人清晨清空；`plan()` 里**没有任何跨回合的角色状态**（§1.0）。

### 0.3 还没动的牌（策略空间，都未实现）

| 牌 | 规则出处 | 现状 |
|---|---|---|
| **宝藏线** `summonTreasure` | §5.2：开拓者在周围一格内献祭任务用品召唤祭坛宝藏；**地点 / 条件 / 时间都要从民间传闻推**；一张图只有一个宝藏，同回合双方都正确则都有 | `actions.py` 里**没有这个 Action** ⇒ 发都发不出去；`folkLegends` 也不读 |
| **消耗品** `DizzyWeapon`（100 金，3×3 眩晕 5 回合）/ `Bomb`（100 金，3×3 造成 100 伤害） | §4.6.2：**无使用距离限制**、**结算优先级 > 机器人移动**、只对双方机器人有效 | 没买、没用（`buy`/`use` 只认券） |
| **机器人召唤令** | §4.7.3：会**叠加**在基础浪潮之上 | 不买。⚠️ 多出来的那波按谁的 `targetTeam` 打、值不值，**规则没写、也没验过**（旧稿那句"等于给对面送击杀分"是猜的） |
| 民间传闻 | §4.8 世界新闻第二板块 | 不读（只读 `officialNews`） |

### 0.4 红线

**5 次异常即整场出局** ⇒ 策略层的每一处失败都**只丢一条指令**，绝不冒泡（`_emit` 收
`PermissionError`、`step_toward` 走不到返回 `None`、`handle` 兜底空指令）。所有"打不了"的情形
一律退化成**空指令**（合法、不计异常）。三类异常：连接/响应超时、响应格式错、指令非法（字段缺失 /
动作码非法）；**"指令执行失败"（撞墙、打空）不计**。

---

## 1. 每回合的决策骨架：角色决策状态机

```
app.handle(payload)
 ├─ task.task_channel(turn)   → (prompt, executeCmd)   # LLM 线唯一的对外通道（§4）；回合末尾压缩闸门填 ③ 落空的 prompt 槽
 ├─ planner.plan(turn)         → roleCommandMap         # 本文的主体
 └─ 组装三个顶层字段的报文（永远都在）
```

### 1.0 这里的"状态"指什么（先读这一段，否则下面整节都会读歪）

⚠️ **`plan()` 里没有任何跨回合的角色状态。** 所谓"某角色处在哪个状态"，是**每回合从 `Turn`
现推**的一次判定：同一个工人昨天在砌墙、今天在采石，代码里并没有"它处于砌墙态"这条记录 ——
只是这一回合的判据链先命中了砌墙那一级。所以下面那张判据链**既是状态定义（处境），也是
转移规则**（下回合重推时会落到哪一级）。**任何"加个跨回合标志位记住它"的念头都先打住**
（第 11 步的教训：跨回合标志位一旦卡住会**静默关掉整条线**）。全场**只有四处**真跨回合状态：
`AGENT._sop`（流程表，整场）/ `AGENT._context`（任务内会话）/ 价格期望表（新闻线，§4.3）/
`night._fired`（本地开火账，§5）。

### 1.1 两段式：第一段定行为，第二段解路径

`plan()` 分两段（第 51 步），**这是读代码时最容易迷路的地方**：

| 段 | 函数 | 产出 | 纪律 |
|---|---|---|---|
| 一 | `_intents` → `_day_intents` / `_night_intents` | **初步行为**：能当场干的 act 直接落 `q.cmds`；要**走**路的只交一个意图（`(角色, 目标)` 或 provider 型）。**按昼夜分成两条独立的链**（第 120 步），工人那一支走 `day.DAY_CHAIN` 那组状态类（第 93 步） | 黑板账全在这一段按角色顺序累计 —— 白天那本在 `_Ctx` 里（`sites` / `taken` / `repair_taken` / `ore_taken` / `budget`）+ `segments` 在本函数；**夜里那本只是两个局部集合**（`taken` / `ore_taken` + `assigned`），**不碰 `_Ctx`** —— **角色之间的协调发生在决策层** |
| 二 | `_walk_out` | 按**同一顺序**批量解走路意图：BFS 落一格 | 落子账 `claimed` 与路径预留账 `paths` 在这一段累计；后解的让开先落的（先落的子当硬障碍、已预留的路当**软避让**，绕不开就退回硬障碍照走，绝不原地卡死） |

⇒ 一个角色一回合最多一个动作，而"这个动作是什么"**在第一段就定了**，第二段只负责把它落到
格子上。**读某一支策略时只看第一段**；只有问"谁先走、谁让谁"才需要去第二段。

### 1.2 判据链（逐角色跑，先命中先定夺）

`for role in turn.roles`（顺序 = payload 顺序），**命中即 `continue`**（这个角色的回合到此结束），
没命中就落到下一级。**这张表的顺序就是优先级**：

### 1.2 判据链（逐角色跑，先命中先定夺）

`for role in turn.roles`（顺序 = payload 顺序），**命中即 `continue`**（这个角色的回合到此结束），
没命中就落到下一级。**这张表的顺序就是优先级**：

| 级 | 状态 | 闸门（命中条件） | 命中之后 | 什么时候不占这一级 |
|---|---|---|---|---|
| ① | **钉死** `TASK` | 开拓者 **且** `phase_task` 非空 **且人手够**（白天恒够；夜里看 `_pioneer_mans_guns`） | `task.answer_task`：原样交手上的答案（**不移动**） | 非开拓者；`phase_task` 空；**夜里人手不够**（工人阵亡 ⇒ 弃任务回炮位，落 ④） |
| ② | **收工门** `GO_HOME`（`day.BACK_TO_POST`，第 0 级） | 白天 **且** `白天剩余 ≤ 5 + 手里的券数` | **只发 `move`** 往**那一组的岗位**挪（`core._post_spots`：多座组 = 共用操作位、单座组 = 炮旁）；已在岗 ⇒ 空指令待命 | 还没到窗口；**开拓者而工人够操满所有组**（补位炮手） |
| ③ | **开拓者的白天** `DAY_PIONEER` | `isinstance(role, Pioneer)` | 有任务点 ⇒ `task.take_task`；全空 ⇒ `day.voucher_errand`（§3） | 非开拓者 |
| ④ | **夜里** `NIGHT` | `not is_day` | 见 §1.3 —— 三支各自 `continue`，夜里**必然**停在这一级 | 白天 |
| ⑤ | **工人的白天** `DAY_WORKER` | 其余（工人） | 走 `day.DAY_CHAIN` 那四级（§1.4） | —— 这级**必然**给出结论（最差是空指令待命） |

**为什么是这个顺序**：

- **① 与昼夜无关**，排在最前：离开任务点周围一格任务立即作废 ⇒ 它连夜里都不回炮位。
- **② 在最前**（第 121 步）：到点就停下手里的活 —— 判据**只看还剩多少回合、不看距离**，
  也**不问环砌完没有**（旧口径的"补墙优先于收工"作废）。🔴 **它绝不能复用 `night.defend`**：
  到岗会调 `_fire` 发 `attack`，而 `attack` 仅黑夜 ⇒ 白天发就是**非法指令**（红线）；
  守门员是 `DayEndGateTest.test_a_day_round_never_fires`。
  目标是**那一组的岗位**（与 N3 同一个 `core._post_spots`，不是"最近的空闲炮位"）⇒ 天黑时人已经
  站在共用操作位上、夜里第一回合就能交替开火。步数口径见 `core._steps_to_post`（多座组的岗位是
  空地、要站上去 ⇒ `steps_between` 的结果 +1）。
  ⚠️ **开拓者是补位炮手、不走这一支**：`_pioneer_mans_guns` = "工人数 < 武器组数"才为真；为假 ⇒
  `BackToPost.run` 直接 `False`（它不去占岗位，任务线才是它的活）。**与 N3 的 `night.defend`
  同一个判据，两处必须同源**：只拦夜里那一处 ⇒ 白天照样把它送进岗位（火箭对只有一格岗位）、
  夜里它又不认领 ⇒ 那格被它占着，**整个火箭组没人操**。

### 1.3 ④ 夜里的三支（严格按序，各自 `continue`）

| 序 | 状态 | 闸门 | 命中之后 |
|---|---|---|---|
| N1 | **基地升级** `STATION_USE` | 持基地券 **且** `station_health` 已知 **且** 基地血 < 满血 1/4 | 贴基地 `use`（升级 + 回满血一次到位；**当回合放弃开火**） |
| N2 | **出门采矿** `NIGHT_MINE`（第 119 步） | **没有还会打我方的活机器人**（`night.is_cleared`）**且**是工人 | `night.mine_ore`：去采**收购价最高**的那座矿，囤到第二天由白天那条线卖掉。**不卖、不买、不回炮位** —— 只发 `collect` / `move`（`build` / `remove` 夜里非法）。⚠️ **第 85 步：判据不是"场上全空"** —— 机器人全图可见（L95）⇒ 对方那一波也在 `turn.robots` 里、我们从不打它（`night._fire` 同样过 `_foe_robots`）⇒ 照全空判，工人整夜守在炮位上、这一支永远进不去。⚠️ **与白天那条经济线分成两条是有意的**：白天按"走得到 **且 回得来**"筛（回程参照 = 最近的武器位），夜里清场后没有回炮位的义务 ⇒ 照它筛会把整段夜里筛成"一座矿都不可行"、工人整夜空指令 |
| N3 | **操炮** `DEFEND` | 其余全部（**开拓者须 `_pioneer_mans_guns`：工人数 < 武器组数，否则这一支不认领任何组**；唯一例外 = 它**已站在**某组的操作位上，那就认领那一组） | 认领最近未认领的**一组**武器（`_weapon_groups`），走到那一组的**岗位**：**先开火、打不了才挪岗**（见 §5） |

⚠️ N1 / N2 是**当回合放弃开火**，不是"打不了才退而求其次" —— 别把它们读成兜底。
⚠️ 夜里**绝不发 `build` / `remove`**（仅白天，非法即红线），所以 N2 只发 `collect` / `move`。
⚠️ `round_no` 缺失（< 0）⇒ N3 一发 `attack` 都不发（见 §5）。

### 1.4 ⑤ 工人白天的阶梯：四级（`day.DAY_CHAIN` + 链前的 `BACK_TO_POST`）

**第 0 级**（`day.BACK_TO_POST`，链之前，见 §1.2 的 ②）不在这条链里 —— 到点它先说话。

| 级 | 状态 | 闸门 | 命中之后 |
|---|---|---|---|
| 1 | **建武器** `BuildWeapons` | 还有名额 **且** `can_build_all`（回合开始的钱够建满） | 按 `day.assign_sites` 的分工认领自己那几座（预扣金币、占建造格）⇒ 贴着就 `build`、否则走过去 |
| 1 | **筹资** `RaiseForWeapons` | 武器有缺 **且** 钱不够建满 **且** 地图上有小贩有价 | **两个工人一起挖最贵的矿**（`core._priciest_ore`）；团队金币 + 两人背包货值 ≥ 缺口 ⇒ 各自去卖 |
| 2 | **建墙** `WallLine` | 武器齐了 **且** 环不完备（`need = 本段缺口数 + 该拆的 L1 弱墙数 > 0`） | 石头不够先去采（`_mine_stone`：挑"去 + 回"最省的石矿，**采够就好**）；石头够则**先补缺口、再拆 L1 弱墙** |
| 3 | **挖矿买券** `VoucherLine` | 武器齐了 **且** 环完备（第 3 级的前提是 `weapon_gap` 为假） | `day.voucher_errand`：挖最贵的矿 → 攒够券价就卖 → 买 → **立刻用**；全升满 ⇒ 继续挖矿 |

⚠️ **级别的衔接是判据、不是状态**：武器没建满就永远停在 1，武器满了才问环上有没有缺口，
环也补齐了才轮到 3。`State.run` 的契约照旧：**返回 `False` 必须意味着这一回合什么都没排、
什么都没发** —— 违反它这个角色就会落两条动作、后一条把前一条盖掉，而报文仍然合法、本地未必抓得住。
⚠️ **第 3 级是链尾**：`VoucherLine.run` 恒返回 `True`，所以它的差事**必须自己落回挖矿**
（走不通时干等 = 这个角色整回合空转，第 121 步踩过）。

### 1.5 共用记账（都是"本回合"的局部变量，不跨回合）

第 93 步起这些账装在 `_Ctx` 里（`_day_intents` 循环外构造一次、循环里所有角色共用；
夜里也用同一个袋子）—— ⚠️ **一个回合只有一个 `_Ctx`**，谁把它挪进角色循环，黑板就散了
（症状是两个工人抢同一格墙）。`segments` 留在 `_day_intents` 里，`target` / `remaining` 每进一个
工人时写进 `ctx`。

- `claimed` —— 已被认领的**落脚格**（两人冲同一格会双双停住）；
- `sites` —— 已被认领的**建造格**（武器与围墙共用，免得工人走到建造格**上**去）；
- `taken` —— 已被认领的**武器组**（夜里一人只能操一组）；
- `assigned` —— 本回合各机器人**已被许掉的伤害**（多炮协防的账）；
- `budget` —— 金币，**认领即预留**（宁可这回合少建，不可超支）；
- **差事黑板（第 40 步）**：`segments`（环缺口按**走到阶梯的工人顺序**切段，A 领前段、B 领后段沿环同向推进 —— 整体最优的关键是**错开**，不是各自最优；后段的任何一格都不高于前段还剩下的）、`ore_taken`（**矿格认领**，B 不再跟着 A 奔同一座矿）、`paths`（**路径预留**：先动身的工人把 BFS 路径记进黑板，后处理的当 `avoid` **整条让开** —— 软的，绕不开就退回硬障碍照走；**预留只在动身成功后**，先记再走会绕开自己的路）。黑板每回合从 `Turn` 现算、**不跨回合**。
  ⚠️ 走过 ② / ③ / ⑤ 而没走到阶梯的工人**不占段号**（`worker_no` 只随阶梯递增）—— 段首给下一个工人，不会被漏掉。

**并列一律按坐标 / id 排**：先后不能取决于 payload 里的顺序，否则用例复现不了、日志也对不上号。

---

## 2. 白天·工人：四级链里的三条

### 2.0 建武器（第 1 级）

- **完备** = `day.slots(turn)` 产出不了名额（份额 = `len(角色) − len(武器)`，落点必须是空的 ——
  建在已有武器上会把它覆盖成 level1，25 金币打水漂还降级）。
- **钱够建满**（`can_build_all` = 回合开始时的 `turn.gold ≥ 25 × 缺数`）⇒ `day.assign_sites`
  枚举把这批落点分给两个工人的划分（≤ 2³ 种），每人按"最近的先去"算完工回合，取
  **"最后一个建完的回合最早"**的那份，并列取更均衡的。⚠️ 这个标志按**回合开始的**金币算一次 ——
  `budget` 会随认领往下掉，第二个工人再去比就永远嫌少（用户口径"钱不够就两人一起去挖矿"）。
- **钱不够** ⇒ 两人一起挖最贵的矿（`core._priciest_ore`），团队金币 + 两人背包货值 ≥ 缺口就各自
  把货卖掉，钱到账后下一回合接着建。**没有小贩 ⇒ 凑不出钱**，落回第 2 级（墙是剩下最值得干的事）。

### 2.1 建墙（第 2 级）—— 只看 L1

- **不完备**两种：**缺口**（那格没墙）与 **L1 弱墙**（`health × 4 < WALL_MAX_HP[1]`(1000)）。
  `need = 本段缺口数 + 该拆的弱墙数`，`role.stone < need` ⇒ 去采石。
- **L1 弱墙直接拆了重砌**（用户口径）：拆一回合、砌一回合 = 2 回合 1 块石头，比 20 金的围墙
  升级券便宜。`remove` 的唯一调用点是 `day._demolish`。
  ⚠️ **L2/L3 的弱墙不归这一级管**：拆了只能重砌回 L1（掉一级），它们的损伤交给第 3 级的升级券
  （升级同时回满血）。
- **顺序：先补缺口、后拆弱墙** —— 缺口是夜里机器人进来的门；弱墙好歹还挡着，拆完砌不回来就是
  白开一个洞（所以手里没石头不拆）。
- **采石**（`day._mine_stone`）：对每座石矿算 `走到矿 + 从矿回工地` 的 BFS 步数，取最省的那座；
  采量 = "还差几块"，**够了就行、不留存货**（旧口径的 `STONE_RESERVE`(3) 与"按回合预算解 k"的
  算法都删了）。一座能用的石矿都没有 ⇒ **拿手里那点石头先砌**（能做多少做多少），别干等。
- 砌墙沿用三条边角规则（都是历史实测换来的）：① **环上只剩最后一格 ⇒ 站到盒外砌**（站在环里
  砌完自己就被封在里面）—— 见 §6；② **自己正站在目标格上 ⇒ 这一回合只挪开一格**（人站在墙上
  时那格在网格里只剩 "worker"，`_ring` 的"自己人算路过"又把它复活成候选 ⇒ 每回合对同一格
  `build`、石头白花）；③ 贴着目标 ⇒ `build`（`step_toward` 停在贴着它的一格，那正是建造位）。

### 2.2 挖矿买券（第 3 级）

- **挖实际单价最高的矿**（`core._priciest_ore`，第 122 步）：`单价 × 剩余 / (剩余 + 去 + 回)`
  —— 把这座矿采空的平均收益（`剩余` 来自本地账 `core._collected`，`回` = 矿到最近武器位的 BFS 步数）；
  并列取近的、再取坐标序，**不读新闻修正**。动身前先看**顺路卖矿**（`day._detour_sell`：绕去小贩 ≤
  `DETOUR_MAX`(2) 格且背包里有货 ⇒ 先朝小贩迈一步）。
- **券的目标**（`day._voucher_target`）：按 `VOUCHER_CHAIN` 取**第一张还有东西可升**的 ——
  **武器二级 > 武器三级 > 围墙二级 > 围墙三级**。它是**目标清单**，不跳级去买更便宜的券
  （墙券 20 金也没用）。目标建筑在 `day._voucher_spot` 里挑：武器券挑"还升得动的那座"
  （**火箭优先**），围墙券挑同等级里**残血的**（升级当修）、再按坐标序。
- **四种动作**：手里的券 ⇒ 走到目标 `use`（**买完就用**，攒着几张就一张一张用）；买得起 ⇒
  **按需要买够**再朝商店走 —— 需要几张 = 这张券还升得动的目标有几个，买得起几张 = 金币 ÷ 券价，
  取小的那个（用户口径"尽可能多买"：200 金、两张 100 金的券、两座待升的武器 ⇒ 一条 `buy num=2`），
  并从 `ctx.budget` **预扣券价 × N**；现钱 + 背包货值够 ⇒ 先去卖（`day._sell_cargo`，一次卖光最贵
  的那一种）；其余 ⇒ 接着挖矿。全升满了 ⇒ 链尾：继续挖矿。
- ⚠️ **卖货只剩两道门**（有货、有小贩且走得到）：旧的"够本门"（货值 ≥ 2 × 路程）与"石头留底"
  两档都删了 —— 什么时候去卖由券价决定。
- ⚠️ **第 3 级的前提是武器建满**：`weapon_gap` 为真就整条不跑（拿建武器的钱去买券是本末倒置）。

## 3. 白天·开拓者

- **有任务点** ⇒ `task.take_task`（贴着就 `acceptTask`，否则走一步）；接上就被钉死（§1.2 的 ①）。
- **任务点全空**（冷却 / 做完）⇒ 跑**同一条买券差事** `day.voucher_errand`，与工人那条
  **各自独立跑**（用户口径）：谁有钱谁买，靠共享金币的 `budget` 预扣去重。
  ⚠️ 规则限制：`collect` 仅工人、**没有转移物品的指令** ⇒ 开拓者采不了矿，"攒钱"那一半它跑不起来
  （`_mine_for_voucher` 对非工人直接返回 False），能做的只有"买"和"用"。
- **要买就买完立刻用**（用户口径）：持券 ⇒ 直接走到目标 `use`，不囤在背包里。

## 4. LLM 线（`task_channel` → 响应顶层的 `prompt` / `executeCmd`）

两条通道：**`prompt` → `llmResp`**（唯一的 LLM 通道）、**`executeCmd` → `lastCmdResult`**
（唯一的沙盒通道；⚠️ **沙盒故障与命令超时不计异常**，这条通道不吃红线）。`executeCmd` 只在
**任务期间**可用（接口文档 L208/210），**没有昼夜限制**（任务跨夜是常态，别加 `is_day` 门）。

### 4.1 任务线的判据链（一个函数、先命中先定夺）

| # | 判据 | 结果 |
|---|---|---|
| ① | **没任务**（`phase_task` 空）⇒ 问一次**新闻查价**（§4.3，指纹去重）；**有任务但名册里没有开拓者** ⇒ 两个都不发（沙盒仅任务期间可用；开拓者活着是**显式补的**闸门；**在压缩闸门之前** ⇒ 场外绝不压缩） | 见左 |
| ② | `cmd_result` 非空（沙盒刚交作业）**且这轮没有新命令** | **回灌结果**、不发命令 |
| ③ | 回复是完整工具调用且工具给了命令（**且这条回复是新的**，§4.4） | 把命令交给沙盒 —— **这个字段归它**（回执同轮到达也照发，那一轮 prompt = 回灌的渲染，两个字段各装各的）；prompt 槽留给压缩闸门（原料 = 原始上下文全文，下一回合初摘要就位） |
| ③′ | *（隐式）*完整调用但拿不到命令（`SOP2Prompt` / `python_exec`（本地纯计算，产出当回合进会话） / `readSandboxFile` **命中**（正文当回合进会话，§4.5） / 未知工具 / 空参数） | 有答案 ⇒ **⑤**（不发模型请求）；没答案才掉到 ⑥，**不活锁**（任务期间 prompt 不限量不计数，出口有三个） |
| ④ | 判题器报 `code 2`（答案错）且回复不是工具调用 | 带"上次答错了"重问；**没答案也装反馈**（只把判题器的 `description` 装进提示词，绝不落「请继续。」） |
| ⑤ | 回复是最终答案 | **只交答案**：不发模型请求、**不压缩**（压缩与 `<answer>` 互斥；开拓者那边每回合在 `submitAnswer`）。**不提前返回** ⇒ 空着的命令槽归链尾的探查 |
| ⑥ | 否则（首问 / 畸形回复） | 只把题目问出去 |
| 链尾 | **压缩闸门**：`prompt == ""` 且不是答案轮（只剩 ③ 会到这里） | 填**压缩请求**（没开过会话给 `""`） |
| 链尾 | **探查闸门**（排在压缩之后）：`cmd == ""` **且这轮 LLM 没点名调 `executeCmd`** | 交给 `cmd_explore` 摸沙箱环境（§4.5；回执归它自己收） |

- **③的优先级与粘住判别是一对**（第 108 步，用户口径"有 `executeCmd` 工具，它才是使用返回字段的
  第一优先级"）：`lastCmdResult` 文档明说"未发命令时为空字符串"（**不粘**），而对 `llmResp`
  一个字没写 ⇒ 必须按**可能粘住**设计。挡"同一条命令被反复丢进沙盒"的从"②一律压在③前"换成了
  `Agent.hear` 的返回值（这条回复我们上一轮已经行动过了吗，§4.4）——旧口径顺手把"回执与新命令
  同轮"那批命令**整个丢掉**，那才是 bug。
- **④是"修饰符"不是"分支"**：`errors` 说的是**本轮产生**的错误，与我们上轮发了什么不同步 ——
  做成独立分支会把纠错整段吞掉。触发条件是 `code == 2`（= `submitAnswer` 交的答案不对，
  **任务继续**，实测确认）。
- **⑤必须用 `answer_of` 判**，不能用"取不出命令"判：畸形工具调用会被当答案交上去，而
  `_answer_task` 又跳过工具回复 ⇒ **两条通道同时哑火、永久空转**。同一个谓词还负责"该骂什么"，
  一旦分家，纠错段就会把带 `<answer>` 标签的原文骂回去。
- **`_answer_task` 每回合都交**（判题器按"提交过的通过率最高的答案"算分，反复提交是预期用法）、
  **从来不移动**、**空答案不发**（可能被判成字段缺失 = 指令非法）。
- **③′ 那条支路是主路径，不是边角料**：`SOP2Prompt` 会**和答案写在同一条回复里** ——
  调它本身不产出命令，所以那一轮走 ③′ 落到 ⑤：**既沉淀、又同回合作答**，省下的是完整一回合。
  `score1` 的分母就是回合数，省一回合直接加分。
- **`executeCmd` 的闸门要显式补**：`submitAnswer` 走 `roleCommandMap`、阵亡的人不在 `roles` 里 ⇒
  白送一道闸门；而 `executeCmd` 是响应**顶层字段**，不经过角色循环也不经过 `Action` 的权限闸门
  ⇒ ① 里那句"名册里得有开拓者"必须手写。

### 4.2 工具协议与答案（我们自己定的形状，最大的单点风险）

文档没规定 LLM 该用什么形状要命令 ⇒ 协议是**我们自己定的**（第 37 步起**严格只认嵌套形状**）：

- **调工具**：`<tool><tool_name>名</tool_name><tool_param><参数名>值</参数名></tool_param></tool>`
  —— 参数是 `<tool_param>` 里的**具名元素**；无参数的工具不写 `<tool_param>`。
  属性式 / 裸参数 / 裸 `<tool>cmd</tool>` **全部不再是工具调用** ⇒ 落重问（丢一回合、不碰红线）。
  值过五实体反转义（`&amp;` 最后换）。
- **直接作答**：`<answer>答案本身</answer>`（**块外**；块内的 `<answer>` 一律不算 —— 分不清那是真
  答案还是 SOP 正文里的示例，宁可不交）。
- **两者同回合** = 两块并列（沉淀 + 作答）。
- **交出去的到底是哪一段**（第 102/103 步）：**带标签** ⇒ 只交标签里那一段（标签外的推演、解释
  不污染提交）；**没带标签** ⇒ 整段原文当答案交上去（`answer_of` 第三级"原文即答案"）。prompt
  侧因此是**一对**：推演**鼓励**写在标签外面（第 103 步的 COT 引导）＋ 标签本身**不可省**（第 102
  步把后果写进了【输出约定】）。**解析侧一行没动** —— 收紧成"没标签就不交"是留着的退路，不是现状。

三个谓词分居宽严两端（`agent/chat.py`）：`tool_of` **严格**（形状不对 ⇒ `None`，绝不猜）；
`looks_like_tool` **宽**（只认前缀 `<tool`，`<tool` 有开无闭 / 只有名字没参数 ⇒ 走 ⑥ 重问，
**不当答案**）；`answer_of` **三级**（先挖掉第一个工具块再扫 → 否则用原文判"像工具调用" ⇒ `""`
→ 否则原文即答案）。⚠️ **挖掉工具块是防 SOP 正文污染的第一道闸门**：SOP 讲的就是"答案要用
`<answer>` 包"，正文里必然出现字面量；第二道在入库那一头 —— `SOP2Prompt` 用 `chat.strip_answers`
把成对的挖掉再存（写进日志的是"剔除 N 处"）。

**注册表是唯一真相**：`Agent.__init__` 里那张 `self._tools`（名 → 实现 / 描述 / **参数表**）——
参数表**一表两用**：生成 prompt 里的工具块、按名调度（**只收具名参数**，声明参数一个不少且非空
才 `impl(**resolved)`）。`SOP2Prompt` 的参数表是 `(("name", …), ("sop", …))`（多流程口径：
`name` 是条目名、存储是**流程表** —— 同名覆盖、异名追加、超 `SOP_FLOWS_MAX`(5) 丢最旧且日志点名；
空文本 = 删掉那条）。**铁律：`tool_call` 的返回值就是那条要进 `executeCmd` 的命令**，`""` =
不产出命令 / 调用不成立，下游不需要区分；不成立的原因（未知工具 / 缺参数 / 形状没写对）**回灌进
会话**（第 110 步）—— 没有它，那一轮 LLM 只看得见「请继续。」，会以为命令已在跑、等一个不会来的
回执。四个工具（第 99 步起）：`executeCmd`（把命令搬进沙盒，
本地一字不执行）、`readSandboxFile`（**路径是枚举值**：只收探明过的那几份，写整条全路径或只写
文件名都行，见 §4.5）、`python_exec`
（本地纯计算，1 回合）、`SOP2Prompt`（只沉淀、不产命令）。⚠️ `readSandboxFile` **从不产出命令**
（第 101 步起）：命中 ⇒ 正文进会话、清单里没有 ⇒ 清单进会话，两条都走 ③′；**工具块恒在**
（第 107 步，旧口径"一份都没探明时不列"已作废 —— 它的名字被别的工具描述点过，藏起来就是悬空指引），
探明过才在描述尾部挂清单（`Agent.prompt_tools`）。

### 4.3 新闻查价线（没任务时跑的那一支）

- **触发**：`phase_task` 为空 ⇒ 把当天的**官方消息原文**问出去（`AGENT.news_question(turn.news)`）。
  额度 = **每游戏日 3 次**（任务线之外就这么多），所以**同一份 news 只问一次**（指纹去重；
  判题器不答也只是"不再问了"，不重发）。
- **回复**：裸 `<prices>` 块，行格式 `矿种: 方向`（`up`/`down`/`flat`）⇒ 进**价格期望表**
  （`_price_hints`，`up ×2 / down ×0.5 / flat ×1`）。**不进会话表**。
- **用途**：本来是修正挖矿性价比的排序（旧 `_mine_spare_ore` 里的 `收购价 × 系数`）。⚠️ 第 121 步起**两条挖矿线都不读它了**（只按 `vendor_prices` 挑最贵的）—— 这个旋钮暂时空着，要接回来是一处改动。payload 的实时收购价
  **每回合照读**，期望是**叠加项、不是替代**。没问过 ⇒ 系数 1.0（无修正）。
- **民间传闻**（第二板块）不读 —— 那是宝藏线的线索（§0.3）。

### 4.4 任务内的会话上下文（跨回合状态之一）

判题器每回合只看到 `prompt` 一段字符串 ⇒ `Context` 把这道题的会话**渲染成标准 messages JSON**
塞进去。身份 = **题目原文**（换题即换新会话；冷却后同文再现则续上旧会话，它记得试过什么）；
assistant 消息记原文（**发命令/交答案那轮没有 prompt，回复照样得记**，否则回灌那轮 LLM 看见的是
"题目 → 莫名其妙的结果"）；system（`prompt.py` 的段模板）**每轮现刷** —— SOP 是活的，任务进行中
沉淀的下一轮就得看得见。`Agent.hear` **返回"这条算不算新的"**（第 108 步：与最后一条消息相同 =
粘住的 `llmResp` ⇒ 那条命令不再发第二遍；中间隔了回执/重问就算又说了 ⇒ 命令不会被永久压住）。
**存储全量、渲染有窗**（题目 + `【历史摘要】`（`tool` 角色）+ 最近 2 轮）；
**压缩**在 ③ 命令轮发、原料是**原始上下文全文**（原文永久保留、压缩总从原文重来）；
回复是裸 `<summary>`（`is_summary_reply` 判别、进摘要不进会话表，判题器不答就保旧摘要）。
**重问轮的收尾**（`Context.NUDGE`「请继续。」）只在会话**真停在它自己的输出上**时才补
（第 111 步）：本地工具轮与"调用不成立"那几轮的尾巴已经是 `tool` 产出，不补 —— 与回执轮的
判据 ② 同形。

### 4.5 沙盒探查（第 87 步，用户口径"把空着的命令槽全用起来"；第 98 步改成一条 python 命令）

任务期间**每个空着的 `executeCmd` 槽**都归 `agent/cmd_explore` 用：**一条**
`python3 -c '<脚本>' <skip>`（用户口径"用 python 代码去实现功能"），脚本自己在沙箱根下走目录、
按 `FETCH_MAX`(60KB) 预算贪心取一批带 `task` 的 md、每份打一段 `@@@FILE <路径>@@@`、
末尾报还剩几份（`@@@MORE <n>@@@`）。**清单与正文同一趟拿到**，且每条命令都带着一份**排除表**
（= `_files` 的全部键，第 114 步）：脚本先把读过的那几份剔掉再取 ⇒ **`_files` 就是游标**，
`n > 0` ⇒ 下一回合接着取，`n == 0` ⇒ **这趟收工**（`_done`，空槽不再占），**下道题由
`new_task` 重开一趟**（第 113 步）。它**不给任务线看**（用户口径"隐藏"）：`observe` 认领自己
那条回执并返回 `""` ⇒ 判据 ② 不触发、LLM 自己的命令照发，两条通道自然交替。

四条口径（第 88/89/98 步）：

- **只认全路径含 `task` 的 md**：口径写在**脚本**里（`_NEEDLE`，第 98 步从"代码里滤"挪过去 ——
  清单与正文合成一条命令之后，命令本身就是那个"清单"）。⚠️ 任务书若不叫 `*task*` ⇒ 一份都探不到，
  日志上只剩"一份都没取到 ⇒ 下一回合从头再来"（表 #56）。
- **路径进 prompt**：探明的路径经 `known_paths()` 挂进 `readSandboxFile` 的描述尾部
  （`Agent.prompt_tools` 每轮现刷，与 SOP 段同源；第 104 步从 system 的独立一段挪进工具块 ——
  那张表就是那个 `path` 参数的取值表；第 105 步每行再带一个文件名，两种写法都可填）。
  ⚠️ **三条规矩（第 113 步，用户口径"每个任务开始时启动此次沙箱的存档，k 个任务启动 k 次"）**：
  ① **一趟存档 = 一道题**（`new_task(phase_task)` 认边界：任务文本一变就重开，走完就停）；
  ② **探明的成果只累积不清**（跨任务叠加、后面每道题都能读）；③ 判题器的**沙盒每道任务独立**
  （同一个路径上的文件跨沙盒一致，但每道沙盒里**有哪些**文件可能不同）⇒ 靠 ① 的"换题重开"
  才拿得到这道沙盒特有的那几份。⚠️ **①的"重开"不是"重读"**（第 114 步，用户口径"排除掉之前
  读取过的"）：排除表把读过的挡在脚本里，重开的那一趟只搬没读过的。⚠️ **空文本那一轮也记**：
  同一道题冷却后同文再现要算新任务。旧口径"每回合都重走一趟"（第 106 步）已作废。已知代价：
  清单是**跨任务的并集**（某一份在当前沙盒里可能并不存在，但同一个路径上的内容一致，所以照旧
  可用），且**同一个路径上的正文不再重取**（内容真变了也发现不了）。
- **正文进内存 dict（路径 → 正文）+ 进日志**（`BODY_LOG_MAX` 4000 字），**落盘那一步已删**
  （第 98 步），**不整份灌进 prompt**（用户口径"内容太多了"）。⚠️ **日志只记新增 / 变化的那几份**：
  一条正文动辄几千字，重复打会把 stdout 管道顶掉（有了排除表这成了兜底，正常不该命中）。
- **正文的消费者 = `readSandboxFile` 工具**（第 99 步，**按需**）：LLM 点名一份，命中
  `cmd_explore.body_of(名字)` ⇒ 正文当回合进会话、返回 `""`（**省 1 回合**）；名字两种写法
  （整条全路径、或它的文件名，第 105 步），由 `matches` 一处认 —— **文件名对上不止一份 ⇒ 不成立**
  （替它挑一份就是把错的那份正文交出去，而它看不出拿错了）。
  判据是**"这个路径在不在字典里"**，不是"shell 有没有报错"—— 后者在本地永远为真、给出的是假回执
  （为什么不认 shell 形状见 `code-task.md` 第 99 步）。
- ⚠️ **未命中不再是"转沙盒取"，而是"调用不成立"**（第 101 步，用户口径：**这张表就是枚举值**）：
  路径不在字典里 ⇒ 返回 `""` + 把清单当工具产出回给 LLM（清单空着则指它去 `executeCmd`）。
  旧支（拼一条 `cat -- '<路径>'` 交沙盒）删掉的理由是实测：LLM 拿到一个文件名就自己拼目录
  （日志里那条 `/opt/task/api.md` 沙盒里根本没有），那趟必然报错 —— 白烧一个沙盒往返、还引它接着猜。
  **第 107 步改口径：工具块恒列**（旧口径"一份都没探明时整个工具都不列"已作废）—— 它的名字被别的
  工具描述点过（"已知文件用 `readSandboxFile`"），藏起来就是一条悬空指引；探明过才在描述尾部挂
  清单，一份都没探明时只不挂那几行（**"我们还没摸过"不等于"沙盒里没有"**）。
- **一趟一个预算**：`FETCH_MAX` 56KB（给判题器那 64KB 留头），单个文件就超预算 ⇒ 它独占一趟、
  接受截断（不重试）。⚠️ 上游单点从"`find` / `wc` 在不在"换成"**`python3` 在不在**"
  （任务书只说沙盒"能执行基础的 shell 指令与 python 指令"，那个解释器叫什么**没实测**，
  用户拍板单押 `python3`）；且**一趟的日志量上限从 4000 字变成 ~56KB**（表 #56 / 第 89 步
  不确定性 3）。

---

## 5. 夜里的操炮与开火（`night.defend` / `night._fire`）

> 夜里那一级（§1.2 的 ④）**有三支、严格按序**：N1 基地升级 → N2 清完后的出门采矿 → N3 操炮
> （§1.3 的表）。**本节只讲 N3**；N1 / N2 都不是兜底，它们**先于**开火。

- **回合号缺失（`round_no < 0`）⇒ 一发不发**：`is_day` 把缺失判成夜里，那个降级方向对"白天不许建造"
  安全，对"白天开火非法"就反了。
- ⚠️ **开拓者是补位炮手**（第 74 步，用户口径）：`_pioneer_mans_guns` 为假（= 工人数 ≥ 武器组数）
  ⇒ 它**一个组都不认领、直接返回**。工人夜里除操炮没别的活，开拓者却是任务线的主力 ⇒ 它抢一组
  等于把一个工人挤成闲置。payload 里它常排在最前，不拦就会先挑走最近的组。判据与收工闸门
  **同源**。⚠️ **唯一例外**（第 77 步）：它**已经站在**某组的操作位上 ⇒ 认领那一组 —— 那一格被它
  占着、工人站不上去，它再不打就是整组白丢一夜。
- 选**最近且没被本回合别人认领**的一组武器（`_weapon_groups`：2 火箭一组、加特林单独一组），
  走到那一组的**岗位**（`_post_spots`：多座组 = 同时贴着所有座的那一格、单座组 = 那座炮本身）。
  **先开火、打不了才挪岗**：贴着组内某座就直接打它 —— **只贴一座也打**（另一座就绪而这座在冷却时，
  别白丢一回合）；站在整组的岗位上、这回合又打不了（没敌人 / 在冷却 / 够不着）⇒ 原地待命；
  只贴一部分、或压根没贴着 ⇒ 朝岗位挪一格。⚠️ **开火那一支排在岗位判据之前**（第 77 步）：岗位格
  被占 / 走不到都不是"这一组不用管"，旧顺序（先算 `_post_spots`、表空就跳过）让"贴着炮但岗位格被占"
  的角色跟着整组一发不打（用户报的死锁）。**主岗位一个都站不上时退到 `_near_spots`**（贴着组内
  **任意一座**的格子）：只守得了一座也照打。⚠️ **退路排在第二遍**（主岗位先试完）—— 混在一起会让
  工人把同事站着的那一组抢过来。**不换组**：站哪组是走到位那一刻定下的，每回合重挑
  会让角色在炮位之间来回走。组内多座时挑**冷却好了的那座**开火（火箭 3 回合冷却 ⇒ 两火箭交替
  = 一人操两座）。走不到 / 发不出去就换下一组。什么都不发是合法空指令。
  ⚠️ **自己人不算障碍、别人站着的那格要让开**：`model._entries` 把我方角色写进网格 ⇒ 整份
  `blocked` 里混着队友和**自己**；不放行的话站在岗位上的操作者**看不见自己的岗位**、整组被跳过
  （就是"敌人进了射程不打"的根因）。别人**站着**的岗位则要排掉 ⇒ 换一组去，否则一个走不进去、
  一个干等着，两人一起卡住。
  ⚠️ 岗位是**空地、要踩上去** ⇒ 走路用 `path.step_onto`：`step_toward` 的契约是"贴着 goal 即到"，
  停不进这种格子（每回合返回 `None` ⇒ 人原地卡死，同时把整组认领走、别人也别想用）。
  ⚠️ 不在 `weapon_sites` 里的武器（测试手搭 / 重建偏移）⇒ 单独成组，降级为"一人操一座"。

**开火 `_fire`（第 27 步方针：最大伤害落点、打死所有机器人）**：

| 武器 | 落点怎么挑 |
|---|---|
| **火箭** `_rocket_site` | 落点任选（导弹指哪打哪、不被弹道阻挡）⇒ **全场**算账：中心 20 + 周围 8 格溅射 10，取总分最高的格子。候选 = 机器人格及其 8 邻格（别的格子一分伤害都摸不到），**落点**须在射程内 —— **溅射可以够到射程外的机器人** |
| **加特林 / 电磁** `_beam_site` | 弹道武器 ⇒ 落点在**某台机器人身上**（终点必在弹道上 ⇒ 必命中）。取**有效伤害** = `min(伤害, 剩余血)` 最高的，并列打近的 |

- **冷却中不打**（`cooldown > 0`）；⚠️ **字段缺失解析成 -1 ⇒ 不算冷却**。⚠️ **火箭的冷却有本地账
  兜底**（用户方案）：`night._fired` 记下每次真发出去的火箭 `attack`（武器 id → 回合号），
  之后 `ROCKET_COOLDOWN`(3) 回合内不选它 —— 样例三座炮都没有 `cooldown` 字段，没有这本账就
  整晚连打、每回合吃一次执行失败。**这是策略侧唯一的跨回合状态**（现在住 `night.py`）（按 id 键控、期满自动过期；
  武器毁/重建换 id ⇒ 旧账自愈）。
- **只打打我方基地的机器人**（`_foe_robots`）：`Robot.target_team` 明确是对方阵营 ⇒ 跳过
  （火箭射程远尤其需要，否则导弹常砸到对方半场的机器人簇里——既浪费火力、又帮对方减轻基地压力）。
  降级：`our_team` / `target_team` 缺失 ⇒ 照打（不打比打错更糟）。⚠️ 已毁的机器人**不从列表里移除**
  （`health == 0` 照旧在表里）⇒ 判空 / 判射程 / 记账一律过 `_alive`。
- **`assigned` 记账**：先开火的炮把估计伤害记在机器人身上，后开的按**剩余血**算 ⇒ 不挤同一个将死的
  目标。⚠️ 估计有偏差（加特林实际命中的是弹道上**最近**那台），代价只是次优、不会非法。
- **`attack` 的报文是唯一的例外**：`roleCommandMap` 的 **key 是武器 id**，操控角色在 `controllerId` 里。
- **伤害与目标位数都随 `Weapon.level` 放大**（`_beam_damage` / `_rocket_damage`）：加特林/电磁每级
  +10，火箭按枚数叠加（中心 `20 × 等级`、溅射 `10 × 等级`）；`targetPos` 的**份数 = 等级**（电磁恒 1
  —— 接口文档 L218，发错了是指令非法、红线）。**多出来的目标位重复同一个最优落点**（用户口径的
  "最小合法化"）：火箭同点叠加、加特林同弹道连着吃同一台，90° 锥的约束因此天然成立。
  ⚠️ 代价是**分点覆盖那点收益没拿**（两枚分打两格能多带走一只）。

**已知折扣**：开拓者一旦接任务就被钉死，夜里只剩两个人操炮（⚠️ **例外**：夜里
`_pioneer_mans_guns` 为真时它弃任务回炮位 —— 工人阵亡正是这种局面）—— 但阵形只有**两个操作组**
（火箭对共用一个操作位 + 加特林），两人恰好一人一组 ⇒ 火力**不打折**。任务那半边是白赚的积分。
⚠️ **反过来说：两个工人活着时开拓者也不该占炮位** —— 它占一组就把一个工人挤成闲置，火力一分不多、
任务线的主力还白拴着。

---

## 6. 几何与静态阵形（一切坐标由己方 station 的 pos 推导，禁止绝对坐标）

- **固定 36 格防御盒子**：基地 2×2 + 12 格武器环 + 20 格围墙环。
  可建造区公式**只存在于图里**（`docs/pic/build_map.png`），任务书正文没有。
  ⚠️ `pos` 展开成哪四格以 `grid.base_cells` 为准 = `[bx, bx+1] × [by-1, by]`（接口文档说
  `pos` 是"左上角"，照那个说法展开的框**对不上样例**，以代码为准）。
- **武器环 = `x ∈ [bx−1, bx+2]` × `y ∈ [by−2, by+1]` 去掉基地 4 格**。
- **炮位 = 前排上方相邻两格（2 火箭）+ 前排下方一格（加特林）**，三个落点**各自与基地的一角
  切比雪夫距离 1**（升级券/维修包要"目标建筑周围一格内"）。两火箭相邻 ⇒ 内侧那一格
  （武器环里的空格）同时贴着两座，是**双火箭共享的操作位**。左半基地 `(10,24)` ⇒ 火箭
  `(12,24)` / `(12,25)`、加特林 `(12,22)`，操作位 `(11,25)`。
  ⚠️ 那格是**空地、要踩上去**（`path.step_onto`），且**是个口袋**：唯一通路是 `x=9` 那条一格宽的
  走廊（`code-task.md` 表 34）。它同时是**"2 人操满 3 座"的由来**。
- **正面 / 背面**：机器人从**朝向地图中心**那一侧水平逼近，判据在 `grid._front_back`（**只此一处**，
  `wall_cells` 与 `weapon_sites` 都从这里取），按**基地坐标**判而不用队伍类型 —— 换边后基地会挪。
  ⚠️ 刷新方向**只来自示意图**，正文没写。
- **后方通道（第 1/2 天 6 格、第 3 天起 4 格）里没有任何建筑**（三座炮全在武器环的正面列、不在这一列）
  ⇒ 能堵门的只有**单位** —— "自己人站在待砌格上算不算障碍"只在这里有意义。⚠️ 它**常年开着**
  （第 57 步起，U 形墙）：既是"后方留出路径"的落地，也是机器人可能摸进来的口子。
- **自己人算不算障碍，四个口径**：`_ring` 问"这格要不要砌" ⇒ 自己人**算路过**（否则两人对着改目标）；
  `_walled` 问"砌满之后会不会有人出不来" ⇒ 自己人**只在后方通道那几格**算障碍（环内的 16 格是
  基地 + 武器环，站着的自己人是过路的）；`_stuck_inside` 问"**现在**出不出得去" ⇒ 自己人**一律
  不算障碍**（同事站在通道格上只是路过，算成障碍会**白拆一次**）；**全部距离估算**
  （`utils._passable`）问"路有多远" ⇒ 自己人**一律算路过** —— 与 `_Queue.step` / `_walk_out`
  **必须把同事当硬障碍**（撞上就是执行失败）是**两套口径**，别混。
  ⚠️ 这两条都付过学费：`_walled` 早先把自己人一律算障碍 ⇒ 两人互相判成"被围"、**来回踱步一整天
  不砌墙**（实测 16 回合 0 座）；估算那一路若把同事当障碍，环砌满后盒内只剩一格宽走廊 ⇒ 每座矿的
  "矿 → 最近炮位"都是 -1 ⇒ 整条经济线静默放弃、**工人一整天一条指令都不发**。
- ~~**首日豁免 `_after_first_day`**~~：**第 68 步删除**（用户口径："只放开砌完的第一天"）—— 门改由
  **环满没满**把关。留下的结论仍成立：环上"孤零零一个缺口"这**一个状态既是"刚挖的洞"也是
  "还差一格没砌"**，地图上**逐字节同形**，任何无状态判据都分不开 —— 所以判据是"**环满不满**"
  （有据可依：环敞着时人本来就走得出去），不再看第几天。代价换了个形态：`_ring` 判"已砌"用的是
  `blocked` **代理** ⇒ 机器人踩在缺口上时环"看着齐了"。

---

## 7. 旋钮（改策略先看这张表）

大多在 `game/core.py` 顶部；三个例外：`RETURN_MARGIN` / `DETOUR_MAX` / `WEAPONS_BY_SITE` /
`SELLABLE` 在 `game/day.py`，`ROCKET_COOLDOWN` 在 `game/night.py`。

| 常量 | 值 | 管什么 |
|---|---|---|
| `RETURN_MARGIN` | 5 | 收工门：`白天剩余 ≤ 5 + 手里的券数` 就回岗位（拍的） |
| `DETOUR_MAX` | 2 | 顺路卖矿的绕路上限 |
| `WEAPON_COST` / `WALL_COST` | 25 / 1 | 建一座武器 / 砌一格墙 |
| `VOUCHER_CHAIN` | 4 张券 | 买券优先级：武器二级 > 武器三级 > 围墙二级 > 围墙三级 |
| `WALL_MAX_HP` | 1000/1500/2000 | L1 弱墙判据（血 < 1/4）；L2/L3 的数值是推断 |
| `STATION_MAX_HP` | 1500/3000/4500 | 夜里的基地残血判据（血 < 1/4） |
| `ROCKET_COOLDOWN` | 3 | 火箭发射后的冷却回合数（`night._fired` 本地账） |
| `WEAPONS_BY_SITE` | `("rocket", "rocket", "gatling")` | 落点配种类（与 `weapon_sites` 下标一一对应） |

⚠️ **第 121 步把一串旋钮删了**（`TIME_MARGIN` / `POST_MARGIN` / `STONE_RESERVE` /
`STONE_KEEP_RAISING` / `HOLE_MIN_LEFT` / `NIGHT_WANDER`）—— 白天只剩"收工门"这一道时间判据，
夜里连时间预算都不算。`AGENT.price_hint`（新闻修正）也暂时空着（两条挖矿线都只读 `vendor_prices`）。

## 8. 这套策略的薄弱处（按嫌疑排序）

完整的"仍生效的已知不确定性"在 `docs/design/code-task.md` 文末那张表里，这里只列**策略层面**的：

1. **工具协议与沙盒链路一次实盘都没跑过** —— 判题器的 LLM 认不认嵌套形状是最大的单点风险；
   ⚠️ **严格只认新形状**（用户拍板）：LLM 按旧形状 / 属性式回复 ⇒ 落重问、每条白花一回合，
   降级安全但**逐回合掉分**，首场日志先看这个；
2. **答案格式完全未知**（通过率 = 正确字段数 / 全量字段数 ⇒ 多字段结构化答案），`prompt.py` 的措辞
   是唯一的杠杆；**"沉淀与作答同轮"也全靠措辞**，本地测不出来 —— 但它的坏方向是安全的
   （LLM 照旧只调 `SOP2Prompt` ⇒ 走 ③′→⑥ 重问，白花的那一回合与改动前一样，**不碰红线**）；
3. **`sell` / `buy` / `use` 三个动作没有实证报文**（`docs/response.txt` 只有 `move`/`build`/`remove`）
   —— 若判题器报"指令非法"，先查这三处的 `to_wire`；
4. **`vendorShopList` 是收购价还是售价是推断** —— "价 0 的矿不去采"在售价口径下会误伤；
5. **卖矿阈值（`2 × 距离`）与 `UPGRADE_CHAIN` 的先后都是拍的**，没有文档依据（链的先后是用户口径）；
6. **对外可见的东西只有图片背书**：可建造区公式、机器人刷新方向、机器人浪潮曲线；
7. **弹道是否被墙挡没有明写**（只明说火箭不被挡）—— 若被挡，整个防守阵形要重想；
8. **会话 prompt 无上界增长**（先不压缩，用户拍板）⇒ 长任务 + 大沙盒回执可能撞上"响应超时"这条红线；
9. **白天末尾砌不完环**（第 121 步把"快天黑就掉头"删了：采石只看还差几块）⇒ 夜里正面留着缺口，
   而夜里不能 `build`、当天无法补救。日志症状 = "**一条 `remove` 找不到对应的 `build`**"；
10. **第 57 步的新几何整个没有实测背书**：14/16 格的**建造顺序**（正面优先）与"第 3 天补背面两角"
    都是推出来的，只有 `DayOneFinishTest`（合成局面、2 座石矿）兜着。首场看"第一个白天末尾砌了几格"；
11. **新闻查价线（§4.3）没跑过**：`<prices>` 的形状同样是文档没规定的自定形状，且**吃每日 3 次额度**
    （发出去收不回）。它的坏方向也是安全的（回复看不懂 ⇒ 系数保持 1.0、照旧按实时价挑矿）。

**每一处都留了单点回退**：协议 / 新闻形状改 `chat.tool_of` / `is_prices_reply` 一处、
报文形状改对应 `to_wire` 一处、阈值改一个常量、刷新方向翻 `grid._front_back` 一处。
